#!/usr/bin/env python3
"""Batch 83(手動モードの音の待ちを事実へ移す = 候補 U2)の壊し検証(裁定116)。

★★★<b>回すのは `tools/break_check_runner.py` である</b>(Batch 82・設計判断64)——
  このファイルは `CASES` の表だけを持つ。

```bash
python3 tools/break_check_runner.py --batch 83
```

★<b>壊すのは複製であり、本体は1バイトも書き換わらない</b> ——
  <b>だから分けて回す必要も、前後で `git diff --stat` を突き合わせる必要も無い</b>。

---

## ★★★このバッチが直したもの

81 が通常モードで直した形(候補 U)を、**手動モードの `snd` の節**へ当てた。

★★★<b>ただし引き継ぎ書の見立てとは違った</b> ——
  <b>「200ms / 400ms の固定待ち」ではなく、実際は 200ms×2 と 60ms×7 だった</b>し、
  ★<b>働いていたのは `goto` の直後の 200ms のほうで、
    unlock の直後の 60ms は死に荷物だった</b>(実測: 60ms を7件消しても 793/793・2並列でも緑)。
  ★★<b>症状を母集団にしなかったから出てきた</b>(79 の教訓)。

| 何を | どうしたか |
|---|---|
| `goto` 後の 200ms | ★**撤去**。<b>あれは「取得の完了」を待っていた</b>が、待つべきは**復号の完了**である |
| unlock 後の 60ms | ★★★**事実待ちへ**(`sfxBuffers` が全部そろったこと)+ わざとの遅れ 600ms |
| 配信・クリック後の 60ms × 6 | ★**撤去**。<b>実装が同期に鳴らすので、そもそも待つ必要が無い</b> |
| `reload` 後の 200ms | ★**撤去**。★★<b>いちど事実待ちにしたが、外しても2並列で 797/797 だった</b> ——<br>Playwright の locator が実操作の前に待つ。<b>壊せない待ちを残すのは偽の番人である</b>(裁定196) |

★★<b>復号には AudioContext が要るので、`goto` の直後には構造的に揃わない</b> ——
  <b>実測で8秒待っても揃わなかった</b>。★<b>取得が unlock に間に合わなくても、
  届いた時点で自分で復号へ回る</b>(`sfxFetch` の `if (sfxCtx) sfxDecode(name)`)ので、
  <b>取得の完了を待つ必要そのものが無い</b>。

---

## ★★★実測した余裕(82 の教訓: 効かなくしたときにどれだけ余るかも測る)

| | 実測 | 閾値 400ms との余裕 |
|---|---|---|
| **正しい状態**(遅らせ 600ms) | **1522〜1783ms** | ★余裕 1122ms 以上 |
| **壊した状態**(遅らせ 0ms) | **34ms** | ★★**余裕 366ms** |

★<b>直す前の固定待ち 60ms に対する余裕は 26ms しか無かった</b> ——
  79 の候補U(5ms)・81 の軸21(29ms)と同じ一族である。

---

## ★★★軸と番人の対応(82 の教訓: 1対1であることを数える)

| 軸 | 壊すもの | 落ちる番人 |
|---|---|---|
| 1 | わざとの遅れを外す | 遅らせが効いている(83) |
| 2 | 事実待ちを固定待ちへ戻す | 手動モードも音のバッファは時間ではなく事実で待つ(83) |
| 3 | 片付け(遅らせを戻す)をしない | 遅らせは自分で元に戻す(83) |
| 4 | 音を配信の外(非同期)で鳴らす | 音は配信の中で同期に鳴る(83) |

★★★**軸4 は最初 NG だった**(81 の軸9 と同じ形)——
  <b>`setTimeout(..., 0)` で鳴らしても、既存の番人は1本も落ちなかった</b>。
  ★理由: <b>`deliver` と `audioNodes` が別の evaluate なので、
    そのあいだにマクロタスクが走ってしまう</b> ——
    <b>既存の番人は「同期であること」ではなく「往復より速いこと」しか測っていなかった</b>。
  ★★<b>そこで番人を1本足した</b> —— <b>配信と計数を同じ evaluate に閉じて数える</b>。
  ★★★<b>これが「配信のあとの固定待ちを6件撤去した」ことの根拠である</b> ——
  <b>根拠にしたものには、その日に番人を置くこと</b>(77 の教訓)。

---

## ★★壊しどころが無いもの(裁定196 の正直な扱い)

1. **「鳴らないこと」を測っている2件**(差分が上限を超える / 確認を出しただけ)。
   ★<b>「何も起きないこと」には待つべき事実が無い</b> —— <b>事実待ちへは移せない</b>。
   ★★<b>ただし 83 は固定待ちを<b>撤去</b>したので、以前より強くなっている</b> ——
   <b>配信が返った直後という決まった一点で 0件を測る</b>ようになった。
2. **`manual-battle.js` の音の仕組みそのもの。**
   ★83 は<b>1文字も触っていない</b>(軸4 は複製の上で壊すだけである)。
   ★★<b>だから版数も据え置きである</b>(7-5)。
3. **`reload` 後の待ち。**★事実待ちにしても<b>外しても落ちない</b>ので、待ちごと撤去した ——
   <b>壊せない待ちを残すのは、偽の番人を1本置くのと同じである</b>。
   ★★<b>race は既存の check が自分で捕まえる</b>(台本が載る前に押していたら '55' が当たらない)。
4. **通常モード側の音の待ち。**★81・82 が既に事実待ちである。83 は触っていない。
5. **並列度の天井。**★<b>83 は上げていない</b> —— 4並列の赤は 3本 → 1本に減ったが、
   <b>ゼロではない</b>(残っているのは「自席マナのタップは transform の遷移が実際に走る」)。
   ★<b>だから `WORKERS` も番人 S6 の「2」も据え置きである</b>(候補 P2 は残る)。
"""

VERIFY = "verify/verify.js"
MANUAL_JS = "src/main/resources/static/js/manual-battle.js"

# ★壊しても落ちないことが分かっているもの(理由つき)。
#   ★★<b>Batch 83 には1件も無い。</b>4軸すべてが落ちるはずである。
EXPECTED_NG = {}

# (説明, ファイル, 置換前, 置換後, 走らせ方, JUnitのクラス, 照合先の名前の一部)
CASES = [
    # 軸1: ★★★わざとの遅れを外す(設計判断61)
    #   ★<b>遅らせが無い待ちは、次の人に固定時間へ戻されても
    #     「赤」ではなく「揺れ」にしかならない</b>(79 の教訓)。
    ("わざとの遅れを外す", VERIFY,
     "  SOUND_RESPONSE.delayMs = 600;\n  await snd.goto(",
     "  SOUND_RESPONSE.delayMs = 0;\n  await snd.goto(",
     "verify", None, "遅らせが効いている(83"),

    # 軸2: ★★★事実待ちを固定待ちへ戻す
    #   ★<b>わざと 1000ms にしてある</b> —— 60ms に戻すと軸1の番人まで道連れになり、
    #     <b>1つの改変が2つの番人に当たる</b>(82 の教訓)。
    #   ★★<b>これは「たいてい通る長さの固定待ちへ戻した」という、いちばん有りそうな戻し方である</b>。
    ("事実待ちを固定待ちへ戻す", VERIFY,
     "  const sndBuffers = await settled(snd, () => Object.keys(\n"
     "    // eslint-disable-next-line no-undef\n"
     "    SFX_SPECS).every((nm) => sfxBuffers[nm] && sfxBuffers[nm].length), null);",
     "  const sndBuffers = await snd.waitForTimeout(1000);",
     "verify", None, "手動モードも音のバッファは時間ではなく事実で待つ"),

    # 軸3: ★★片付け(遅らせを戻す)をしない(78・79・81 の教訓)
    #   ★<b>戻し忘れても誰も赤くならないので、遅くなったことに気づけない</b> ——
    #     <b>だから番人を置いた</b>。
    ("遅らせを元に戻さない", VERIFY,
     "  // ★★<b>片付けてから次へ渡す</b>(78・79・81 の教訓)。以降の節は遅れの無い音で回る\n"
     "  SOUND_RESPONSE.delayMs = 0;\n",
     "  // ★★<b>片付けてから次へ渡す</b>(78・79・81 の教訓)。以降の節は遅れの無い音で回る\n",
     "verify", None, "遅らせは自分で元に戻す"),

    # 軸4: ★★★音を非同期にする
    #   ★★★<b>これが「配信のあとの固定待ちを6件撤去した」根拠を測る軸である</b> ——
    #     <b>実装は rAF を待たずに同期で鳴らす</b>({@code fxSpawn} のコメント:
    #     「待ち時間に依存する検証にしない」)。<b>だから待つ必要が無い</b>。
    #   ★★<b>最初はこの軸が NG だった</b> —— <b>既存の番人は往復より速いことしか測っていなかった</b>。
    #     <b>だから「配信と同じ evaluate の中で数える」番人を1本足した</b>(冒頭の表)。
    ("音を配信の外(非同期)で鳴らす", MANUAL_JS,
     "    sfxPlayForEffects(pending.effects);",
     "    setTimeout(() => sfxPlayForEffects(pending.effects), 0);",
     "verify", None, "音は配信の中で同期に鳴る"),

]
