# QTE 対戦アプリ — 引き継ぎ書

最終更新: 2026-08-14(**Batch 38(儀式 = ゲーム開始の一括演出 + 山札のシャッフル + SFX 3種)を
実装・納品**。**★★納品後の追加指示(手動のシャッフル)とマスター裁定 Q1 = b
(ピュア・エレメント)を同バッチに取り込み、型の名前から `Start` を外している。**
商業水準レビュー(`notes/commercial-quality-review-2026-08-11.md`)の
優先順位 **7** を消化した。**★★Java を変更している。`mvn test` を走らせること。**
JUnit も +9件足してある。
**★テンプレートは版数しか変えていないが、実サーバで開くことだけ確認してほしい。**
**★★★裁定81 の問い「差分の抑制を緩めずに総入れ替えをどう演出するか」への答えは
「関門を `applyView` から `diffViews` の中へ移し、儀式だけが通る道を1本足す」である。
裁定8 も裁定11 も1文字も緩めていない。**
js v=31 / css v=39。verify **424/424**。設計解説は `notes/batch38-design-notes.md`)

**★★37 の反映は fresh clone(`55ba353`)で照合し、verify 397/397 を確認したうえで 38 に入っている。**

**★新しいチャットを始めるときは 5章「チャット開始テンプレート」を使うこと。**

このファイルは、新しいチャットで作業を再開するための最小限の情報をまとめたものである。
ゲームルール・設計判断の詳細は `qte-project-reference.md`、カードの詳細は後述の
「カードデータの正」を参照。

| 系統 | 前提ドキュメント |
|---|---|
| **今後の優先順位(商業水準レビュー)** | **`notes/commercial-quality-review-2026-08-11.md`** |
| **Batch 38(儀式 = 開始の一括演出 + シャッフル・SFX3種・完了)** | **`notes/batch38-design-notes.md`** |
| Batch 37(SFX・設定パネル・完了) | `notes/batch37-design-notes.md` |
| Batch 36(Esc・フォーカス・confirm 置換・完了) | `notes/batch36-design-notes.md` |
| Batch 35(勝敗の帯・ログ強調・ターン帯の退役・完了) | `notes/batch35-design-notes.md` |
| Batch 34(ヘルプ・title・ロビー・og:image・hotfix・完了) | `notes/batch34-design-notes.md` |
| Batch 33(切断UX・共有導線・完了) | `notes/batch33-design-notes.md` |
| Batch 32系(盤面演出・完了) | `notes/batch32-effects-design.md`(32系の唯一の正) |
| Batch 32c / 32b / 32a | 各 design-notes |
| **開始シーケンスの仕様(23)** | **`notes/batch23-design-notes.md` + `notes/batch23-game-start-design.md`** |
| Batch 31〜24 | 各 design-notes(v43 の表を参照) |
| Ver0.4 対応(15c/15d/15e・**要再計画**) | `notes/ver0.4-transcription-notes.md`(★下の「カードデータの正」を先に読むこと) |

---

## 0. 最初にやること

1. `qte-project-reference.md` を読む。★1章の実装状況は2026-07-21のまま古い。
2. 本ファイルと、作業対象の design-notes を読む。3章「既知の落とし穴」は必読。
3. ソースコード取得: `git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git`
   (codeload の zip URL は 403 で落ちることがある)
4. **「反映済み」を信じず、直近バッチの変更箇所を実際に読んで照合する。**
   ★38 の照合は「24〜38 の確認項目」の 38 の行で行い、**verify 424/424** を確認する。

## ★カードデータの正(2026-08-06 マスター裁定、30で3件反映済み)

- **テキスト・数値とも DeckMaker Verβ(`QTE-Vol1-Vol2-Vol3-Ver-1.1.json`)が正。**
- 手動モードは `manual-cards.json` に反映済み(数値44枚更新・text/keywords追加)。
  **30 で追加修正**: 悪夢(QTE-M-DARK-27)コスト1→13、苗木植えの精霊
  (QTE-M-EARTH-16)コスト1→2、ゾンストライカー(QTE-M-DARK-16)の
  `unlimitedCopies` 削除(通常どおり4枚まで)。★これで `unlimitedCopies` を
  持つカードは全カード中 **0枚**になった。
- **台帳 `qte-cards.json` と通常モードの効果実装はVer0.4基準のまま乖離している。**
  Ver1.1への追随は転記ワークフローの再実行として要計画(15c系はこの前提で再スコープ)。
  ★台帳側のゾンストライカーのテキストに「このカードは4枚以上入れられる。」が
  まだ残っている。再計画に含めること。
- 進化ミニオン18枚・Vol2/Vol3のカードは台帳に存在しない(手動モードのみ)。

## 24〜38 の確認項目(履歴・照合済み)

いずれも当時のfresh cloneで全項目確認済み。再照合は不要。要点のみ残す。
★24〜36 の詳細は v43 に残してある。ここでは 37 以降を厚く書く。

- **24**: `manual-cards.json` が全カードに `text` を持ち `keywords` は存在しないこと。
  `unlimitedCopies` の構築は `ManualCardRepository.toMaster()` の1箇所のみ。
- **25**: `manual-battle.js` から画像URL組み立てが消え `cardFace` 系に統一。
  カラーパレットは `.mcard-frame` + `--mc` に統一(旧CIV_COLORS廃止)。
- **26**: パイル最上段は `pileTopCard(zoneName, pile)`。差分チップ `.manual-stat-chip` 廃止。verify 243。
- **27**: `ManualOperationService` に `unstack` / `willUnstack`。verify 245。
- **28**: STOMPハートビート・`sendBufferSizeLimit` 1MB・無言 reload 廃止。verify 248。
- **29**: `LOG_TAIL`(=60)・`deliverable`・`buildZoneCards`・`logTotal`・`zoneContents`・
  `measurePhase` / `pendingMeasure` / `LOG_DOM_MAX`。verify 257。
- **30**: `manual-cards.json` 3件。マナのタップは `rotate(90deg)`。verify 273。
- **31**: ハーネスに実ページの黒背景。verify 274。
- **32a**: fx層一式(`applyView` / `diffViews` / `fxIndex` / `fxSpawn` ほか)。verify 288。
- **32b**: `tapChanged` / `flipped` / `stackGrew` / `turnAdvanced`。verify 300。
  ★`turnAdvanced` と `fxBuildTurn` は **35 で退役した**(裁定17)。
- **32c**: `.mcard-*` の多層 background・`--mc-gloss` / `--mc-sheen`・`faceshot.js`。verify 301。
- **33**: 切断中のロック(`send()` のガード)・部屋の共有・`error.html`・meta 一式。verify 319。
- **34**: 初回ヘルプ・`TITLE_HINTS` / `tileTitle()`・`PublicUrlAdvice`・ロビーのダークテーマ・
  hotfix(`.info-modal-head` / `applyManaOverlap` の重なり上限)。verify 341。
- **35**: Java 新規2(`ManualLogDeclaration` / `ManualDeclarationView`)。
  `ManualLogEvent.declaration(...)`・`ManualGameView.declarations`・
  `fxBuildDeclare` / `FX_DECLARE_MS`(2200)・`.manual-fx-declare`(+4色)・`.manual-log-decisive`。
  **`.manual-fx-turn` 系は削除**(裁定17)。verify 346。
- **36**: Java 変更ゼロ。「0-3) モーダルの層」(`modalStack` / `openInfoModal` / `closeInfoModal` ほか)と
  「0-4) 確認モーダル」(`askConfirm` / `#confirm-modal`)。**`confirm(` は0件**。
  `:root { color-scheme: dark }`。`battle.html` の css を v=36 に揃えた。verify 373。
- **37**: **Java 変更ゼロ。** 「0-5) 効果音」(`SFX_SPECS`(8種)/ `SFX_FOR_KIND` /
  `SFX_PRIORITY` / `sfxReady` / `sfxUnlock` / `sfxPlay` / `sfxChoose` / 設定パネル)。
  8-2章に `fxDiffNeeded` と `sfxPlayForEffects`。取り付けは **`fxSpawn` の1行**と
  **確認モーダルの [実行]** の2箇所だけ。`#btn-sound`(♪)と `#sound-modal`。
  `.sound-*` の CSS。verify の targets に音の設定パネルの文字5件。
  **js v=29 / css v=37。verify 397/397。**
- **38**: **★★Java 変更あり(4新規 + 5修正)。**
  **新規**: `ManualRiteKind`(**DICE / DEAL / MULLIGAN / SHUFFLE**)/
  `ManualRiteDeal`(seat / back / drew)/
  `ManualLogRite`(kind / diceA / diceB / winner / label / dealt / **pureSeat**。ファクトリ
  `dice` `deal` `mulligan` **`shuffle`**)/ `view/ManualRiteView`(seq + rite)。
  ★★**型の名前に `Start` は入っていない**(裁定98)。器は「開始の儀式」ではなく
  <b>差分では語れない出来事</b>である。
  **修正**: `ManualLogEvent` に構成要素 **`rite`** と **`rite(kind, actorSeat, rite, text)`**
  ファクトリ(★種別を引数で受ける。★`plain(START, …)` は**通る**)/
  `ManualStartService` に `startLog(actor, rite, text)`・`deal(…, diceSeed)`・
  **`Finish(text, pureSeat)`**(★員数は本文と同じ1周で拾う)/
  **`ManualOperationService.shuffleDeck` が儀式を返す**(裁定99)/
  `ManualGameView` に **`rites`** / `ManualViewBuilder` がログの末尾60行と**同じ1周**で拾う。
  `manual-battle.js`: 0-5章に **`dice` / `deal` / `shuffle`** と **`sfxRattle`**、
  `SFX_FOR_KIND` に4行、`SFX_PRIORITY` を10要素へ。
  8-2章に **`FX_RITE_*` 定数**(+ `FX_RITE_CENTER` / `FX_RITE_PURE_GAP_MS` /
  `FX_SHUFFLE_SCATTER`)・**`fxLatestRite` / `fxNewRite`**・
  **`fxRiteCenterRect` / `fxRiteSpan` / `fxRiteLegs` / `fxRiteDuration` / `fxRiteHoldMs`**・
  **`fxBuildDice` / `fxBuildRiteFlights`**・**`riteHolding` / `riteHold`**。
  **★★`diffViews` の中に `out.rite` の採取と関門が入った**(`applyView` からは消えた)。
  **★★★関門の条件は `out.rite || fxStartLocking(prev) || fxStartLocking(next)`**(裁定93)。
  `renderStartUi` / `syncMulliganOverlay` が `holding` を見る。
  `battle.css` に `.manual-fx-dice` / `-chip` / `-win` / `-note` / `.manual-fx-rite` /
  `.manual-fx-shuffle`(+ reduced-motion で無効化)。
  `verify/fixture.js` に `rites` / `rite()` / `dealRite()` / **`shuffleRite()`**、
  `verify/shot.js` に **`RITE=dice|deal|mulligan|shuffle|pure`**。
  **js v=31 / css v=39。verify 424/424。JUnit +9件。**

## ★裁定記録

**2026-08-06 確定:**

1. **リーダーのcostは0を維持する**(Verβの1はデータ入力上の既定値と扱う)。
2. **進化ミニオンはメインデッキ40枚に算入する**。
3. **[ゲームを始める] は対戦部屋にも表示する**。
4. **進化スタックは FIELD を離れたら常に解体する。** 席をまたぐ FIELD → FIELD だけは束のまま。

**2026-08-10・08-11 確定(Batch 32 系):**

5. 演出は「移動系・LPポップ・状態系・節目系」+カードフェイス質感を採用。
6. 盤面DOMの差分更新は演出の前提にしない。
7. **★「窓」のゾーンでは出現・消滅を演出しない。**
   ★★**Batch 38 の儀式はこれに触れていない。** 儀式が運ぶのは<b>員数</b>であり、
   枚数(`counts`)は元から公開情報である。ゴーストは裏面以外を描きようがない。
8. **差分が8件を超えたら演出を丸ごと出さない。**
   ★★★**Batch 38 でも1文字も緩めていない**(裁定82)。
9. **Undo の逆向き演出は抑制しない。**
10. **★状態系は「居場所が変わっていないカード」だけに出す。**
11. **★節目の帯は上限(8件)の特例にしない。要らない特例を作らない。**
    ★★★**Batch 38 の儀式も特例ではない。** 9枚のゴーストを出しても
    `fxRegister` に積むのは**1本**であり、上限の勘定でも1と数えられる(裁定83)。
12. **★★★フェイスの質感に transform と filter を使わない。**(機械判定・項目63)
13. **★擬似要素も使わない。**
14. **★光沢の「形」は `--mc-gloss` 1箇所、変えるのは強さ(`--mc-sheen`)だけ。**
15. **★盤面タイル(`.mcard-frame`)も質感の対象に含める。**

**2026-08-11 確定(商業水準レビュー・前提の更新):**

16. **Discord 通話併用が前提である。**
    → **作らないもの**: チャット・エモート・ターンタイマー・BGM・ロビー自動更新・
    フルリザルト画面・再戦フロー・コーチマーク。

**2026-08-12 確定:**

17. **ターン帯を退役させる。** → **★★Batch 35 で実施済み。復活させないこと。**
    番人は verify 項目58-2 である。

**2026-08-12 確定(Batch 33 実装時):**

18. **★★★切断中の操作を止める番人は `send()` であって、オーバーレイではない。**
19. **★部屋消失と切断の案内を同時に出さない。**
20. **★「再接続しました」は初回の接続では出さない。**
21. **★エラーページは外部CDNに依存しない。** **機械判定あり。**
22. **★`og:image` は絶対URLで書く。**
23. **★Thymeleaf フラグメント化は、実サーバで確認できない間はやらない。**

**2026-08-12 確定(Batch 33 の質問3件への回答):**

24. **★`og:image` の公開URLは設定(`qte.public-base-url`)に出す。** → 34 で実施済み。
25. **★切断オーバーレイの [盤面を確認する] は残す。**
26. **★次バッチはレビュー優先順位 4・5 をまとめて行う。** → 34 で実施済み。

**2026-08-12 確定(Batch 34 実装時):**

27. **★★`og:image` の受け方は `@ControllerAdvice` + `${ogImageUrl}` である。**
    **失敗の広さで選ぶ**、というのがこの裁定の一般形である。
    ★★**Batch 37 の「音声ファイルを同梱せず合成する」も、この一般形の適用である。**
28. **★★URL の組み立ては `PublicUrlAdvice` 1箇所である。** **機械判定あり。**
29. **★★`title` の文言は `TITLE_HINTS` / `tileTitle()` 経由で組み立てる。** **機械判定あり。**
30. **★裏向きのカードの名前を `title` に出さない。**
31. **★初回ヘルプの localStorage キーは部屋ごとにしない**(`qte-manual-help-seen`)。
    ★★**Batch 37 の音の設定(`qte-manual-sound`)も同じ理由で部屋ごとにしない。**
32. **★★ロビーの色は Bootstrap の色モードに任せきりにしない。**
33. **★ダークテーマの範囲は手動モードの3枚**(盤面・ロビー・カード一覧)。
34. **★★モーダルの出口は sticky な見出し帯に置く。** **機械判定あり。**
    ★★開始シーケンスの2つ(start-method / start-order)とマリガンには出口を作らない。
35. **★★× は独自に `d-none` を付けず、[閉じる] を `click()` する。**
36. **★★★「読みやすさ」と「隣を侵食しないこと」が衝突したら後者を採る。**
37. **★★重なりの上限は「そのタイル自身の占有幅」から引く。**
38. **★★ストリップの幅は枚数に比例させる。**
39. **★★★ハーネスのスタブに書いてよいのは、落とした CDN の代わりだけである。**

**2026-08-14 確定(Batch 35 実装時):**

40. **★★★「ログを演出の材料にしない」(32a)が禁じているのは書式であって構造ではない。**
    ★★**Batch 38 はこの一般形の2度目の適用である。** 35 は「盤面に痕跡を残さない操作」、
    38 は「盤面を全部書き換える操作」——どちらも差分では語れず、どちらも構造を足して解いた。
41. **★★宣言のログは `ManualLogEvent.declaration(...)` でしか作れない。**
42. **★★`declarations` はログの末尾60行と同じ1周で作る。**
    ★★**Batch 38 の `rites` も同じ1周で作る。**
43. **★決着の合図は `seq` が増えたときだけ出す。**
    ★★**Batch 38 の儀式も同じ**(`fxNewRite`)。**機械判定あり。**
44. **★決着後も盤面はロックしない。**
45. **★ログの決着行は色を1つにする。**
46. **★表示名の正はサーバの列挙体1箇所である。**
    ★★**Batch 38 のダイスの `label` も同じ**(対戦は「選択権」、ソロは「先攻」)。**機械判定あり。**

**2026-08-14 確定(Batch 36 実装時):**

47. **★★★画面を覆うものは1本のスタック(`modalStack`)で持つ。**
48. **★★Esc は × と同じ資格しか持たない。** **[閉じる] を持たないモーダルでは Esc も効かない。**
49. **★★★Esc は下の層へ落とさない。** **機械判定あり。**
50. **★トラップするのはモーダルだけである。**
51. **★★モーダルの開閉は `openInfoModal` / `closeInfoModal` を通す。** **機械判定あり。**
52. **★初期フォーカスの既定は「× を除いた最初の焦点可能要素」**である。
53. **★★確認は素の `confirm()` を使わない**(`askConfirm`)。**機械判定あり。**
54. **★確認はコールバックで受ける。**
55. **★確認のボタンには動詞を書く。**
56. **★★確認モーダルの重ね順は 1965。**
57. **★★★`position: sticky` の要素を負マージンで持ち上げようとしない。**
58. **★「2つの流儀」は揃えなかった。** → 裁定64 で確定。

**2026-08-14 確定(34・35 の質問へのマスター回答):**

59. **★★`battle.css` の `:root` に `color-scheme: dark` を足す。** → 36 で実施済み。
    ★あわせて **`battle.css` の版数が全テンプレートで揃っていること**も機械判定にした。
60. **★デッキメーカーの見た目の統合だけ先に独立のバッチで行う**(34 Q2 = b)。
61. **★一覧・全面表示のカードに `title` は付けない**(34 Q3 = a)。
62. **★勝敗の帯の文言は「席A の勝利」のままとする**(35 Q1 = a)。
63. **★引き分けの帯にも席を出す**(35 Q2 = a)。

**2026-08-14 確定(36 の質問4件へのマスター回答):**

64. **★★「2つの流儀」は揃えない**(36 Q1 = a)。**揃えるべきは流儀の数ではなく規則である。**
65. **★確認モーダルに背景クリックの取り消しは足さない**(36 Q2 = a)。
66. **★★★盤面のカードのキーボード操作は当面やらない**(36 Q3 = a)。
    ★**これは積み残しではない。** 再検討するなら優先順位11(タブレット対応)と同時である。
67. **★★次バッチ(37)はレビュー優先順位8**(SFX + 設定パネル)(36 Q4)。→ 37 で実施済み。

**2026-08-14 確定(Batch 37 実装時):**

68. **★★★効果音の発火点は1つも増えなかった。** 取り付けは
    **`fxSpawn` の1行**と**確認モーダルの [実行]** の2箇所だけである。
    ★★★**Batch 38 の儀式の音も1行も足していない**(裁定85)。
69. **★★★音は動きではない。** `prefers-reduced-motion` を音のゲートにしない。
    ゲートは **`fxAllowed()`(見た目)と `sfxReady()`(音)の2本**。**機械判定あり。**
70. **★★1配信につき鳴らす音は1つである。** **機械判定あり。**
    ★★**Batch 38 の儀式でも守っている**(ソロのランダムはダイスだけ鳴る)。
71. **★★珍しい出来事ほど優先する**(`SFX_PRIORITY`)。順序は1箇所にある。
    ★★**理由は「珍しさ」1本である。** 「大事だから上」と書かない ——
    理由が2本になると次に足す音の位置が決まらなくなる(38)。
72. **★★音の語彙は演出の語彙より粗くてよい。** ★**取りこぼしには番人を置いた**。
    **機械判定あり。** ★★**38 の3種はこの番人が何も直さずに検出した。**
73. **★★★unlock は「最初のユーザー操作」であって特定の操作ではない。** **機械判定あり。**
74. **★★音声ファイルを同梱しない。コードで合成する**(裁定27 の適用)。
    ★★**38 で `rattle` 型を1つ足した。** 足したのは音の「作り」であって「性格」ではない。
75. **★★音そのものは機械で測れない。** 測ったのは「どの音を選ぶか」と「実際に鳴ったか」。
    ★★**38 の儀式は「鳴ったか」をノード数 `=== 1` では測れない**(`rattle` は粒が複数)。
    数えているのは<b>出来事</b>であって音源ノードではない、と読み替えて `> 0` で測っている。
76. **★状態行の色が意味を持つのは「鳴らない理由がある」ときだけである。** **機械判定あり。**
77. **★ゲーム開始・シャッフルの音は優先順位7 で演出と同時に足す。** → **38 で実施済み。**

**2026-08-14 確定(37 の質問4件へのマスター回答):**

78. **★★設定パネルは音だけにする**(37 Q1 = a)。★★**`fxEnabled` は検証の入口のままである。**
79. **★`commit`(確認の [実行])の音はこのまま残す**(37 Q2 = a)。
80. **★LPの増加と減少は2種のままとする**(37 Q3 = a)。
    ★★**「音の総数は8で確定」は 37 の時点の話である。** 裁定77 / 81 により
    38 で3種(`dice` / `deal` / `shuffle`)が加わり、**総数は11**になった。
81. **★★次バッチ(38)はレビュー優先順位7**(ゲーム開始の一括演出)(37 Q4 = a)。→ **38 で実施済み。**

**2026-08-14 確定(Batch 38 実装時。詳細は `notes/batch38-design-notes.md`):**

82. **★★★開始シーケンスは「上限で落ちていた」のではなく「数えられてすらいなかった」。**
    `applyView` の入口に `!fxStartLocking(prevView) && !fxStartLocking(view)` があり、
    差分そのものを採っていなかった。**上限(裁定8)を緩めても何も出ない。**
    ★**指摘の文言ではなくコードを読んでから直す。**
83. **★★★「例外を作る」と「別の道を作る」は違う。**
    38 がやったのは、関門を `applyView` から **`diffViews` の中へ移し**、
    その手前で儀式(`out.rite`)だけを採ることである。裁定8 も裁定11 も1文字も緩んでいない。
    ★儀式が上限に引っかからないのは**特例だからではなく、作られる件数が1〜2件だから**である
    (9枚のゴーストは入れ物1つとして `fxRegister` に積む)。**機械判定あり。**
84. **★★★差分は同一性を語り、儀式は員数を語る。**
    開始の配りでは `instanceId` が全部作り直されるので、差分は嘘ではないが無意味である。
    一方、枚数(先攻4 / 後攻5)は総合ルール 2-5 そのものである。
    ★入口が違うのは経路が違うからではなく、**運ぶものが違うから**である。
    ★この性質のおかげで相手席の配りも演出できる(枚数は元から公開情報)。
85. **★★★守れない約束を型に書かない。**
    35 は「DECLARE 行は必ず構造を持つ」を型で守れた(裁定41)。
    START は守れない —— 構造を持たない START 行が正当に存在する(準備の開始・選択権の告知)。
    型で守るのは「**構造を作るならこの入口を通る**」までにし、
    その入口を呼ぶのは `ManualStartService.startLog(actor, rite, text)` の1箇所に集約した。
86. **★★1行に2つの儀式が同居する場合は、主たる儀式 + 別欄で表す。**
    ソロのランダムはダイスと配りが1回の操作である。ログを2行に割ると
    **起きていない操作**が増える(23 1-4)。儀式をリストにすると全ログ行が重くなる。
    → `kind` が主たる儀式、ダイスは独立した欄(`diceA` / `diceB` / `winner` / `label`)。
    ★**画面には推測が1つも要らない**形にすること。
87. **★★★儀式のあいだ開始シーケンスの画面は開かない(保留)。**
    fx層(1030)は開始モーダル(1950/1960)より下にあり、待たせなければ儀式は1pxも見えない。
    ★**z-index の並びを演出の都合で崩さない。層の順序ではなく時間で解く**(裁定83 と同じ形)。
    ★**保留は時刻で明ける**(`FX_RITE_HOLD_MAX_MS` = 2400ms)。
    組み立てが失敗しても必ず開くので、行き止まりが構造的に作れない。**機械判定あり。**
88. **★★保留するのは「開く」だけである。** 閉じるほうは保留しない
    (「儀式のあいだ閉じられない画面」を作らない)。
89. **★★★演出を切っている人(`prefers-reduced-motion`)は待たせない。**
    保留のゲートは `fxDiffNeeded()` ではなく **`fxAllowed()`** である。
    見えていないものの終わりを待たせるのは、操作が遅れるだけである。
    ★裁定69「音は動きではない」の裏側 ——**待つ理由は音ではなく絵にある。機械判定あり。**
90. **★儀式のゴーストは最初に全部作り、飛ばす時刻だけをずらす。**
    途中で作ると、その1枚だけ開始状態が確定しておらず**瞬間移動**する
    (32a の `offsetHeight` は、確定の時点でDOMに在るものにしか効かない)。
91. **★ダイスの帯の内容は最初からDOMにある。** 段階的に現れるのは `animation-delay` であって
    JS がテキストを書き換えるのではない。**「待ってから測る」検証にしない**(31 の規約)。
92. **★0枚のマリガンでも儀式は作る。**「儀式が無い」と「儀式が空だった」を
    画面が区別できなくなると、判断が画面側へ移る。

**2026-08-14 確定(38 の質問4件へのマスター回答):**

94. **★ピュア・エレメントはマリガン完了の儀式に1枚ぶんの飛翔を足す**(38 Q1 = b)。
    ★音は増やさない。→ 38 追補で実施済み。
95. **★リセット・すべてアンタップは儀式にしない**(38 Q2 = a)。
    リセットは見せ場ではなく、すべてアンタップは毎ターン起きるので重くなる。
96. **★儀式のゴーストの寸法は既存のドロー演出と同じ決め方のままとする**(38 Q3 = a)。
97. **★★次バッチ(39)はデッキメーカーの見た目の統合**(38 Q4 = a・裁定60)。

**2026-08-14 確定(38 追補。手動のシャッフルの実装時):**

93. **★★★1つの配信に語り手は1人である。**
    儀式が語る配信では差分を語らない(`diffViews` の関門に `out.rite ||` を足した)。
    シャッフルは山札の最上段が入れ替わりうるので、放っておくと
    「シャッフルした」と「1枚消えて1枚出た」を同時に語る。
    後者は<b>シャッフルの結果の一部</b>であって別の出来事ではない。**機械判定あり。**
    ★★初版が「今は同居しない」に**寄りかからなかった**のが効いた。
98. **★★★名前は最初の例ではなく性質に付ける。**
    `ManualStartRite` → `ManualRiteKind` ほか、型名から `Start` を外した。
    器が表していたのは「開始シーケンス」ではなく
    <b>「差分では語れない、員数と段取りで語る出来事」</b>である。
    ★2つ目の例が出た時点で名前を直すのがいちばん安い。
    ★配信の JSON は1文字も変わっていない(`rites` / `kind` / `dealt`)。
99. **★★シャッフルは「盤面に何も起きない操作」である。**
    枚数もゾーンも変わらず、非公開の並びだけが変わる。差分からは完全な無変化に見える。
    ★サーバ側の変更は `shuffleDeck` の**返り値1つ**、クライアントは
    `fxEffects` に3行と `SFX_FOR_KIND` に1行だけで済んだ ——
    **演出の関数も音も1つも新しく作っていない**(器を1つにしておいた配当)。
100. **★混ざる所作は揺れだけでは足りない。** 散らばった3枚が束へ戻る絵を重ねる。
    ★傾き(`rotate`)を入れているのは、ゴーストが山札のタイルと同じ寸法だからである。
    ★★<b>同じ絵をマリガンの中の混ざる所作にも当てている</b>(同じ出来事に2つの描き方を作らない)。
101. **★★ピュア・エレメントは中央から飛ぶ。** デッキの外から渡されるカードであり、
    山札から飛ばすのは絵として嘘である。**機械判定あり(座標で測っている)。**
    ★出発点の矩形は「中央に、<b>着地点と同じ寸法で</b>」作る ——
    出発点は「場所」であって「その要素」ではない。
102. **★儀式を作る入口は2つになった**(開始シーケンスと `ManualOperationService`)。
    儀式は<b>操作の種類ごとに生まれる</b>ものであり、1箇所に集めようとすると
    開始シーケンスのサービスがシャッフルを知ることになる。
    ★守るのは「構造を作るなら `ManualLogRite` のファクトリと
    `ManualLogEvent.rite(...)` を通る」までである。

---

## 1. 次の作業の候補(商業水準レビューの優先順位順)

**★レビュー優先順位 1・2 は Batch 33、4・5 は 34、3 は 35、6 は 36、8 は 37、
7 は Batch 38 で消化した。★★残っているのは 9・10・11 と、裁定60 の単独バッチである。**

1. **★★★Batch 39 = デッキメーカーの見た目の統合(裁定60・97 で確定)**(規模 小〜中)
   ★独自 `:root` パレット・Georgia セリフのブランドマーク・独自トースト・独自 `confirm()` を
   手動モードの系統へ寄せる。★★**36・37 で作った規約(モーダルの層・確認・音)が
   出来上がっているので、いま寄せると「寄せる先」が完成している**(38 Q4 の推奨)。
2. 優先順位9: デッキメーカーのマナカーブ + 検証一覧 + autosave(規模 中)
   ★1 と同じファイルを触る。順番を逆にすると二度手間になりうる。
3. **優先順位10: 部屋の永続化**(規模 大。通話前提でセッションが長時間化するため
   タッチ対応より先に着手する価値がある、とレビューは提言している)
4. 優先順位11: Pointer Events 移行 + 盤面スケーリング(タブレット対応)(規模 大)
   ★盤面のキーボード操作(`tabindex`)を再検討するならこのバッチと同時である(裁定66)。
5. テキスト66枚(台帳未転記分)の目視校正(マスター作業)
6. 台帳のVer1.1追随の再計画(通常モード側。ゾンストライカーの誤テキスト含む)
7. `qte-project-reference.md` の更新(1章の実装状況・カードデータの正・裁定記録の反映)

### 完了済みバッチ

17a〜20c(手動モード フェーズ1)/ 21a〜21c(フェーズ2)/ 22(UI8点)/
23(ゲーム開始シーケンス)/ 24(デッキメーカー組み込み)/ 25(盤面のカードフェイス化)/
26(盤面フェイスの仕上げ)/ 27(進化スタックの解体)/ 28(接続の安定化)/
29(配信・描画の軽量化)/ 30(UI指摘9点)/ 31(ハーネス黒背景 hotfix)/
32設計 / 32a / 32b / 32c(= 32系 完了)/ 33(切断UX + 共有導線)/
34(初回ヘルプ + title + ロビー + og:image + hotfix)/
35(勝敗の帯 + ログの決着行 + ターン帯の退役)/
36(Esc + フォーカス管理 + confirm() の置換)/ 37(SFX 8種 + 音の設定パネル)/
**38(ゲーム開始の一括演出 + 開始まわりのSFX 3種)**

---

## 2. コンテキスト効率・発注者とのやりとり

grep 優先でファイルを渡り歩き、全体読み込みは避ける。
判断に迷う点は**まとめて質問する。**1つずつ聞かない。
呼び方は「クロエ」、発注者は「マスター」。会話は日本語カジュアル体、
ドキュメントは通常文体(である調)。

---

## 3. 既知の落とし穴

### サンドボックスの制約

- **★★Maven Central へ到達できない(403)。`mvn test` は納品後にマスターの手元で。**
  `javac -proc:none` による構文走査まではできる(型解決は不可)。
- **★★実サーバを起動できない。したがって Thymeleaf の解決は確認できない。**
  ★テンプレートを変えたら**マスターに実サーバでの確認を必ず依頼すること**。
- **★★codeload の zip URL は 403 で落ちることがある。git clone は通る。**
- **★Playwright は `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers` で通る。**
  ★★同梱 chromium の revision を合わせること(**1194 = playwright@1.56.x**)。
  ★38 の環境にも `playwright@1.56.0` + revision 1194 が入っており、追加導入は不要だった。
  ★グローバル導入されているので `NODE_PATH=/home/claude/.npm-global/lib/node_modules` が要る。
- **★このセッションから GitHub へ push できない。** 納品は zip の添付である。
  ★**マスターは納品 zip を展開したら push まで済ませること。**
- **★★音は聞けない。** ヘッドレス Chromium で `AudioContext` は動くが出力は捨てられる。
  **音色の良し悪しは実機のマスターにしか判断できない。**

### 開始シーケンスの儀式(Batch 38)

- **★★★儀式は差分ではない。** 材料はサーバの `rites`(`ManualStartRiteView`)であり、
  `diffViews` の**先頭**で採る。開始シーケンス中は、そこで差分を打ち切る。
  ★★**関門を `applyView` へ戻さないこと**(裁定82・83)。戻すと儀式が丸ごと消える。
  **機械判定あり**(開始シーケンス中の差分が0件で、かつ儀式が通ることを測っている)。
- **★★儀式は上限(`FX_BULK_LIMIT`)の特例ではない。** 9枚のゴーストは
  **入れ物1つ**(`.manual-fx-rite`)として `fxRegister` に積む。**機械判定あり。**
- **★★★開始画面の保留(`riteHold`)を消さないこと**(裁定87)。
  消すと儀式がモーダルの下に隠れて1pxも見えない。**機械判定あり。**
  ★保留のゲートは **`fxAllowed()`** である。`fxDiffNeeded()` にすると
  **演出を切っている人まで待たされる**(裁定89)。**機械判定あり。**
- **★保留するのは「開く」だけ**(裁定88)。`syncMulliganOverlay(start, holding)` の第2引数。
- **★儀式のゴーストは最初に全部作る**(裁定90)。飛ばす時刻だけをずらすこと。
- **★ダイスの文言はサーバの `label`。** 対戦は「選択権」、ソロは「先攻」で意味が違う。
  **機械判定あり。**
- **★シャッフルの揺れは実要素(山札のアンカー)に当たる。** `onStop` で必ず剥がすこと。
  **機械判定あり。** ★reduced-motion では CSS 側でも無効化してある。
- **★儀式が運ぶのは席と枚数だけである。** カードの中身は構造上持てない
  (`ManualStartDeal`)。ゴーストは `fxGhost(from, null)` で作る。**機械判定あり。**
- **★`ManualLogEvent.startRite(...)` を呼ぶのは `ManualStartService.startLog` の1箇所である**
  (裁定85)。`plain(START, …)` は通る —— 構造を持たない START 行は正当である。
- **★★★儀式が語る配信では差分を語らない**(裁定93)。`diffViews` の関門の
  `out.rite ||` を消さないこと。**機械判定あり。**
- **★★シャッフルは盤面に何も起こさない操作である**(裁定99)。
  演出と音の材料は `ManualOperationService.shuffleDeck` が返す儀式だけである。
- **★混ざる所作の絵はシャッフルとマリガンで共通である**(裁定100)。片方だけ変えないこと。
- **★★ピュア・エレメントは中央から飛ぶ**(裁定101)。**機械判定あり(座標)。**
- **★飛翔が1枚も無い儀式にも長さが要る**(`fxRiteDuration` の `shuffleAt` 加算)。
  忘れると出た瞬間に消える。**機械判定あり。**
- **★目視は `RITE=deal AT=430 node verify/shot.js`**
  (`dice` / `deal` / `mulligan` / **`shuffle`** / **`pure`**)。

### 効果音(Batch 37・38)

- **★★★発火点を増やさないこと**(裁定68)。音を足したいときは
  **`SFX_FOR_KIND` の表**を直す。取り付け点は `fxSpawn` の1行と確認モーダルの [実行] の2つだけ。
  ★★**38 の3種も1行も足していない。**
- **★★演出の種類(`fx.kind`)を足したら、音の表にも足すこと**(裁定72)。**機械判定あり。**
- **★★★`prefers-reduced-motion` を音のゲートにしないこと**(裁定69)。**機械判定あり。**
- **★1配信につき1音である**(裁定70)。優先順位は `SFX_PRIORITY` の1箇所。**機械判定あり。**
  ★★順序の理由は**珍しさ1本**である(裁定71)。
- **★★unlock を特定の操作へ結び直さないこと**(裁定73)。**機械判定あり。**
- **★音の調整は `SFX_SPECS` の表だけを触ること。** 現在 **11種**
  (draw / place / tap / flip / lpDown / lpUp / decisive / commit / **dice / deal / shuffle**)。
- **★音声ファイルを同梱しないこと**(裁定74)。版数の系統を4つ目にしない。
- **★★`rattle` 型は粒が複数だが「1つの音」である**(裁定75)。
  儀式の音は**ノード数ではなく「鳴ったか」**で測る。
- **★設定の保存は `qte-manual-sound`(部屋に紐づかない)。** localStorage は握りつぶす。
- **★状態行の警告色は「鳴らない理由があるとき」だけ**(裁定76)。**機械判定あり。**

### モーダル・Esc・フォーカス(Batch 36)

- **★★★画面を覆うものは `modalStack` 1本で持つ**(裁定47)。
- **★★モーダルの開閉は `openInfoModal` / `closeInfoModal` を通す**(裁定51)。**機械判定あり。**
  ★★37 の設定パネルも 38 も、この規約に載っているだけで × も Esc も効いている。
- **★★★Esc を下の層へ落とさないこと**(裁定49)。**機械判定あり。**
- **★出口が無いもの**: 開始シーケンスの2つ(start-method / start-order)と
  マリガン、および入室前の席選択ゲート(裁定34)。
- **★★破壊的操作に `confirm()` を書かない**(裁定53)。**機械判定あり。**
- **★★確認モーダルの z-index は 1965**(裁定56)。機械判定は**座標で**行っている。
- **★★★`position: sticky` を負マージンで持ち上げない**(裁定57)。

### 勝敗の宣言・決着(Batch 35)

- **★★★宣言は盤面に痕跡を残さない操作である。** 配信は `declarations`(構造)を別に運ぶ。
- **★★宣言のログは `ManualLogEvent.declaration(...)` でしか作れない**(裁定41)。
- **★★`declarations` と `rites` はログの末尾60行と同じ1周で作る**(裁定42)。
- **★決着の帯は `seq` が増えたときだけ出す**(裁定43)。**機械判定あり。**
- **★★ターン帯は退役した(裁定17)。復活させないこと。機械判定あり。**
- **★決着後も盤面はロックしない**(裁定44)。
- **★目視は `DECLARE=WIN … node verify/shot.js`。**

### メタ情報・設定(Batch 33・34)

- **★★`og:image` の絶対URLは設定 `qte.public-base-url` から来る(裁定24・27)。**
  組み立ては `web/PublicUrlAdvice.java` の1箇所。**機械判定あり。**
- **★アイコンと OGP 画像は `tools/make_brand_assets.js` が生成する。**
- **★部屋URLの組み立ては `roomShareUrl()` の1箇所。**
- **★エラーページ(`templates/error.html`)に CDN を書かないこと(裁定21・機械判定あり)。**

### ツールチップ・オンボーディング(Batch 34)

- **★★`title` の文言は `TITLE_HINTS` / `tileTitle()` 経由で書く(裁定29)。機械判定あり。**
- **★裏向きのカードの名前を `title` に出さない(裁定30)。**
- **★fx層(演出のゴースト)には `title` を付けない。**
- **★★初回ヘルプの既定はハーネスでは「見た人」である。**

### 見た目の統一(Batch 34)

- **★★ロビーとカード一覧は黒背景である。** 盤面と同じ `#212529` / `#f8f9fa`。
- **★★`verify/build_harness.py` の `lobby_stub` も黒である。片方だけ直さないこと。**
- **★デッキメーカーは第3のデザイン系統のまま(裁定33)。★裁定60 で単独バッチが決まっている。**

### 切断・接続(Batch 33)

- **★★★操作を止めているのは `send()` のガードである(裁定18)。**
- **★★`send()` は真偽を返す。**
- **★切断の案内と部屋消失の案内は排他である(裁定19)。**
- **★z-index の並び: fx層 1030 < 軌跡 1035 < ゾーンの帯 1040 < 全面表示 1050 <
  ポップオーバー 1055 < トースト 1058 < 席選択ゲート 1060 < 開始の帯 1900 <
  開始モーダル 1950/1960 < 確認モーダル 1965 < 切断オーバーレイ 1970 < 接続の帯 1975。**
  **★★`.info-modal` は z-index 1000 で、いちばん下である。**
  ★★**決着の帯も開始の儀式も fx層(1030)にいる。**
  ★★★**この並びを演出の都合で崩さないこと**(裁定87)。38 は時間で解いている。
- **★★検証の途中で `showRoomLostFatal` を通ると、以後は接続が落ちたままになる。**

### カードフェイスの質感(Batch 32c)

- **★★★フェイス(`.mcard-*`)に transform / filter を足さないこと。**(項目63 が番人)
- **★フェイスに擬似要素を足さないこと。**
- **★★光沢の形は `--mc-gloss` の1箇所、強さは各所の `--mc-sheen` だけ。**
- **★フェイスはコントラストの機械判定の対象外である。そのぶん目視が必須。**

### 演出・fx層(Batch 32a・32b・35・37・38)

- **★★演出のイベント検出は「ビューの差分」である。** ★★材料にしてよいのは
  書式ではなく**構造**である(裁定40)。★★**37 の音も 38 の儀式も同じ列を読んでいる。**
- **★★「窓」のゾーン(counts > 届いた配列の長さ)では出現・消滅を出さない。**
- **★★配信の反映は `applyView` 1本である。** 差分は再描画の**前**に採る。
  ★★差分を採るかどうかのゲートは `fxDiffNeeded()`(37)。
  ★★★**開始シーケンスの関門は `diffViews` の中にある**(38・裁定83)。
- **★★fx層は `position: fixed` で `#manual-root` の外にある。**
  **新しい文字を作ったら必ず `verify.js` の `targets` に足すこと。**
  (34 で `.info-modal-x`、35 で `.manual-fx-declare` と `.manual-log-decisive`、
  36 で `#confirm-modal-*`、37 で音の設定パネルの文字5件、
  **38 で `.manual-fx-dice-chip` と `.manual-fx-dice-note`**)
- **★★★遷移のクラスは「最終状態」と一緒に当てる。旧状態と一緒に当てない。**
- **★★`fxSpawn` の確定は `document.documentElement.offsetHeight` である。**
  ★★**確定が効くのは、その時点でDOMに在るものだけ**である(裁定90)。
- **★★実要素へ当てる演出は `onStop` で必ずクラスを剥がす。**(38 の `.manual-fx-shuffle` も)
- **★動かすのは transform と opacity(と 32b の filter)だけ。**
- **★演出の時間・上限は定数1箇所**(`FX_*` / `FX_LIMIT` / `FX_BULK_LIMIT` / **`FX_RITE_*`**)。
  ★★**音のほうは `SFX_SPECS` / `SFX_PRIORITY` の2つが正である。**
- **★`fxEnabled` は検証の「わざと壊す」入口である。消さないこと(裁定78)。**

### 性能・接続(Batch 28・29)

- **★★1配信は27KBで頭打ち(Batch 29 で解消)。**
  (a) **★★山札は「最上段の1枚」しか届かない。**
  (b) **★★ログは末尾60行しか届かない。** ★35 から宣言、★38 から儀式も構造で届く。
  (c) **★★描画関数の中で幅を実測しないこと。**
  (d) **★ログの差分追記は seq の連続性が前提。**
  (e) 非同期で取りに行くものは通し番号で古い応答を捨てる(`zoneFetchSeq`)。
- **★★`TaskScheduler` 型のBeanを増やさないこと。**
- **★★無言の `location.reload()` を書かないこと。**
- **★★部屋はメモリ上にしか無い。** 接続の寿命がゲームの寿命である。
- **★静的ファイルの版数は `manual-battle.js` / `battle.js` / `battle.css` の3系統ある。**
  現在 **js v=30 / battle.js v=14 / css v=38**。
  ★★**4つ目の系統を増やさないこと**(裁定74)。
  ★★**`battle.css` の版数は全テンプレートで揃える。機械判定あり。**
  ★★版数を上げたら `verify/build_harness.py` の置換文字列も同時に直すこと。

### カードデータ・デッキ(Batch 24)

- **★★`manual-cards.json` が手動モードとデッキメーカーの共通の正である。**
- **★★デッキの標準形式は JSON。** 判別はサーバが先頭バイト(`PK`)で行う。
- **★同名無制限はコードに書かない。**(`unlimitedCopies`)
- **★★Thymeleafテンプレートのインライン式に注意(24 hotfixで踏んだ)。**

### 進化スタック(Batch 27)

- **★★進化スタックは FIELD にしか存在しない(不変条件)。**
- **★★1つの ref から複数枚が入りうる。**
- **★★「状態にはあるが画面のどこからも触れない」は消失と同じ扱いである。**

### 手動モードの原則(要点)

- **★★手動モードで「判断」を実装しない — 例外はウェポン付け替え(20b)と
  ゲーム開始前処理(23)だけ。** 演出も音も同じ原則に服する。
  ★★**38 の儀式も「何枚動いたか」を描いているだけである**(なぜ配ったかは判断していない)。
- **★★共有ゾーン(PLAY / REVEAL)は席に属さない。`seatId` の無条件参照を書かない。**
- **★★スナップショットに項目を足したら `copy()` を直す。**
- **★★公開範囲・押せるか、の判定を2箇所に書かない。**
- **★★操作の型は `ManualOperationService.apply` が握る。** リセットは絶対に止めない。
- **★MPを直接増減する操作を作らない。カードIDをJavaにリテラルで書かない。**

### UI の落とし穴(要点)

- **静的ファイルを変えたらキャッシュバスティングを上げ、
  `verify/build_harness.py` の置換文字列も同時に直す。**
- **★カードフェイスは `.mcard-*`(battle.css 末尾・手動モード専用)。**
- **★★パイルの最上段は「山札だけ先頭・それ以外は末尾」。**(`pileTopCard`)
- **★★★タップ表現は場のタイル=減光+バッジ、マナ=回転で、意図的に異なる。**
- **★★回転する要素の幅は `applyManaOverlap` が確保している。**
- **★★高さを画面から逆算するときは「入れ物のパディング」まで引くこと。**
- **★盤面のラベルはコントラスト比 4.5:1 以上であることが機械判定される。**
  新しいラベルは `verify.js` の `targets` に足すこと。フェイスだけは対象外(目視)。
- **★ログのカード名リンクはサーバの `《名前》` 表記に依存している。**
- **★★レイアウト計算で `getBoundingClientRect().top` を素で使わない。**
- **★★差分チップ `.manual-stat-chip` は廃止済み。復活させないこと。**
- **★★リーダーのフェイス化は自席のみ。**
- **★★★描画の席と送信の席を混ぜない。**
- **★★クリック規約: 左=見る / 右=動かす。新しい `contextmenu` には必ず
  `e.preventDefault()`。★この規約は業界慣習と逆であり、34 で `title` にも書いてある。**
- **★★対戦部屋では非公開ゾーンが `zones` にキーごと現れない。`zoneCount` を使う。**
- **★★flex: 可変幅の列に `min-width:0`。**
- **★★D&D検証は合成DragEventではなく実マウス(`page.mouse`)で。**
- **★`battle.css` は共有ファイル。** ★★★読み込むのは `battle.html` と
  `manual-battle.html` の2枚だけであり、どちらも黒背景である(裁定59)。
  **白い画面から `battle.css` を読み込まないこと。機械判定あり。**
- **★検証ハーネス: `python3 verify/build_harness.py && node verify/verify.js`
  (416項目)。UIを足したら項目を足す。**

### その他

- occupantId は localStorage(`qte-manual-occupant-{roomId}`)。切断猶予5分は
  `ManualCleanupScheduler` と `ManualViewBuilder` の2箇所にある。片方だけ変えない。
  **★34 で `qte-manual-help-seen`、★37 で `qte-manual-sound` が加わった。**
- 無人部屋は5分で自動削除。ゲームの正式名称は「クイン・タブーエレメント」。
- `tools/check_structure.py` は `List.<String>of()` を誤検出(一時変数に受けて回避)。
- `tools/check_records.py` の既知の誤検出3件(GameActions:775 / GameService:1144 /
  CardEffectRegistry:748)。
- **★`fxRunning` などの `const` はグローバルオブジェクトに載らない。**
  `page.evaluate(() => window.fxRunning.size)` は落ちる(裸の `fxRunning` なら通る。38)。
- **★検証項目54(タップ表現の非対称)は 120ms の観測窓に依存しており、まれに揺れる。**

### ★★検証環境と実環境の差

- **★★★盤面もロビーも黒背景である。** 白いページは通常モードの lobby / cards のみ。
- **★★ハーネスは Bootstrap を落として代替を当てている。**
  **★★★スタブに書いてよいのは、落とした CDN の代わりだけである(裁定39)。**
- **★★判定を入れたら、判定が落ちることを1回確かめること。**
  ★36 では12通り、★37 でも12通り、★**38 では13通り**の「わざと壊す」確認を行った。
  **★★そのうちいくつかは「上位の項目が素通りする」ことを示している。**
  ★38 の1件目(儀式の採取を後ろへ移す)は**8項目**を落とす —— 下位の項目が
  「入口が生きていること」に寄りかかっていることの証拠である。
- **★★「クラスが付いたか」を見る検証は、演出の検証にならない。**
  **★★測るのは「式」ではなく「結果の座標」である。**
- **★★`waitForFunction` が投げると検証スクリプトごと死ぬ。** try/catch で FAIL に。
- **★★機械で測れないものを機械で測ろうとしない。測れる決めごとのほうを測る。**
- **★★検証の項目は状態を引き継ぐ**(38 で踏んだ)。末尾に足すときは、
  そこまでで何が起きているかを読むこと(37 の最後でミュート + 再読込されている)。
- **★★イベントは実際の入口から起こす。**
- **★半透明の背景は下地で意味が変わる。**
- **★`color-mix()` の計算結果は `color(srgb r g b / a)` 形式(0〜1)で返る。**

## 4. デリバリー形式

zip(変更・新規ファイルのみ)+ `batchNN-design-notes.md` + ハンドオフ更新。
ドキュメントはチャットにファイルとして添付する。
パッケージング前に必ず実行:

```bash
python3 tools/check_structure.py src/main/java
python3 tools/check_all.py .
python3 tools/check_records.py src/main/java
python3 tools/check_undeclared.py src/main/resources/static/js/*.js
node --check src/main/resources/static/js/manual-battle.js
NODE_PATH=/home/claude/.npm-global/lib/node_modules \
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers \
  python3 verify/build_harness.py && node verify/verify.js
javac -proc:none -d /tmp/jc $(find src/main/java src/test/java -name '*.java') 2>&1 \
  | grep error: | grep -viE "cannot find symbol|package .* does not exist"
```

★フェイス(`.mcard-*`)に触れたバッチでは、加えて**目視**を必ず行う:

```bash
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers OUT=/tmp/faces.png node verify/faceshot.js
```

★演出に触れたバッチでは:

```bash
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers DECLARE=WIN OUT=/tmp/declare.png node verify/shot.js
# ★Batch 38 から: 開始の儀式
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers RITE=dice     AT=560 OUT=/tmp/dice.png node verify/shot.js
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers RITE=deal     AT=430 OUT=/tmp/deal.png node verify/shot.js
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers RITE=mulligan AT=760 OUT=/tmp/mull.png node verify/shot.js
```

★ブランド資産(アイコン・OGP画像)を変えたバッチでは:

```bash
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node tools/make_brand_assets.js
```

---

## 5. チャット開始テンプレート

★★**Batch 39 の範囲は裁定97(38 Q4 = a)で確定している** —— デッキメーカーの見た目の統合(裁定60)。

```
QTE Battle の開発を継続する。Batch 39(デッキメーカーの見た目の統合。裁定60)を行う。

1. プロジェクトナレッジ内の `qte-project-reference.md` を読む。
2. プロジェクトナレッジ内の `claude/qte-handoff-v44.md` を読む。
   3章「既知の落とし穴」と「カードデータの正」は必読。
3. `notes/commercial-quality-review-2026-08-11.md` を読む(今後の優先順位の正)。
   `notes/batch24-design-notes.md` と `notes/deckmaker-v2-design-notes.md`
   (デッキメーカーの正)も読む。
   ★寄せる先の規約は `notes/batch36-design-notes.md`(モーダル・確認)と
   `notes/batch37-design-notes.md`(音)にある。
4. git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git
5. 38 の反映を「24〜38 の確認項目」の 38 の行で照合し、
   verify 424/424 を確認する。反映されていなければ止めて報告する。

Batch 39 の範囲(裁定60):
- デッキメーカーの独自 `:root` パレット・Georgia セリフのブランドマーク・
  独自トースト・独自 `confirm()` を手動モードの系統へ寄せる
- ※`confirm()` は裁定53 のとおり `askConfirm` 相当へ。モーダルは裁定47・51 の規約に載せる
- ※文明色の定義が3箇所に重複している(レビュー B-2)。1箇所にできるか検討する
- ※新しい文字を作るなら verify.js の targets へ足すこと

制約:
- サンドボックスから Maven Central へ到達できない。`mvn test` は納品後にこちらで
  走らせるので必ず依頼すること。機械チェック(tools/ と verify/)は実行すること。
  ★playwright は同梱 chromium の revision 1194(playwright@1.56.x)。
   グローバル導入なので NODE_PATH=/home/claude/.npm-global/lib/node_modules が要る。
- テンプレート(Thymeleaf)を変えたら、実サーバでの確認をこちらに依頼すること。
- 納品形式はハンドオフ4章のとおり。ドキュメントはチャットにファイルとして添付。
- 判断に迷う点はまとめて質問する。1バッチ1チャット。

呼び方は「クロエ」、こちらの呼び方は「マスター」で。会話は日本語カジュアル体、
ドキュメントは通常文体(である調)で。
```

---

## 6. 38 完了時点の積み残し

### マスターにお願いすること(Batch 38 分)

- **★★★先に納品した zip(追補前)を展開している場合は、次の4ファイルを削除すること。**
  型の名前を一般化した(裁定98)ので、古い名前のファイルが残ると<b>使われない死んだ型</b>になる。
  ★ビルドは通ってしまうので、消し忘れても気づけない。
  - `src/main/java/com/example/qte/manual/ManualStartRite.java`
  - `src/main/java/com/example/qte/manual/ManualStartDeal.java`
  - `src/main/java/com/example/qte/manual/ManualLogStartRite.java`
  - `src/main/java/com/example/qte/manual/view/ManualStartRiteView.java`
- **★納品 zip を展開したら GitHub へ push すること。**
- **★★`mvn test` を走らせること。** 38 は **Java を変更している**
  (`ManualLogEvent` に構成要素が1つ増えた)。JUnit も +7件足してある。
  ★34・35 のぶんがまだなら、この機会にまとめて。
- **★実サーバでテンプレートが開くことの確認**(版数しか変えていないが念のため)。
- **★★開始シーケンスを一度通してほしい**(38 設計解説 9-2 の1)。
  ★**待たされる感じが強すぎないか**が最大の論点である(ソロのランダムで最長 1.7秒ほど)。
- **★配りのゴーストの大きさ**(9枚同時。既存のドロー演出と同じ寸法の決め方)。
- **★★音3種の試聴**(`shuffle` / `dice` / `deal`)。
  ★とくに `deal` と既存の `draw` が聞き分けられるか。直すのは `SFX_SPECS` の表1つ。
- **★ダイスの帯の読みやすさ / マリガンの往復が読めるか。**
- **★演出を切った環境で開始画面が待たされないこと。**
- ★**質問4件は回答済みである**(裁定94〜97)。追加でお願いすることは無い。
- **★★山札の [シャッフル] を押してみてほしい**(追補)。今まで手応えが1つも無かった場所である。
- **★ピュア・エレメントが中央から後攻の手札へ飛ぶこと**(追補)。

### 継続中の積み残し

- **35 の実機確認3点**が未報告。
- **36 の実機確認6点**が未報告。
- **37 の実機確認・試聴6点**が未報告(音8種の試聴を含む)。
- **部屋の永続化**(未実施。レビュー優先順位10)。
- **タッチ非対応**(レビュー S-1。優先順位11)。
- ~~アクセシビリティの残り~~ → **裁定66 で「当面やらない」と確定。積み残しではない。**
- **デッキメーカーの見た目が第3系統のまま**(裁定33)。
  ★★**単独バッチで統合する裁定が出ている(裁定60)。次バッチの推奨である。**
- ~~ゲーム開始・シャッフルの演出と音(優先順位7)~~ → **★★Batch 38 で完了。**
- ~~ピュア・エレメントの配布は演出していない~~ → **★裁定94 で実施済み(38 追補)。**
- **リセット・すべてアンタップは今も無演出である**(★裁定95 で「足さない」と確定。積み残しではない)。
- 盤面DOMの差分更新(低優先で据え置き)。
- 演出の速さ・強さ、フェイスの質感の強さの実機確認(マスター)。
- テキスト66枚(台帳未転記分)の目視校正。
- 台帳・通常モードのVer1.1追随(要再計画)。
- 入室前の席選択で「切断中」を表示できない(21bから継続)。
- 拡大画像パネル204px幅 / 相手上段マナ名8px(継続・据え置き)。
- `qte-project-reference.md` 1章の実装状況が古い。

**完了**: Batch 32 系 / 33(優先順位1・2)/ 34(4・5 + hotfix)/ 35(3 + 裁定17)/
36(6 + 34 hotfix の修正)/ 37(8)/ **38(7)**。
**★★レビューの優先順位で残っているのは 9・10・11 だけである。**
