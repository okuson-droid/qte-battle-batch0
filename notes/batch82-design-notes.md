# Batch 82 設計解説 —— 壊し検証を複製の上で2並列に(候補 P)

対象: 壊し検証の**回し方**そのもの(`tools/break_check_runner.py` を新設)。

発端はマスターの「検証に時間がかかりすぎている」であり、Batch 81 がそれを受けて実測し、
候補 P として書き残したものである。

★★★**このバッチは Java・カード定義・`battle.js`・`battle.css`・テンプレートを1文字も触っていない。**
変えたのは `tools/` と、★**`verify/verify.js` の閾値1つ**である(9章)——
★<b>後者は 82 が並列にしたことで初めて見つかった、余裕 5ms の番人である</b>。
★★**版数は上げていない**(7-5)—— <b>静的ファイルを1つも触っていないからである</b>
(`verify/verify.js` は配信されない)。

---

## 0. ★★★母集団(作業を始める前に固定した)

★**「壊し検証が遅い」を母集団にしない。**それは症状である(79 の教訓)。
数えるのは「**壊し検証を1回転させるとき、何がどこで何秒かかり、そのあいだ何が危険にさらされるか**」
の全体である。

★★母集団を **3段**で取った(80 の教訓)——
**A: 何が観測できるか** / **B: 何を作れるか** / **C: それがどこへ着地できるか**。

### 0-1. ★★★母集団A —— 実測(この箱は `nproc` = 2)

| 測ったもの | 実測 |
|---|---|
| リポジトリの複製(43MB・`target` 込み・`cp -a`) | **0.17秒/本** |
| `verify` 1回転(直列) | **82秒** |
| JUnit 1クラス(テンプレートだけ・再コンパイル無し) | **10秒** |
| JUnit 1クラス(Java 再コンパイル込み) | **22秒** |
| `verify` 2並列(複製2本) | **94.5秒/2本** —— ★**1.74倍速**・両方 793/793 |
| ★**JUnit 2並列(複製2本)** | **44.6秒/2本** —— ★★★**1.00倍速(1秒も速くならない)** |
| ★**`verify` と JUnit の混走** | **87.7秒** —— ★★**verify は 793/793 のまま** |
| `verify` 4並列(複製4本) | 138秒/4本 —— ★★★**3本が赤**(0-3) |

★81 の壊し検証(21軸)の内訳は **JUnit 9軸・verify 12軸**である。
直列の見積もりは **12×82 + 7×22 + 2×10 + 最後の1回転 82 = 約21分**であり、
81 の報告(verify 12軸で16分)と一致する。

### 0-2. ★★★母集団A で出たもの(1) —— JUnit は並列にしても速くならない

★★<b>これは着手前の見立てと違った</b>。
「複製にして2並列にすれば全部が1.8倍になる」と考えていたが、**JUnit は 1.00倍だった**。

★理由は明快である —— <b>`verify` は1回転 82秒のうち 26秒が固定待ち(眠っている)</b>のに対し、
<b>`mvn` は最初から最後まで2コアを使い切っている</b>。
★★<b>眠っている番人は隣に仕事を入れられるが、走っている番人には入れられない。</b>

> ★★★<b>並列で得をするのは「待っている番人」だけである。</b>

### 0-3. ★★★母集団A で出たもの(2) —— 天井は「時間で待っている番人」が決めている

4並列にすると、**793件のうち3本だけ**が赤くなった。**毎回この2種類である**:

- 「★★タップ表現の非対称: 場のタイルは filter(減光)だけで回らない」
- 「★自席マナのタップは transform(回転)の遷移が実際に走る」

★<b>どちらも「アニメーションを時間で捕まえる」項目である</b> ——
★★<b>実装の性質ではなく、番人の書き方が並列度の天井を決めている</b>(81 の追補と同じ結論)。

> ★★★<b>固定待ちを事実待ちへ移す工事(設計判断61)は、速さの工事でもある。</b>
> ★<b>速くなるのは副産物で、本当の見返りは「並列度を上げられるようになる」ことである。</b>

### 0-4. ★★★母集団A で出たもの(3) —— 本当の見返りは速さではない

81 までの `batchNN_break_check.py` は、**本体を直接書き換えて `finally` で書き戻す**形だった。

★<b>これは殺されると壊したまま残る</b> —— **70 で実際に起き、77・79 でも殺された**。
★★81 は「5回に分けて回し、前後で `git diff --stat` を突き合わせること」という
**運用**でしのいでいた。

> ★★★<b>複製の上で壊せば、この事故は運用ではなく構造で消える。</b>
> <b>本体に `open(..., "w")` を1度もしないからである。</b>

★★★**82 は、このバッチの作業中に自分でそれを踏んで証明した** ——
<b>81 の21軸を通した1回目が10分で殺された</b>。
★<b>殺された直後の `git status` は完全にきれいで、`git diff --stat` は空だった</b>。
★★<b>81 までの形なら、そこには壊れた `battle.js` が残っていた。</b>

### 0-5. ★母集団B —— 何を作れるか

| # | 作るもの | 82 で置くか |
|---|---|---|
| B1 | 複製の上で壊す共通ランナー | ★**置く** |
| B2 | 2並列の待ち行列 | ★**置く** |
| B3 | 過去の `batchNN_break_check.py` をそのまま回す口(`--batch NN`) | ★**置く** |
| B4 | ランナー自身の番人(`--self-test`) | ★**置く**(3章) |
| B5 | 殺されたときの作業場の後始末 | ★**置く**(4章・作業中に必要だと分かった) |
| B6 | 並列度を箱に合わせて自動で決める | ★置かない(0-7) |
| B7 | 固定待ちの番人を事実待ちへ移す | ★置かない(0-7・候補 U2 の一族) |
| B8 | JUnit を全件1回で回して軸ごとに読み分ける | ★置かない(0-7) |

### 0-6. ★★★母集団C —— 番人はどこに着地できるか

★**番人を置く場所を選ぶ前に、そこまで届くかを確かめる**(70〜81 の教訓)。

| 置き場所 | 届くか | 判定 |
|---|---|---|
| JUnit | ★**届かない** —— Java から Python の道具は見えない | ★不可 |
| `verify/verify.js` | ★**届かない** —— あちらはブラウザの中の話である | ★不可 |
| `tools/check_*.py` | ★届く。ただし<b>いまの `check_*` はどれも「本番のコードを読む」道具である</b> | ★筋が違う |
| ★**ランナー自身の `--self-test`** | ★★**ここしか無い** | ★★★**採用** |

★★★<b>そのうえで「届く」を最後まで通した</b> ——
<b>`tools/README.md` の納品前チェックに1行足した</b>。
★<b>置いただけで誰も回さない番人は、番人ではない</b>(70 の教訓)。

### 0-7. ★母集団の外だと決めたもの(理由つき・設計判断56)

- **並列度を自動で決める(B6)** …… ★`nproc` を読んで並列度を決めると、
  <b>速い箱で勝手に4並列になり、番人が嘘をつく</b>。
  ★★<b>天井を決めているのは箱ではなく番人の書き方である</b>(0-3)——
  <b>箱を測っても、番人が直るわけではない</b>。★**2で固定し、上げるときは人が測って上げる。**
- **固定待ちを事実待ちへ移す(B7)** …… ★これは<b>候補 U2 と同じ一族の、もっと大きい工事</b>である
  (`waitForTimeout` は 248箇所)。★★<b>82 の中でやると、速さの工事と番人の工事が混ざって
  どちらが効いたか読めなくなる</b>。★**別のバッチである。**
- **JUnit を全件1回で回す(B8)** …… ★1クラスだけ回すのは<b>軸ごとに独立させるため</b>である。
  ★★<b>全件1回にすると、1つの改変が他の軸の照合先まで赤くする</b> ——
  <b>「軸ごとに1件ずつ当てる」(57 の教訓)が壊れる。</b>
- **`manual-battle.js`・カード定義・Java・CSS** …… ★**1文字も触っていない。**

---

## 1. ★★★何を作ったか

```
tools/break_check_runner.py
  ├ make_workspace()        …… ★作業場は本体の木の<b>外</b>に作る(番人 S8)
  ├ clone_into()            …… ★複製は cp -a(0.17秒/本)。★★ハードリンクにしない
  ├ write_into()            …… ★★★複製の外へは書かせない(番人 S7)
  ├ run_junit() / run_verify()
  │                         …… ★★どちらも <b>cwd は複製</b>(番人 S9・S11・S12)
  ├ junit_report()          …… ★報告書も<b>複製から</b>読む(番人 S10)
  ├ _Gauge                  …… ★同時に走った最大数を<b>数える</b>(番人 S6)
  ├ install_signal_traps()  …… ★★殺されても作業場を残さない(番人 S14・4章)
  ├ run()                   …… ★2本の待ち行列。★★終わったら本体と複製の両方を照合する
  └ self_test()             …… ★★★このランナー自身の番人(15本)
```

### 1-1. ★★過去のスクリプトは1文字も書き換えていない

`--batch NN` は `tools/batchNN_break_check.py` を **読み込むだけ**である ——
あちらの `CASES` と `EXPECTED_NG` をそのまま使う。

★<b>だから 81 の21軸は、書き換えなしでそのまま新しいランナーに載った</b>(6-3)。
★★<b>50〜81 のスクリプトも同じ表の形をしているので、同じように回せる。</b>

### 1-2. ★★★全軸を複製の上で回す(JUnit も)

★<b>JUnit は速くならない</b>(0-2)。★★<b>それでも複製の上で回す。</b>

理由は2つである:

1. ★★★<b>速さではなく安全のためである</b> —— <b>本体を汚さない</b>のは JUnit 軸でも同じ得である。
   ★81 の軸3〜9 は `GameViewBuilder` / `CardEffectRegistry` / `GameActions` を壊す ——
   <b>ここで殺されたら、壊れたサーバが本体に残る</b>。
2. ★★<b>規則が1つで済む</b> —— 「verify だけ複製へ」にすると、
   <b>壊し方の規則が種別ごとに2つになる</b>。
   ★<b>「同じ規則を入口の数だけ書き忘れる」形を、自分から作りに行くことになる</b>(76・77・79〜81)。

★★★<b>混走(verify + mvn)でも verify は 793/793 のままだった</b>ので、
<b>軸を種別ごとに固めず、来た順に2つの待ち行列へ流している</b>。

---

## 2. ★★★測ったら、着手前の見立てが1つ変わった(81 の教訓の実演)

★**着手前に実寸を測ってマスターへ出した**(80・81 の教訓)。
そのとき出た数字のうち、**見立てと違ったのは JUnit の 1.00倍**である。

★★<b>これが「対象範囲をどこまでにするか」の問いを生んだ</b> ——
<b>速くならないものまで複製にするか</b>。
★実寸(1.00倍・複製 0.17秒)を出したうえで、マスターが **「全軸を複製へ」** を選んだ。

> ★★★<b>実寸を出すと、工事の順番だけでなく答えそのものが変わることがある</b>(81 の教訓)——
> ★<b>82 では「答え」ではなく<b>工事の範囲</b>が変わった。</b>
> <b>速さの数字だけ見れば「verify だけ」が正しく、安全まで数えると「全軸」が正しい。</b>

---

## 3. ★★★番人をどこに置いたか(設計判断45)

★**ランナーは Python の道具であり、JUnit にも verify にも照合先が無い**(0-6)。
★★<b>だから番人はランナー自身の中にしか置けない</b> —— `--self-test`(**15本**)。

| 番人 | 何を測るか |
|---|---|
| **S1** | ★★★本体の対象ファイルは**一度も書き換えられない**(★**更新時刻まで見る**) |
| **S2** | 壊しは複製に載る(壊した軸は OK になる) |
| **S3** | 各軸のあと複製は元へ戻る |
| **S4** | 一致が1箇所でなければ SETUP-NG |
| **S5** | ★verify の照合先が1件も走らなければ EMPTY |
| **S5b** | ★JUnit の照合先が1件も走らなければ EMPTY(★★**入口が2つあるので番人も2本**) |
| **S6** | ★★★同時に走るワーカーは**2を超えない** |
| **S7** | 複製の外へは書けない(書き込みのガード) |
| **S8** | 作業場は本体の木の外にある |
| **S9** | JUnit は複製の中で走る |
| **S10** | JUnit の報告書は複製から読む |
| **S11** | verify のハーネス生成は複製の中で走る |
| **S12** | verify の実行は複製の中で走る |
| **S13** | ワーカーは起こした数だけ**別々の複製で**走る |
| **S14** | ★★殺されても作業場を残さない(割り込みの罠・4章) |

### 3-1. ★★★S1 が更新時刻まで見る理由

★<b>「壊して書き戻した」は「触っていない」ではない。</b>

中身だけを見る番人だと、<b>本体を壊してから書き戻す形が素通りする</b> ——
★★<b>それこそが 70 の事故(殺されて壊したまま残る)の正体である</b>。
<b>殺されなければ中身は戻るので、中身を見るだけでは危険が見えない。</b>

★<b>だから S1 は `st_mtime_ns` を控えて突き合わせる</b> ——
<b>1バイトでも書いたら、戻しても赤くなる</b>。

### 3-2. ★★S6 が「2」をここに書いている理由(設計判断41)

★`WORKERS` を読み直す番人にすると、<b>`WORKERS` を上げた瞬間に番人も一緒に上がる</b> ——
**何も測っていない緑になる。**

★★<b>だから番人の側に 2 と書いた</b> —— <b>並列度を上げた人が、ここで赤を見る</b>。
★<b>上げてよいのは、0-3 の2項目を事実待ちへ移したあとである。</b>

### 3-3. ★★★EMPTY の入口は2つある

`verify_verdict` と `junit_verdict` は、**別々に** EMPTY を返す。
★<b>規則が n 入口ぶんあるなら、番人も n 入口ぶん要る</b>(77・79・80・81 の教訓)——
★★<b>だから S5 と S5b の2本がある</b>。

---

## 4. ★★★直したあとに、何が新しく起きたか(78 の教訓)

★**本体はもう汚れない。**★★**その代わり、作業場の後始末が新しく要るようになった。**

★★★**82 はこれを机上ではなく実地で踏んだ** ——
<b>81 の21軸を通した1回目が10分で殺され、43MB×2 の複製が `/tmp` に残った</b>。

| | 81 まで | ★82(罠なし) | ★★82(罠あり) |
|---|---|---|---|
| 殺されたとき本体は | ★**壊れたまま残る** | ★★**無傷** | ★★**無傷** |
| 殺されたとき作業場は | (無い) | ★**残る** | ★★**消える** |

★<b>既定の SIGTERM では `finally` が走らない</b> ——
`install_signal_traps()` で例外に変えて初めて、`shutil.rmtree` まで届く。

★★<b>実際に殺して確かめた</b> —— <b>SIGTERM のあと `/tmp/qte-break-*` は1つも残らなかった</b>。

> ★★★<b>「片付けは自分でしてから次へ渡す」の、いちばん外側の顔である</b>(78・79・81 の教訓)。
> ★<b>ページに当てた差し替えも、スタブに当てた遅らせも、そして<b>作業場そのもの</b>も片付ける。</b>

---

## 5. ★★★なぜ 82 は裁定を1件も採番しないか

★**ゲームのルールにも画面の振る舞いにも1つも触れていないからである**(79 と同じ理由)。
★★詳細は `notes/batch82-ruling-requests.md` に書いた。

★<b>マスターは3つ選んだ(候補 P・対象範囲・番人の置き方)が、
どれも<b>工事の選択</b>であって<b>本文の読み方</b>ではない</b> ——
★★<b>裁定184 が求めているのは「本文が2通りに読めるとき」であり、ここはそれに当たらない。</b>

★★★**裁定は 1〜361 のままである。次は 362 から。**

---

## 6. 番人と壊し検証

### 6-1. ★★★壊し検証(`tools/batch82_break_check.py`)—— 15軸・OK 15 / NG 0 / EMPTY 0

★★★**このスクリプトはランナーを使わない。**
<b>壊す相手を、壊す道具として使うことはできない</b> ——
軸1でランナーの並列度を壊した状態のまま、そのランナーに軸2を回させるのは
<b>壊れた番人に自分の壊れ具合を尋ねること</b>である。

★<b>だから自前で複製を1本作り、複製の中のランナーを壊して `--self-test` を回す</b>。
★★<b>本体には `open(..., "w")` を1度もしない</b>(この点では 82 のスクリプトのほうが厳しい)。

| 軸 | 壊すもの | 落ちる番人 |
|---|---|---|
| 1 | 並列度を 4 にする | S6 |
| 2 | ワーカーを1本しか起こさない | S13 |
| 3 | ★ワーカーの**1本だけ**が本体を指す(片肺) | S1 |
| 4 | 書き込みのガードを緩める | S7 |
| 5 | 作業場を本体の木の中に作る | S8 |
| 6 | 片付けの書き戻しをしない | S3 |
| 7 | 壊しを当てずに書き戻す | S2 |
| 8 | SETUP-NG の判定を外す | S4 |
| 9 | verify の EMPTY を NG と答える | S5 |
| 10 | JUnit の EMPTY を NG と答える | S5b |
| 11 | JUnit を本体で走らせる | S9 |
| 12 | JUnit の報告書を本体から読む | S10 |
| 13 | verify のハーネス生成を本体で走らせる | S11 |
| 14 | verify を本体で走らせる | S12 |
| 15 | ★割り込みの罠を外す | S14 |

★★★**軸3 が「1本だけ」なのには理由がある。**
<b>2本とも本体を指すように壊すと、S1 と S13 が同時に落ちる</b> ——
★<b>1つの改変が2つの番人に当たったら、それは軸の切り方が悪い</b>(81 の教訓 3-6)。
★★<b>しかもこの「片方の入口だけが直っていない」形は、
このプロジェクトが 71・76・77・80・81 で繰り返し踏んできた片肺そのものである。</b>

### 6-2. ★★★1対1であることを**測った**

★<b>「1本ずつ当てたつもり」で終わらせなかった</b> ——
15軸それぞれについて、`--self-test` が落とした番人の**本数を数えた**。

> ★★★**15軸すべて、落ちた番人はちょうど1本だった。**

★<b>81 は「2つの軸が同じ番人に当たったら、番人の側を分ける」を教訓として残した</b>(3-6)。
★★<b>82 はそれを<b>毎回数える</b>形にした</b> —— 数えなければ、混ざっていても気づかない。

### 6-3. ★★★実物での証拠 —— 81 の21軸をそのまま回した

★<b>偽の木と偽の軸だけで済ませない</b>(それは 3章の番人の仕事である)。
★★<b>本物の21軸(JUnit 9・verify 12)を、書き換えなしで新しいランナーに載せた。</b>

| | 81(本体を直接壊す・直列) | ★82(複製・2並列) |
|---|---|---|
| 結果 | OK 21 / NG 0 / EMPTY 0 | ★**OK 21 / NG 0 / EMPTY 0** |
| 所要 | 約21分(見積もり) | ★★**585秒 = 9分45秒**(★2回目は 763秒。箱の混み具合で振れる) |
| 倍率 | — | ★★**約2倍** |
| 回し方 | ★5回に分け、毎回 `git diff --stat` を突き合わせる | ★★**1回で通す** |
| 殺されたら | ★**壊れたまま残る** | ★★★**本体は無傷**(★実地で確かめた・0-4) |

★★★<b>ただし1回目は OK 20 / NG 1 だった</b> —— <b>軸21 が2並列で当てられなかった</b>。
★<b>それは新しい壊れではなく、余裕 5ms の番人があぶり出されたのである</b>(9章)。
★★<b>閾値を広げたあと、3回目で OK 21 になった。</b>

### 6-4. ★★壊しどころが無いもの(裁定196 の正直な扱い)

1. **本体の差分照合(`dirty_root`)。**
   ★これは<b>番人 S1 の道具</b>であって番人ではない ——
   <b>S1 は更新時刻まで見ているので、`dirty_root` を殺しても S1 は落ちる</b>。
   ★★<b>「守りが二重にある」形である</b>(80 の12番目の形)。**軸には立てない。**
2. **`cp -a` そのもの。** ★失敗すれば `check=True` が例外を投げ、self-test はそもそも動かない。
   ★★<b>落ちる番人ではなく、落ちる床である。</b>
3. **JUnit と verify の中身。** ★82 は1文字も触っていない。**触っていないものは壊せない。**
4. **`--batch NN` の読み込み。** ★過去のスクリプトは読み込むだけである。
   ★★読み込みが壊れれば `CASES` が取れず、その場で止まる(これも床である)。
5. **並列で赤くなる2項目そのもの。** ★★<b>82 はあれを1文字も直していない</b> ——
   <b>直すのは候補 U2 の一族の工事である</b>(0-7・B7)。

---

## 7. 版数(7-5)

| ファイル | 81 | ★82 | 理由 |
|---|---|---|---|
| `battle.js` | v=42 | **v=42(据え置き)** | ★★★**1文字も触っていない** |
| `battle.css` | v=57 | **v=57(据え置き)** | ★★★**1文字も触っていない** |
| `manual-battle.js` | v=34 | v=34(据え置き) | ★同上 |
| `SFX_VERSION` | 1 | 1(据え置き) | ★音声ファイルは1つも増えていない |

★★★**静的ファイルを触っていないなら、版数は上げない**(73・77・79 の教訓)。
★<b>据え置きの番人は1本も新設していない</b> —— <b>ここは「変えうる場所」ではないからである</b>
(81 と同じ扱い)。

---

## 8. ★★★このバッチが残した教訓

### 8-1. 並列で得をするのは「待っている番人」だけである

`verify` は 1.74倍になり、JUnit は **1.00倍**だった。
★<b>差は「眠っている時間があるか」だけである</b> —— <b>`verify` は 82秒のうち 26秒眠っている</b>。

> ★★<b>速くしたければ、まず「どれだけ眠っているか」を測ること。</b>
> <b>眠っていないものは、並べても速くならない。</b>

### 8-2. ★★★安全の工事は、速さの数字には出てこない

★<b>速さだけで決めれば「verify 軸だけ複製にする」が正しい</b>(JUnit は 1.00倍なのだから)。
★★<b>だが JUnit 軸こそ、壊す先が `CardEffectRegistry` や `GameViewBuilder` である</b> ——
<b>殺されたときにいちばん困るのはそちらである</b>。

> ★★★<b>実寸を出すときは、速さの列だけでなく「壊れたら何が残るか」の列も出すこと。</b>

### 8-3. ★★★「壊して戻した」は「触っていない」ではない

★中身だけを見る番人では、<b>本体を壊してから書き戻す形が素通りする</b>。
★★<b>70 の事故が長く見つからなかったのは、まさにその形だからである</b> ——
<b>殺されなければ中身は戻る。</b>

> ★<b>危険は「結果」ではなく「経路」に在る。経路を測るには更新時刻のような別の目が要る。</b>

### 8-4. ★★直したあとに、必ず新しい後始末が生まれる(78 の教訓)

複製にしたことで本体は無傷になったが、**作業場が残るようになった**。
★★★<b>82 は机上ではなく実地で踏んだ</b> —— <b>自分が10分で殺され、`/tmp` に 86MB 残した</b>。

> ★<b>「直したあとに何が新しく起きるかを1つは考える」は、考えても足りないことがある。</b>
> <b>実際に殺されてみると1つ出てくる。</b>

### 8-5. ★★★壊す相手を、壊す道具として使わない

★`batch82_break_check.py` が `break_check_runner.py` を使わないのは、潔癖ではなく必然である。
★★<b>軸1でランナーを壊した状態のまま、そのランナーに軸2を回させたら、
その答えは何の証拠にもならない</b>。

> ★<b>番人を作るバッチでは、番人を試す道具を番人から独立させること。</b>

### 8-6. ★★1対1は「そのつもり」ではなく数える

★81 は「2つの軸が同じ番人に当たったら番人を分ける」を教訓として残した。
★★82 は<b>15軸すべてについて、落ちた番人の本数を実際に数えた</b>。

> ★★★<b>教訓は、毎回数える形にして初めて次のバッチに効く</b>(81 の 9-4 の続きである)。

---

## 9. ★★★並列にしたら、余裕 5ms の番人が1本あぶり出された

★★★**81 の21軸を新しいランナーで通したところ、軸21 だけが NG になった**
(「音のファイルのわざとの遅れを外す」)。★<b>81 の手元では OK だった軸である</b>。

### 9-1. ★まず番人を疑い、頻度ではなく「余裕」を測った(75・79・80 の教訓)

★<b>「2並列にしたせいだ」で済ませず、単独でも当て直した</b> ——
<b>単独なら OK、2並列なら NG</b>。★★<b>つまり箱の混み具合で答えが変わる軸である</b>。

★★★<b>そこで余裕を測った</b>(79 の教訓: 回数ではなく余裕)——
この番人は `bWaited >= 150`(ミリ秒)という**時間の閾値**である。

| | 実測 | 閾値 150ms との余裕 |
|---|---|---|
| **正しい状態**(遅らせ 600ms) | **1407ms** | ★余裕 1257ms(十分) |
| **壊した状態**(遅らせ 0ms・単独) | **121ms** | ★**余裕 29ms** |
| **壊した状態**(遅らせ 0ms・2並列) | **134ms / 145ms** | ★★★**余裕 5〜16ms** |

★<b>危なかったのは「正しい側」ではなく<b>壊した側</b>だった</b> ——
<b>遅らせを外しても、バッファが届くまでどうしても 121〜145ms 掛かる</b>。
★★<b>箱が混むとそれが 150ms を越え、<b>壊しても落ちなくなる</b>。</b>

> ★★★<b>79 の教訓「5ミリ秒しか余裕が無い番人は、番人ではない」の直系である。</b>
> ★<b>81 はこの番人を置いた当日、余裕を測っていなかった</b>(遅らせが効くことは測ったが、
> <b>効かなくしたときにどれだけ余るか</b>は測っていない)。

### 9-2. ★★★直したのは閾値1つだけである(測って、片方の案を外した)

★**最初は遅らせの側も広げるつもりだった**(600ms → 1200ms)。
★★<b>測ったら要らなかった</b> —— <b>正しい側は元から 1407ms あり、
広げても余裕が増えるのは<b>すでに足りている側だけ</b>だった</b>。

```
verify/verify.js   bWaited >= 150   →   bWaited >= 400     ★これだけ
```

| | 直す前 | ★直したあと |
|---|---|---|
| 正しい側の余裕 | 1257ms | **1007ms** |
| ★**壊した側の余裕** | ★★**5〜29ms** | ★★★**255ms** |

★★★<b>`SOUND_RESPONSE.delayMs = 600` は据え置いた</b> ——
<b>だから `tools/batch81_break_check.py` は1文字も触っていない</b>(軸21 の置換文字列がそのまま生きる)。
★<b>測って外した案も書き残す</b>(設計判断56・80 の追記)。

★★**verify の件数は 793 のままである** —— <b>項目を1件も足していない</b>。閾値を1つ広げただけである。

### 9-3. ★★★これは「並列にしたから壊れた」のではない

★<b>軸21 は 81 の時点ですでに余裕 29ms だった</b> —— <b>単独でも、箱が少し混めば落ちる</b>。
★★<b>2並列にしたことは、原因ではなく<b>あぶり出し</b>である</b>。

> ★★★<b>並列は、余裕の無い番人を見つける道具でもある。</b>
> ★<b>0-3 で「4並列にすると3本赤くなる」と書いたのと、これは同じ現象の別の顔である</b> ——
> <b>どちらも「時間で測っている番人」が、箱の混み具合で答えを変えている</b>。
> ★★<b>違うのは、あちらが<b>番人本体</b>で、こちらが<b>壊し検証の軸</b>だったことだけである。</b>

---

## 10. ★★★マスターにお願いすること

★**このバッチは画面を1ピクセルも変えていない。**実機で見えるものは何も変わらない。

- ★★★**Eclipse で refresh(F5)→ Run As → JUnit Test。**
  期待: **全964件が緑**(★**81 から1件も増減していない**)。
  ★<b>82 は Java を1文字も触っていないので、増えないのが正しい</b>。
- ★**`python3 tools/break_check_runner.py --self-test` が 15/15 になること。**
  ★★<b>これは1秒で終わる</b> —— 納品前チェックに足してある。
- ★★★**70〜81 の実機確認がまだ未報告である**(81 の5項目・78 の11項目・77 の6項目・76 の8項目)。
  ★<b>80 と 81 で画面が2バッチ続けて動いたので、いちばん確かめてほしいのはここである。</b>
- ★**62 の音の実機確認もまだ未報告である**(手動モード11種・通常モード9種)。
