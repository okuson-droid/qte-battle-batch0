# QTE 対戦アプリ — 引き継ぎ書 v81

最終更新: 2026-08-27。**Batch 73(総点検の完了 + WS の受け口の箱型化 + 裁定324〜327 の起票)を実施。**

| 項目 | 現在値 |
|---|---|
| JUnit | ★**840件 全緑**(814 → +26)|
| verify | **706件 全緑**(★**73 は1件も足していない** —— **JavaScript を1行も変えていない**ので照合先が無い。71 のちょうど逆である)|
| 効果の未実装 | **0枚**(据え置き) |
| **本文と実装の突き合わせ** | ★★★**235 / 235枚(完了)**(47 → +188)|
| 版数 | ★**据え置き**(`battle.css` v=53 / `battle.js` v=35 / `manual-battle.js` v=34 / `SFX_VERSION`=1)——<br>★**静的ファイルを1つも触っていない。触っていないのに上げると番人が意味を失う** |
| 裁定 | ★**1〜327 の全327件が確定。欠番ゼロ**<br>★**324〜327 は Batch 72 の回答を 73 で採番した**。★**73 自身は起こしていない**。**新しい裁定は 328 から** |
| カード定義 | 235枚(★**1文字も触っていない**。★★ただし**効果は8枚ぶん変えた**) |
| ★**Java** | ★★★**変えた**(効果6ファイル + WS の受け口2ファイル) |
| ★新設 | `tools/batch73_break_check.py` / `src/test/java/com/example/qte/Batch73TextImplTest.java` / `…/Batch73PayloadGuardTest.java` / `…/ManualWsRequestPayloadTest.java` / `…/support/PeekingBroadcaster.java` / `notes/batch73-design-notes.md` / `notes/batch73-ruling-requests.md` |
| ★削除 | `PersistentAura.Expiry.ON_NEXT_SPELL` と `untilNextSpell`(使い手を失った。裁定178) |
| 壊し検証 | `tools/batch73_break_check.py` **20軸(OK 20 / NG 0)** |

---

## ★★★0. 最初にやること —— 73 の着手時に踏んだ罠

### ★★★clone に「そのバッチの続き」が入っているとは限らない

73 の着手時、`git clone` は `87ff679 batch72` を持ってきたが、
**そこに入っていたのは 72 の本体までだった。**

| ファイル | GitHub | ワークスペース | 中身 |
|---|---|---|---|
| `GameWsController.java` | 21024 | **25277** | 72b(`Boolean enhanced` / `onUnreadableMessage`) |
| `WsRequestPayloadTest.java` | 無い | 13661 | 72b の番人 |
| `WsErrorRoutingTest.java` | 無い | 5661 | 72b の番人 |
| `tools/batch72b_break_check.py` | 無い | 11251 | 72b の壊し検証 |
| **`verify/verify.js`** | 515158 | **520477** | ★**72 の11章追補(70-6b の4件)** |
| **`tools/batch72_break_check.py`** | 40771 | **43398** | ★**72 の追補(軸38〜40)** |
| `notes/qte-pitfalls.md` | 147877 | **154039** | 72b の節 |

★**納品後に足したものが、丸ごと未 push だった。**
最初に verify を回したら **702/702** で、ハンドオフの 706 に**4件足りなかった** ——
**書いてある数字がそのまま番人として効いた。**

> ★★★**push は「そのバッチが終わった時点」で一度に行うこと。**
> 納品後に足したものは、次の clone から抜け落ちる。

### ★clone の「新しさ」の確かめ方

`git log -1` の**日付でもコミットメッセージでも測れない**(69 で実際に踏んだ)。確かめるのは:

1. **`GameWsController.java` に `required(` が在るか**(73 の関門である)
2. **`PersistentAura.java` に `ON_NEXT_SPELL` が**<b>無い</b>**か**(73 で消した)
3. **`mvn test` の件数が 840件か**
4. **`tools/batch73_break_check.py` が在るか**

### 手順

1. `qte-project-reference.md` を読む(★**設計判断53 が新設された**)。
2. `notes/qte-pitfalls.md` の該当節を読む
   (★特に**「総点検の続き —— 残り188枚(Batch 73)」**と
   **「総点検の続き —— WS の受け口の箱型化(Batch 73)」**、
   「クライアントとサーバの『あいだ』(Batch 72b)」、
   「試合の出入り(Batch 72)」)。
3. 本ファイルと **`notes/batch73-design-notes.md`** を読む。
4. ★★**`notes/batch73-ruling-requests.md` を読む** —— **判断待ちが14件ある**。
   ★とりわけ **A(進化ミニオンの13枚)は次のバッチの主題になりうる**。
5. ソースコード取得: `git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git`
6. `m2repo.zip` を取り込んで `mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test`。
   ★**840件全緑が出発点である。**緑でなければ止めて報告する。

---

## 1. ★★★73 がやったこと(要点)

### 1-1. 総点検が完了した —— 188枚を見て、8枚を直した

67 が見た48枚の**残り188枚**を本文と実装で突き合わせた。

| カード | Ver1.1 の本文 | 72 までの実装 |
|---|---|---|
| 《アルキンティス》WATER-39 | 自分の場の【知識】の**枚数**Attack+1 | 自分が【知識】を持つ扱いになり、**装備しただけで1枚引く** |
| 《詠唱の疾風騎士》WIND-18 | ターンエンド時、このターン5回以上唱えていたら | **相手のターンエンドでも発動**(1巡で最大4枚回収) |
| 《悪夢》DARK-27 | 墓地にある**カード**1枚につきコスト-1 | **スペルを除いた枚数**しか数えない |
| 《這い寄る生霊》DARK-7 | 【特殊召喚】【突進】【知識】 | **本文に無い自壊**を足していた |
| 《降臨の伝道師》LIGHT-22 | 【守護】**ミニオン**を1体場に出す | 種別を見ておらず、**進化が素材ゼロで場に立つ** |
| 《詠唱の宝珠》LIGHT-28 | 次の自分のターンに唱える光のスペル**すべて** | **次の1枚だけ**・期限がターンに紐づかない |
| 《墓穴の呪い》DARK-24 | 体力が墓地以下のミニオンを**2枚選び**破壊 | 該当するものを両者の場から**全部**破壊 |
| 《風のマナ変換》WIND-23 | 自分の表向きのマナを**1**手札に戻す | どれを戻すか尋ねず、**末尾を自動で選ぶ** |

★★**8枚を直しても、既存の826件は1件も落ちなかった** —— 67 とまったく同じである。

### 1-2. ★★★67 と違ったこと —— 「表」が在った

8枚のうち**4枚は、Ver1.1 で何がどう変わったかが `notes/ver0.4-transcription-notes.md` に
はっきり書いてあった**(5章の52枚の表 + 4章のルーリング #9)。

> **表は在ったのに、実装がそこまで届いていなかった。**

★**あの表そのものを母集団にした照合を、別に1本走らせた**(52枚 + ルーリング7件)——
上の4枚以外に反映漏れは無く、**64・67 が直した6枚も現在の実装が直っていることを確認した**。

### 1-3. ★★★いちばん怖い形 —— 《詠唱の宝珠》

Batch 56 の試験にはこう書いてあった。

> 旧: 「次の自分のターンに唱えるスペル**すべて**のコスト-1」/ 新: 「光のスペル」限定になった
> ★確認の結果、実装は既に新本文どおりに一致している

★**「旧」が間違っている。** 旧は「次の**1枚**」だった。
56 は**旧を取り違えていた**ので「残る差は文明だけ」と結論し、**枚数の側を1度も見なかった**。

> ★★★**誤った前提は、正しい確認を素通りさせる。**
> 「確認した」という記録は、**何を確認しなかったか**を教えてくれない。

★`Batch56ReworkTest` のコメントは 73 で**訂正した**(消さずに、誤りだったことを書き残した)。

### 1-4. ★★72b の宿題を果たした —— WS の6項目を箱型に

★★★**箱型にすることと、畳むことは別である**(設計判断53)。

| 項目 | 無いときに畳むと | 73 の扱い |
|---|---|---|
| `enhanced`(72b) | false = **通常の使用** | **畳む** |
| `goFirst` | false = **後攻を選んだ**。やり直せない | **断る** |
| `handIndex`(2箇所) | 0 = **手札の1枚目をプレイする** | **断る** |
| `trashIndex` | 0 = **墓地のいちばん古い1枚**を出す | **断る** |
| `tabooIndex` | 0 = **禁忌デッキの1枚目**。使うと消滅する | **断る** |
| `ManualDragCue.active` | false = **矢印を消す** | **畳む** |

★**断るときの例外は `IllegalArgumentException` である** ——
素の自動開封に任せると `NullPointerException` になり、`execute` が捕まえず**また無言に戻る**。

★★**手動モードにも本文の表を作った**(`ManualWsRequestPayloadTest`。72b の宿題2)。

### 1-5. ★裁定324〜327 を起こした

72 のマスター回答4件(観戦不可の部屋での席 / 投了の範囲 / 再戦の同意 / 確認の出し方)を
`notes/qte-rulings.md` に採番した。

★★**確定の順と採番の順が一致しない2例目である**(1例目は 312〜314)。
★**確定したその場で採番しておくこと。**
「カード本文の読みではないから」は、次の人が結論だけを読むという事実を何も変えない。

---

## 73 の確認項目(★これを照合する)

- **新設**: `tools/batch73_break_check.py` / `src/test/java/com/example/qte/Batch73TextImplTest.java` /
  `…/Batch73PayloadGuardTest.java` / `…/ManualWsRequestPayloadTest.java` /
  `…/support/PeekingBroadcaster.java` / `notes/batch73-design-notes.md` /
  `notes/batch73-ruling-requests.md`
- **`web/GameWsController.java`** …… `private static <T> T required(T value, String field)` が在り、
  `ChooseOrderRequest.goFirst` / `HandActionRequest.handIndex` / `PlayCardRequest.handIndex` /
  `GraveSummonRequest.trashIndex` / `TabooRequest.tabooIndex` が**すべて箱型**である
- **`manual/ManualDragCue.java`** …… `Boolean active` と `public Boolean active()` が在る
- **`master/CardTextKeywords.java`** …… `REFERENCE` が `^(を持|を与|を付|付与|を行|を得|を失|の)` である
- **`effect/PersistentAura.java`** …… `Expiry` が `AFTER_TURN_NUMBER` **1つだけ**である
- **`effect/ResumePoint.java`** …… `GRAVE_CURSE_DESTROY` と `MANA_CONVERT_RETURN` が在る
- **`game/GameActions.java`** …… `onWeaponLeftPlay(GameRoom room, …)` と
  `nextOwnTurnNumber` が在る
- **`effect/CardEffectRegistry.java`** …… `registerLightSpellsAndWeapons()` と
  `manaConvertReturnThenPut` が在る
- JUnit **840件全緑**
- verify **706/706**(★**73 は足していない**)
- `python3 tools/batch73_break_check.py` が **OK 20 / NG 0**
  ★★**長いので3つに分けて回し、前後で `git diff --stat` を突き合わせること**
- `python3 tools/report_effects.py --summary` で **未実装 0枚**
- `python3 tools/check_all.py .` が **「項目 1・3・5・6 はすべてパスしました」**
- `python3 tools/check_card_text_numbers.py` が **OK**
- `python3 tools/mark_text_reviewed.py --check` が **「全235枚が突き合わせ済み」**
- ★`python3 tools/mark_text_reviewed.py --stale batch73` が **47枚**(67 が見た側)
- `python3 tools/check_structure.py src/main/java` が **exit 0**
- `python3 tools/check_records.py src/main/java` の**不一致は3件のまま**(★既知の誤検出)
- ★**版数は据え置き**(`battle.js` v=35 / `battle.css` v=53)

---

## 2. 次の作業(Batch 74)

どれをやるかは**マスターの選択**である。

★★**73 で「候補 D″(総点検の続き)」は終わった。**表から消えている。

| 候補 | 内容 | 大きさ |
|---|---|---|
| **★★★P. 進化ミニオンの一族(73 の裁定依頼 A)** | ★**73 が見つけた13枚。**「ミニオン」の絞り込みが `== CardType.MINION` で書かれており、**進化が候補から落ちる**(手札4・墓地5・マナ4)。裁定308(b) が「暫定」と断じた形がそのまま残っていた。★**直すには「墓地・マナ・山札から進化を出すときの素材」を作る工事が要る**(68 が手札にだけ作った仕組みの3経路ぶん)。★★**まず裁定をもらうこと** | 大 |
| **★★Q. 73 の判断待ち B(13件)** | ★**読みが2通りに分かれるもの。**回答をもらった順に直せるので、**1件ずつが小さい**。★P より先に片付けてもよい | 小〜中 |
| **★★L. 70〜73 の手触りの調整** | ★**実機で触ってみて出てくるもの**。★62 の音と同じ扱いで、**マスターの報告待ち**である | 小 |
| **A. 音の調整** | ★**実機で聞いた印象を反映する**(62 からの持ち越し。まだ未報告) | 小 |
| **★M+N. 部屋消失の検出 + 無人部屋の掃除** | ★71 が残した穴と 66 の積み残し。**同じ工事の裏表であり、まとめてやるのが安い**。★72 が退室を作ったので「部屋が空になる」経路が日常的になった | 中 |
| **B. 通常モードの演出** | 62 が作った差分の採取層に fx 層を乗せる。32a・32b・35・38 の4バッチぶん | 大 |
| **C. カードフェイスの1本化** | デッキメーカーの `.tile` を `.mcard` へ寄せる。★verify 28件の作り替えが同時に要る | 中〜大 |
| **★O. 通常モードの確認を1本化する** | ★72 が片肺として書き残したもの。宣言のための素の `confirm()` が7箇所残っており、裁定53 の理由3(JS を止めると STOMP の受信も止まる)はそちらにも効く。★フォーカストラップ(裁定50)も同時に要る | 小〜中 |

★**わたし(クロエ)の推し**: **Q →(裁定をもらってから)P**。
★理由:
1. **73 が14件の判断待ちを残した。**判断待ちは**時間が経つほど文脈が薄れる** ——
   いま調べたばかりのわたしたちが、いちばん安く直せる。
2. **B の13件は1件ずつ独立している。**回答をもらった順に片付けられるので、
   途中で止まっても中途半端にならない。
3. **A(進化の13枚)は工事が大きい**が、**裁定226 に反している状態**でもある ——
   《降臨の伝道師》は 73 が暫定で塞いだが、**残り13枚は「進化が使えない」ままである**。

★★**次点は L(と A)。**70 は遊び方を、71 は事故のときの見え方を、72 は試合の出入りを変えた ——
**どれも人が触ってみないと良し悪しが決まらない**種類の変更であり、
**実機確認が 35〜73 まで積み上がっている**。

### 各バッチがやること(P2から継続・★73 で4つ足した)

1. `python3 tools/report_effects.py --summary` で未実装の枚数を確認する(**0枚が正常値**)。
2. ★★`python3 tools/mark_text_reviewed.py --check` も通す(67 で新設)。
   **カード本文を1文字でも触ったら、そのカードの実装を突き合わせ直して記録すること。**
   ★★★**73 で 235/235 が一周した。**以後 `--stale` で出るのは
   「本文を触ったのに突き合わせ直していないカード」だけである。
3. **本文が2通り以上に読めるカードは、実装で決めずに必ず裁定を仰ぐ**(裁定184)。
   ★★★**73 の追記: 依頼文は `notes/batchNN-ruling-requests.md` に1本にまとめること** ——
   設計解説に散らすと、回答をもらうときに探し直すことになる。
4. **カード毎の JUnit を `support/AutoGameFixture` の上に書く**(裁定187)。
5. `mvn test` で回すこと(`surefire:test` はコンパイルしない。裁定208)。
6. ★**壊し検証の改変は「軸」ごとに1件ずつ当てる**(57 の教訓)。
   ★★★**出口が複数ある関数は、出口ごとに当てること**(71)。
7. ★★**「壊しても落ちない」を見たら、まず改変が当たっているかを疑う**(11の形)。
   ★★★**73 は2つの新しい顔を踏んだ。**
   **(a) 試験の書き方が壊せるかどうかを決めていた** —— 受け口を原始型に戻す軸が、
   試験の `new Request(id, null)` のせいで**コンパイルを壊して EMPTY** になった。
   **箱型の変数を経由して渡す**形に変えて成立した。
   **(b) 「まだ踏まれていない地雷」は測れない** —— 表(実際に送る本文)は、
   **送っているものしか守れない**。
8. ★**画面(テンプレート)を作ったら、MockMvc で 200 と中身を測る試験を1件は置く**(61 の教訓)。
   ★★★**ただしそれを「効いている」の番人だと読まないこと**(71・72)。
9. ★★**音を触ったら、実機でマスターに聞いてもらう**(62 の教訓)。
10. ★★**「暫定でこうしておく」は、前提が消えても自分では戻らない**(64・**時間**)。
11. ★★**「簡易版」は、本家が直っても自分では直らない**(65・**空間**)。
12. ★★★**「いつ直すか」を書いても、実装は自分では動かない**(66・**予定**)。
13. ★★★**コメントに書いた本文は、本文が変わっても自分では変わらない**(67・**写し**)。
14. ★★★**別の性質の「代用」として書いた条件は、代用元が変わっても自分では変わらない**(68・**代用**)。
15. ★★★**「まずは〜から」と書いた実装は、そこで止まったまま動かない**(69・**途中**)。
16. ★★★**「書いてあるのに効いていない」は、クラスの数を数える検証では見つからない**(70・**空文**)。
17. ★★★**片方のモードで直したことは、もう片方が直ったことではない**(71・**片肺**)。
18. ★★★**理由の幅と結論の幅を揃えて書く**(72・**幅**)。
19. ★★★**送る側と受ける側を別々に見ている番人は、あいだを見ていない**(72b・**あいだ**)。
20. ★★★**誤った前提は、正しい確認を素通りさせる**(★73・**前提**)。
    **「確認した」という記録は、何を確認しなかったかを教えてくれない。**
21. ★★★**移行の「後に」書かれた実装は、移行の取りこぼしを探す目では見つからない**(★73・**後発**)。
22. ★★★**母集団は、作業を始める前に固定して控えておく**(★73)。
    **「残り」で母集団を取ると、見ていないものが混ざる。**
23. ★★**UI を直すバッチは、先に端の盤面を作って測る**(65 の教訓)。
    ★★**実測は捨ててよいが、実測から出た不変量は番人に昇格させること**(71・72)。
24. ★★**「番人が無い」と思ったら、まず在るかどうかを見る**(65 の教訓。66〜72 で踏んだ)。
    ★★★**73 でも踏んだ** —— 《詠唱の宝珠》の新しい期限は、
    既存の `AFTER_TURN_NUMBER` に**次の自分のターン番号**を渡すだけで表せた。
25. ★★**消した番人は必ず書き残す**(裁定196)。★**作らなかった番人も**(67)。
    ★★★**「壊しどころが無いもの」も**(70・72・★73)。
26. ★★★**番人は「回る場所」で選ぶ**(69・設計判断45)。
    ★★★**選ぶ前に「そこまで届くか」を確かめる**(70)。
    ★★★★**逆向きもある**(71)。★★★★★**両方あるバッチもある**(72)。
    ★★★★★★**片方しか無いバッチもある**(★73 —— JS を変えないと verify に軸が置けない)。
27. ★★**静的ファイルの版数はファイルに付く。**`battle.css` を触ったら**5枚とも上げる**(7-5)。
    ★★★**73 の追記: 触っていないなら上げない。**
    上げると「前のバッチの値でないこと」を測る番人が意味を失う。
28. ★★★**長い壊し検証は、前後で差分を見る**(70)。殺されると壊したまま残る。
29. ★★★**遷移を起こしうる検証の項目は末尾に置く**(72)。
    **死ぬ検証は、番人ではなく無音である。**
30. ★★★**WS の宛先や入口を増やしたら、その本文を表に足すこと**(72b・★73)——
    通常モードは `WsRequestPayloadTest`、**手動モードは `ManualWsRequestPayloadTest`**。
    ★★**新しい項目は「畳むのか断るのか」を先に決めること**(設計判断53)。
31. ★★★**push は「そのバッチが終わった時点」で一度に行う**(★73 の着手時に踏んだ)。
    納品後に足したものは、次の clone から抜け落ちる。

---

## 3. 発注者とのやりとり

- grep優先でファイルを渡り歩き、全体読み込みは避ける。
- **判断に迷う点はまとめて質問する。**1つずつ聞かない。
  ★★★**73 は判断待ちを `notes/batch73-ruling-requests.md` に1本化した**(14件)——
  **A(構造の案件1つ)と B(読みが分かれる13件)**に分けてある。
- ★★**カード235枚のような広い突き合わせは、文明ごとに分けて並行させると速い**(★73)。
  ★**報告だけを返させ、実装の変更は1箇所でまとめて行うこと。**
- 呼び方は「クロエ」、発注者は「マスター」。会話は日本語カジュアル体、
  **ドキュメントは通常文体(である調)。**

---

## 4. 既知の落とし穴

**★全文は `notes/qte-pitfalls.md` にある。**73 で2節を新設した。

| 節 | 内容 |
|---|---|
| ★**総点検の続き —— 残り188枚(Batch 73)** | 母集団は先に固定する / 誤った前提は正しい確認を素通りさせる / 表は在ったのに届いていなかった / 移行の後に書かれたものは見つからない / 数え方を借りると本文の違いが吸収される / 同じカードの中で扱いが割れる形 / 「【X】の◯◯」は参照 / 新しい期限を作る前に在るものを見る |
| ★**総点検の続き —— WS の受け口の箱型化(Batch 73)** | 箱型と畳みは別 / 断るときの例外の型 / 試験の書き方が壊せるかを決める / 「まだ踏まれていない地雷」は測れない / 手動モードの表と休眠の3宛先 |
| **クライアントとサーバの「あいだ」(Batch 72b)** | 原始型の受け口 / 送ったと届いたは別 / 実物 → ハーネスの順 |
| **試合の出入り(Batch 72)** | 理由の幅と結論の幅 / id は変えない / 送りっぱなしにしてよいのは失敗しない操作だけ / ほか |
| **通常モードの切断(Batch 71)** | 番人は send でオーバーレイではない / 出口ごとに番人を当てる / 偽の緑 |
| **モーダル・Esc・フォーカス(Batch 36・39・40)** | 裁定49・52・53・56・327 |

---

## 5. デリバリー形式(変更なし)

マスターの Eclipse ワークスペース
(`C:\Users\奥村優斗\OneDrive\ドキュメント\eclipse_workフォルダ\qte-battle-batch0`)へ
直接書き込む。手順はv62の4章のとおり。

★★**73 はファイル単位の削除を1件も行っていない。**変更と新設だけである
(★`PersistentAura` の中の `ON_NEXT_SPELL` は消したが、ファイルは残っている)。

反映前に必ず実行:

```bash
python3 tools/check_structure.py src/main/java
python3 tools/check_all.py .
python3 tools/check_records.py src/main/java     # ★既知の誤検出3件
python3 tools/check_undeclared.py src/main/resources/static/js/*.js
node --check src/main/resources/static/js/manual-battle.js
node --check src/main/resources/static/js/battle.js
NODE_PATH=/home/claude/.npm-global/lib/node_modules \
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers \
  python3 verify/build_harness.py && node verify/verify.js
mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test
python3 tools/check_legacy_ids.py
python3 tools/report_effects.py --summary
python3 tools/check_leader_abilities.py
python3 tools/check_card_text_numbers.py
python3 tools/mark_text_reviewed.py --check
```

---

## 6. チャット開始テンプレート

```
QTE Battle の開発を継続する。Batch 74 を行う。

読む順:
1. プロジェクトナレッジ内の `qte-project-reference.md`
   ★4章の設計判断53(箱型にすることと畳むことは別である)が Batch 73 で新設された。
2. プロジェクトナレッジ内の `claude/qte-handoff-v81.md`(本ファイル)
3. プロジェクトナレッジ内の `notes/batch73-ruling-requests.md`
   ★★★判断待ちが14件ある。A(進化ミニオンの13枚)は次の主題になりうる。
4. プロジェクトナレッジ内の `notes/batch73-design-notes.md`
5. `notes/qte-pitfalls.md` の該当節
   (特に「総点検の続き —— 残り188枚(Batch 73)」
    「総点検の続き —— WS の受け口の箱型化(Batch 73)」
    「クライアントとサーバの『あいだ』(Batch 72b)」
    「試合の出入り —— 席・退室・投了・再戦(Batch 72)」)

環境:
5. git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git
   ★★★clone に「そのバッチの続き」が入っているとは限らない(73 で実際に踏んだ)。
     確かめるのは「GameWsController.java に required( が在るか」
     「PersistentAura.java に ON_NEXT_SPELL が無いか」「mvn test が 840件か」である。
6. 接続フォルダの m2repo.zip を device_stage_files で取り込んで /root/m2work へ展開し、
   mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test を回す。
   ★840件全緑が出発点である。緑でなければ止めて報告する。
7. 73 の反映を本ファイルの「確認項目」で照合し、verify 706/706 と
   report_effects の「未実装0枚」、check_card_text_numbers と
   mark_text_reviewed --check の OK を確認する。
   ★mark_text_reviewed --stale batch73 は 47枚である(67 が見た側。これで正しい)。

作業:
8. 本ファイル2章「次の作業(Batch 74)」の候補からマスターが選んだものを行う。
   ★設計解説は新規ファイル `notes/batch74-design-notes.md` に書く。
   ★裁定を仰ぐ点は `notes/batch74-ruling-requests.md` に1本化する。
   ★壊し検証は `tools/batch74_break_check.py` を新設し、改変は「軸」ごとに1件ずつ当てる。
     ★★出口が複数ある関数は、出口ごとに当てること(71 の教訓)。
     ★★長い壊し検証は前後で差分を見ること(70 で殺されて壊したまま残った)。
     ★★★試験の書き方が壊せるかどうかを決めることがある(73 の教訓)。
   ★★カード本文を1文字でも触ったら、そのカードの実装を突き合わせ直し、
     tools/mark_text_reviewed.py で記録すること(理由の記入は必須)。
   ★★★母集団は作業を始める前に固定して控えること(73 の教訓)。
   ★★UI を触るなら、先に端の盤面を作って測ること(65・69〜72 の教訓)。
   ★★★番人を置く場所を選ぶ前に、そこまで届くかを確かめること(70・71・72・73 の教訓)。
   ★★★WS の宛先や入口を増やしたら、その本文を表に足すこと
     —— 通常モードは WsRequestPayloadTest、手動モードは ManualWsRequestPayloadTest。
     ★新しい項目は「畳むのか断るのか」を先に決めること(設計判断53)。
   ★★★静的ファイルを触っていないなら、版数は上げないこと(73 の教訓)。

呼び方は「クロエ」、こちらの呼び方は「マスター」で。会話は日本語カジュアル体、
ドキュメントは通常文体(である調)で。
```

---

## 7. 73 完了時点の積み残し

### ★★★73 が新たに載せたもの(マスターに諮る案件)

**★全文は `notes/batch73-ruling-requests.md` にある。**

- **★★★A: 進化ミニオンの一族(13枚)。**
  「ミニオン」の絞り込みが `== CardType.MINION` で書かれており、進化が候補から落ちる ——
  手札4枚(機神兵長茶爺 / ギガマウス・バイト / 灰ノ霊呼者 / 英術・スケアロック)・
  墓地5枚(禁忌の代償 / 死者蘇生 / リボーンライヴ・ノア / サモンズライト / 黄泉の召喚主)・
  マナ4枚(勝鼓美 / 素手喧嘩 / 喧嘩上等 / 俺等地上覇夜露死苦)。
  ★**裁定308(b) が「暫定」と断じた形が残っていた。**
  ★★**直すには「墓地・マナ・山札から進化を出すときの素材」を作る工事が要る。**
- **★★B: 読みが2通り以上に分かれるもの13件。**
  ゾディアックの攻撃禁止の範囲 / 蒼海の賢者の起動条件 / 分那愚利の任意 /
  風弾の跳弾の使用条件 / 静空の風使いのアンタップ先 / 回帰の風穴の再詠唱を数えるか /
  神風の大号令の期限 / 豊穣の地霊主の側 / 傷痕の闘帝の「そうしたら」/
  背水の炎壁の「リーダーが」/ 墓地に「自分の」が無い2枚 /
  聖光の守護聖のリーダー破壊防止 / 詠唱の宝珠の付け替え発動。

### ★★マスターにお願いすること(実機確認)

- **Eclipse で refresh(F5)→ Run As → JUnit Test。**期待: **全840件が緑**。
- ★★★**73 の確認**(詳細は `notes/batch73-design-notes.md` の10章)
  - ★**73 は画面を1つも変えていない。**確かめてほしいのは挙動である。
  - **《アルキンティス》を装備しても1枚も引かないか**(72 までは引いていた)。
  - **《悪夢》のコストが、墓地のスペルぶんも下がっているか。**
  - **《這い寄る生霊》を特殊召喚したミニオンが、ターンの終わりに消えないか。**
  - **《墓穴の呪い》で「2体まで選ぶ」問いが出るか**(候補が3体以上のとき)。
  - **《風のマナ変換》で「手札に戻す表向きのマナを選んでください」が出るか。**
  - **《詠唱の宝珠》が壊れた次の自分のターン、光のスペルを2枚以上唱えても両方-1されるか。**
  - ★**操作したときに「操作に必要な項目が送られていません」が出ないか**
    (出たら 73 の関門が誤爆している。**すぐ知らせてほしい**)。
- ★★★**70・71・72 の実機確認がまだ未報告である。**
  ★**70 は遊び方そのものを、72 は試合の出入りを変えている**ので、そちらの価値が高い。
- ★**62 の音の実機確認がまだ未報告である**(手動モード11種・通常モード9種)。
- ★**64 の問い合わせ・65 のマナ行・66 のロビーと観戦・67 の7枚・68 の手触り・
  69 の盤面も未報告である。**
- ★★★**GitHub の push は「そのバッチが終わった時点」で一度に行ってほしい** ——
  73 の着手時、**72 の追補と 72b が丸ごと未 push だった**(0章)。

### 継続中の積み残し

- ★★**通常モードには無人部屋の掃除が無い**(66 の積み残し・候補 N)。
- ★★★**通常モードには「部屋が消えた」の検出が無い**(71 で明文化・候補 M)。
- ★★**通常モードには宣言のための素の `confirm()` が7箇所ある**(72 で片肺として明文化・候補 O)。
  ★フォーカストラップ(裁定50)も通常モードには無い。
- ★★**マリガンと割り込みの選択が「送れなかったら消えない」ことは名指しで測っていない**(71)。
- ★**右列の中段が溢れたことに人が気づけるか**は分からない(69)。
- ★★**ドラッグはタッチでは動かない**(70)。HTML5 のドラッグの制約である。
- ★★**本文と実装の突き合わせは 235/235 で完了した**(★73)。
  ★**以後は「本文を触ったら突き合わせ直す」の運用に入る。**
- ★★**手動モードの `applyManaOverlap` は重なりの上限を「覆う側」の添字で取っている**(65)。
  ★実害は出ていない。
- ★**通常モードに演出が無い。**62 が差分の採取層を作ったので土台はできている(候補 B)。
- ★**カードフェイスの実装が2つある**(候補 C)。
- ★**カード名の表記は揃っていない**(「愚乱怒土地(グランドランド)」の半角括弧など)。
- **【常在】：のコロン**(3枚)。
- 35〜73 の実機確認が未報告のまま(46bと48は報告あり)。
- 進化召喚がモアニールの登場置換で止まったときの素材の扱い(裁定232)。
- **本物の入口から観測できない挙動が3件**(★73 で4件目の候補が出た)——
  《英霊・コレキ》/《風弾の跳弾》/《悪夢》の封じの範囲 / ★《聖光の守護聖》のリーダー破壊防止。
- 手動モード関連の積み残しは一時停止中。

★**裁定は 1〜327 の全327件が確定し、欠番はゼロである。新しい裁定は 328 から。**
