# Batch 82 裁定依頼 —— 採番0件。実装で決めたことと、次への申し送り

★★★**Batch 82 は裁定を1件も採番していない。**
★**Batch 79 に続く2例目**である。

★理由は 79 とまったく同じである ——
<b>ゲームのルールにも画面の振る舞いにも1つも触れないバッチだったからである</b>。
★★<b>82 が変えたのは `tools/` と、`verify/verify.js` の閾値1つだけである</b> ——
<b>Java・カード定義・`battle.js`・`battle.css`・テンプレートは1文字も触っていない</b>。

★★★**裁定は 1〜361 のままである。次は 362 から。**
★★**判断待ちは1件も残していない**(74〜81 に続く**9バッチ連続**)。

---

## 0. ★このバッチの発端 —— 候補 P

マスターの「**検証に時間がかかりすぎている**」を受けて、Batch 81 が実測して書き残したものである ——

> `verify` 1回転は **80秒**で、81 の壊し検証は verify 12軸 = **16分**掛かっている。
> リポジトリの複製は1秒未満、スタブサーバは `listen(0)` なので<b>同時に回せる</b>。
> ★<b>速さより副産物が大きい</b>: <b>複製側で壊せば本体が一度も汚れない</b>。

★81 は測っただけで着手せず、候補 P として残した。★★<b>82 はそれを作った</b>。

---

## 1. ★★★なぜ裁定を仰がなかったか

裁定184 が求めているのは「**本文が2通り以上に読めるカードは、実装で決めずに必ず裁定を仰ぐ**」である。
★<b>82 はカード本文を1文字も読んでいない</b>。

★★**マスターは着手前に3つ選んでいる**が、どれも**工事の選択**であって**本文の読み方**ではない:

| 何を選んだか | 選ばれたもの | 裁定か |
|---|---|---|
| Batch 82 で何をやるか | **候補 P** | ★<b>いいえ</b> —— 引き継ぎ書2章の候補選択である |
| 複製の上で回す対象 | **全軸(verify + JUnit)** | ★<b>いいえ</b> —— 実寸を見た工事の範囲の判断である(2章) |
| ランナー自身の番人 | **self-test + 壊し検証** | ★<b>いいえ</b> —— 番人の置き場所の判断である(設計判断45) |

★★★<b>裁定にしないことにも理由が要る</b> ——
<b>台帳(`notes/qte-rulings.md`)は「本文と規則の読み方」の台帳であって、
工事の履歴ではない</b>。★<b>工事の履歴は設計解説と引き継ぎ書が持つ。</b>

★★<b>したがって `notes/qte-rulings.md` は 82 では1文字も動いていない</b>(81 が 355〜358 を写して以降、穴はゼロのままである)。

---

## 2. ★★82 が実装で決めたこと(裁定を仰がなかったもの)

読みが1つに定まったので、作業を止めずに実装した(リファレンス 1-7 の流儀)。
**間違っていたら戻す。**

| 決めたこと | 根拠 |
|---|---|
| ★★★**並列度は 2 で固定し、`nproc` から自動で決めない** | ★<b>天井を決めているのは箱ではなく番人の書き方である</b>(設計解説 0-3)—— 4並列で赤くなるのは「アニメーションを時間で捕まえる」2項目だけであり、<b>速い箱でも同じ番人が同じように嘘をつく</b>。★★<b>自動にすると、速い箱で黙って4並列になって嘘の緑が出る</b> |
| ★★★**全軸(JUnit も)を複製の上で回す** | ★JUnit は<b>1.00倍で1秒も速くならない</b>(実測)。★★<b>それでも複製にするのは、壊す先が `CardEffectRegistry` や `GameViewBuilder` だからである</b> —— <b>殺されたときにいちばん困るのはそちらである</b>。★★★<b>種別ごとに規則を分けると、「同じ規則を入口の数だけ書き忘れる」形を自分から作りに行くことになる</b>(76・77・79〜81) |
| ★★**過去の `batchNN_break_check.py` は1文字も書き換えない** | ★`--batch NN` は `CASES` を<b>読み込むだけ</b>にした。★★<b>過去のバッチの記録は、そのバッチの姿のまま残すべきものである</b> —— <b>回し方が変わったからといって、当時の表を書き換える理由は無い</b> |
| ★★★**S1(本体無傷)は更新時刻まで見る** | ★<b>「壊して書き戻した」は「触っていない」ではない</b>。★★<b>中身だけを見る番人では、70 の事故の形(殺されなければ中身は戻る)が素通りする</b> |
| ★★**S6(並列度の上限)の「2」は番人の側に書く** | ★`WORKERS` を読み直すと、<b>上げた瞬間に番人も一緒に上がる</b> —— 何も測っていない緑になる(設計判断41)。★★<b>上げた人がここで赤を見るのが正しい</b> |
| ★★**EMPTY の番人を2本置く** | ★`verify_verdict` と `junit_verdict` は別々に EMPTY を返す —— <b>規則が n 入口ぶんあるなら番人も n 入口ぶん要る</b>(77・79・80・81) |
| ★★★**`batch82_break_check.py` はランナーを使わない** | ★<b>壊す相手を、壊す道具として使うことはできない</b> —— 軸1でランナーを壊した状態のまま、そのランナーに軸2を回させたら、その答えは証拠にならない |
| ★★**割り込みの罠を仕掛ける** | ★★★<b>作業中に実際に殺されて分かった</b> —— 既定の SIGTERM では `finally` が走らず、<b>43MB×2 の複製が `/tmp` に残った</b>(78 の教訓: 直したあとに何が新しく起きるかを1つは考えること) |
| ★★★**軸21 の番人は、閾値だけを 150ms → 400ms へ広げる** | ★<b>2並列にしたら 81 の軸21 が NG になった</b>。★★<b>頻度ではなく余裕を測った</b>(79 の教訓)—— <b>壊した側の余裕が単独で 29ms・2並列で 5〜16ms しか無かった</b>。★★★<b>遅らせの側(600 → 1200ms)も広げる案は、測って外した</b> —— <b>正しい側は元から 1407ms あり、広げても足りている側しか増えない</b>(設計判断56)。★<b>据え置いたので `tools/batch81_break_check.py` は1文字も触らずに済んだ</b> |
| ★**版数は上げない** | ★★★<b>静的ファイルを1文字も触っていない</b>(7-5・73・77・79 の教訓)。★<b>`verify/verify.js` は配信されない</b> |

---

## 3. ★次のバッチへの申し送り

### 3-1. ★★★並列度を上げてよくなる条件は、はっきりしている

★4並列で赤くなるのは **793件のうち3本**であり、**毎回この2種類だけ**である:

- 「★★タップ表現の非対称: 場のタイルは filter(減光)だけで回らない」
- 「★自席マナのタップは transform(回転)の遷移が実際に走る」

★★<b>この2つを事実待ちへ移せば(設計判断61)、その日から並列度を上げてよい</b>。
★★★<b>上げるときは、番人 S6 の「2」も一緒に上げること</b> ——
<b>あれは「上げた人が赤を見る」ために置いてある</b>。

> ★<b>いま並列度の天井を決めているのは、箱でも実装でもなく<b>番人の書き方</b>である。</b>

### 3-2. ★★固定待ちは 248箇所ある(候補 U2 の一族)

`verify` 1回転 82秒のうち **26秒が固定待ち**である(`waitForTimeout` が 248箇所)。
★<b>82 はこれを1箇所も直していない</b> —— <b>速さの工事と番人の工事を混ぜると、
どちらが効いたか読めなくなるからである</b>。

★★<b>手動モードの音の待ち(候補 U2)は、その中でいちばん小さくて確実な1歩である</b> ——
<b>器(`settled`)も遅らせの口(`SOUND_RESPONSE.delayMs`)も 81 が作ってある</b>。

### 3-3. ★★★83 以降の壊し検証は、表だけ書けばよい

`tools/batch83_break_check.py` は **`CASES` と `EXPECTED_NG` の表だけ**を持てばよく、
回すのは `python3 tools/break_check_runner.py --batch 83` である。

★★<b>ただし「ランナー自身を壊すバッチ」だけは例外である</b>(2章の最後)——
<b>そのときは 82 と同じく、自前で複製を作って回すこと</b>。

★<b>`--self-test` を納品前チェックに足してある</b>(`tools/README.md`)——
★★<b>ランナーを触ったら、まずそれが 15/15 であることを確かめること</b>。

### 3-4. ★★★時間で測っている番人は、まだ他にもある(82 が1本あぶり出した)

★82 は 81 の軸21 を直したが、**同じ形はまだ残っている**:

- ★★**4並列で赤くなる2項目**(3-1)—— <b>あれも「時間で捕まえている」から赤くなる</b>。
- ★★**固定待ち 248箇所・26秒**(3-2)。

★★★<b>82 が学んだのは「並列は余裕の無い番人をあぶり出す道具でもある」ことである</b> ——
<b>軸21 は 81 の時点ですでに余裕 29ms だった</b>。<b>2並列は原因ではなく、発見の手段だった。</b>

> ★<b>次に番人を置くときは、「効いていること」だけでなく
> <b>「効かなくしたときにどれだけ余るか」</b>も同じ日に測ること</b>(裁定186 の一歩先)。

### 3-5. ★★「壊しても落ちない」の13番目の形

81 までに12の形が集まっている。★82 が1つ足す:

> ★★★**13. 壊した先と、測った先が別の木だったとき。**
> ★<b>複製で壊して本体を測ると、全軸が NG になる</b> —— <b>しかも、それらしく見える</b>。
> ★★<b>「番人が足りない」と読んで番人を増やしにいくと、増やした番人も NG になる</b>。
> ★★★<b>NG が<b>全部</b>のときは、番人ではなく<b>測っている場所</b>を疑うこと</b>
> (75・80 の教訓「NG が出たらまず番人を疑う」の、さらに手前にある形)。

★<b>82 の軸12・14 がまさにこの形を作って当てている</b>(報告書を本体から読む / verify を本体で走らせる)。

### 3-6. ★★残っている積み残し(82 は触っていない)

- ★★**手動モードの音の待ちが固定時間のままである**(81・候補 U2)。
  ★<b>82 が通常モード側の余裕を広げたので、手動モード側との差はさらに開いた</b>。
- ★★**通常モードに「相手が切断中」が出ない**(75・候補 S)。
- ★★**手動モードの部屋消失は本文の文字列で判定している**(75・候補 T)。
- ★★**相手の墓地を参照する器が無い**(74・裁定340・候補 R)。
- ★★★**サーバにカードの同一性が無い**(80・候補 V)—— 実寸は**読む側194 / 動かす側77 / 試験503箇所**。
- ★★**儀式が描けない**(80・81・候補 R2)—— <b>解決の途中で配信する口が無い</b>。
- ★★**通常モードと手動モードのモーダルの層は複製である**(78)。
- ★**カードフェイスの実装が2つある**(候補 C)—— ★★<b>82 は JS を1文字も触っていない</b>。
- ★**`GameRoom` が 459行のままである**(75)。
- ★**宣言時に選んだ進化が「選べるのに出ない」**(74)。
- ★★★**70〜81 の実機確認がまだ未報告である。**
  ★<b>82 は画面を1ピクセルも変えていないので、確かめる対象は 81 までのままである</b>。
