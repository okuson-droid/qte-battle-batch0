# QTE 対戦アプリ — 引き継ぎ書 v90

最終更新: 2026-09-04。**Batch 82(壊し検証を複製の上で2並列に = 候補 P)を実施。**

| 項目 | 現在値 |
|---|---|
| JUnit | ★**964件 全緑**(★★**据え置き** —— <b>82 は Java を1文字も触っていない</b>) |
| verify | ★**793件 全緑**(★★**据え置き** —— <b>項目を1件も足していない</b>。★閾値を1つ広げただけである) |
| 効果の未実装 | **0枚**(据え置き) |
| 本文と実装の突き合わせ | **235 / 235枚(完了・据え置き)**。★★★**82 はカード本文を1文字も触っていない** |
| 版数 | ★★★**4系統すべて据え置き** —— `battle.js` v=42 / `battle.css` v=57 /<br>`manual-battle.js` v=34 / `SFX_VERSION`=1。<br>★<b>静的ファイルを1つも触っていない</b>(7-5・73・77・79 の教訓) |
| 裁定 | ★★★**1〜361 のまま。採番0件**(★**79 に続く2例目**)——<br>★<b>ゲームのルールにも画面の振る舞いにも1つも触れていないからである</b>。**新しい裁定は 362 から** |
| カード定義 | 235枚(★★★**1文字も触っていない**) |
| ★**変えたファイル** | ★★**`tools/README.md`**(納品前チェックに1行 + 表に1行)/<br>★★★**`verify/verify.js`**(★<b>閾値1つだけ</b> —— `bWaited >= 150` → `>= 400`) |
| ★新設 | ★★★**`tools/break_check_runner.py`**(壊し検証の共通ランナー・番人15本)/<br>`tools/batch82_break_check.py`(15軸)/<br>`notes/batch82-design-notes.md` / `notes/batch82-ruling-requests.md` |
| ★★消した名前(裁定196) | ★**1つも無い。**★★**番人も1本も撤去していない**(据え置きの番人は 78 以降1本も無い) |
| 壊し検証 | `tools/batch82_break_check.py` ★**15軸(OK 15 / NG 0 / EMPTY 0)**<br>★★★**15軸すべて、落ちた番人はちょうど1本である**(実測した) |
| ★判断待ち | ★★★**1件も残していない**(74〜81 に続く**9バッチ連続**) |

---

## ★★★0. 最初にやること

### ★clone の「新しさ」の確かめ方

`git log -1` の**日付でもコミットメッセージでも測れない**(69 で実際に踏んだ)。確かめるのは:

1. **`tools/break_check_runner.py` が在るか**(★82 で新設)
2. **`tools/batch82_break_check.py` が在るか**(★82 で新設)
3. **`break_check_runner.py` に `def install_signal_traps()` が在るか**(★82 が最後に足した段)
4. **`verify/verify.js` に `bWaited >= 400` が在るか**(★**`>= 150` ではない**)
5. **`tools/README.md` の使い方に `--self-test` の行が在るか**
6. **`node verify/verify.js` が 793件か**(★81 から据え置き)
7. **`mvn test` の件数が 964件か**(★81 から据え置き)
8. **版数が `battle.js` v=42 / `battle.css` v=57(5枚とも)か**(★★**82 は上げていない**)

★★★**77〜82 は同じ push に載るはずである**(78〜82 とも clone し直さずに積んだ)。
★**push は「そのバッチが終わった時点」で一度に行うこと。**

### 手順

1. `qte-project-reference.md` を読む。
2. `notes/qte-pitfalls.md` の該当節を読む。
3. 本ファイルと **`notes/batch82-design-notes.md`** を読む。
4. ★**`notes/batch82-ruling-requests.md` を読む**(判断待ちは0件だが、申し送りが 3-1〜3-6 にある)。
5. ソースコード取得: `git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git`
6. `m2repo.zip` を取り込んで `mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test`。
   ★**964件全緑が出発点である。**緑でなければ止めて報告する。
7. ★★★**壊し検証はもう分けて回さなくてよい** ——
   `python3 tools/break_check_runner.py --batch NN` が<b>複製の上で2並列に回す</b>。
   ★<b>本体は1バイトも書き換わらない</b>ので、殺されても壊れたまま残らない。

---

## 1. ★★★82 がやったこと(要点)

### 1-1. 発端 —— マスターの「検証に時間がかかりすぎている」

81 がそれを受けて実測し、着手せず候補 P として書き残していた ——
<b>`verify` 1回転 80秒 / 壊し検証 verify 12軸で16分 / 複製は1秒未満</b>。

### 1-2. ★★★実寸を測ったら、着手前の見立てが1つ変わった

| 測ったもの | 実測 |
|---|---|
| リポジトリの複製(43MB・`target` 込み) | **0.17秒/本** |
| `verify` 1回転 | **82秒**(うち<b>固定待ちが 26.1秒・248箇所</b>) |
| `verify` 2並列 | **94.5秒/2本** —— ★**1.74倍速**・両方 793/793 |
| ★**JUnit 2並列** | **44.6秒/2本** —— ★★★**1.00倍速(1秒も速くならない)** |
| ★**`verify` と JUnit の混走** | **87.7秒** —— ★★**verify は 793/793 のまま** |
| `verify` 4並列 | 138秒/4本 —— ★★★**3本が赤**(「アニメーションを時間で捕まえる」2種) |

★★★**JUnit は並列にしても速くならない** —— <b>CPU で詰まっているからである</b>。
★<b>`verify` は 82秒のうち 26秒眠っているので、隣に仕事を入れられる。</b>

> ★★<b>並列で得をするのは「待っている番人」だけである。</b>

★**この数字をマスターへ出したうえで、「全軸(verify + JUnit)を複製へ」を選んでもらった** ——
★★<b>速さの数字だけ見れば「verify だけ」が正しく、安全まで数えると「全軸」が正しい</b>。

### 1-3. ★★★本当の見返りは速さではない

81 までの `batchNN_break_check.py` は**本体を直接書き換えて `finally` で書き戻す**形だった ——
★<b>殺されると壊したまま残る</b>(70 で実際に起き、77・79 でも殺された)。

★★★**82 は作業中に自分でそれを証明した** ——
<b>81 の21軸を通した1回目が10分で殺された</b>。
★<b>そのとき `git status` は完全にきれいで、`git diff --stat` は空だった</b>。
★★<b>81 までの形なら、そこには壊れた `battle.js` が残っていた。</b>

### 1-4. ★★★ランナー自身に番人を15本置いた

★**ランナーは Python の道具であり、JUnit にも verify にも照合先が無い** ——
★★<b>だから番人はランナー自身の `--self-test` にしか置けない</b>(設計判断45)。

`S1` 本体無傷(★**更新時刻まで見る**)/ `S2` 壊しが載る / `S3` 片付け / `S4` SETUP-NG /
`S5`・`S5b` EMPTY(★**入口2つ**)/ `S6` 並列度2以下 / `S7` 書き込みのガード /
`S8` 作業場は木の外 / `S9`〜`S12` 走らせる場所(JUnit・報告書・ハーネス・verify)/
`S13` 別々の複製で走る / `S14` 殺されても作業場を残さない

★★<b>`tools/README.md` の納品前チェックに1行足した</b> ——
<b>置いただけで誰も回さない番人は、番人ではない</b>(70 の教訓)。

### 1-5. ★★★並列にしたら、余裕 5ms の番人が1本あぶり出された

★**81 の軸21(音のわざとの遅れ)が、2並列にすると NG になった。**
★★<b>「2並列のせいだ」で済ませず、余裕を測った</b>(79 の教訓)——

| | 実測 | 閾値 150ms との余裕 |
|---|---|---|
| 正しい状態(遅らせ 600ms) | 1407ms | 余裕 1257ms |
| 壊した状態(遅らせ 0ms・単独) | 121ms | ★**余裕 29ms** |
| 壊した状態(遅らせ 0ms・2並列) | 134 / 145ms | ★★★**余裕 5〜16ms** |

★<b>危なかったのは「正しい側」ではなく<b>壊した側</b>だった</b>。
★★★**直したのは閾値1つだけである**(`bWaited >= 150` → `>= 400`)——
★<b>遅らせの側(600 → 1200ms)も広げる案は、測って外した</b>
(正しい側は元から 1407ms あり、広げても足りている側しか増えない)。
★★<b>据え置いたので `tools/batch81_break_check.py` は1文字も触らずに済んだ</b>。

> ★★★<b>2並列は原因ではなく、あぶり出しである</b> ——
> <b>軸21 は 81 の時点ですでに余裕 29ms だった</b>。

---

## 82 の確認項目(★これを照合する)

- **新設**: `tools/break_check_runner.py` / `tools/batch82_break_check.py` /
  `notes/batch82-design-notes.md` / `notes/batch82-ruling-requests.md`
- **`break_check_runner.py`** に `WORKERS = 2` / `make_workspace` / `clone_into` /
  `write_into` / `junit_report` / `_Gauge` / `install_signal_traps` / `run` / `self_test` が在る
- **`verify/verify.js`** に `bWaited >= 400` が在る(★**`>= 150` ではない**)
- **`tools/README.md`** の使い方に `python3 tools/break_check_runner.py --self-test` が在る
- ★★★**カード定義・効果の中身・Java・`battle.js`・`battle.css`・
  `manual-battle.js`・テンプレートは1文字も変わっていない**
- ★★★**`tools/batch81_break_check.py` も1文字も変わっていない**
- `python3 tools/break_check_runner.py --self-test` が **15/15**(★**1秒で終わる**)
- `python3 tools/batch82_break_check.py` が **OK 15 / NG 0 / EMPTY 0**(★**約20秒**)
- `python3 tools/break_check_runner.py --batch 81` が **OK 21 / NG 0 / EMPTY 0**
  ★**実測 585秒(9分45秒)**。★直列の見積もりは約21分なので**約2倍**である。
  ★★**分けて回す必要は無い。本体は1バイトも書き換わらない**
- JUnit **964件全緑**(★**81 から増減なし**)
- verify **793/793**(★**81 から増減なし**)
- `python3 tools/report_effects.py --summary` で **未実装 0枚**
- `python3 tools/mark_text_reviewed.py --stale batch73` が **47枚**(据え置き)
- `python3 tools/check_records.py src/main/java` の**不一致は3件のまま**(★既知の誤検出)

---

## 2. 次の作業(Batch 83)

どれをやるかは**マスターの選択**である。

| 候補 | 内容 | 大きさ |
|---|---|---|
| **★★★L. 実機で触って出てくるもの** | ★★★**80 と 81 で画面が続けて動いた。いちばん確かめてほしい。**<br>★**78 の11項目**・**77 の6項目**・**76 の8項目**も待っている。<br>★★<b>演出も一時公開の帯も「速すぎる / 遅すぎる / 出すぎる / 出ない / 邪魔」が実機でしか分からない</b>。<br>★★★<b>82 は画面を1ピクセルも変えていないので、確かめる対象は 81 までのままである</b> | 小〜中 |
| **★★★U2. 手動モードの音の待ちを事実へ移す** | ★★★**81 が申し送り、82 が「同じ形がまだ残っている」と数え直したもの。**<br>手動モードの `snd` のページは<b>まだ 200ms / 400ms の固定待ち</b>である。<br>★★<b>器(`settled`)も遅らせの口(`SOUND_RESPONSE.delayMs`)も 81 が作ってある</b>ので、そのまま当てるだけで済む。<br>★★★<b>82 の教訓を足すこと</b>: <b>「効いていること」だけでなく<br>「効かなくしたときにどれだけ余るか」も同じ日に測る</b>(裁定186 の一歩先)。★裁定待ちが要らない | 小 |
| **★★S. 「相手が切断中」を通常モードのビューに出す** | ★**75 が書き残した穴**。相手がタブを閉じても「動かなくなった」としか見えない。<br>★直すのは `RoomView.SeatView` に真偽値を1つ足して配信を1行足すだけ。★★**裁定待ちが要らない** | 小 |
| **★★★P2. 固定待ちを事実待ちへ移す(並列度の天井を上げる)** | ★★★**82 が「天井を決めているのは番人の書き方である」と実測して明文化したもの。**<br>`verify` 1回転 82秒のうち<b>26.1秒が固定待ち</b>(`waitForTimeout` が **248箇所**)。<br>★<b>4並列で赤くなるのは 793件のうち3本だけ</b>であり、<b>毎回この2種類である</b>:<br>「タップ表現の非対称」「自席マナのタップは transform の遷移が走る」。<br>★★<b>この2つを事実待ちへ移せば、その日から並列度を上げてよい</b>(番人 S6 の「2」も一緒に上げる)。<br>★★★<b>U2 と同じ一族であり、U2 はその最小の1歩である</b> | 中 |
| **A. 音の調整** | ★**実機で聞いた印象を反映する**(62 からの持ち越し。まだ未報告)。<br>★★**80 が `flip` の音を1つ足した**ので、そこも聞いてほしい | 小 |
| **★★V. サーバにカードの同一性を入れる** | ★★★**80 が実寸を測って外したもの。**<br>**読む側194 / 動かす側77 / 試験503箇所**。<br>★★<b>77箇所のうち1箇所で落としても964件が1件も赤くならない</b> ——<br><b>同一性そのものを測る番人を77本置いて初めて成立する</b> | 大 |
| **★R2. 儀式(配り・マリガン・ダイス)を描く** | ★★★**81 が「本当の理由」を明らかにした** ——<br><b>「材料があっても、それを見せる<b>配信の機会</b>が無い」</b>。<br>★<b>解決の途中で配信する口が要る</b>(設計判断48 の兄弟) | 大 |
| **C. カードフェイスの1本化** | デッキメーカーの `.tile` を `.mcard` へ寄せる。★verify 28件の作り替えが同時に要る。<br>★★<b>80〜82 とも実装は2つのままである</b> | 中〜大 |
| **★T. 手動モードの部屋消失を型へ揃える** | ★75 が片肺として書き残したもの。★★**手動モードは停止中**なので再開時に一緒にやるのが筋 | (再開時) |
| **★R. 相手の墓地を参照する器** | ★74 が「無い」と書き残したもの(裁定340)。★**使い手が0枚**なので作らないのが筋(裁定178) | (作らない) |
| ~~**P. 壊し検証を2並列で回す**~~ | ★★★**82 で入った。** | (完了) |
| ~~**W. 一時公開ゾーン**~~ | ★★★**81 で入った。** | (完了) |
| ~~**B. 通常モードの演出**~~ | ★★★**80 で入った。** | (完了) |
| ~~**U. verify の「時々落ちる2項目」**~~ | ★★★**79 で片付いた。**★★<b>弟は 81、その次は 82</b> | (完了) |

★**わたし(クロエ)の推し**: **L → U2 → S**。
★理由:
1. ★★★**80 と 81 で画面が2バッチ続けて動いたのに、実機確認がまだ1度も返ってきていない。**
   ★<b>82 は画面を触っていないので、この行列は伸びる一方である</b> ——
   ★★<b>verify は「出た / 出ない / 重なっていない」しか測れない</b>。
2. ★★**U2 は 81 が道具ごと用意して申し送ったものであり、82 が「まだ残っている」と数え直した。**
   ★<b>器も遅らせの口も既に在るので、当てるだけで終わる</b>。
   ★★<b>そして 82 の教訓(余裕を測る)を、置いた当日に実演できる</b>。
3. ★★**S は 75 が自分で開けた穴**であり、器を1つ足すだけで終わる。**裁定待ちが要らない。**

★★<b>P2 を推さないのは、U2 がその最小の1歩だからである</b> ——
<b>U2 を1本やってから全体に広げるほうが、効いたかどうかを読み違えない。</b>

### 各バッチがやること(P2から継続・★82 で4つ足した)

1. `python3 tools/report_effects.py --summary` で未実装の枚数を確認する(**0枚が正常値**)。
2. ★★`python3 tools/mark_text_reviewed.py --check` も通す(67 で新設)。
3. ★★★**`python3 tools/break_check_runner.py --self-test` を通す**(★**82 で新設**)——
   ★<b>ランナーを触ったら、まずこれが 15/15 であることを確かめること</b>。
4. **本文が2通り以上に読めるカードは、実装で決めずに必ず裁定を仰ぐ**(裁定184)。
   ★★★★**確定したその場で採番すること**、★★★★★**採番したらその場で
   `notes/qte-rulings.md` へも写すこと**(81 の教訓)。
   ★★<b>ただし「工事の選択」は裁定ではない</b>(★82 の 1章)——
   <b>台帳は本文と規則の読み方の台帳であって、工事の履歴ではない</b>。
5. ★★★**大きい工事は、着手する前に実寸を測ってマスターへ出すこと**(80・81・★82)。
   ★★<b>実寸を出すと、工事の順番だけでなく<b>範囲</b>も<b>答え</b>も変わることがある</b>。
6. **カード毎の JUnit を `support/AutoGameFixture` の上に書く**(裁定187)。
7. `mvn test` で回すこと(`surefire:test` はコンパイルしない。裁定208)。
8. ★★★**壊し検証は `break_check_runner.py` に回させる**(★**82 で新設**)——
   ★<b>`tools/batch83_break_check.py` は `CASES` の表だけを持てばよい</b>。
   ★★<b>本体は1バイトも書き換わらないので、分けて回す必要も、
   前後で `git diff --stat` を突き合わせる必要も無くなった</b>。
   ★★★<b>例外は「ランナー自身を壊すバッチ」だけである</b> ——
   <b>壊す相手を、壊す道具として使うことはできない</b>(★82 がそうした)。
9. ★**改変は「軸」ごとに1件ずつ当てる**(57 の教訓)。
   ★★★**出口ごと・入口ごと・席ごとに当てること**(71・75〜81)。
   ★★★★**82 の追記: <b>1対1であることを「そのつもり」で終わらせず、数えること</b>** ——
   <b>軸ごとに、落ちた番人の本数を数える</b>(82 は15軸すべてちょうど1本だった)。
10. ★★★**壊し検証で NG が出たら、まず実装ではなく番人を疑うこと**(75)。
    ★★★★**80 の追記: <b>EMPTY も同じである</b>**。
    ★★★★★**82 の追記: <b>NG が「全部」のときは、番人ではなく<b>測っている場所</b>を疑う</b>** ——
    <b>壊した先と測った先が別の木だと、全軸が NG になり、しかもそれらしく見える</b>(13番目の形)。
11. ★★★**非同期の向こうを、時間で待たない**(79・設計判断61)。
    ★★★★**81 の追記: 遅らせを入れる場所を間違えると効かない**。
    ★★★★★**82 の追記: <b>「効いていること」だけでなく
    「効かなくしたときにどれだけ余るか」も同じ日に測ること</b>** ——
    ★<b>81 の軸21 は、置いた当日から余裕が 29ms しか無かった</b>(裁定186 の一歩先)。
12. ★★★**母集団は、作業を始める前に固定して控えておく**(73)。
    ★★★★**78: 途中でやめたときも** / ★★★★★**79: 症状を母集団にしない** /
    ★★★★★★**80: 「観測・実装・着地点」の3段で** /
    ★★★★★★★**81: 「観測できるもの」は誰に届いているかまで数える**。
13. ★★★**「変えない」と決めたことにも番人を置く**(74)。
    ★★★★**80: 「違える」と決めたことにも**(裁定358)。
    ★★★★★**81: 「予約」の番人も、塞いだ日に差し替える**。
14. ★★★**規則が n 入口ぶんあるなら、番人も n 入口ぶん要る**(77・79・80・81・★82)。
15. ★★★**器を用意したら、それを読む番人を同じバッチで置く**(77)——
    ★★★★**「あちらが絞ってくれる」と書いたコメントは証拠ではない**(81)。
16. ★★★**直したあとに何が新しく起きるかを1つは考える**(78)。
    ★★★★**82 の実例**: <b>複製にしたら本体は無傷になったが、
    <b>作業場の後始末</b>が新しく要るようになった</b>(殺されて実地で踏んだ)。
17. ★★★**演出は「観測できたこと」だけを語る**(80・設計判断62)。
18. ★★★**描いていない欄は、漏れていても誰も気づかない**(81・設計判断63)。
19. ★★**UI を触るなら、先に端の盤面を作って測る**(65・81)。
20. ★★★**番人を置く場所を選ぶ前に、そこまで届くかを確かめる**(70〜81)。
    ★★★★**82 の追記: <b>届く場所が1つも無いなら、道具自身の中に置く</b>** ——
    <b>そして「誰が回すのか」まで通すこと</b>(納品前チェックに書く)。
21. ★★★**静的ファイルを触っていないなら、版数は上げない**(73・77・79・★82)。
22. ★★★**「作らない」「測って外した」「測って答えが変わった」を書き残す**(75・80・81)。
    ★★★★**82 の追記: <b>「測って要らなかった」も書き残す</b>** ——
    <b>遅らせを 1200ms へ広げる案は、測ったら要らなかった</b>。
23. ★★★**並列で得をするのは「待っている番人」だけである**(★82)——
    <b>眠っていないものは、並べても速くならない</b>。
24. ★★★**「壊して書き戻した」は「触っていない」ではない**(★82)——
    <b>危険は結果ではなく経路に在る。経路を測るには更新時刻のような別の目が要る</b>。

---

## 3. 発注者とのやりとり

- grep優先でファイルを渡り歩き、全体読み込みは避ける。
- **判断に迷う点はまとめて質問する。**1つずつ聞かない。
  ★★★**74 は14件、75 は4件、76 は6件、77 は1件、78 は2件、80 は4件、81 は3件**を着手時に伺った。
  ★**79 と 82 は0件である。**
- ★★★**大きい工事は、実寸を測ってから伺うこと**(80・81・★82)——
  ★<b>マスターは実寸を見て判断を変えられる。数字が無いと変えようがない。</b>
  ★★<b>82 は「JUnit は並列で速くならない」という数字を出したことで、
  工事の<b>範囲</b>の問いが生まれた</b>。
- 呼び方は「クロエ」、発注者は「マスター」。会話は日本語カジュアル体、
  **ドキュメントは通常文体(である調)。**

---

## 4. 既知の落とし穴

**★全文は `notes/qte-pitfalls.md` にある。**

| 節 | 内容 |
|---|---|
| ★★★**壊し検証は複製の上で回す(Batch 82)** | 並列で得をするのは待っている番人だけ / JUnit は 1.00倍 / 「壊して書き戻した」は「触っていない」ではない / 壊す相手を壊す道具として使わない / 1対1は数える / 複製にすると作業場の後始末が新しく要る / NG が全部なら測っている場所を疑う(13番目の形) / 並列は余裕の無い番人をあぶり出す |
| **描いていない欄は、漏れていても誰も気づかない(Batch 81)** | (据え置き) |
| **観測できないものは演出できない(Batch 80)** | (据え置き) |
| **非同期の向こうを、時間で待たない(Batch 79)** | ★**81 が2例目、★82 が3例目を片付けた**(軸21 の閾値) |
| **ボタンの文言が嘘をつくと、押した人が損をする(Batch 78)** | (据え置き) |
| **直すと、直した理由の裏返しが出てくる(Batch 78)** | ★**82 が実例を1つ足した**(作業場の後始末) |
| **用意した側は「使われている」と思い込む(Batch 77)** | (据え置き) |
| **通常モードには「相手が切断中」の表示が無い(Batch 75)** | (据え置き・候補 S) |

---

## 5. デリバリー形式(変更なし)

マスターの Eclipse ワークスペース
(`C:\Users\奥村優斗\OneDrive\ドキュメント\eclipse_workフォルダ\qte-battle-batch0`)へ
直接書き込む。

★★**82 はファイル単位の削除を1件も行っていない。**変更2本と新設4本だけである。
★**関数名は1つも消していない。**★★**番人も1本も撤去していない。**

反映前に必ず実行:

```bash
python3 tools/check_structure.py src/main/java
python3 tools/check_all.py .
python3 tools/check_records.py src/main/java     # ★既知の誤検出3件
python3 tools/check_undeclared.py src/main/resources/static/js/*.js
node --check src/main/resources/static/js/manual-battle.js
node --check src/main/resources/static/js/battle.js
NODE_PATH=/home/claude/.npm-global/lib/node_modules \
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers \
  python3 verify/build_harness.py && node verify/verify.js
mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test
python3 tools/check_legacy_ids.py
python3 tools/report_effects.py --summary
python3 tools/check_leader_abilities.py
python3 tools/check_card_text_numbers.py
python3 tools/mark_text_reviewed.py --check
python3 tools/break_check_runner.py --self-test   # ★★★Batch 82 で追加
```

---

## 6. チャット開始テンプレート

```
QTE Battle の開発を継続する。Batch 83 を行う。

読む順:
1. プロジェクトナレッジ内の `qte-project-reference.md`
2. プロジェクトナレッジ内の `claude/qte-handoff-v90.md`(本ファイル)
3. プロジェクトナレッジ内の `notes/batch82-ruling-requests.md`
   ★判断待ちは0件だが、申し送りが 3-1〜3-6 にある。
4. プロジェクトナレッジ内の `notes/batch82-design-notes.md`
5. `notes/qte-pitfalls.md` の該当節
   (特に「壊し検証は複製の上で回す(Batch 82)」)

環境:
5. git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git
   ★★★clone に「そのバッチの続き」が入っているとは限らない(73 で実際に踏んだ)。
     確かめるのは「tools/break_check_runner.py が在るか」
     「tools/batch82_break_check.py が在るか」
     「break_check_runner.py に def install_signal_traps() が在るか」
     「verify/verify.js に bWaited >= 400 が在るか(150 ではない)」
     「tools/README.md の使い方に --self-test の行が在るか」
     「node verify/verify.js が 793件か」
     「mvn test が 964件か」である。
   ★★★77〜82 は同じ push に載るはずである。
6. 接続フォルダの m2repo.zip を device_stage_files で取り込んで /root/m2work へ展開し、
   mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test を回す。
   ★964件全緑が出発点である。緑でなければ止めて報告する。
7. 82 の反映を本ファイルの「確認項目」で照合し、verify 793/793 と
   report_effects の「未実装0枚」、check_card_text_numbers と
   mark_text_reviewed --check の OK を確認する。
   ★★★あわせて python3 tools/break_check_runner.py --self-test が 15/15 であること。
   ★mark_text_reviewed --stale batch73 は 47枚である。
   ★版数は battle.js v=42 / battle.css v=57(5枚とも)で、82 は上げていない。

作業:
8. 本ファイル2章「次の作業(Batch 83)」の候補からマスターが選んだものを行う。
   ★設計解説は新規ファイル `notes/batch83-design-notes.md` に書く。
   ★裁定を仰ぐ点は `notes/batch83-ruling-requests.md` に1本化し、
     ★★確定したその場で採番すること。新しい裁定は 362 から。
     ★★★採番したら、その場で notes/qte-rulings.md へも写すこと(81 の教訓)。
     ★★工事の選択は裁定ではない(82 の教訓)。
   ★★★大きい工事は、着手する前に実寸を測ってマスターへ出すこと(80・81・82 の教訓)。
     ★★実寸を出すと、工事の順番だけでなく範囲も答えも変わることがある。
   ★★★壊し検証は `tools/batch83_break_check.py` に CASES の表だけを書き、
     python3 tools/break_check_runner.py --batch 83 に回させること(82 で新設)。
     ★★本体は1バイトも書き換わらないので、分けて回す必要は無い。
     ★★★例外は「ランナー自身を壊すバッチ」だけである。
     ★★出口ごと・入口ごと・席ごとに当てること(71・75〜81 の教訓)。
     ★★★1対1であることを「そのつもり」で終わらせず、軸ごとに落ちた番人の本数を数えること(82)。
     ★★★NG が出たら、まず実装ではなく番人を疑うこと(75・80)。
       ★★★★EMPTY も同じである(80)。
       ★★★★★NG が「全部」のときは、測っている場所を疑うこと(82・13番目の形)。
     ★★★★軸を入口ごとに立てて当てる先が無かったら、それは足りない番人である(81)。
     ★★★★2つの軸が同じ番人に当たったら、番人の側を分けること(81)。
   ★★カード本文を1文字でも触ったら、そのカードの実装を突き合わせ直し、
     tools/mark_text_reviewed.py で記録すること(理由の記入は必須)。
   ★★★母集団は作業を始める前に固定して控えること(73 の教訓)。
     ★★★★途中でやめたときも母集団を書き残すこと(78)。
     ★★★★★症状を母集団にしないこと(79)。
     ★★★★★★「観測・実装・着地点」の3段で取ること(80)。
     ★★★★★★★「観測できるもの」は誰に届いているかまで数えること(81)。
   ★★「変えない」「違える」と決めたことにも番人を置くこと(74・80)。
     ★★★据え置きの番人も「予約」の番人も、役目が終わった日に撤去・差し替えること(76・78・81)。
   ★★★規則が n 入口ぶんあるなら、番人も n 入口ぶん置くこと(77・79〜82 の教訓)。
   ★★★器を用意したら、それを読む番人を同じバッチで置くこと(77 の教訓)。
     ★★★★番人の届く場所が1つも無いなら、道具自身の中に置き、
       誰が回すのかまで通すこと(82 の教訓・納品前チェックに書く)。
   ★★★非同期の向こうを待つなら、時間ではなく事実で待つこと(79・設計判断61)。
     ★★待ち方を入れたら、遅らせも同じバッチで入れること。
     ★★★遅らせを入れる場所を間違えると効かない(81 の教訓)。
     ★★★★「効いていること」だけでなく「効かなくしたときにどれだけ余るか」も
       同じ日に測ること(82 の教訓・裁定186 の一歩先)。
   ★★★演出を触るなら、観測できることだけを語らせること(80・設計判断62)。
   ★★★ビューに在るのに画面に無い欄を塞ぐなら、先に公開範囲を確かめること(81・設計判断63)。
   ★★★直したあとに何が新しく起きるかを1つは考えること(78 の教訓)。
     ★★82 の実例: 複製にしたら、作業場の後始末が新しく要るようになった。
   ★★UI を触るなら、先に端の盤面を作って測ること(65・69〜72・81 の教訓)。
   ★★★番人を置く場所を選ぶ前に、そこまで届くかを確かめること(70〜82 の教訓)。
   ★★★静的ファイルを触っていないなら、版数は上げないこと(73・77・79・82 の教訓)。

呼び方は「クロエ」、こちらの呼び方は「マスター」で。会話は日本語カジュアル体、
ドキュメントは通常文体(である調)で。
```

---

## 7. 82 完了時点の積み残し

### ★★★82 が片付けたもの

- ★★★**壊し検証が本体を壊す形だった**(候補 P)…… **完了**。
  ★<b>複製の上で回すので、70 の事故が構造的に起きない</b>。
- ★★★**壊し検証が verify 12軸で16分掛かっていた**(候補 P)…… **完了**。
  ★<b>81 の21軸を通しで <b>585秒(9分45秒)</b> で回した</b>(直列の見積もり約21分・<b>約2倍</b>)。
- ★★**81 の軸21 の番人に余裕が 5〜29ms しか無かった**(★82 が発見)…… **完了**。
- ★★**壊し検証を回す道具に番人が1本も無かった**…… **完了**(`--self-test` 15本)。

### ★★★82 が新しく見つけたもの

- ★★★**JUnit は並列にしても1秒も速くならない**(実測 1.00倍)——
  ★<b>並列で得をするのは「待っている番人」だけである</b>。
- ★★★**並列度の天井を決めているのは、箱でも実装でもなく番人の書き方である** ——
  ★<b>4並列で赤くなるのは 793件のうち3本だけであり、毎回同じ2種類である</b>(候補 P2)。
- ★★★**並列は、余裕の無い番人をあぶり出す道具でもある** ——
  ★<b>81 の軸21 は、置いた当日から余裕が 29ms しか無かった</b>。
- ★★**複製にすると、作業場の後始末が新しく要る**(78 の教訓の実例)——
  ★<b>82 は自分が殺されて実地で踏んだ</b>。

### ★★マスターにお願いすること(実機確認)

- **Eclipse で refresh(F5)→ Run As → JUnit Test。**期待: **全964件が緑**(★**増減なし**)。
- ★**`python3 tools/break_check_runner.py --self-test` が 15/15 になること**(1秒で終わる)。
- ★★★**82 は画面を1ピクセルも変えていない。**
  ★<b>したがって確かめてほしいのは 81 までのままである</b>:
  1. ★**《降臨の伝道師》を出したとき、公開した4枚が画面に出るか**(81)。
  2. ★★**《愚乱怒土地》の【賢魂】で見た2枚に「一時公開(あなただけが見ている)」と出るか**(81)。
     ★★★<b>相手の画面には何も出ないはずである</b>。
  3. ★**ログに公開したカードの名前が並ぶか**(81)。
  4. ★★**帯が盤面の邪魔になっていないか**(81)。
  5. ★**帯が消えるタイミングは自然か**(81)。
- ★★★**70〜81 の実機確認がまだ未報告である**(78 の11項目・77 の6項目・76 の8項目を含む)。
- ★**62 の音の実機確認がまだ未報告である**(手動モード11種・通常モード9種)。
- ★★★**GitHub の push は「そのバッチが終わった時点」で一度に行ってほしい** ——
  ★**77・78・79・80・81・82 は同じ push に載る**。

### 継続中の積み残し

- ★★★**固定待ちが 248箇所・26.1秒ある**(★82 で実測・候補 P2)——
  <b>これが並列度の天井を決めている</b>。
- ★★**手動モードの音の待ちが固定時間のままである**(81 で新設・候補 U2)——
  ★<b>82 が通常モード側の余裕を広げたので、差はさらに開いた</b>。
- ★★**サーバにカードの同一性が無い**(80・候補 V)。
- ★★**儀式が描けない**(80・81・候補 R2)—— <b>解決の途中で配信する口が無い</b>。
- ★★**通常モードに「相手が切断中」が出ない**(75・候補 S)。
- ★★**手動モードの部屋消失は本文の文字列で判定している**(75・候補 T)。
- ★★**通常モードと手動モードのモーダルの層は複製である**(78)。
- ★★**手動モードのデッキ読込(`#deck-file-a` / `-b`)は、まだ verify が起こしていない**(79)。
- ★★**マリガンと割り込みの選択が「送れなかったら消えない」ことは名指しで測っていない**(71)。
- ★**右列の中段が溢れたことに人が気づけるか**は分からない(69)。★★81 の実測で余りは 30px。
- ★★**ドラッグはタッチでは動かない**(70)。
- ★★**本文と実装の突き合わせは 235/235 で完了した**(73)。★**74〜82 は本文を触っていない。**
- ★★**手動モードの `applyManaOverlap` は重なりの上限を「覆う側」の添字で取っている**(65)。
- ★**カードフェイスの実装が2つある**(候補 C)—— ★★**80〜82 とも増やしていない。**
- ★**カード名の表記は揃っていない**。**【常在】：のコロン**(3枚)。
- 進化召喚がモアニールの登場置換で止まったときの素材の扱い(裁定232)。
- **本物の入口から観測できない挙動が5件**(76 で5件目が確定)。
- ★★**相手の墓地を参照する仕組みが1つも無い**(74・裁定340・候補 R)。
- ★★**`resumeChoice` は 76 で 318行になり、`resumeChoiceLate` へ分割した**。
- ★**`GameRoom` が 459行のままである**(75)。★**76〜82 は触っていない。**
- ★**宣言時に選んだ進化が「選べるのに出ない」**(74)。
- ★**「通常プレイする」を選んだあとに強化を問い直さない**(78 で明文化)。
- 手動モード関連の積み残しは一時停止中。

★**裁定は 1〜361 の全361件が確定し、欠番はゼロである。新しい裁定は 362 から。**
