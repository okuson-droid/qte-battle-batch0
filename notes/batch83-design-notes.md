# Batch 83 設計解説 —— 手動モードの音の待ちを事実へ移す(候補 U2)

対象: `verify/verify.js` の**手動モードの音の節**(`snd`)だけ。

★★★**本番のコードは1バイトも変えていない。**
Java・カード定義・`battle.js`・`battle.css`・`manual-battle.js`・テンプレートのすべてが無変更である。
★**したがって版数は4系統すべて据え置きである**(7-5)。

---

## 0. ★★★着手前に、候補 V を測って外した

★**マスターは最初に候補 V(サーバにカードの同一性を入れる)を選んだ。**
★★**着手せず、先に実寸を測った**(80・81・82 の教訓)—— <b>そして外した</b>。

### 0-1. ★候補 V の実寸(80 の数字を測り直した)

| 何 | 実測 | 80 の記録 |
|---|---|---|
| 読む側(6つの String ゾーン) | **195箇所** | 194 |
| 読む側(`manaZone`) | **66箇所** | (別枠になっていなかった) |
| 動かす側(String ゾーン) | **84箇所** | 77 |
| 動かす側(`manaZone`) | **9箇所** | 同上 |
| 試験 | **読み650 / 動かし184 / 25ファイル** | 503 |

★<b>80 の「194」は String ゾーンの195とほぼ一致する</b> ——
★★<b>だが `manaZone` を同じ数に混ぜていたのが、実は間違いだった</b>(次節)。

### 0-2. ★★★測ったら、工事の中身が空になった

**マスターが選んだのは「ビューには載せない」+「ManaCard から始める」だった。**
★<b>この2つは噛み合わない</b>。

1. **`ManaCard` にはもう同一性が在る。**
   `manaZone` の要素の特定のしかたは2種類あり、
   ★<b>`remove(mana)` という<b>オブジェクト参照による特定</b>を、コードが既に使っている</b>
   (`GameService` 526・1249行、`CardEffectRegistry` 499行)。
   ★★<b>`id` を足すのは「できることの二重化」であり、設計判断28 に触れる。</b>
   ★★★<b>`id` が意味を持つのは「線を跨いで名指しする」ときだけである</b> ——
   <b>`MinionInstance` が `instanceId` を持つのは、クライアントが
   「この instanceId を攻撃する」と送ってくるからである</b>。

2. **String ゾーンも、いまは得が無い。**
   `hand.remove(cardId)` は「最初に見つかった1枚」を消すが、
   ★<b>同名の2枚はいま完全に交換可能である</b> ——
   <b>1枚ごとに違う性質(ダメージ・タップ・付与キーワード)を持つのは場のミニオンだけ</b>だからである。
   ★★<b>違う1枚を消しても、観測できる差が1つも生まれない。</b>

> ★★★<b>同一性は、1枚ごとに違う性質を持たせたい日か、
> 線を跨いで名指ししたい日に、初めて意味を持つ。</b>

### 0-3. ★★★もう1つ出たもの —— 載せると構造的な安全が壊れる

`battle.js` の 4209行にこう書いてある:

> ★裏面は名前を1文字も運ばないので、非公開情報が漏れることは構造的に無い。

★<b>裏向きのカードに識別子を載せた瞬間、この性質が壊れる</b> ——
<b>墓地は公開情報なので、「墓地から手札へ戻った id」が後で裏向きマナに現れたら、
相手はそのカードの正体を追える</b>。
★★<b>設計判断63(描いていない欄は、漏れていても誰も気づかない)の警告そのものである。</b>

★★★**だから候補 V は「入れる/入れない」ではなく「公開範囲の裁定」から始まる工事である。**
<b>マスターが「載せない」を選んだ以上、いまの形では中身が空になる</b> —— <b>だから外した</b>。

★<b>次に候補 V を検討するときの入口は3つある</b>(引き継ぎ書の候補表に書いた)。

---

## 1. ★★★母集団(作業を始める前に固定した)

★**「248箇所ある」を母集団にしない。**それは全体の数であって、このバッチの相手ではない。
数えるのは「**手動モードの音の節の待ちが、それぞれ何を待っていて、外したら誰が困るか**」である。

★★母集団を **3段**で取った(80 の教訓)。

### 1-1. ★★★母集団A —— `snd` の節の固定待ちを1つずつ数えた

★**引き継ぎ書は「200ms / 400ms の固定待ち」と書いていたが、実際は違った。**
★★<b>症状を母集団にしなかったから出てきた</b>(79 の教訓)。

| 行 | ms | 何を待っていたか | 実際に働いていたか |
|---|---|---|---|
| 4249 | **200** | `goto` 後 —— <b>音のファイルの<b>取得</b></b> | ★★★**働いていた** |
| 4265 | 60 | 配信の後(`moveDelivery` の中) | ★**死に荷物** |
| 4279 | 60 | unlock 後 —— <b>バッファの<b>復号</b></b> | ★**死に荷物**(下記) |
| 4300 | 60 | 配信の後(3枚同時) | ★**死に荷物** |
| 4318 | 60 | 配信の後(上限超え・**0件を測る**) | ★**死に荷物** |
| 4355 | 60 | クリックの後(**0件を測る**) | ★**死に荷物** |
| 4358 | 60 | クリックの後(鳴る) | ★**死に荷物** |
| 4373 | **200** | `reload` 後 | ★**死に荷物** |
| 4375 | 60 | クリックの後(モーダル) | ★**死に荷物** |

★★★<b>実測: 60ms を7件すべて消しても 793/793(2並列でも緑)。
だが `goto` 後の 200ms を消すと「カードが動く配信で実際に音が鳴る」が `[]` で赤くなる。</b>

> ★<b>「まだ 200ms / 400ms の固定待ちのままである」は症状の記述だった。</b>
> <b>働いていたのは1つだけで、残りは8件とも死に荷物だった。</b>

### 1-2. ★★★母集団A で出たもの —— 待つべき事実は「取得」ではなく「復号」である

仕組みを読むと、音は2段で用意される:

```
ページを開く   → sfxPreload() → sfxFetch()  … バイト列を取る(AudioContext は要らない)
最初の操作     → sfxUnlock()  → sfxDecodeAll() … 復号する(★AudioContext が要る)
```

★★★<b>だから `goto` の直後には、バッファは<b>構造的に</b>揃わない</b> ——
<b>実測で8秒待っても揃わなかった</b>(`ok=false`)。
★<b>200ms が働いていたのは「取得を unlock に間に合わせていた」からである。</b>

★★<b>ところが、取得が間に合わなくても困らない</b> ——
`sfxFetch` は届いた時点で `if (sfxCtx) sfxDecode(name)` を呼ぶ。
★★★<b>つまり「取得の完了を待つ」という段そのものが要らない。</b>

### 1-3. ★母集団B —— 何を作れるか

| # | 作るもの | 83 で置くか |
|---|---|---|
| B1 | unlock 後に**復号の完了**を事実で待つ | ★**置く** |
| B2 | わざとの遅れ(600ms)を `goto` の前に | ★**置く**(81 の教訓: 遅らせる場所) |
| B3 | 遅らせを自分で戻す番人 | ★**置く**(3章) |
| B4 | 配信の後の固定待ちを撤去する | ★**置く** |
| B5 | 撤去の根拠(**同期に鳴ること**)を測る番人 | ★**置く**(壊し検証が要求した・5章) |
| B6 | 「鳴らないこと」を事実で待つ | ★置かない —— <b>待つべき事実が存在しない</b> |
| B7 | 残り 239箇所の固定待ち | ★置かない(候補 P2・別のバッチ) |
| B8 | 並列度を上げる | ★置かない(6章・**天井は上がっていない**) |

### 1-4. ★★母集団C —— 着地点

★**すべて `verify/verify.js` の中である。**
★★<b>本番のコードには着地点が無い</b> —— <b>実装は既に正しく、待ち方だけが時間依存だった</b>。

---

## 2. ★★★何を作ったか

| 何を | どうしたか |
|---|---|
| `goto` 後の 200ms | ★**撤去**し、代わりに<b>わざとの遅れ 600ms を `goto` の前に</b>置いた |
| unlock 後の 60ms | ★★★**事実待ちへ**(`sfxBuffers` が全部そろったこと)+ 遅らせを自分で戻す |
| 配信・クリック後の 60ms × 6 | ★**撤去**(根拠は 5章の軸4 が測る) |
| `reload` 後の 200ms | ★**撤去**(1-5) |

★★★**`snd` の節の固定待ちは 9箇所 → 0箇所になった。**
★verify 全体では **248箇所 / 26059ms → 239箇所 / 25239ms**(**−9箇所 / −820ms**)。

### 2-1. ★★実測した余裕(82 の教訓)

★**「効いていること」だけでなく「効かなくしたときにどれだけ余るか」も同じ日に測った。**

| | 実測 | 閾値 400ms との余裕 |
|---|---|---|
| **正しい状態**(遅らせ 600ms) | **1522〜1783ms** | ★余裕 1122ms 以上 |
| **壊した状態**(遅らせ 0ms) | **34ms** | ★★**余裕 366ms** |

★<b>直す前の固定待ち 60ms に対する余裕は 26ms しか無かった</b> ——
79 の候補U(5ms)・81 の軸21(29ms)と同じ一族である。

### 2-2. ★`reload` 後の待ちは、事実待ちにしてから<b>撤去した</b>

いちど「台本が載ったこと」を待つ形にしたが、**外しても2並列で 797/797 だった** ——
Playwright の locator が実操作の前に待つからである。

> ★★<b>壊せない待ちを残すのは、偽の番人を1本置くのと同じである</b>(裁定196)。

★<b>ここの race は既存の check が自分で捕まえる</b> ——
<b>台本が載る前に押していたら、localStorage の '55' が当たらない</b>。

---

## 3. ★★★番人(4本新設・verify 793 → 797)

| 番人 | 何を測るか |
|---|---|
| ★**手動モードも音のバッファは時間ではなく事実で待つ**(83・設計判断61) | `settled` が真を返したこと |
| ★**遅らせが効いている**(83・裁定186) | unlock から 400ms 以上掛かったこと |
| ★**遅らせは自分で元に戻す**(83) | 節を抜けた時点で `delayMs === 0` であること |
| ★★★**音は配信の中で同期に鳴る**(83) | <b>配信と計数を同じ evaluate に閉じて数えたこと</b> |

★★★**4本目は壊し検証が要求して生まれた**(5章)。

---

## 4. ★★直したあとに、何が新しく起きたか(78 の教訓)

★**遅らせを入れたので、戻し忘れという新しい事故が生まれた。**
★<b>戻し忘れても誰も赤くならない</b> —— <b>この先の節が全部 600ms ずつ遅い箱で回るだけである</b>。
★★<b>だから「遅らせは自分で元に戻す」を番人にした</b>(79 が同じ形を置いている)。

---

## 5. ★★★壊し検証(`tools/batch83_break_check.py`)—— 4軸・OK 4 / NG 0 / EMPTY 0

★★**回すのは `tools/break_check_runner.py` である**(82・設計判断64)——
このファイルは `CASES` の表だけを持つ。★<b>本体は1バイトも書き換わらない</b>。

| 軸 | 壊すもの | 落ちる番人 |
|---|---|---|
| 1 | わざとの遅れを外す | 遅らせが効いている(83) |
| 2 | 事実待ちを固定待ち(1000ms)へ戻す | バッファは時間ではなく事実で待つ(83) |
| 3 | 片付け(遅らせを戻す)をしない | 遅らせは自分で元に戻す(83) |
| 4 | 音を配信の外(非同期)で鳴らす | 音は配信の中で同期に鳴る(83) |

★★★**1対1であることを数えた**(82 の作法)—— <b>4軸すべて、落ちた番人はちょうど1本だった</b>。

### 5-1. ★★★軸4 は最初 NG だった —— 根拠に番人が無かった

★<b>`setTimeout(..., 0)` で鳴らしても、既存の番人は1本も落ちなかった</b>。

理由: <b>`deliver` と `audioNodes` が別の evaluate なので、そのあいだにマクロタスクが走る</b>。
★★<b>既存の番人は「同期であること」ではなく「往復より速いこと」しか測っていなかった</b>。

★★★<b>ここで気づいた</b> ——
<b>83 は「実装が同期に鳴らすから待たなくてよい」を根拠に固定待ちを6件撤去したのに、
その根拠を測る番人が1本も無かった</b>。

> ★<b>根拠にしたものには、その日に番人を置くこと</b>(77 の教訓「器を用意したら、
> それを読む番人を同じバッチで置く」の、根拠の側の顔である)。

★<b>そこで「配信と計数を同じ evaluate に閉じて数える」番人を1本足した</b> ——
<b>往復を1回も挟まないので、`setTimeout(..., 0)` でも捕まる</b>。

### 5-2. ★★軸2 をわざと 1000ms にした理由

★<b>60ms に戻すと、軸1の番人(遅らせが効いている)まで道連れになる</b> ——
<b>1つの改変が2つの番人に当たったら、それは軸の切り方が悪い</b>(82 の教訓)。
★★<b>1000ms なら「遅らせは効いている」ままで「事実で待っていない」だけを作れる</b>。
★★★<b>しかもこれは「たいてい通る長さの固定待ちへ戻した」という、
いちばん有りそうな戻し方そのものである。</b>

### 5-3. ★★壊しどころが無いもの(裁定196 の正直な扱い)

1. **「鳴らないこと」を測っている2件**(差分が上限を超える / 確認を出しただけ)。
   ★<b>「何も起きないこと」には待つべき事実が無い</b> —— <b>事実待ちへは移せない</b>。
   ★★<b>ただし固定待ちを撤去したので、以前より強くなっている</b> ——
   <b>配信が返った直後という決まった一点で 0件を測る</b>ようになった。
2. **`manual-battle.js` の音の仕組み。**★83 は1文字も触っていない
   (軸4 は複製の上で壊すだけである)。
3. **`reload` 後の待ち。**★撤去したので壊しどころが無い(2-2)。
4. **通常モード側の音の待ち。**★81・82 が既に事実待ちである。
5. **並列度の天井。**★83 は上げていない(6章)。

---

## 6. ★★★並列度の天井は上がっていない

★**4並列で赤くなる項目は 3本 → 1本に減った**が、**ゼロではない**。

| | 82 の実測 | ★83 の実測 |
|---|---|---|
| 4並列で赤くなる本数 | **3本** | ★**1本** |
| 残っているもの | タップ表現の非対称 / 自席マナのタップ | ★**自席マナのタップ**(transform の遷移) |

★★★<b>だから `WORKERS` も番人 S6 の「2」も据え置きである。</b>
★<b>上げてよいのは、残った1本を事実待ちへ移した日である</b>(候補 P2)。

> ★★<b>「赤が減った」は「上げてよい」ではない。</b>
> <b>上限は、いちばん危ない1本が決める。</b>

---

## 7. 版数(7-5)

| ファイル | 82 | ★83 | 理由 |
|---|---|---|---|
| `battle.js` | v=42 | **v=42(据え置き)** | ★★★**1文字も触っていない** |
| `battle.css` | v=57 | **v=57(据え置き)** | ★同上 |
| `manual-battle.js` | v=34 | **v=34(据え置き)** | ★★<b>軸4 は複製の上で壊すだけである</b> |
| `SFX_VERSION` | 1 | 1(据え置き) | ★音声ファイルは1つも増えていない |

★★★**静的ファイルを触っていないなら、版数は上げない**(73・77・79・82 の教訓)。

---

## 8. ★★★このバッチが残した教訓

### 8-1. 申し送りの見立ても、母集団にしてはいけない

引き継ぎ書は「200ms / 400ms の固定待ち」と書いていた。
★実際は **200ms×2 と 60ms×7** で、★★<b>働いていたのは `goto` 後の 200ms だけ</b>だった。

> ★★★<b>79 の教訓「症状を母集団にしない」は、<b>前のバッチが書いた症状</b>にも当てはまる。</b>
> <b>申し送りは出発点であって、母集団ではない。</b>

### 8-2. ★★★根拠にしたものには、その日に番人を置く

83 は「実装が同期に鳴らすから待たなくてよい」を根拠に固定待ちを6件撤去した。
★<b>その根拠を測る番人は1本も無かった</b> —— <b>壊し検証の軸4 が NG で教えた</b>。

> ★<b>77 の教訓(器を用意したら、それを読む番人を同じバッチで置く)の、<b>根拠の側の顔</b>である。</b>
> ★★<b>「実装がこうなっているから省ける」と書いたら、その「こうなっている」を測ること。</b>

### 8-3. ★★壊せない待ちは、偽の番人である

`reload` 後の待ちは、事実待ちにしても**外して落ちなかった**。
★<b>そこで待ちごと撤去した</b> —— <b>残せば「何かを守っているように見えるが何も守らないもの」になる</b>。

> ★<b>裁定196(消した番人は書き残す)は、<b>置かなかった待ち</b>にも効く。</b>

### 8-4. ★★「赤が減った」は「上げてよい」ではない

4並列の赤は 3本 → 1本に減ったが、★<b>上限はいちばん危ない1本が決める</b>。
★★<b>だから並列度は 2 のままである。</b>

### 8-5. ★★★実寸は、工事の中身が空であることも教える(候補 V)

★<b>80 が「大きい」と測った工事を、83 の着手前に測り直したら<b>中身が空になった</b></b> ——
<b>`ManaCard` には既に同一性が在り、String ゾーンには区別する理由がまだ無い</b>。

> ★★<b>実寸は「順番」も「範囲」も「答え」も変えるが、</b>
> <b>ときには「その工事は今やることではない」も教える。</b>

---

## 9. ★★★マスターにお願いすること

★**このバッチは画面を1ピクセルも変えていない。**実機で見えるものは何も変わらない。

- ★★★**Eclipse で refresh(F5)→ Run As → JUnit Test。**
  期待: **全964件が緑**(★**81 から1件も増減していない**)。
- ★**`python3 tools/break_check_runner.py --self-test` が 15/15 になること**(1秒で終わる)。
- ★★★**70〜81 の実機確認がまだ未報告である**(81 の5項目・78 の11項目・77 の6項目・76 の8項目)。
  ★<b>82 も 83 も画面を触っていないので、この行列は伸びる一方である。</b>
- ★**62 の音の実機確認もまだ未報告である**(手動モード11種・通常モード9種)。
  ★★<b>83 は音の<b>検証の待ち方</b>を直しただけで、<b>音そのもの</b>は1つも変えていない。</b>
