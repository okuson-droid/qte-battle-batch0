# QTE 対戦アプリ — 引き継ぎ書 v80

最終更新: 2026-08-27。**Batch 72(試合の出入り)を実施し、続けて ★★★不具合修正 72b(賢魂がスペル枠で使えない)を実施。**

| 項目 | 現在値 |
|---|---|
| JUnit | ★**814件 全緑**(777 → 805 → **+9**)|
| verify | ★**706件 全緑**(据え置き。★**72b は verify を1件も足していない** —— 理由は下の 72b 節)|
| 効果の未実装 | **0枚**(据え置き) |
| 本文と実装の突き合わせ | **47 / 235枚**(据え置き。★**72 はカード本文を1文字も触っていない**) |
| 版数 | ★**`battle.css` v=53(★参照する5枚すべて)/ `battle.js` v=35**<br>(`manual-battle.js` v=34 / `SFX_VERSION`=1 は据え置き) |
| 裁定 | **1〜323 の全323件が確定。欠番ゼロ**<br>★★**72 も裁定を1件も起こしていない**が、**マスターの回答4件が実質の裁定である**(下の 2章)。**新しい裁定は 324 から** |
| カード定義 | 235枚(★72 では1文字も触っていない) |
| ★**Java** | ★★★**変えた**(72: `GameRoom` / `GameService` / `GameActions` / `PlayerState` / ビュー / WS の入口)<br>★**72b が変えたのは `GameWsController.java` ただ1つである**(`PlayCardRequest.enhanced` を箱型に / 変換失敗の受け皿) |
| ★新設 | (72)`tools/batch72_break_check.py` / `Batch72SeatTest.java` / `notes/batch72-design-notes.md`<br>★(72b)`tools/batch72b_break_check.py` / `src/test/java/com/example/qte/WsRequestPayloadTest.java` / `src/test/java/com/example/qte/WsErrorRoutingTest.java` / `notes/batch72b-bugfix-notes.md` |
| ★削除 | **ファイル単位の削除は無い**(★事前実測用の使い捨てスクリプトは納品に含めていない) |
| 壊し検証 | `tools/batch72_break_check.py` **40軸(OK 40 / NG 0)**<br>★`tools/batch72b_break_check.py` **6軸(OK 6 / NG 0)** |

---

## ★この文書の構成(v56 から継続)

| 文書 | 役割 | 更新のしかた |
|---|---|---|
| `notes/qte-rulings.md` | **裁定1〜323 の全文・欠番ゼロ** | 追記専用。★**72 は1件も足していない** |
| `notes/qte-pitfalls.md` | 既知の落とし穴 | 追記・訂正のみ。★**72 で1節を新設し、72b でもう1節(「クライアントとサーバの『あいだ』」)を新設した** |
| `qte-project-reference.md` | プロジェクト定義・ゲームルール・設計判断 | 72 で 1-3/1-4/1-5/1-7・2-6・4章(**設計判断50・51 を新設**・9/27/28/38/40/45/46/49 に追記)・6章・7-5 |
| 本ファイル(ハンドオフ) | 直近バッチの要点・次の作業 | 毎バッチ書き直す |
| ★**`notes/batch72b-bugfix-notes.md`** | ★★**72b の調査記録と修正の解説**(新規)**先に読むこと** | — |
| ★**`notes/batch72-design-notes.md`** | ★**72 の設計解説** | — |
| `notes/batch71-design-notes.md` | 71 の設計解説。★**10章の実機確認の依頼はまだ未報告である** | — |
| `notes/batch70-design-notes.md` | 70 の設計解説。★**10章の実機確認の依頼はまだ未報告である** | — |

---

## 0. ★最初にやること

★**71 → 72 では消え残りは起きていない。**72 が**ファイル単位で削除したものは無い**。

### ★★★clone の「新しさ」の確かめ方(69 で実際に踏んだ)

`git log -1` の**日付でもコミットメッセージでも測れない**。確かめるのは次の3つである。

1. **72 が作ったはずのファイルが在るか**(下の「72 の確認項目」)
2. **`GameRoom.java` に `public Spectator standUp(` が在るか**(72 の中心である)
3. **`mvn test` の件数がそのバッチの値か**(**814件**)
4. ★**`GameWsController.java` に `onUnreadableMessage` が在るか**(72b の中心である)

### 手順

1. `qte-project-reference.md` を読む(★**4章の設計判断50・51 が新設された**)。
2. `notes/qte-pitfalls.md` の該当節を読む
   (★特に**「★★★試合の出入り —— 席・退室・投了・再戦(Batch 72)」**と
   「通常モードの切断(Batch 71)」、「通常モードの受付(Batch 66)」、
   「モーダル・Esc・フォーカス(Batch 36・39・40)」)。
3. 本ファイルと **`notes/batch72b-bugfix-notes.md`**(★不具合の教訓)と **`notes/batch72-design-notes.md`** を読む。
4. ★**`notes/batch70-design-notes.md` と `batch71-design-notes.md` は
   「読む必要があれば読む」で足りる** —— ただし ★**どちらも 10章の実機確認の依頼が生きている**。
5. ソースコード取得: `git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git`
6. `m2repo.zip` を取り込んで `mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test`。
   ★**814件全緑が出発点である。**緑でなければ止めて報告する。

---

## ★★★72b がやったこと(不具合修正)

調査記録と解説は **`notes/batch72b-bugfix-notes.md`**(新規)。
マスターの報告は **「スペルのエリアにドラッグしても賢魂が使えない。落とせるけど何も起きない」**。

### 1. 原因は「クライアントとサーバのあいだ」に在った

クライアントは正しく `play-soul` を送り、`send()` も `true` を返していた。
サーバのハンドラも正しかった。壊れていたのは **受け口の型** である。

```java
// 72 まで
public record PlayCardRequest(String playerId, int handIndex,
        List<TargetChoice> targets, boolean enhanced, ...) {}
```

`play-soul` は `enhanced` を送らない —— **そしてそれは正しい**。
【賢魂】に強化使用は無く、`playSoul` の javadoc 自身が「`enhanced` は読まない」と書いている。
だが Spring Boot 4(Jackson 3)は、**原始型の部品が本文に無いと変換ごと失敗させる**。
変換は `@MessageMapping` に入る前に起きるので **`execute` も `sendError` も通らない** ——
サーバのログにだけ例外が出て、押した人の画面には何も返らない。

★**クリックから賢魂を使う道も同じ理由で壊れていた**(あちらも `extra` が `{}`)。

### 2. 直したのは2つ

1. **原因** …… `enhanced` を `Boolean` にし、`materialIds()` / `manaIndexes()` と同じ
   **防御アクセサ**で null を false に畳む(新しい筋を持ち込んでいない。裁定130)
2. **無言そのもの** …… `@MessageExceptionHandler(MessageConversionException.class)` を置き、
   **開けなかったメッセージの送り主へ理由を返す**(設計判断51)。
   ★宛先から `roomId`、読めない本文から `playerId` だけを拾う。
   拾えなければサーバのログだけが残る

### 3. 番人を2つ新設した(★どちらも「境目」に立っている)

- **`WsRequestPayloadTest`**(7件)……
  `battle.js` が実際に組み立てる本文を、**アプリと同じ変換器**へ通す。
  宛先22すべて・**入口ごとに1行**の全29件の表を持つ。
  ★**入口を足したらこの表に1行足すこと。**
- **`WsErrorRoutingTest`**(2件)……
  Spring が実際に使う `SimpAnnotationMethodMessageHandler` に壊れた本文を流し込み、
  ブローカー行きの通路に ERROR が出ることを見る。
  ★例外ハンドラを直接呼ぶ試験では「届かないこと」を見張れない(70・71 の教訓)。

### 4. ★★★verify に軸を1つも置いていない理由

ハーネス(`verify/build_harness.py`)は **Java を起動しない**。
`verify/fixture.js` がビューを `render()` へ直接流し込む形なので、
**送った本文がサーバで開けるかは、あちらでは永久に測れない**。
★これは手抜きではなく、**この不具合を見つけられなかった理由そのもの**である。

### 5. ★★調べ方の教訓

ハーネスの上で何度測っても再現しなかった。再現したのは
**本物のサーバを起こして本物のページを叩いた**ときである。

```
java -cp "target/classes:$(find /root/m2work/repository -name '*.jar' \
        | grep -v sources | tr '\n' ':')" com.example.qte.QteBattleApplication
```

★`mvn spring-boot:run` はオフラインではプラグインが無くて動かない。

---

## 72b の確認項目(★これを照合する)

- **新設**: `tools/batch72b_break_check.py` /
  `src/test/java/com/example/qte/WsRequestPayloadTest.java` /
  `src/test/java/com/example/qte/WsErrorRoutingTest.java` /
  `notes/batch72b-bugfix-notes.md`
- **`web/GameWsController.java`** …… `PlayCardRequest` の `enhanced` が **`Boolean`** であり、
  `public Boolean enhanced()` が在る。`onUnreadableMessage` / `roomIdOf` / `playerIdOf` が在る
- JUnit **814件全緑**(★`WsRequestPayloadTest` 7件 + `WsErrorRoutingTest` 2件)
- `python3 tools/batch72b_break_check.py` が **OK 6 / NG 0**
- ★**verify・カード定義・版数(`battle.js` v=35 / `battle.css` v=53)は72から変わっていない**

---

## ★★72 がやったこと(要点)

設計解説は **`notes/batch72-design-notes.md`**(新規)。
マスターの指示は **「I(席を立てるようにする)」** に、着手後の追加で
**「投了の宣言」** と **「決着後にデッキを読み込みなおして再戦」** が加わった。

### 1. ★★★3つは同じ1つの話題だった

「この試合に居るかどうかを、人が自分で決められるようにする」——
**66 は入るところだけを作り、出るところを1つも作らなかった。**

| 状態 | 席を立つ | 席に着く | 退室 | 投了 | 再戦 |
|---|---|---|---|---|---|
| WAITING(`GameState == null`) | ○(観戦可の部屋) | ○ | ○ | × | × |
| SETUP / PLAYING | × | × | **観戦者のみ** | ○ | × |
| FINISHED | × | × | ○ | × | ○ |

★**どれがいつ通るかは `GameState` の有無で決まる**(設計判断50)。

### 2. ★★★66 の「席を立てない」は、理由のほうが結論より狭かった

66 の Javadoc は「席は `GameState` の2人と1対1であり、**試合が始まったあとに**
動かすと盤面の持ち主が消える」と書いていた —— **その理由は「始まったあと」にしか掛かっていない。**
72 は理由の掛かる範囲だけを守り、掛からない時間帯(WAITING)を開けた。

★★**64 の「暫定」・65 の「簡易版」と似ているが、こちらは誤りではない** ——
理由と結論が同じ幅で書かれていただけである。

### 3. ★★★id は変えない / 型は分けたまま

席と観戦者を行き来しても id は同じ文字列である(**配信の宛先**であり **localStorage の値**)。
★★**不変条件: 1つの id は席と観戦者のどちらか一方にしか居ない。**
両方に居ると同じ宛先へ2回配信され、どちらにも居ないと1回も届かない ——
**画面からは決して見えない事故である。**

### 4. ★★★手動モードの退室の形は写せなかった(71 の教訓の2例目)

手動モードは送って即遷移する。**あちらの退室が失敗しないから**成り立つ形である。
通常モードは対戦中の着席者を断る —— 同じ形だと、拒否の理由が**遷移後の端末**へ届いて
誰も読まず、しかも localStorage は消えているので**席を持ったまま戻れなくなる**。

→ **受理されたことを1通返す**(`WsMessage` の `type=LEFT`)。
★**返事を待つ形は切断にも強い**(送れなければ返事も来ない = 何も起きない)。

### 5. ★★★投了は決着の5つ目の入口。いつでも押せる

`requireTurnPlayer` を通さない —— あれは「相手が選択中です」でも弾くので、
**割り込みで盤面が固まったときに投了もできなくなる**。
★手動モードの「リセットは絶対に止めない」と同じ筋である。

★★**そのせいで「割り込み待ちのまま決着する盤面」が初めて作れるようになった。**
→ `GameActions.finish` が決着時に**答えようのない待ち**をたたむ
(問い合わせの待ち行列 / 保留していた戦闘 / ターンの受け渡し / 解決中のカード)。
★**盤面そのものは触らない**(決着後も読める)。

### 6. ★★再戦は申し込み → 承諾の2段(マスターの選択)

片方が押しただけで捨てる形も採れた(デッキを読み直すまで始まらないので、読むことが同意を兼ねる)。
**採らなかったのは、それが相手の見ている画面を相手の同意なしに消すからである。**

★捨てるもの: 盤面・ダイスの勝者・申し込みの旗・両席のデッキ・ログ。
★捨てないもの: 席・名前・id・**`ready`(購読の旗)**。

### 7. ★★確認モーダルを通常モードにも作った(裁定53)

★CSS は複製せず、`battle.css` の `.manual-confirm` と**同じ宣言ブロックを共有**した
(71 の「規則は1つ、名前は2つ」)。1965 は**切断オーバーレイ(1970)より下**である(裁定56)。
★確認を置いたのは**取り返しのつかない4つだけ**(投了・席を立つ・退室・再戦に応じる)。

### 8. ★作らなかったもの・直さなかったもの

- **無人部屋の掃除** …… 66 の積み残しのまま。退室しても**部屋は残る**。
  ★「まずは作らない」ではなく**別の工事である**。
- **通常モードの素の `confirm()` 7箇所** …… 宣言(【賢魂】・特殊召喚・強化)であり層が違う。
  ★★**ただし裁定53 の理由3(JS を止める)は宣言にも効く。片肺として書き残した。**
- **フォーカストラップ**(裁定50) …… 通常モードには `modalStack` も焦点の閉じ込めも元から無い。
  72 は初期フォーカス(裁定52)だけを守った。★**作っていないものは壊せない。**

### 9. ★★★壊し検証が番人の穴を2つ見つけた

- **軸5** …… 2つの誤りが重なっていた。(1) 改変が当たっていなかった(形1)、
  (2) 直しても**番人が届かなかった**(**65 の形**)——
  両席が埋まった盤面では「席が埋まっている」の判定が先に弾く。
  → `決着後に空いた席にはまだ座れない` を新設。
  ★★★**新しく作れるようにした状態には、新しく番人が要る。**
- **軸36** …… 72-16 が `onMessage` を直接叩いており、**`leaveRoom()` を1度も通っていなかった**。
  ★71 の「番人が壊した枝を通っていない」の新しい顔である。
  ★★★**しかもその改変は検証スクリプトを途中で殺した**(前の項目が遷移を起こした)——
  遷移を起こしうる項目は末尾に置き、以降の `evaluate` には必ず `catch` を持たせること。
  **死ぬ検証は、番人ではなく無音である。**

---

## 72 の確認項目(★これを照合する)

- **新設**: `tools/batch72_break_check.py` / `src/test/java/com/example/qte/Batch72SeatTest.java` /
  `notes/batch72-design-notes.md`
- **`room/GameRoom.java`** …… `standUp` / `takeSeat` / `leave` / `rematchOfferedBy` /
  `resetForRematch` が在る。★`spectate` が**名前を必須にしている**
- **`room/PlayerSlot.java`** …… `clearDeck` が在る
- **`game/GameService.java`** …… `concede` / `standUp` / `takeSeat` / `leave` / `rematch` /
  `RematchAction`(OFFER / ACCEPT / DECLINE)が在る。
  ★**`concede` が `requireTurnPlayer` を呼んでいない**
- **`game/GameActions.java`** …… `finish` が `clearPendingChoices` / `setPendingAttack(null)` /
  `setTurnHandoffPending(false)` / `setResolvingCardId(null)` を呼ぶ
- **`game/PlayerState.java`** …… `clearPendingChoices` が在る
- **`game/view/RoomView.java`** …… `rematchOfferedBySeat` / `rematchOfferedByName` が在る
- **`web/GameWsController.java`** …… `/seat` `/leave` `/concede` `/rematch` が在り、
  `execute` に `onSuccess` の口が在る
- **`web/GameBroadcaster.java`** …… `sendLeft` と `WsMessage.ofLeft`(type=LEFT)が在る
- **`static/js/battle.js`** …… `askConfirm` / `closeAutoConfirm` / `isAutoConfirmOpen` /
  `toggleSeat` / `standUpFromSeat` / `concede` / `leaveRoom` /
  `offerRematch` / `acceptRematch` / `declineRematch` /
  `openSeatChangeGate` / `closeSeatChangeGate` / `seatGateMode` /
  `renderRoomControls` / `renderResult` / `forgetOccupant` が在る。
  ★**`isGateVisible` が `seatGateMode === 'JOIN'` を見る**
- **`templates/battle.html`** …… `id="btn-seat"` / `id="btn-concede"` / `id="btn-leave"`
  (★**ヘッダ行の中**)/ `id="auto-result"` / `id="btn-rematch-offer"` /
  `id="btn-rematch-accept"` / `id="btn-rematch-decline"` / `id="auto-confirm"` が在り、
  **`battle.js` v=35 / `battle.css` v=53**
- **`static/css/battle.css`** …… `.info-modal.manual-confirm,` と `.info-modal.auto-confirm` が
  **同じ宣言ブロックを共有**している(値が2度書かれていない)
- **`battle.css` v=53 は5枚とも**(`battle.html` / `cards.html` / `manual-cards.html` /
  `manual-battle.html` / `manual-deck-maker.html`)
- JUnit **814件全緑** / verify **706/706**
- `python3 tools/report_effects.py --summary` で **未実装 0枚**
- `python3 tools/check_all.py .` が **「項目 1・3・5・6 はすべてパスしました」**
- `python3 tools/check_card_text_numbers.py` が **OK**
- `python3 tools/mark_text_reviewed.py --check` が **「全235枚が突き合わせ済み」**
- `python3 tools/check_records.py src/main/java` の**不一致は3件のまま**(★既知の誤検出)

---

## 1. 次の作業(Batch 73)

どれをやるかは**マスターの選択**である。

★★**72 で「候補 I(席を立てる)」は終わった。**表から消えている。

| 候補 | 内容 | 大きさ |
|---|---|---|
| **★★L. 70〜72 の手触りの調整** | ★**実機で触ってみて出てくるもの**(ドラッグの当たり判定・光りの濃さ・確定の位置・切断の帯・★72 のボタンの並びと確認の文言)。★62 の音と同じ扱いで、**マスターの報告待ち**である | 小 |
| **A. 音の調整** | ★**実機で聞いた印象を反映する**(62 からの持ち越し。まだ未報告) | 小 |
| **★M. 部屋消失の検出** | ★**71 が意図的に残した穴である。**通常モードは部屋が消えても「再接続中」のまま永遠に見える。手動モードの `showRoomLostFatal` にあたるものを作り、71 の案内と排他にする。★**旗(`connectionFatal`)は「立てる人」ができてから作る**。★★**72 で退室ができたので、「部屋が消える」経路そのものが近づいた**(候補 N と合わせると自然である) | 中 |
| **★★N. 無人部屋の掃除** | ★**72 が退室を作ったのに、部屋は残ったままである**(66 からの積み残し)。手動モードの `ManualCleanupScheduler` にあたるものを作る。★M と同じ工事の裏表であり、**まとめてやるのが安い** | 中 |
| **B. 通常モードの演出** | 62 が作った差分の採取層に fx 層を乗せる。32a・32b・35・38 の4バッチぶん | 大 |
| **C. カードフェイスの1本化** | デッキメーカーの `.tile` を `.mcard` へ寄せる。★verify 28件の作り替えが同時に要る | 中〜大 |
| **D″. 総点検の続き** | 67 の残り188枚。`tools/mark_text_reviewed.py --stale batch67` がそのまま母集団である | 中 |
| **★O. 通常モードの確認を1本化する** | ★**72 が片肺として書き残したものである。**宣言のための素の `confirm()` が7箇所残っており、裁定53 の理由3(JS を止めると STOMP の受信も止まる)はそちらにも効く。★フォーカストラップ(裁定50)も同時に要る | 小〜中 |

★**わたし(クロエ)の推し**: **L(と A)**。
★理由は 71 のときと同じで、しかも強くなっている。
1. **70 は遊び方を、71 は事故のときの見え方を、72 は試合の出入りを変えた。**
   どれも**人が触ってみないと良し悪しが決まらない**種類の変更である。
2. **実機確認が 35〜72 まで積み上がっている**(46b と 48 以外ほぼ未報告)。
   ★48 の教訓 —— **目視だけが唯一の番人である領域で、目視が滞ると事故になる**。
3. **触ってから決めるほうが安い。**

★★**次点は M + N をまとめて。**72 が退室を作ったことで、
「部屋が空になる」という状態が初めて日常的に起きるようになった ——
**掃除が無いまま退室だけが増えると、部屋の一覧が死んだ部屋で埋まる。**

### 各バッチがやること(P2から継続・★72 で3つ足した)

1. `python3 tools/report_effects.py --summary` で未実装の枚数を確認する(**0枚が正常値**)。
2. ★★`python3 tools/mark_text_reviewed.py --check` も通す(67 で新設)。
   **カード本文を1文字でも触ったら、そのカードの実装を突き合わせ直して記録すること。**
3. **本文が2通り以上に読めるカードは、実装で決めずに必ず裁定を仰ぐ**(裁定184)。
4. **カード毎の JUnit を `support/AutoGameFixture` の上に書く**(裁定187)。
   ★★**割り込みに答えるときは `answerChoice` を使い、添字を直接書かない**(68 で新設)。
5. `mvn test` で回すこと(`surefire:test` はコンパイルしない。裁定208)。
6. ★**壊し検証の改変は「軸」ごとに1件ずつ当てる**(57 の教訓)。
   ★★★**出口が複数ある関数は、出口ごとに当てること**(71)。
7. ★★**壊しても落ちないとき、「試験が足りない」の前に疑うことが11ある**
   (59・60・64・65・66・67・68・69・70 で2つ)。★**72 も新しい形を増やしていない**が、
   既知の2つの**新しい顔**を踏んだ ——
   **番人が壊した枝を通っていない**(65 の形。★72 は「**作れるようになったばかりで、
   まだその盤面を作っていなかった**」という顔)/
   **番人が実際の入口を通っていない**(71 の形。★`onMessage` を直接叩いていた)。
8. ★**画面(テンプレート)を作ったら、MockMvc で 200 と中身を測る試験を1件は置く**(61 の教訓)。
   ★★★**ただしそれを「効いている」の番人だと読まないこと**(71・72)——
   MockMvc が測れるのは**宣言**だけである(設計判断46)。
9. ★★**音を触ったら、実機でマスターに聞いてもらう**(62 の教訓)。
10. ★★**「暫定でこうしておく」は、前提が消えても自分では戻らない**(64 の教訓・**時間**)。
11. ★★**「簡易版」は、本家が直っても自分では直らない**(65 の教訓・**空間**)。
12. ★★★**「いつ直すか」を書いても、実装は自分では動かない**(66 の教訓・**予定**)。
13. ★★★**コメントに書いた本文は、本文が変わっても自分では変わらない**(67 の教訓・**写し**)。
14. ★★★**別の性質の「代用」として書いた条件は、代用元が変わっても自分では変わらない**
    (68 の教訓・**代用**)。
15. ★★★**「まずは〜から」と書いた実装は、そこで止まったまま動かない**(69 の教訓・**途中**)。
16. ★★★**「書いてあるのに効いていない」は、クラスの数を数える検証では見つからない**
    (70 の教訓・**空文**)。
17. ★★★**片方のモードで直したことは、もう片方が直ったことではない**(71 の教訓・**片肺**)。
    ★★**72 で新しい片肺を1つ書き残した** —— 通常モードの素の `confirm()` 7箇所(候補 O)。
18. ★★★**同じ穴を塞ぐことと、同じ形で塞ぐことは別である**(71 の教訓)。
    **写すのは判断の筋であって、値や置き場所ではない。**
    ★★★**72 で2例目が出た** —— 71 は「値と置き場所」を写さなかったが、
    72 は**手順そのもの**を写せなかった(退室は失敗しうるので、送って即遷移できない)。
19. ★★★**理由の幅と結論の幅を揃えて書く**(★72 の教訓・**幅**)。
    66 の「席を立てない」は、理由が「試合が始まったあと」にしか掛かっていなかった ——
    ★**誤りではない。**時間・空間・予定・写し・代用・途中が「離れていく/動かない」形なのに対し、
    これは**最初から結論のほうが広かった**という形である。
    ★**次の人は結論だけを読むので、理由の狭さは見えない。**
20. ★★★**送りっぱなしにしてよいのは、失敗しない操作だけである**(★72 の教訓)。
    失敗しうる操作は**受理されたことを返す** —— でないと、
    拒否の理由が「もう居ない相手」へ届く。
21. ★★**UI を直すバッチは、先に端の盤面を作って測る**(65 の教訓。69 で2つ・70 で1つ・
    71 で2つ・★**72 で1つ**拾った ——「ヘッダの伸びしろが 940px もあった」ので、
    メニューボタンに畳む案を採る理由が消えた)。
    ★★**実測は捨ててよいが、実測から出た不変量は番人に昇格させること**(71)。
22. ★★**「番人が無い」と思ったら、まず在るかどうかを見る**(65 の教訓。66・67・69・70・71・
    ★**72 でも踏んだ** —— 席替えの画面は `#seat-gate` がそのまま使えた)。
23. ★★**消した番人は必ず書き残す**(裁定196)。★**作らなかった番人も**(67)。
    ★★★**「穴が構造ごと消えた」ときも**(68)。★★★★**「作らないと決めたもの」も**(69)。
    ★★★★★**「壊しどころが無いもの」も**(70・★72)。★★★★★★**「代用で済ませたもの」も**(71)。
24. ★★★**番人は「回る場所」で選ぶ**(69・設計判断45)。
    ★★★**選ぶ前に「そこまで届くか」を確かめる**(70)——
    **verify はフィクスチャで描く。Java のビューは verify からは見えない。**
    ★★★★**逆向きもある**(71)—— Java を変えないバッチは **verify にしか照合先が無い**。
    ★★★★★**72 は両方あった** —— 席の状態は JUnit、出し分けと遷移は verify である。
25. ★★★**「宣言」だけを先に作らない**(69・設計判断46)。**安全側に見える簡易版を作らない。**
26. ★★★**「1回きりの操作」を1回のクリックで成立させない**(70・設計判断47)。
    ★★**72 は「取り返しのつかない4つ」に確認を置いた** ——
    ★<b>全部に置かない</b>。全部に確認を出すと、確認そのものが読み飛ばされる。
27. ★★★**送れなかった操作は、ローカルの選択状態を畳まない**(71・設計判断49)。
    ★★**72 の追記: 「戻す処理を書く」ことではなく「畳んだものを失わない」ことである** ——
    返事を待つ形にすれば、畳むものが無いので戻す処理も要らない。
28. ★★**静的ファイルの版数はファイルに付く。**`battle.css` を触ったら**5枚とも上げる**(7-5)。
29. ★★★**長い壊し検証は、前後で差分を見る**(70)。殺されると壊したまま残る。
    ★★**72 は37軸あり、3つに分けて回して前後の `git diff --stat` を突き合わせた。**
30. ★★★**遷移を起こしうる検証の項目は末尾に置く**(★72)。
    途中に置くと、壊れた実装のときに**そこで検証スクリプトごと死に、以降が1つも走らない**。
    ★以降の `evaluate` には必ず `catch` を持たせること。**死ぬ検証は、番人ではなく無音である。**

---

## 2. 発注者とのやりとり

- grep優先でファイルを渡り歩き、全体読み込みは避ける。
- **判断に迷う点はまとめて質問する。**1つずつ聞かない。
  ★**72 は実装の前に4件まとめて聞いた**(観戦不可の部屋での席の立ち方 / 投了の範囲 /
  再戦の同意 / 確認の出し方)。**UI と運用は好みが出るので、測ってから聞くのがよい。**
- ★★**72 のマスターの回答4件は実質的な裁定である。**カード本文の読みではないので
  `notes/qte-rulings.md` へは起こしていない(裁定は 1〜323 のまま)。
  ★**324 番として記録するかはマスターに伺いたい。**
- 呼び方は「クロエ」、発注者は「マスター」。会話は日本語カジュアル体、
  **ドキュメントは通常文体(である調)。**

---

## 3. 既知の落とし穴

**★全文は `notes/qte-pitfalls.md` にある。**72 で1節を新設した。

| 節 | 内容 |
|---|---|
| ★**試合の出入り(Batch 72)** | 理由の幅と結論の幅 / id は変えない・型は分ける / 送りっぱなしにしてよいのは失敗しない操作だけ / 逃げ道は他の規則に左右されない / 決着で待ちをたたむ / 同意は「誰の画面が消えるか」で決める / ready は購読の旗 / 決着後に空いた席には座れない / status===WAITING の一致 / ゲートは同じ要素でも意味が2つ / 確認は4つだけ / 確認は 1965 / 素の confirm 7箇所は片肺 / フォーカストラップは無い / 番人は実際の入口から / 無人部屋の掃除は無い |
| **通常モードの切断(Batch 71)** | 番人は send でオーバーレイではない / 1970 が最前面かは読んで決めない / 同じ穴を同じ形で塞がない / 送れなかったのに畳む / 出口ごとに番人を当てる / 偽の緑 |
| **通常モードの受付(Batch 66)** | 席・観戦・ゲート・TDZ・効いていない CSS の1行 |
| **モーダル・Esc・フォーカス(Batch 36・39・40)** | ★**72 で通常モードにも確認モーダルが来た。**裁定49・52・53・56 はそのまま効く |

---

## 4. デリバリー形式(変更なし)

マスターの Eclipse ワークスペース
(`C:\Users\奥村優斗\OneDrive\ドキュメント\eclipse_workフォルダ\qte-battle-batch0`)へ
直接書き込む。手順はv62の4章のとおり。

★★**72 はファイル単位の削除を1件も行っていない。**変更と新設だけである。

反映前に必ず実行:

```bash
python3 tools/check_structure.py src/main/java
python3 tools/check_all.py .
python3 tools/check_records.py src/main/java     # ★既知の誤検出3件
python3 tools/check_undeclared.py src/main/resources/static/js/*.js
node --check src/main/resources/static/js/manual-battle.js
node --check src/main/resources/static/js/battle.js
NODE_PATH=/home/claude/.npm-global/lib/node_modules \
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers \
  python3 verify/build_harness.py && node verify/verify.js
mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test
python3 tools/check_legacy_ids.py
python3 tools/report_effects.py --summary
python3 tools/check_leader_abilities.py
python3 tools/check_card_text_numbers.py
python3 tools/mark_text_reviewed.py --check
```

---

## 5. チャット開始テンプレート

```
QTE Battle の開発を継続する。Batch 73 を行う。

読む順:
1. プロジェクトナレッジ内の `qte-project-reference.md`
   ★4章の設計判断50(試合の出入りは GameState の有無で決まる)と
     51(失敗しうる操作は送りっぱなしにしない)が Batch 72 で新設された。
2. プロジェクトナレッジ内の `claude/qte-handoff-v80.md`(本ファイル)
3. プロジェクトナレッジ内の `notes/batch72b-bugfix-notes.md`
   ★★不具合修正の記録である。**次に何かが「押しても何も起きない」ときは、まずこれを読む。**
4. プロジェクトナレッジ内の `notes/batch72-design-notes.md`
5. `notes/qte-pitfalls.md` の該当節
   (特に「クライアントとサーバの『あいだ』(Batch 72b)」
    「試合の出入り —— 席・退室・投了・再戦(Batch 72)」
    「通常モードの切断(Batch 71)」
    「通常モードの受付(Batch 66)」
    「モーダル・Esc・フォーカス(Batch 36・39・40)」)

環境:
5. git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git
   ★★clone の「新しさ」はコミットメッセージでも日付でも測れない(69 で実際に踏んだ)。
     確かめるのは「GameWsController.java に onUnreadableMessage が在るか」
     「mvn test が 814件か」である。
6. 接続フォルダの m2repo.zip を device_stage_files で取り込んで /root/m2work へ展開し、
   mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test を回す。
   ★814件全緑が出発点である。緑でなければ止めて報告する。
7. 72 と 72b の反映を本ファイルの「確認項目」2つで照合し、verify 706/706 と
   report_effects の「未実装0枚」、check_card_text_numbers と
   mark_text_reviewed --check の OK を確認する。

作業:
8. 本ファイル1章「次の作業(Batch 73)」の候補からマスターが選んだものを行う。
   ★設計解説は新規ファイル `notes/batch73-design-notes.md` に書く。
   ★壊し検証は `tools/batch73_break_check.py` を新設し、改変は「軸」ごとに1件ずつ当てる。
     ★★出口が複数ある関数は、出口ごとに当てること(71 の教訓)。
     ★★長い壊し検証は前後で差分を見ること(70 で殺されて壊したまま残った)。
     ★★★遷移を起こしうる検証の項目は末尾に置き、以降の evaluate には catch を持たせること
       (72 の教訓。死ぬ検証は番人ではなく無音である)。
   ★★カード本文を1文字でも触ったら、そのカードの実装を突き合わせ直し、
     tools/mark_text_reviewed.py で記録すること(理由の記入は必須)。
   ★★UI を触るなら、先に端の盤面を作って測ること(65・69・70・71・72 の教訓)。
     ★★★他モードから写すときは「判断の筋」を写し、値・置き場所・手順は測り直すこと
       (71・72 の教訓)。
   ★★★番人を置く場所を選ぶ前に、そこまで届くかを確かめること(70・71・72 の教訓)。
   ★★★「新しく作れるようにした状態」には、新しく番人が要る(72 の教訓)。
   ★★★WS の宛先や入口を増やしたら、その本文を WsRequestPayloadTest の表に足すこと
     (72b の教訓。送る側と受ける側を別々に見ている番人は、あいだを見ていない)。

呼び方は「クロエ」、こちらの呼び方は「マスター」で。会話は日本語カジュアル体、
ドキュメントは通常文体(である調)で。
```

---

## 6. 72b 完了時点の積み残し

★**72b が新たに載せたもの(マスターに諮る案件を含む):**

- **★★★まだ原始型で受けている WS の項目がある。** いまはどの入口も送っているので落ちないが、
  同じ形の地雷である —— `ChooseOrderRequest.goFirst` / `HandActionRequest.handIndex` /
  `PlayCardRequest.handIndex` / `GraveSummonRequest.trashIndex` /
  `TabooRequest.tabooIndex` / 手動モードの `ManualDragCue.Request.active`。
  ★全部を箱型にするなら、`handIndex` が null のときに **`IllegalArgumentException` で断る**
  関門が要る(素の自動開封は `NullPointerException` になり、`execute` が捕まえないので
  **また無言に戻る**)。→ **設計判断としてマスターに諮ること。**
- **手動モードには `WsRequestPayloadTest` にあたる表がまだ無い**
  (`ManualOpRequest` 系の本文を受け口へ通す試験)。

★以下は 72 完了時点からの持ち越しである。


### ★★マスターにお願いすること(実機確認)

- **Eclipse で refresh(F5)→ Run As → JUnit Test。**期待: **全805件が緑**。
- ★★★**72 の確認**(詳細は `notes/batch72-design-notes.md` の10章)
  - **受付の画面で [席を立つ]、観戦者で [席に着く] が効くか。**
  - **対戦中に [投了] が押せるか**(相手のターン中・割り込み待ちでも)。
    ★決着後に問いと [確定] が消えているか。
  - **決着すると右列に決着の面が出て、盤面はまだ読めるか。**
  - **[再戦を申し込む] → 相手の [応じる] で、両者ともデッキ読み込みへ戻るか。**
  - **[退室] でロビーへ戻るか。対戦中は [退室] が出ないか。**
  - **確認モーダルの見た目・初期フォーカス・Esc。**
  - **ヘッダにボタンが3つ増えて窮屈でないか**(実測ではまだ 714px の伸びしろが残っている)。
- ★★**マスターの回答4件を裁定324〜327 として `qte-rulings.md` に起こすか**を伺いたい(2章)。
- ★★★**70・71 の実機確認がまだ未報告である。**
  ★**70 は遊び方そのものを変えている**ので、そちらを先に触ってもらうほうが価値が高い。
- ★**62 の音の実機確認がまだ未報告である**(手動モード11種・通常モード9種)。
- ★**64 の問い合わせ・65 のマナ行・66 のロビーと観戦・67 の7枚・68 の手触り・
  69 の盤面も未報告である。**
- ★**GitHub の push はコミットメッセージを合わせてほしい**(69 の着手時、
  中身は 68 なのにメッセージが `batch67` のままだった)。

### 継続中の積み残し

- ★★**通常モードには無人部屋の掃除が無い**(★72 で改めて表に出た・候補 N)。
  72 が退室を作ったので、**部屋が空になる経路が日常的になった** ——
  掃除が無いまま退室だけが増えると、一覧が死んだ部屋で埋まる。
- ★★★**通常モードには「部屋が消えた」の検出が無い**(71 で明文化・候補 M)。
  サーバが再起動すると STOMP は再接続を試み続け、購読しても盤面が二度と来ない ——
  人には**「再接続中」のまま永遠に**見える。
- ★★**通常モードには宣言のための素の `confirm()` が7箇所ある**(★72 で片肺として明文化・候補 O)。
  ★フォーカストラップ(裁定50)も通常モードには無い。
- ★★**マリガンと割り込みの選択が「送れなかったら消えない」ことは名指しで測っていない**(71)。
  実装は同じ形だが**代用で済ませてある**。
- ★**右列の中段が溢れたことに人が気づけるか**は分からない(69)。
- ★★**ドラッグはタッチでは動かない**(70)。HTML5 のドラッグの制約である。
- ★**本文と実装の突き合わせは 47/235 枚である**(67・候補 D″)。
- ★★**手動モードの `applyManaOverlap` は重なりの上限を「覆う側」の添字で取っている**(65)。
  ★実害は出ていない。
- ★**通常モードに演出が無い。**62 が差分の採取層を作ったので土台はできている(候補 B)。
- ★**カードフェイスの実装が2つある**(デッキメーカーの `.tile` と盤面の `.mcard`。候補 C)。
- ★**カード名の表記は揃っていない**(「愚乱怒土地(グランドランド)」の半角括弧など)。
- **【常在】：のコロン**(3枚)。
- 35〜72 の実機確認が未報告のまま(46bと48は報告あり)。
- 進化召喚がモアニールの登場置換で止まったときの素材の扱い(裁定232)。
- **本物の入口から観測できない挙動が3件** ——
  《英霊・コレキ》/《風弾の跳弾》/《悪夢》の封じの範囲。
- 手動モード関連の積み残しは一時停止中。

★**裁定は 1〜323 の全323件が確定し、欠番はゼロのままである。**
★★**70・71・72 は裁定を1件も起こしていない。新しい裁定は 324 から。**
