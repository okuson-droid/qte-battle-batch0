# QTE 対戦アプリ — 引き継ぎ書 v56

最終更新: 2026-08-19。**Batch 49(水文明の効果6枚)完了**。P2 の2本目。

| 項目 | 現在値 |
|---|---|
| JUnit | **260件 全緑** |
| verify | **533/533** |
| 効果の未実装 | **50枚**(進化18 + 非進化32) |
| 版数 | `manual-battle.js` v=33 / **`battle.js` v=22** / `battle.css` v=46 |
| 裁定 | 197番まで確定(次は **198** から) |

---

## ★★★この文書の構成が変わった(2026-08-19 改訂)

**v55 まではハンドオフ書を毎バッチ全文書き直していた。**その結果 v56 は 65KB になり、
中身の8割が前版からの引き写しになっていた。**これは裁定163(同じ規則を2箇所に置かない)と
裁定171・188(引き写しは腐る)に、この文書自身が反していた状態である。**
実際 v51 以降は「詳細は v51 にある」という参照が増え、限界が見えていた。

**そこで3つに割った。**

| 文書 | 役割 | 更新のしかた |
|---|---|---|
| **`notes/qte-rulings.md`** | 裁定1〜197 の**全文** | **追記専用。**新しい裁定を末尾に足すだけ |
| **`notes/qte-pitfalls.md`** | 既知の落とし穴(28節) | **追記・訂正のみ。**該当する節に足す |
| **本ファイル**(ハンドオフ) | 直近バッチの要点・確認項目・次の作業・開始テンプレート | 毎バッチ書き直す(**が、20KB 前後に収める**) |

★**ハンドオフ書に裁定の全文や落とし穴の全文を書き戻さないこと。**
番号と一行要約、または節の名前だけを書き、正は上の2ファイルに置く。

★**実測: 65KB → 19KB。**割る前の見積もりは「8KB」だったが、これは外れた。
理由は2つあり、どちらも**割ってみて初めて見えた**ものである。

1. **バッチごとの中身に下限がある。**「49 が変えたこと + 確認項目」で 3KB、
   「積み残し」で 2.5KB。これは引き写しではなく<b>そのバッチにしか無い情報</b>なので削れない。
2. **ハンドオフ書の中にも重複が育っていた。**「次の作業」と「開始テンプレート」が
   互いを写しており、合わせて 11KB あった。テンプレートを<b>手順だけ</b>にして
   中身は1章を指すようにし、5.5KB → 1.5KB になった。
   ★**割る作業そのものが、同じ病気をもう1件見つけた**(裁定163 は入れ子で効く)。

---

## 0. 最初にやること

1. **`notes/ver11-migration-plan.md`** を読む(46〜55 の作業の正。★バッチ番号は 49 で詰めた)。
2. **`notes/qte-pitfalls.md`** を読む。★作業対象に関係する節は必読。
   Batch 50 なら「サンドボックスの制約」「通常モードのゲームプレイ試験」
   「効果の実装状況・効果未実装の印」「対象指定(TargetSpec)の落とし穴」
   「消えたフィールド」の5節。
3. **`notes/qte-rulings.md`** は**引く**もので、通しで読むものではない。
   実装中に番号が出てきたら引く。
4. 本ファイルと、直近バッチの design-notes を読む。
5. `qte-project-reference.md` を読む。★1章の実装状況は2026-07-21のまま古い(P5 で更新)。
6. ソースコード取得: `git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git`
7. **「反映済み」を信じず、直近バッチの変更箇所を実際に読んで照合する**(下の「49 の確認項目」)。

---

## ★カードデータの正(46b で確定)

- **`src/main/resources/cards/manual-cards.json`(Ver1.1・全235枚)が全モード共通の正。**
  通常モード・手動モード・デッキメーカーの3つとも同じファイルを読む(裁定D1 の案B)。
- **カードが実際に持つキーは10個だけ**(★49 で実測):
  `id / name / type / civilization / cost / attack / hp / imageId / ledgerCardId / text`。
  **`keywords` と `unlimitedCopies` は存在しない**(落とし穴「消えたフィールド」)。
- **キーワードはテキストから作る。**`CardTextKeywords.extract(text)` が唯一の出どころ(裁定158)。
- 内訳: リーダー18 / ミニオン119 / **進化18** / スペル61 / ウェポン19。6文明×39 + 文明なし1。
- **進化18枚はデッキ構築で弾かれる**(裁定166)。P3(52〜53)で解禁する。
- **進化以外は全部デッキに入れられる。**未実装なら盤面に印が出る(裁定D2)。
- 実装状況: **登録あり124 / 宣言あり41 / 未実装50**(`python3 tools/report_effects.py --summary`)。

---

## ★★49 が変えたこと(要点)

設計解説は **`notes/batch49-design-notes.md`**。

1. **水文明の未実装6枚を実装した。**ロロイヨ伯爵(リーダー)/ 海獣リューグー /
   潮獣ビシャカワ / 潮獣コアンチ / ギガマウス・バイト / アルキンティス。
   ★**水に残る未実装は進化3枚だけ**になった(風と同じ状態)。
2. **「場にミニオンが出た」の発火口 `CardEffectRegistry.fireAnyMinionEntered` を作った。**
   ★**`TriggerType` は足していない**(裁定194)。使い手2枚がどちらもリーダーの【常在】で、
   足せば「誰も登録しない値」が増えるだけだったため。
3. **`PlayerState` にターン刻印を2本足した**(守護用・潜伏用。独立。裁定156(1))。
4. **`GameActions.putIntoFieldByEffect` が `MinionInstance` を返すようになった**(void から変更)。
5. **`TargetSpec.Filter.WATER_CIVILIZATION` を足した。**
   ★`battle.js` の2箇所にも同じ `case` が要る(裁定195)。版数 v=21 → v=22。
6. **verify 49-1(フィルタの Java↔JS 突き合わせ・2項目)を足した**(531 → 533)。
7. **新しい裁定を8つ確定した(190〜197)。**全文は `notes/qte-rulings.md`。

## 49 の確認項目(★これを照合する)

- **`CardEffectRegistry.fireAnyMinionEntered`** がある / 反復が**両者のリーダー**である /
  ドロー先が `ctx.owner()` ではなく **`watcher`** である
- **`TriggerType` に新しい値が増えていない**(48 の `ON_TURN_START` が最後)
- **`GameService.summonToField`** と **`GameActions.putIntoFieldByEffect`** の
  **両方**に `effects.fireAnyMinionEntered(ctx)` がある
- **`PlayerState`**: `guardEntryDrawTurn` / `stealthEntryDrawTurn` の**2本**(1本にまとめない)
- **`CardEffectRegistry`**: `LOLOIYO_LEADER` 定数 + `IMPLEMENTED_CARDS` に6枚 /
  `registerWaterVer11Cards()` / `resolveChoice` に `TIDE_COANCHI_DISCARD` の分岐
- **`StatCalculator`**: `GIGAMOUSE_BITE` / `ARKINTIS` 定数 + `IMPLEMENTED_CARDS` に24枚 /
  `effectiveCost` の減算が **`owner.getHand().size()` そのまま**(自身を含む。裁定190)
- **`TargetSpec.Filter.WATER_CIVILIZATION`** + `GameService` の `case` +
  **`battle.js` の2箇所の `case 'WATER_CIVILIZATION'`**
- **版数 `battle.js` v=22** —— `battle.html` と `verify/build_harness.py` の**両方**
- **`WaterVer11EffectTest`(28件)** / `EffectImplementationTest` 9 → 11 = **JUnit 260件**
- **`EffectImplementationTest`**: 「50枚」「進化18 + 非進化32」/ 水6枚を名指しする試験 /
  ★「効果が未実装のスペルに印が付く」の題材が**《英術・グラーニス》に差し替わっている**
  (《潮獣ビシャカワ》は 49 で実装したため)
- **verify 533/533** / `report_effects.py --summary` で **WATER の未実装が 3**
- `decks/batch49-water-check-deck.json`(`DeckValidator` を通ることを確認済み)

★48 以前の確認項目は各版のハンドオフにある(48 = v55 / 47 = v54 / 46b = v53 / 46a = v52 / 24〜45 = v51)。

---

## 1. 次の作業(★Batch 50〜51 = P2 の残り)

**★★2026-08-19 にバッチの粒度を変えた(マスター指示)。**
49 までは1バッチ1文明だったが、**残り4文明を2バッチにまとめる**。
立ち上げ(clone / m2repo / 全緑の確認 / 直近バッチの照合)は1バッチにつき1回の固定コストであり、
カード数に比例しない。文明を2つ束ねれば、その固定コストが半分になる。

| バッチ | 文明 | 今すぐ実装できる枚数 | 後送り |
|---|---|---|---|
| **50** | **闇 + 光** | 闇6 + 光5 = **11枚** | 闇2(【賢魂】→ P4)/ 光2(【進化】→ P3) |
| **51** | **火 + 土** | 火5 + 土8 = **13枚** | 火3(進化を参照 → P3)/ 土1(【賢魂】→ P4) |

★**50 は闇から手を付けること。**P2 に残る最後の発火の仕組み
(場以外から自分の墓地に置かれたとき = カムバックキーパー)の使い手を持っている。
**ただし器の種類は使い手を読むまで決めない**(裁定194)。カムバックキーパーはミニオンなので
今度は `TriggerType` が正しいかもしれないが、**読んでから決める。**

### 各バッチがやること

1. 未実装の一覧を `python3 tools/report_effects.py`(--summary なし)で出し、実装する。
2. **本文が2通り以上に読めるカードは、実装で決めずに必ず裁定を仰ぐ**(裁定184)。
   ★**2文明ぶんをまとめて1回で聞く。**
3. **ルール側に実装したら、そのクラスの `IMPLEMENTED_CARDS` に足す**(裁定176)。
   ただし `CardEffectRegistry` の表への登録は**リテラルのまま**にする(ツールが左辺で数えるため)。
4. `EffectImplementationTest` の**枚数を減らし、その文明のカードを名指しする試験も足す**
   (裁定162・181)。★「印が付く」側の題材が陳腐化していないかも見る(49 で1件差し替えた)。
5. **カード毎の JUnit を `support/AutoGameFixture` の上に書く**(裁定187)。
   `WaterVer11EffectTest` が雛形。**効果は本物の入口から起こす。**
6. **`TargetSpec.Filter` に値を足したら `battle.js` の2箇所にも足し、版数を上げる**(裁定195)。
7. 成果物の書き方は下の「運用ルール」に従う。

### ★★運用ルール(2026-08-19 にマスターと決めた)

**効率化の合意である。守るものと削るものをはっきりさせてある。**

**削るもの:**

- **ハンドオフ書は 20KB 前後に収める。**裁定と落とし穴の全文を書き戻さない。
  ★**テンプレートに1章の中身を写さない**(ここが v56 でいちばん膨らんでいた)。
- **設計解説は「新しい判断」だけを厚く書く。**
  既存カードの写しで済むもの(49 のアルキンティス = 影潜む水刺客の写し、
  潮獣ビシャカワ = 数えて回復するだけ)は**表に1行**でよい。
  ★**ただし裁定の根拠と「なぜそう決めたか」は今までどおり全部残す。**
  削るのは「何をしたか」の描写のほうである。目安 12KB 前後。
- **壊し検証(裁定116)は新しい仕組みに集中する。**
  発火口・カウンタ・フィルタのような**新設の仕組み**は今までどおり全通り確かめる。
  既存の形に乗っただけのカードは**1通りで足りる**。目安 10通り前後(49 は23通りやった)。

**削らないもの(削るとかえって高くつく):**

- **裁定を仰ぐこと(184)。**実装で勝手に決めると、後で全部やり直しになる。
- **本物の入口からの JUnit(187)。**P2 で積み上がっている唯一の資産である。
- **機械チェック(`tools/` と `verify/`)。**実行は数分で、実際に事故を止めている。

### その先

| フェーズ | バッチ | 内容 |
|---|---|---|
| P3 進化 | 52〜53 | 進化エンジン(裁定154・157)+ 18枚 + UI + **デッキ構築の解禁** |
| P4 賢魂 | 54 | 裁定152 の実装(アクション種別 + 手札UIの2導線 + 7枚) |
| P5 仕上げ | 55 | プリセットの Ver1.1 版 / デッキメーカー JSON の受け入れ / E2E /
  **`qte-cards.json` の削除**(`LedgerCards` と台帳照合3件も同時に消す)/
  **`unlimitedCopies` の死んだ分岐3箇所の掃除**(49 設計解説 6-2)/ `qte-project-reference` 更新 |

★**バッチ番号を詰めた**(旧 54〜57 → 52〜55)。P2 が2本減ったぶんである。

### 完了済みバッチ

17a〜23 / 24〜31 / 32設計・32a〜32c / 33〜45 /
46a(Ver1.1 移行の土台)/ 46b(カードマスタの移行)/ 47(効果未実装の印)/
48(風文明8枚 = P2 の1本目)/ **49(水文明6枚 = P2 の2本目)**

### 保留中

- **作り直し69〜80枚**(本文が Ver1.1 で変わった実装済みカード)。48・49 では範囲外。
  P5 の手前で拾うバッチが要る。
- 印を**デッキメーカー**にも出すか(47 で見送り)/ 音声素材の差し替え(中断中)/
  部屋の永続化(優先順位10)/ タブレット対応(優先順位11)/
  デッキメーカーの名前付きスロット(40 Q2 = b。未着手)。

---

## 2. 発注者とのやりとり

- grep 優先でファイルを渡り歩き、全体読み込みは避ける。
- **判断に迷う点はまとめて質問する。**1つずつ聞かない。
- **本文が2通り以上に読めるカードは、実装で決めずに必ず裁定を仰ぐ**(裁定184)。
- 呼び方は「クロエ」、発注者は「マスター」。
  会話は日本語カジュアル体、**ドキュメントは通常文体(である調)。**

---

## 3. 既知の落とし穴

**★全文は `notes/qte-pitfalls.md`(28節)にある。**ここには置かない。

Batch 50 で特に効くのは次の5節である。

| 節 | なぜ効くか |
|---|---|
| サンドボックスの制約 | m2repo.zip / 日本語テスト名が選べない / Jackson が無い |
| 通常モードのゲームプレイ試験 | `AutoGameFixture` の使い方。`endTurn` の後は `nextPhase` が要る |
| 効果の実装状況・「効果未実装」の印 | `IMPLEMENTED_CARDS` の足し方。ここを外すと動くカードに印が付く |
| 対象指定(TargetSpec)の落とし穴 | `Filter` を足したら `battle.js` にも足す |
| 消えたフィールド | `keywords` / `unlimitedCopies`。46b の置き土産 |

---

## 4. デリバリー形式(Batch 42 から)

**zip は廃止。** マスターの Eclipse ワークスペース
(`C:\Users\奥村優斗\OneDrive\ドキュメント\eclipse_workフォルダ\qte-battle-batch0`、
デバイス接続フォルダ)へ、変更・新規ファイルを**直接書き込む**。

手順(Claude 側):

1. サンドボックスのクローンで実装・検証。
2. **書く前に `device_stage_files` の mtime で「マスターが触っていないか」を確かめる。**
3. SendUserFile → `device_commit_files` でワークスペースへ反映する。
4. 設計解説とハンドオフ更新も同じ経路で `notes/` へ入れ、プロジェクトナレッジにも書く。

手順(マスター側): Eclipse で **refresh(F5)** → **Run As → JUnit Test**(★mvn CLI は無い)
→ 確認して問題なければ**自分で git commit / push**。

反映前に必ず実行:

```bash
python3 tools/check_structure.py src/main/java
python3 tools/check_all.py .
python3 tools/check_records.py src/main/java     # ★既知の誤検出3件あり
python3 tools/check_undeclared.py src/main/resources/static/js/*.js
node --check src/main/resources/static/js/manual-battle.js
node --check src/main/resources/static/js/battle.js
NODE_PATH=/home/claude/.npm-global/lib/node_modules \
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers \
  python3 verify/build_harness.py && node verify/verify.js
mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test
python3 tools/build_id_map.py --check      # 「ベタ書きされた台帳ID: 0 種」であること
python3 tools/report_effects.py --summary  # ★宣言の過不足もここで検出される
```

---

## 5. チャット開始テンプレート

★**このテンプレートは手順だけを書く。**やることの中身は1章、制約は
`notes/qte-pitfalls.md` にある —— **ここへ写すと、また同じ重複が育つ。**

```
QTE Battle の開発を継続する。Batch 50(P2 の3本目・闇文明 + 光文明)を行う。

読む順:
1. プロジェクトナレッジ内の `notes/ver11-migration-plan.md`(46〜55 の作業の正)
2. プロジェクトナレッジ内の `claude/qte-handoff-v56.md`
   ★v56 から構成が変わっている。裁定の全文は `notes/qte-rulings.md`、
   既知の落とし穴は `notes/qte-pitfalls.md` にあり、ハンドオフ書には要点しかない。
3. `notes/qte-pitfalls.md` の5節(v56 の3章に一覧がある)
   ★`notes/qte-rulings.md` は通読しない。番号が出てきたら引く。
4. `notes/batch49-design-notes.md`(P2 のやり方の雛形)

環境:
5. git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git
6. 接続フォルダの m2repo.zip を device_stage_files で取り込んで展開し、
   mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test を回す
   (手順は pitfalls の「サンドボックスの制約」)。
   ★260件全緑が出発点である。緑でなければ止めて報告する。
7. 49 の反映を v56 の「49 の確認項目」で照合し、verify 533/533 を確認する。
   反映されていなければ止めて報告する。

作業:
8. v56 の1章「次の作業」に従う。
   ★闇と光の2文明を1バッチで実装する(11枚)。闇から手を付ける。
   ★同章の「運用ルール」に、成果物の分量と壊し検証の絞り方が書いてある。
9. 納品は v56 の4章のとおり。ドキュメントはチャットにファイルとして添付する。

呼び方は「クロエ」、こちらの呼び方は「マスター」で。会話は日本語カジュアル体、
ドキュメントは通常文体(である調)で。
```

## 6. 49 完了時点の積み残し

### マスターにお願いすること(★49 分)

- **Eclipse で refresh(F5)→ Run As → JUnit Test。**期待: **全260件が緑**。
- **実機確認。★`battle.js` を v=22 に上げたので Ctrl+F5 推奨**(`battle.css` は v=46 据え置き)。
  添付の **`decks/batch49-water-check-deck.json`**(リーダー = ロロイヨ伯爵)で1試合してほしい。
  1. ⚠効果未実装 の印が水の6枚に出ない(土の《百獣の王 ベヒーモス》では今も出る)
  2. **《海獣ホウェライソ》(【潜伏】【守護】)を1体出すといきなり2枚引く。**
     同じターンにもう1体出しても引かない。**ターンが変わればまた引ける。**
  3. **相手がミニオンを出したときも引く**(【守護】か【潜伏】なら、それぞれターンに1回)
  4. 《海獣リューグー》は自分の場に【潜伏】が居るときだけ2枚引く(居ないと1枚)
  5. 《ギガマウス・バイト》は手札が多いほど安い。使うと**手札から水文明のミニオンを
     3体まで選ぶ画面**が出て、**【突進】つきで場に出る**。★水文明でない手札は光らない
  6. 《アルキンティス》を装備し【知識】ミニオンを並べると、その数だけ攻撃力が上がる
- 問題なければ **自分で git commit / push**。

### 継続中の積み残し

- **作り直し(Ver1.1 で本文が変わった実装済みカード)は手つかず**(48・49 で範囲外)。
- **`unlimitedCopies` に乗ったままの死んだ分岐が3箇所**(49 設計解説 6-2)。**無害。**P5 で掃除。
- **35〜48 の実機確認が未報告のまま**(46b と 48 は報告あり。35〜45 と 47 は未報告)。
- 印をデッキメーカーにも出すか(47 で見送り)/ 部分実装は印で表せない(裁定165)。
- `qte-cards.json` の削除(P5)。`support/LedgerCards` と台帳照合3件も同時に。
- 新カード66枚のテキストの目視校正(46a からの持ち越し)。
- `qte-project-reference.md` の1章が古い(2026-07-21)。P5 で更新。
- 手動モード関連の積み残しは**一時停止中**。再開時は v47 の6章。
- ハーネスが `static/vendor/` を読んでいない(意図的。41 設計解説 1-6)。
- 通常モードの `.log-box` の高さが手動モードの定義に引きずられる(42 設計解説5章。実害小)。
