# QTE 対戦アプリ — 引き継ぎ書 v62

最終更新: 2026-08-20。**Batch 55(作り直し① 棚卸し)完了。★P5 の1/3が完了。**
★★**裁定260〜274(15件)を依頼中・未確定。回答が揃うまで Batch 56・57 は
着手できない項目が残る。**(1章 / `notes/batch55-ruling-requests.md`)

| 項目 | 現在値 |
|---|---|
| JUnit | **480件 全緑**(469 → +11) |
| verify | **543/543**(据え置き) |
| 効果の未実装 | **1枚**(剛火の将。死んだ登録を消した結果。Batch 57 で解消予定) |
| 版数 | `manual-battle.js` v=33 / `battle.js` v=25 / `battle.css` v=46(**据え置き**。JS 未着手) |
| 裁定 | **259番まで確定** / ★**260〜274(15件)を依頼中・未確定** |

---

## ★この文書の構成(v56 から継続)

**ハンドオフ書は3つに割ってある。**

| 文書 | 役割 | 更新のしかた |
|---|---|---|
| **`notes/qte-rulings.md`** | 裁定1〜259 の**全文** | **追記専用。**回答が出た裁定260〜274をここへ追記する |
| **`notes/qte-pitfalls.md`** | 既知の落とし穴(33節) | **追記・訂正のみ。**該当する節に足す |
| **本ファイル**(ハンドオフ) | 直近バッチの要点・確認項目・次の作業・開始テンプレート | 毎バッチ書き直す(**20KB 前後に収める**) |

★**ハンドオフ書に裁定の全文や落とし穴の全文を書き戻さないこと。**

---

## 0. 最初にやること

1. **`notes/ver11-migration-plan.md`** を読む(46〜58 の作業の正)。
2. ★★**`notes/rework-triage.md` を読む。**55〜57 の作業範囲の正である。
   **121枚の仕分け・55 で見つかった食い違い・バッチの割り当て**が入っている。
3. **`notes/qte-pitfalls.md`** を読む。★作業対象に関係する節は必読(5章に一覧)。
4. **`notes/qte-rulings.md`** は**引く**もので、通しで読むものではない。
5. 本ファイルと **`notes/batch55-design-notes.md`**(P5 棚卸しの設計解説)を読む。
6. ★★**`notes/batch55-ruling-requests.md` を読み、マスターの回答が揃っているか確認する。**
   ★**揃っていなければ、揃うまで区分3b・4・5 の該当カードには着手しない**(裁定184)。
   回答が来ていれば `notes/qte-rulings.md` へ裁定260〜274として追記してから着手する。
7. `qte-project-reference.md` を読む。★1章の実装状況は2026-07-21のまま古い(**58 で更新する**)。
8. ソースコード取得: `git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git`
9. **「反映済み」を信じず、55 の変更箇所を実際に読んで照合する**(下の「55 の確認項目」)。

---

## ★カードデータの正(46b で確定・55 で運用が変わった箇所のみ再掲)

- **`src/main/resources/cards/manual-cards.json`(Ver1.1・全235枚)が全モード共通の正。**
- **キーワードはテキストから作る。**`CardTextKeywords.extract(text)` が唯一の出どころ(裁定158)。
- 実装状況: ★**「効果の文があるのにエンジンが処理を持たないカード」は1枚**
  (剛火の将。55 で死んだ登録を削除した結果、常在効果〈【速攻】持ちのHP+2〉が
  未実装として印が付いた。**正しい挙動変化**である。Batch 57 で解消)。
  ★**「持っている処理が古い」カードは 55 の消化分だけ減って 76枚**
  (121 − 45〈区分1・2、証明済〉 = 76。うち13枚〈区分3a〉も 55 で直したため、
  実質の残りは63枚〈区分3b・4・5〉。進捗は `notes/rework-triage.md` の区分で見る)。
- ★**`qte-cards.json`(Ver0.4 の台帳)は 58 まで消さないこと。**作り直しの仕分けが旧本文を要求する。

---

## ★★55 が変えたこと(要点)

設計解説は **`notes/batch55-design-notes.md`**。55 は**実装バッチではなく棚卸し**であり、
やったことは「仕分けの固定」「証明」「食い違いの是正」「番人の設置」「数値13枚」「裁定依頼」の6つ。

1. ★★★**仕分けを人が確認して固定し、`tools/rework_triage.py` として機械化した。**
   `rework-triage.md` 1-1節の畳み方(表記ゆれの再計算)は境目で毎回ぶれるため、
   **4章の確定カード一覧をそのまま固定値として持たせた**(再計算しない)。
   ★《大地の精霊 グラン》の保留(区分3b/4のどちらか)も解消した ——
   実装は既に `TriggerType.ON_SUMMON` で正しく登録済みであり、**実装変更は不要**。
2. ★★★**区分1(35枚)・区分2(10枚)=45枚が「直さなくてよい」ことを試験で証明した**
   (`Batch55TriageTest.java`。裁定187どおり `AutoGameFixture` の上で実測)。
   ★このとき3枚(《知識の守り手》【突進】・《波濤の突撃兵》【潜伏】・《煌めきの盾》【知識】)で
   キーワードが**増えている**ことが分かったが、区分1の定義どおりで実装への影響はない。
   ★★**《ディープシー・シャーク》の【威圧】、《急流の狙撃手》《影潜む水刺客》の【貫通】が
   実戦闘で効くことを初めて実測した**(旧台帳に前例が無かった)。
   ★《影潜む水刺客》は**ミニオンではなくウェポン**であることが分かった
   (`StatCalculator` のコメントに明記済み。試験を書く際に要注意)。
3. ★★**着手前に見つかった4件の食い違いを直した**(`rework-triage.md` 2章)。
   《蒼海の賢者》《傷痕の闘帝》《冥府の禁皇》の起動能力コストが 0 → 本文どおり **1** へ。
   《剛火の将》は Ver1.1 で本文から起動能力が丸ごと消えていたのに登録が残っていたため、
   **登録そのものを削除した**(結果、常在効果が未実装として印が付くようになった)。
4. ★★**番人 `tools/check_leader_abilities.py` を新設した。**
   `【起動：n】` の n と `leaderAbilities` の `mpCost` の一致、および死んだ登録の有無を機械照合する。
   ★《流転の智者》のテキストに `【 起動：2】`(【の直後に半角スペース)という表記ゆれがあり、
   正規表現側で吸収した(データは書き換えない。裁定257と同じ判断)。
5. **区分3a(13枚)の定数を差し替えた。**うち2枚は構造が変わった ——
   **アクア・サーチ**(捨て1→2枚。`PendingChoice` を min=max=2 で直接組み立て、
   解決処理は位置の降順削除に書き換え)、**冥府への道**(破壊1→2体。`playConditions` も
   「相手の場に2体以上」へ揃えた)。残り11枚はダメージ・回復・コスト等の定数変更のみ。
6. ★★**区分3b・4・5(63枚)のうち曖昧な点15件を、裁定依頼としてまとめて出した**
   (`notes/batch55-ruling-requests.md`。裁定260〜274。区分3bが7件・区分4が1件・区分5が7件)。
   ★**56・57 が個別の裁定待ちで止まらないよう、55 のうちに1回でまとめた**(裁定184の狙いどおり)。
7. **壊し検証 8通り**(JUnit 7 + `check_leader_abilities.py` 自体の検出力確認1)。
   ★1回目は NG 1件 —— 《命を削る烈火》の壊し検証で対象に HP1 のミニオンを使っており、
   2ダメージでも3ダメージでも「破壊される」という同じ結果になって見分けが付かなかった
   (裁定196の親戚。「同じ結果になる2つの入力を1つの試験で測らない」)。
   HP3 の対象に差し替えて解消。以後 **8/8 OK**。

## ★55 の確認項目(★これを照合する)

- **`CardEffectRegistry.leaderAbilities`**: `QTE-M-WATER-1`(蒼海の賢者)・
  `QTE-M-FIRE-15`(傷痕の闘帝)・`QTE-M-DARK-1`(冥府の禁皇)の `mpCost` が **1**
  / `QTE-M-FIRE-1`(剛火の将)の `leaderAbilities.put` が**存在しない**(削除済み)
- **`CardEffectRegistry.spellEffects` / `specialSummons` / `targetSpecs` / `playConditions`**:
  区分3a 13枚の定数が新本文どおりか(表は `notes/batch55-design-notes.md` 4章)
  / ★**`QTE-M-WATER-25`(アクア・サーチ)が `PendingChoice` を `min=2, max=2` で直接組み立てている**
  / ★**`QTE-M-DARK-26`(冥府への道)の `Requirement` の件数が2、`playConditions` が
  「相手のミニオン2体以上」になっている**
- **`resolveChoice` の `AQUA_SEARCH_DISCARD`**: 複数選択を**位置の降順**で処理している
  (詠唱の疾風騎士と同じ形)
- **`LeaderAbilitySpec`**: Javadoc に `tools/check_leader_abilities.py` の照合方針が追記されている
- **`StatCalculator`**: 《大地の狂戦士》のマナ条件が **6枚以上**(旧7枚)
- **`tools/rework_triage.py`**: 121枚の仕分けが固定値として入っている / `--check` で
  カード存在と各区分の件数(1=35 / 2=10 / 3a=13 / 3b=28 / 4=20 / 5=15)が合う
- **`tools/check_leader_abilities.py`**: 単独実行で「OK: leaderAbilities 9件、すべて
  テキストの【起動：n】と一致する」が出る
- **`src/test/java/.../Batch55TriageTest.java`**(新規・11件): 区分1・2の証明群
- **`EffectImplementationTest`**: ★**`効果未実装のカードは剛火の将だけである`**
  (`効果未実装のカードは1枚も無い` から書き換え済み)
- **`DarkLightVer11EffectTest`**: `キーパーは場で破壊されたときは戻らない` が
  冥府への道の2体要求に合わせてミニオンを2体並べる形に直っている
- **JUnit 480件**(469 + `Batch55TriageTest` 11件)/ **verify 543/543(据え置き)**
- `python3 tools/rework_triage.py --check` / `python3 tools/check_leader_abilities.py` の
  両方が OK であること
- `tools/batch55_break_check.py` がある(★`mvn test` で回している。8/8 OK が出発点)

★54 以前の確認項目は各版のハンドオフにある(54 = v61 / 53 = v60 / 52 = v59 / 51 = v58 /
50 = v57 / 49 = v56 / 48 = v55 / 47 = v54 / 46b = v53 / 46a = v52 / 24〜45 = v51)。

---

## 1. 次の作業(★Batch 56 = 作り直し② 区分3b・4)

**作業範囲の正は `notes/rework-triage.md` である。**ここには要点だけを置く。

| フェーズ | バッチ | 内容 | 枚数 |
|---|---|---|---|
| P5 作り直し | 55(完了) | 棚卸し。仕分けの固定 / 45枚の証明 / 番人 / 数値13枚 / 裁定依頼15件 | 58 |
| **P5 作り直し** | **56** | **挙動が変わった小〜中(区分3b + 4)。新旧を設計解説に併記** | 48 |
| | 57 | ほぼ書き直し(区分5)。効果が別物・能力が消えた・新しい仕組みが要るもの | 15 |
| P6 仕上げ | 58 | 旧 P5 の項目(v61 5章「58 がやること」を参照。変更なし) | — |

### ★★56 に入る前に必ず確認すること

★★**`notes/batch55-ruling-requests.md` の15件(裁定260〜274)にマスターの回答が
揃っているか。**揃っていなければ、該当カードは着手せず止めて報告する
(区分3b・4の48枚のうち、裁定待ちなのは3bの7件 + 4の1件 = 8件。残り40枚は裁定不要)。

回答が来たら `notes/qte-rulings.md` へ**追記**し(裁定260〜274として)、
`notes/batch55-ruling-requests.md` は「回答済み」であることが分かる形にするか、
役目を終えた文書として扱う(次バッチの設計解説から参照できればよい)。

### 56 がやること(`rework-triage.md` の区分3b・4の定義どおり)

1. 区分3b(条件・対象・誘発の種別が変わったもの。27枚 − グラン1枚 = 実質26枚が対象、
   グランは確認だけで実装変更なし)を直す。
2. 区分4(効果の一部が別物になったもの。21枚。うち《冥府の禁皇》は55でコストを直済み、
   56は効果〈参照ゾーンがマナ裏向き→墓地〉のみを直す)を直す。
3. **新旧の挙動を設計解説に併記する**(rework-triage.md「削らないもの」原則。遊び味が変わるため)。
4. ★《ゾンストライカー》の構築特例廃止は、56 と 58 で扱いが割れないよう
   55 の裁定(裁定267想定)で決まった内容に従う。

### 57(区分5)・58(P6仕上げ)がやること

変更なし。v61 の1章・5章を参照(ここでは繰り返さない)。

### 各バッチがやること(P2 から継続・変更なし)

1. `python3 tools/report_effects.py --summary` で未実装が**1枚(剛火の将)のまま**であることを
   確認する(56 では増減しないはず)。★進捗は `notes/rework-triage.md` の区分の消化数で見る。
2. **本文が2通り以上に読めるカードは、実装で決めずに必ず裁定を仰ぐ**(裁定184)。
3. **ルール側に実装したら、そのクラスの `IMPLEMENTED_CARDS` に足す**(裁定176)。
4. `EffectImplementationTest` は不変条件を測る形になった(裁定209)。
5. **カード毎の JUnit を `support/AutoGameFixture` の上に書く**(裁定187)。
   ★**作り直しでは、Ver0.4 の挙動を測っている既存の試験が落ちる —— それが正しい。**
   消さずに新しい本文へ書き換えること。落ちた数が仕分けと合わないなら仕分けを疑う。
6. **壊し検証は `tools/batch55_break_check.py` をコピーして書く。**
   ★**`mvn test` で回すこと**(`surefire:test` はコンパイルしない。裁定208)。
   ★**HP1 のミニオンをダメージ量変更の壊し検証対象に使わない**
   (55 の教訓。複数のダメージ量で結果が同じになり見分けが付かない)。
7. 成果物の書き方は v61 5章末尾の「運用ルール」に従う(削るもの / 削らないもの。変更なし)。

### 完了済みバッチ

17a〜23 / 24〜31 / 32設計・32a〜32c / 33〜45 /
46a(Ver1.1 移行の土台)/ 46b(カードマスタの移行)/ 47(効果未実装の印)/
48(風8枚)/ 49(水6枚)/ 50(闇6 + 光6)/ 51(火7 + 土8。P2 完了)/
52(進化エンジン + 進化6枚 + 茶爺)/ 53(進化7枚 + スケアロック。P3 完了)/
54(【賢魂】のエンジン + 7枚。P4 完了)/
**55(作り直し① 棚卸し。45枚証明・食い違い4件是正・区分3a 13枚・裁定依頼15件)**

---

## 2. 発注者とのやりとり

- grep 優先でファイルを渡り歩き、全体読み込みは避ける。
- **判断に迷う点はまとめて質問する。**1つずつ聞かない。
- **本文が2通り以上に読めるカードは、実装で決めずに必ず裁定を仰ぐ**(裁定184)。
- 呼び方は「クロエ」、発注者は「マスター」。
  会話は日本語カジュアル体、**ドキュメントは通常文体(である調)。**

---

## 3. 既知の落とし穴

**★全文は `notes/qte-pitfalls.md`(33節)にある。**ここには置かない。

Batch 56(作り直し② 区分3b・4)で特に効くのは次の節である。

| 節 | なぜ効くか |
|---|---|
| Ver1.1 移行(Batch 46 系) | 台帳と正の対応づけ(`ledgerCardId`)。作り直しの土台そのもの |
| 消えたフィールド(46b の置き土産) | キーワードと数値がテキスト/JSON から来ることの根拠 |
| 効果の実装状況・「効果未実装」の印 | ★印は「処理が古い」ことを表せない。進捗の見方が変わる |
| 通常モードのゲームプレイ試験 | `AutoGameFixture` の使い方 |
| サンドボックスの制約 | m2repo.zip / 日本語テスト名が選べない / Jackson が無い |
| 対象指定(TargetSpec)の落とし穴 | ★規則はクライアントに置かない(裁定234)。区分3b・4は対象変更が多い |
| デッキ構築(Batch 47・52 で変わった) | 《ゾンストライカー》の構築特例が消えている(区分4) |

---

## 4. デリバリー形式(Batch 42 から・変更なし)

**zip は廃止。** マスターの Eclipse ワークスペース
(`C:\Users\奥村優斗\OneDrive\ドキュメント\eclipse_workフォルダ\qte-battle-batch0`、
デバイス接続フォルダ)へ、変更・新規ファイルを**直接書き込む**。

手順(Claude 側):

1. サンドボックスのクローンで実装・検証。
2. **書く前に `device_stage_files` の mtime で「マスターが触っていないか」を確かめる。**
3. SendUserFile → `device_commit_files` でワークスペースへ反映する。
4. 設計解説とハンドオフ更新も同じ経路で `notes/` へ入れ、プロジェクトナレッジにも書く。

手順(マスター側): Eclipse で **refresh(F5)** → **Run As → JUnit Test**(★mvn CLI は無い)
→ 確認して問題なければ**自分で git commit / push**。

反映前に必ず実行:

```bash
python3 tools/check_structure.py src/main/java
python3 tools/check_all.py .
python3 tools/check_records.py src/main/java     # ★既知の誤検出4件あり
python3 tools/check_undeclared.py src/main/resources/static/js/*.js
node --check src/main/resources/static/js/manual-battle.js
node --check src/main/resources/static/js/battle.js
NODE_PATH=/home/claude/.npm-global/lib/node_modules \
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers \
  python3 verify/build_harness.py && node verify/verify.js
mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test
python3 tools/build_id_map.py --check      # 「ベタ書きされた台帳ID: 0 種」であること
python3 tools/report_effects.py --summary  # 未実装1枚(剛火の将)であること
python3 tools/rework_triage.py --check     # ★55 で新設。121枚の仕分けが崩れていないこと
python3 tools/check_leader_abilities.py    # ★55 で新設。leaderAbilities の照合
```

---

## 5. チャット開始テンプレート

★**このテンプレートは手順だけを書く。**やることの中身は1章、制約は
`notes/qte-pitfalls.md` にある —— **ここへ写すと、また同じ重複が育つ。**

```
QTE Battle の開発を継続する。Batch 56(作り直し② = 区分3b・4)を行う。

読む順:
1. プロジェクトナレッジ内の `notes/ver11-migration-plan.md`(46〜58 の作業の正)
2. プロジェクトナレッジ内の `claude/qte-handoff-v62.md`
   ★裁定の全文は `notes/qte-rulings.md`、既知の落とし穴は `notes/qte-pitfalls.md` にあり、
   ハンドオフ書には要点しかない。
3. ★★プロジェクトナレッジ内の `notes/rework-triage.md`
   —— 55〜57 の作業範囲の正。区分3b・4(56 の担当48枚)の一覧が入っている。
4. ★★★プロジェクトナレッジ内の `notes/batch55-ruling-requests.md`
   —— 裁定260〜274(15件)の依頼文。★★**マスターの回答が揃っているか、
   まずここで確認すること。揃っていない裁定に関わるカードには着手しない。**
5. `notes/qte-pitfalls.md` の該当節(v62 の3章に一覧がある)。
6. `notes/batch55-design-notes.md`(直近の作り。55 の証明・是正・裁定依頼の経緯)

環境:
7. git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git
8. 接続フォルダの m2repo.zip を device_stage_files で取り込んで展開し、
   mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test を回す
   (手順は pitfalls の「サンドボックスの制約」)。
   ★480件全緑が出発点である。緑でなければ止めて報告する。
9. 55 の反映を v62 の「55 の確認項目」で照合し、verify 543/543 と
   report_effects の「未実装1枚(剛火の将)」、rework_triage.py --check と
   check_leader_abilities.py が両方 OK であることを確認する。
   反映されていなければ止めて報告する。

作業:
10. v62 の1章「56 がやること」に従う。★区分3b(実質26枚)+ 区分4(21枚)=47枚。
    ★**裁定待ちの8枚(3bの7件+4の1件)は回答が揃うまで着手しない。**
    ★**新旧の挙動を設計解説に併記すること**(遊び味が変わるため。省略しない)。
11. 納品は v62 の4章のとおり。ドキュメントはチャットにファイルとして添付する。

呼び方は「クロエ」、こちらの呼び方は「マスター」で。会話は日本語カジュアル体、
ドキュメントは通常文体(である調)で。
```

## 6. 55 完了時点の積み残し

### マスターにお願いすること(★55 分)

- **Eclipse で refresh(F5)→ Run As → JUnit Test。**期待: **全480件が緑**。
- ★★**裁定依頼(`notes/batch55-ruling-requests.md`)への回答をお願いします。**
  15件、番号260から。区分3bが7件・区分4が1件・区分5が7件。
  **これが決まり次第 Batch 56 に進めます**(区分5の7件は Batch 57 まででも構いません)。
- 実機確認は今回不要(JavaScript を触っていないため、盤面の見た目・操作感は無変化)。
- 問題なければ **自分で git commit / push**。

### 継続中の積み残し

- ★★**裁定260〜274(15件)が未回答。**`notes/batch55-ruling-requests.md` 参照。
  56・57 のうち該当カードはこれが揃うまで着手しない。
- **《冥府の禁皇》は55と56で二度触る。**55はコストのみ(0→1、済)、56は効果
  (参照ゾーンがマナ裏向き→墓地)を直す。
- **《剛火の将》の常在効果(【速攻】持ちのHP+2)は未実装のまま Batch 57 へ。**
  `pendingFireMinionDiscount` 関連の死んだコード(`PlayerState` / `GameService` /
  `StatCalculator`)の掃除も57で一緒に行うこと。
- **`unlimitedCopies` に乗ったままの死んだ分岐が3箇所**(49 設計解説 6-2)。**無害。**58 で掃除。
- **裏向きマナと `fireManaPlaced` の非対称**(51 設計解説 6-2)。★裁定を仰いでいない。58 へ。
  ★55 では拾っていない。
- **35〜54 の実機確認が未報告のまま**(46b と 48 は報告あり)。55 は実機確認不要(上記)。
- **演舞の墓守の2本目の発火位置(`summonFromGrave`)は現行のカードプールでは到達しない**
  (50 設計解説 6-1)。★51 の防御的な分岐2つも同じ性質のまま(51 設計解説 6-1)。
- **進化召喚が登場の置換(モアニール)で止まったときの素材の扱い**(裁定232)。
  ★実機で見かけたら違和感の有無を教えてほしい。
- ★**《英霊・コレキ》の「相手のターン中は止めない」を本物の入口から観測できない**
  (53 設計解説 6-2)。
- 印をデッキメーカーにも出すか(47 で見送り。未実装が1枚だけになったので要否は 58 で再確認)/
  部分実装は印で表せない(裁定165)。
- `qte-cards.json` の削除(58)。`support/LedgerCards` と台帳照合3件も同時に。
  ★**57 が終わるまで消さないこと。**
- 新カード66枚のテキストの目視校正(46a からの持ち越し)。
- `qte-project-reference.md` の1章が古い(2026-07-21)。58 で更新。
- 手動モード関連の積み残しは**一時停止中**。再開時は v47 の6章。
- ハーネスが `static/vendor/` を読んでいない(意図的。41 設計解説 1-6)。
- 通常モードの `.log-box` の高さが手動モードの定義に引きずられる(42 設計解説5章。実害小)。
