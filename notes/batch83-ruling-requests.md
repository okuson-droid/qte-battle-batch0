# Batch 83 裁定依頼 —— 採番0件。実装で決めたことと、次への申し送り

★★★**Batch 83 は裁定を1件も採番していない。**
★**Batch 79・82 に続く3例目**である。

★理由は 79・82 と同じである ——
<b>ゲームのルールにも画面の振る舞いにも1つも触れないバッチだったからである</b>。
★★<b>83 が変えたのは `verify/verify.js` の音の節だけである</b> ——
<b>Java・カード定義・`battle.js`・`battle.css`・`manual-battle.js`・テンプレートは1文字も触っていない</b>。

★★★**裁定は 1〜361 のままである。次は 362 から。**
★★**判断待ちは1件も残していない**(74〜82 に続く**10バッチ連続**)。

---

## 0. ★★★着手前に、候補 V を測って外した(裁定362 は生まれなかった)

★**マスターは最初に候補 V(サーバにカードの同一性)を選び、
「ビューには載せない」+「ManaCard から始める」を選んだ。**

★★<b>この2つは噛み合わないことが、着手前の実測で分かった</b> ——

1. ★★★**`ManaCard` にはもう同一性が在る。**
   <b>`remove(mana)` というオブジェクト参照による特定を、コードが既に使っている</b>
   (`GameService` 526・1249行、`CardEffectRegistry` 499行)。
   ★<b>`id` を足すのは「できることの二重化」であり、設計判断28 に触れる。</b>
2. ★★**String ゾーンも、いまは得が無い。**
   <b>同名の2枚は完全に交換可能であり、違う1枚を消しても観測できる差が生まれない</b> ——
   <b>1枚ごとに違う性質を持つのは場のミニオンだけだからである</b>。

★★★<b>したがって「ビューに載せない」を選んだ時点で、この工事の中身は空になる。</b>

### 0-1. ★★★載せる場合は、公開範囲の裁定が要る(裁定362 の予約)

`battle.js` の 4209行:

> ★裏面は名前を1文字も運ばないので、非公開情報が漏れることは構造的に無い。

★<b>裏向きのカードに識別子を載せた瞬間、この性質が壊れる</b> ——
<b>墓地(公開)から手札へ戻った id が後で裏向きマナに現れたら、相手は正体を追える</b>。
★★<b>設計判断63 の警告そのものである。</b>

★★★**次に候補 V を検討するときは、この問いから始めること**:

| 道 | 何が起きるか |
|---|---|
| **公開ゾーンだけ載せる** | ★墓地・消滅・場・公開中の一時公開・自分の手札。★★<b>漏れない</b>し、<b>演出が「墓地から複数戻る」等で実際に良くなる</b>。★★★<b>絞る場所は `GameViewBuilder` の1箇所</b>(設計判断9)であり、<b>絞っていることを番人が測る</b>(81 の教訓) |
| **載せない** | ★<b>今回これを選んで、工事の中身が空になった</b> |
| **すべて載せる** | ★★★<b>漏れる。</b>選んではいけない |

★<b>これは実装で決められない</b>(裁定184 の対象である)—— <b>載せる日に裁定を仰ぐこと</b>。

---

## 1. ★★82 が実装で決めたこと(裁定を仰がなかったもの)

読みが1つに定まったので、作業を止めずに実装した(リファレンス 1-7 の流儀)。
**間違っていたら戻す。**

| 決めたこと | 根拠 |
|---|---|
| ★★★**`goto` 後の 200ms を撤去し、遅らせを `goto` の前に置く** | ★<b>待つべき事実は「取得の完了」ではなく「復号の完了」である</b>。★★<b>復号には AudioContext が要るので `goto` の直後には構造的に揃わない</b>(実測: 8秒待っても揃わない)。★★★<b>取得が unlock に間に合わなくても、届いた時点で自分で復号へ回る</b>(`sfxFetch`)ので、<b>取得の完了を待つ段そのものが要らない</b> |
| ★★★**配信・クリック後の 60ms を6件撤去する** | ★<b>実装は `fxSpawn` で rAF を待たずに同期に鳴らす</b>(「待ち時間に依存する検証にしない」というコメントがその宣言である)。★★<b>実測: 7件すべて消しても 793/793・2並列でも緑</b>。★★★<b>ただし根拠を測る番人が無かったので1本足した</b>(壊し検証の軸4 が要求した) |
| ★★**`reload` 後の待ちは、事実待ちにしてから撤去する** | ★<b>外しても2並列で 797/797 だった</b>(Playwright の locator が実操作の前に待つ)。★★<b>壊せない待ちを残すのは、偽の番人を1本置くのと同じである</b>(裁定196)。★★★<b>race は既存の check が自分で捕まえる</b> |
| ★★**閾値は 400ms にする** | ★<b>81・82 が通常モードで使っている値と同じにした</b> —— <b>同じ形なら同じ値</b>。★★実測の余裕は<b>正しい側 1122ms 以上 / 壊した側 366ms</b> |
| ★★**軸2 の壊し方をわざと 1000ms にする** | ★<b>60ms に戻すと軸1の番人まで道連れになる</b> —— <b>1つの改変が2つの番人に当たったら軸の切り方が悪い</b>(82 の教訓)。★★<b>1000ms は「たいてい通る長さの固定待ちへ戻した」という、いちばん有りそうな戻し方でもある</b> |
| ★★★**並列度は上げない** | ★<b>4並列の赤は 3本 → 1本に減ったが、ゼロではない</b>。★★<b>上限はいちばん危ない1本が決める</b> —— <b>`WORKERS` も番人 S6 の「2」も据え置き</b> |
| ★**版数は上げない** | ★★★<b>静的ファイルを1文字も触っていない</b>(7-5)。★<b>`verify/verify.js` は配信されない</b> |

---

## 2. ★次のバッチへの申し送り

### 2-1. ★★★申し送りの見立ても、母集団にしてはいけない

引き継ぎ書 v90 は「手動モードの `snd` のページは**まだ 200ms / 400ms の固定待ち**である」と書いていた。
★<b>実際は 200ms×2 と 60ms×7 で、400ms は1件も無かった</b>。
★★<b>しかも働いていたのは `goto` 後の 200ms だけで、残り8件は死に荷物だった</b>。

> ★★★<b>79 の教訓「症状を母集団にしない」は、<b>前のバッチが書いた症状</b>にも当てはまる。</b>
> <b>申し送りは出発点であって、母集団ではない。</b>

### 2-2. ★★★根拠にしたものには、その日に番人を置く

83 は「実装が同期に鳴らすから待たなくてよい」を根拠に固定待ちを6件撤去したのに、
★<b>その根拠を測る番人が1本も無かった</b> —— <b>壊し検証の軸4 が NG で教えた</b>。

★<b>既存の番人は「同期であること」ではなく「evaluate の往復より速いこと」しか測っていなかった</b>。
★★<b>配信と計数を同じ evaluate に閉じて初めて、同期性が測れる。</b>

> ★<b>次に「実装がこうなっているから省ける」と書いたら、その「こうなっている」を測ること。</b>

### 2-3. ★★★並列度の天井を上げるには、あと1本である(候補 P2)

★4並列で赤くなるのは **797件のうち1本**だけになった:

- 「★自席マナのタップは transform(回転)の遷移が実際に走る」

★★<b>これを事実待ちへ移せば、その日から並列度を上げてよい</b>。
★★★<b>上げるときは、番人 S6 の「2」も一緒に上げること</b> ——
<b>あれは「上げた人が赤を見る」ために置いてある</b>。

★<b>残りの固定待ちは 239箇所 / 25239ms である</b>(83 の前は 248箇所 / 26059ms)。
★★<b>ただし「全部を事実待ちにする」は目的ではない</b> ——
<b>目的は天井を上げることであり、そのために要るのは<b>あと1本</b>である</b>。

### 2-4. ★★「鳴らないこと」は事実待ちへ移せない

★差分が上限を超えたとき / 確認を出しただけのときは、**0件であること**を測っている。
★★<b>「何も起きないこと」には待つべき事実が無い</b>。

★★★<b>83 の答えは「固定待ちを撤去する」だった</b> ——
<b>配信が返った直後という決まった一点で 0件を測るほうが、
60ms 待ってから測るより強い</b>(時間に依存しなくなる)。

### 2-5. ★★残っている積み残し(83 は触っていない)

- ★★★**固定待ちが 239箇所・25.2秒ある**(候補 P2)—— <b>天井を上げるのに要るのは1本</b>。
- ★★★**サーバにカードの同一性が無い**(80・候補 V)——
  ★<b>83 が測って外した</b>。<b>次は公開範囲の裁定から始めること</b>(0-1)。
- ★★**通常モードに「相手が切断中」が出ない**(75・候補 S)。
- ★★**手動モードの部屋消失は本文の文字列で判定している**(75・候補 T)。
- ★★**相手の墓地を参照する器が無い**(74・裁定340・候補 R)。
- ★★**儀式が描けない**(80・81・候補 R2)—— <b>解決の途中で配信する口が無い</b>。
- ★★**通常モードと手動モードのモーダルの層は複製である**(78)。
- ★**カードフェイスの実装が2つある**(候補 C)—— ★★<b>83 は JS を1文字も触っていない</b>。
- ★**`GameRoom` が 459行のままである**(75)。
- ★**宣言時に選んだ進化が「選べるのに出ない」**(74)。
- ★★★**70〜81 の実機確認がまだ未報告である。**
  ★<b>82 も 83 も画面を触っていないので、確かめる対象は 81 までのままである</b>。
