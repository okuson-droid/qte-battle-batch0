# Batch 81 設計解説 —— 一時公開ゾーンを画面に出す(候補 W)

対象: `revealedZone` の公開範囲(Java)と、その描画・演出(`battle.js` / `battle.css`)。

発端は Batch 80 が演出の着地点を数えたときに見つけた穴である ——
**`revealedCards` は Batch 44 からビューに載っているのに、`battle.js` はこの語を1度も書いていない**
(実測 0件)。80 はそれを塞がず、積み残し(候補 W)として書き残した。

★**掘ったら、描いていないこと以外にもう2つ出てきた。**どちらも「描いた瞬間に効いてくる」ものである。

---

## 0. ★★★母集団(作業を始める前に固定した)

★**「一時公開ゾーンが描かれていない」を母集団にしない。**それは症状である(79 の教訓)。
数えるのは「**`revealedZone` という器が、誰に何を伝えているか**の全体」である。

★★母集団を **3段**で取った(80 の教訓)——
**A: 何が観測できるか** / **B: 何を作れるか** / **C: それがどこへ着地できるか**。

### 0-1. ★★★母集団A —— `revealedZone` の実測

| 何 | 箇所 | 実測 |
|---|---|---|
| 器 | `PlayerState.revealedZone` | **1本**(`List<String>`) |
| **入れる**側 | `CardEffectRegistry` | **2箇所**(1951行 = LIGHT-22 / 5757行 = EARTH-30) |
| **消す**側 | `CardEffectRegistry` | **2箇所**(3986行 = MISSIONARY_SUMMON / 4568行 = GURANDORANDO_MANA) |
| ゾーンを取り出す口 | `GameActions` 1628行 | `case REVEALED -> owner.getRevealedZone()` |
| ビューを作る側 | `GameViewBuilder.buildRevealedCards` | **1箇所**(258行) |
| `battle.js` が読む側 | — | ★**0箇所** |
| JUnit が測る側 | `SoulSpellTest` 546行 | ★**1件**(「空になっていること」だけ) |
| verify が読む側 | — | ★**0件**(80 が置いた「アンカーが無い」の予約のみ) |

★★★**生存期間は「REVEALED の `pendingChoice` が立っている間」と完全に一致している。**
入れる2箇所はどちらも直後に `requestChoice` を呼び、消す2箇所はどちらもその再開点である。

### 0-2. ★★★母集団A で出た穴(1) —— 器は1つだが、意味は2つある

| カード | 本文 | 相手に見せてよいか |
|---|---|---|
| 《降臨の伝道師》(QTE-M-LIGHT-22) | 山札の上から4枚を**公開** | ★**見せてよい**(公開情報) |
| 《愚乱怒土地》(QTE-M-EARTH-30) | 山札の上から2枚**見て**…**相手に見せず**加える | ★★★**絶対に見せてはいけない** |

`GameViewBuilder` の 258〜259行:

```java
buildRevealedCards(player),                    // ← isSelf を通していない
buildPendingChoice(state, player, isSelf));    // ← こちらは通している
```

★★★**両席ぶん、カード名つきで配信されている。**観戦者ビューにも同じものが行く。

★★さらに `CardEffectRegistry` の 5747行には、こう書いてあった:

> 公開領域(revealedZone)は本人のビューにしか出ない(GameViewBuilder が isSelf で絞る)ので、
> 既存の器がそのまま使える。

**絞っていない。**

> ★<b>これは 77 の教訓(用意した側は「使われている」と思い込む)の<b>逆の顔</b>である</b> ——
> <b>使う側が「絞られている」と思い込んでいた。</b>

★**いま実害は出ていない。**`battle.js` が `revealedCards` を1度も読んでいないので、
線には載っているが画面には出ない。★★**描いた瞬間に漏れる。**

### 0-3. ★★★母集団A で出た穴(2) —— ログの文言も器と同じ嘘をついている

`GameActions.revealFromTopOfDeck`(1128行)は、**呼び出し元によらず**こう書く:

```java
room.addLog("%sが山札の上から%d枚を公開しました".formatted(...));
```

★《愚乱怒土地》の本文は「**見て**」であり「公開」ではない。
★★**器が1つなので、ログも1つになっていた。**穴(1)とまったく同じ形である。

### 0-4. ★★★測り直して工事の順番が変わったこと —— 配信は解決の途中で起きない

マスターは最初、片肺(下の 0-5)まで直す道を選んだ。★**着手せず、先に測った**(73・80 の教訓)。

`GameWsController` 355〜359行:

```java
synchronized (room.getLock()) {
    action.apply(room);      // ← 効果の解決はすべてこの中
}
broadcaster.broadcast(room); // ← 配信は「終わってから」1回だけ
```

★★★**効果の解決中に配信は1度も起きない**(通常モードで `broadcast` を呼ぶ口は**この1箇所だけ**である)。
したがって公開領域が誰かに届くのは、**その配信を跨いで残っていたとき**に限られる。

| 場面 | 公開領域が配信に載るか |
|---|---|
| 《降臨の伝道師》【守護】2枚以上 | ★**載る**(問い合わせで解決が止まる) |
| 《降臨の伝道師》【守護】0枚・1枚 | ★★**載らない** —— <b>同じ配信の中で入れて消えるので、誰にも観測できない</b> |
| 《愚乱怒土地》 | ★載る(問い合わせで止まる)。ただし**非公開**なので本人だけ |

★★**「常に公開領域を通す」だけでは片肺は治らない。**入れて消えるあいだに誰も見ていない。

> ★★★<b>これは 80 の申し送り 3-3 とまったく同じ形である</b> ——
> <b>儀式を描きたければ、先にサーバへ「観測できる形」を足すこと。</b>
> ★<b>ここでの「観測できる形」は fx でも器でもなく、<b>配信の機会</b>そのものだった。</b>

★実寸を出したうえで、マスターが道を選び直した(裁定360)——
**器の別は持たせる。片肺はログで埋める。**

### 0-5. ★母集団B —— 何を作れるか

| # | 作るもの | 材料 | 81 で置くか |
|---|---|---|---|
| B1 | 公開/非公開の別 | `PlayerState` に真偽値1つ | ★**置く**(裁定359) |
| B2 | 公開のときログにカード名を並べる | `revealFromTopOfDeck` の引数1つ | ★**置く**(裁定360) |
| B3 | 一時公開ゾーンの描画(自席) | `revealedCards` | ★**置く**(裁定361) |
| B4 | 一時公開ゾーンの描画(相手席) | 同上(公開のときだけ届く) | ★**置く** |
| B5 | 演出のアンカー(`REVEALED`) | B3・B4 の DOM | ★**置く**(母集団C の C9 が○になる) |
| B6 | 「山札 → 一時公開」「一時公開 → 手札 / マナ / 場」の移動 | 80 の `fxZoneDelta` に1行 | ★**置く** |
| B7 | 解決の途中で配信する口 | ★**無い** | 置かない(0-4・c2) |
| B8 | 「公開しました」の確認を挟む | 裁定113 に触れる | 置かない(0-4・c1) |

### 0-6. ★★★母集団C —— 着地点(1280×800 の満杯の盤面で実測した)

★**番人(と演出)を置く場所を選ぶ前に、そこまで届くかを確かめる**(70〜80 の教訓)。

| 置き場所 | 実測 | 判定 |
|---|---|---|
| 右列中段 `.auto-side-mid` | **294px 固定**。最悪ケース(決着 + メッセージ + 問い合わせ帯 + ログバー)で **264px 使用済み → 余り 30px** | ★**溢れる**(面の列は 65px 要る) |
| 中央列 `.auto-main` | 満杯の盤面で **scrollHeight 752 / clientHeight 750** —— <b>すでに 2px 溢れている</b> | ★**帯を足す余地が無い** |
| `fixed` の層 | 場所を奪わない。`#auto-playing`(right:300px / top:64px / 98×157)が<b>まさにその問い合わせ中に出ている</b> | ★★**ここしか無い**(裁定361) |

★カード面の実寸: `small` **64×49** / `large` **98×157**。
★★4枚横並びで **268px** —— `#auto-playing` の幅(98px)より広いが、右列(280px)には収まる。

### 0-7. ★母集団の外だと決めたもの(理由つき)

- **解決の途中で配信する口(B7)** …… 配信の口が2つになる(設計判断48 の一族)。
  ★<b>しかも止めないなら結局見えない</b> —— 「間」の仕組みまで要る。38 と同じ大きさの別のバッチである。
- **「公開しました」の確認(B8)** …… ★裁定113(二択に確認を重ねる意味は無い)に触れる。
  ★★<b>見せるためだけに毎回1手止めるのは、代償が釣り合わない。</b>
- **《光霊・ネフラ》(LIGHT-35)** …… 「表向きにする」が、`revealedZone` を1度も通らない
  (公開したその場で全部の行き先が決まる)。★<b>0-4 により、通しても誰にも見えない。</b>
  ★★ログの文言だけは B2 の対象である。
- **手動モードの一時公開** …… ★手動モードに一時公開ゾーンは無い(ゾーンの帯は別の仕組みである)。
  <b>1文字も触っていない。</b>
- **サーバの同一性(候補 V)** …… 80 が実寸を測って外したまま。★81 も触っていない。

---

## 1. ★★★何を作ったか

### 1-1. サーバ —— 器に「公開か」の別を持たせた(裁定359)

```
PlayerState.revealedPublic          …… ★真偽値1つ。旗の上げ下げは GameActions の2本だけ
GameActions.placeInRevealedZone()   …… ★束を置く「入口」。★★ここで旗を立てる
GameActions.takeRevealedZone()      …… ★束を取り出す「出口」。★★ここで旗を降ろす
GameViewBuilder.buildRevealedCards(player, isSelf)
                                    …… ★★★非公開なら相手・観戦者へは空の列を返す
```

★★**入口と出口を1本ずつにしたのが要点である**(裁定130)。
80 まで「写して消す」は `CardEffectRegistry` の**2箇所に同じ形で書かれていた**(3986行・4568行)。
★<b>旗を降ろす段が増えた時点で、そこは2箇所であってはいけなくなった</b> ——
片方だけ直す形が、76 の `requirePlayable`・77 の素材の段と<b>まったく同じ顔で待っている</b>。

★★★**絞るのは配信の1箇所である**(設計判断9)。
<b>クライアントは「見せてよいか」を1文字も判定していない</b> ——
`battle.js` が読む `revealedPublic` は<b>見出しの言葉を選ぶためだけ</b>である。

### 1-2. サーバ —— ログが「公開」と「見た」を書き分ける(裁定360)

| 入口 | カード | 本文 | ログ |
|---|---|---|---|
| ① | 《降臨の伝道師》(LIGHT-22) | 4枚を**公開** | 「公開しました: 【A】【B】【C】【D】」 |
| ② | 《光霊・ネフラ》(LIGHT-35) | 3枚を**表向きにする** | 「公開しました: 【A】【B】【C】」 |
| ③ | 《愚乱怒土地》(EARTH-30) | 2枚を**見て** | 「**見ました**」(★名前は書かない) |

★80 まで、この3つは**同じ1行**を出していた(「公開しました」・名前なし)——
<b>器が1つだったので、ログも1つになっていた</b>。

★★★**名前を並べるのは飾りではない。**0-4 のとおり、
<b>公開領域へ入れて同じ配信の中で取り出す場面は誰にも観測できない</b> ——
**ログだけがその配信に載って残る**。★《降臨の伝道師》の【守護】が0枚・1枚のときが、まさにそれである。

### 1-3. クライアント —— 描く(裁定361)

```
render(view)
  ├ renderPlayingCard(view)   ← 解決中のカードの面(70 から在る)
  └ renderRevealed(view)      ← ★81。その真下に束を並べる
```

★**両席ぶん見る。**公開の束は相手にも届くからである。
★★**見出しが「誰が見ているか」を書く** ——
「一時公開(相手にも見えている)」/「一時公開(あなただけが見ている)」/「相手が公開中」。
★<b>《愚乱怒土地》の「相手に見せず」が守られていることを、人が確かめられる</b>ようにするためである。

★★★**アンカーは席ごとに登録し、束が消えたら外す。**
80 のアンカーは**18本とも常に画面に在る**ので、外す段が要らなかった ——
★<b>出たり消えたりする着地点は 81 が初めて作った</b>(下の 4章)。

---

## 2. ★★★このバッチも「測られていない一族」を踏んだ

★**《愚乱怒土地》の非公開が相手に漏れていた穴を直しても、既存の954件は1件も赤くならなかった。**

| バッチ | 何を変えたか | 既存の件数 |
|---|---|---|
| 67 | 本文と食い違う実装を7枚直した | 826件が緑のまま |
| 70 | 払い方の順序をまるごと入れ替えた | 758件が緑のまま |
| 80 | (実寸)同一性を77箇所のうち1箇所で落としても | 951件が緑のまま |
| ★**81** | ★**非公開の束が相手へ漏れる穴を塞いだ** | ★**954件が緑のまま** |

★★理由はいつも同じである —— **誰もその欄を測っていなかった**。
81 の着手時点で `revealedCards` を読む番人は **JUnit 1件**(「空になっていること」だけ)・
**verify 0件**だった。

> ★★★<b>「落ちなかった = 壊していない」ではない。「そもそも測られていない」ことがある。</b>

---

## 3. ★★★配信は解決の途中で起きない —— だから儀式もここも同じ壁に当たる

`GameWsController` 355〜359行が通常モードで `broadcast` を呼ぶ<b>唯一の口</b>である。

```java
synchronized (room.getLock()) { action.apply(room); }   // 効果の解決はぜんぶこの中
broadcaster.broadcast(room);                            // 配信は終わってから1回だけ
```

★したがって**公開領域が誰かに届く条件は「その配信を跨いで残っていること」**だけである。
★★<b>それは「問い合わせで解決が止まったとき」と同義である</b> ——
実際、`revealedZone` が非空である期間は `pendingChoice(REVEALED)` の期間と完全に一致している。

★★★**これは 80 の申し送り 3-3 とまったく同じ形である** ——
<b>儀式を描きたければ、先にサーバへ「観測できる形」を足すこと</b>。
★<b>81 での「観測できる形」は、fx でも器でもなく<b>配信の機会</b>そのものだった</b>。

★★<b>作らなかった道も書き残す</b>(設計判断56):

| 道 | なぜ作らなかったか |
|---|---|
| c1. 見せるための確認を挟む | ★**裁定113 に触れる**(二択に確認を重ねる意味は無い)。<br>★★<b>見せるためだけに毎回1手止めるのは、代償が釣り合わない</b> |
| c2. 解決の途中で配信する口を作る | ★<b>配信の口が2つになる</b>(設計判断48 の兄弟)。<br>★★<b>しかも止めないなら結局見えない</b> —— 38 と同じ「間」の仕組みまで要る |

---

## 4. ★★★直したあとに、何が新しく起きたか(78 の教訓)

★**出たり消えたりする着地点を足すと、外す段が新しく要る。**

80 のアンカーは18本とも常に画面に在るので、`render` のたびに<b>同じ鍵へ上書きするだけ</b>で済んでいた。
★一時公開ゾーンは問い合わせの間だけ描かれるので、消えたときに登録したままにすると
<b>表から切り離された DOM がアンカーに残る</b> ——
80 が置いた `live`(すべてのアンカーが画面上の要素であること)が、
**束が消えた次の配信で偽になる**。

★★<b>これは番人が赤くなる前に、設計の側で気づいて塞いだ</b>(`clearAutoAnchor`)。
壊し検証の軸16 が、その段を外して<b>実際に赤が出ることを確かめている</b>。

> ★<b>「描く」と「登録する」を対にしたなら、「捨てる」と「外す」も対にすること。</b>

---

## 5. ★★★置き場所は実測で決めた(65・69〜72 の教訓)

母集団C(0-6)のとおり、**右列も中央列も既に一杯**である。残るのは `fixed` の層だけだった。

| | 実測 |
|---|---|
| `.auto-revealed` | right: 300px / **top: 412px** / z-index: **1010** / pointer-events: **none** |
| 実際の矩形(4枚) | x 646〜980 / y 412〜497(334×85px) |
| `#auto-playing` | x 748〜980 / y 64〜**408** —— ★<b>412px はここから生えた値である</b> |

★★★**412px は `.auto-zoom-card` の 232px から生えた、2箇所に書いた定数である。**
★<b>だから値ではなく「重なっていないこと」を番人が実測する</b>(設計判断41)——
あちらの寸法を変えた人が、ここで赤を見る。

★★**z-index 1010 は `#auto-playing`(1390)と揃えていない。**
<b>ここは演出の着地点だからである</b> —— fx層(1020)より上に置くと、
飛んできたゴーストの最後の数フレームが帯の裏へ隠れる。
★<b>揃えないことにも理由が要る</b>(裁定358 と同じ形の判断)。

---

## 6. 番人(設計判断45: 回る場所で選ぶ)

| 何を測るか | どこで | 件数 |
|---|---|---|
| 公開範囲(本人・相手・観戦者の3視点 × 公開・非公開) | `Batch81RevealedZoneTest` | 4件 |
| ログの書き分け(★**入口3つぶん**) | `Batch81RevealedZoneTest` | 4件 |
| 版数(JS・CSS) | `BattlePageTest` | 2件 |
| 差分の語彙(移動・出現・相手席・非公開が黙ること) | `verify/verify.js` | 4件 |
| 描画(面・印・見出し・両席・アンカー・解除・幾何・層) | `verify/verify.js` | 10件 |
| ★**音のバッファを事実で待つ + 遅らせが効いている** | `verify/verify.js` | 3件 |
| **合計** | | ★**verify +19 / JUnit +10** |

★verify **774 → 793件**。JUnit **954 → 964件**。

★★★**80 が置いた「一時公開ゾーンにはアンカーが無い(積み残し)」は撤去して差し替えた。**
★<b>あれは予約であり、塞いだ日に役目を終える</b>(76 が 74・75 に、78 が 77 にそうした)。
★★<b>消さずに文言だけ書き換えるのは番人を殺すことである</b> ——
いま測っているのは「**束が出ていないとき**は登録されていない」であって、「決して登録されない」ではない。

### 6-1. ★★★壊し検証(`tools/batch81_break_check.py`)

**21軸・OK 21 / NG 0 / EMPTY 0。**★**5回に分けて回し、前後で `git diff --stat` を毎回突き合わせた。**

| 軸 | 壊す先 | 何を確かめるか |
|---|---|---|
| 1・2 | `battle.html` | 版数の上げ忘れ(JS / ★CSS は5枚のうち1枚だけ) |
| 3〜6 | `GameViewBuilder` / `CardEffectRegistry` / `GameActions` | ★**公開範囲** —— フィルタを外す / 旗を逆に立てる(入口2つ)/ 旗を降ろさない |
| 7〜9 | `GameActions` / `CardEffectRegistry` | ★**ログ**(入口**3つ**ぶん) |
| 10・11 | `battle.js` | 差分の語彙 —— 採らない / 「窓」にしてしまう |
| 12〜14 | `battle.js` | 描画 —— 呼ばない / **相手席を見ない** / 見出しを書き分けない |
| 15〜17 | `battle.js` | アンカー —— 登録しない / **外さない** / 【守護】の印 |
| 18〜20 | `battle.css` | 層 —— 操作を吸う / fx より上に出る / プレイ中のカードと重なる |
| 21 | `verify/verify.js` | ★**音のわざとの遅れを外す**(設計判断61) |

★★★**2軸は最初そうではなかった。**

- ★**軸9 は当てる先が無かった。**`revealFromTopOfDeck` の入口は**3つ**あるのに、
  番人は2つぶんしか置いていなかった(《光霊・ネフラ》が抜けていた)。
  → **番人を1本足した。**
  ★<b>これは 77・79・80 の教訓「規則が n 入口ぶんあるなら、番人も n 入口ぶん要る」の、81 での実演である</b> ——
  <b>教訓を知っていても、軸を入口ごとに立てるまで気づかなかった</b>。
- ★★**軸16 は、実装の穴として作業中に先に出た**(4章)。
  番人が赤くなる前に設計の側で気づいたので、この軸は<b>塞いだあとの確認</b>になっている。

★★**照合先が重なっていた2組を、番人の側で分けた。**
軸12/17(描く・印)と軸18/19(操作・層)は、最初<b>1つの `check` にまとまっていた</b> ——
★<b>別の性質を1つの番人にまとめると、壊れたときにどちらが壊れたか読めない</b>
(72 の教訓「理由の幅と結論の幅を揃える」の、番人の側の顔)。

### 6-2. ★★★壊しどころが無いもの(裁定196 の正直な扱い)

1. **解決の途中で配信する口。**81 は作っていない(3章・c2)。**作っていないものは壊せない。**
2. **「公開しました」の確認を挟む段。**裁定113 に触れるので作らなかった(3章・c1)。
3. **一時公開ゾーンの「窓」。**このゾーンは<b>枚数の欄をビューに持たない</b>ので、
   届いた列の長さがそのまま枚数である —— <b>「一部だけ届く」が構造的に起きない</b>。
   ★軸11 はその前提を壊す形で当てているが、**本物の配信でこの形は来ない**。
4. **`manual-battle.js`・カード定義。**1文字も触っていない。★手動モードに一時公開ゾーンは存在しない。
5. **《愚乱怒土地》の「マナが上限なら山札の上へ戻す」。**
   81 は取り出す口を1本にしただけで、行き先の規則には触れていない。

---

## 7. ★★★音の番人を1本直した(候補 U の弟)

★**81 の作業中、verify が1度だけ 62-8(通常モードは配信で音が鳴る)で落ちた。**
★★<b>「もう一度回す」で済ませなかった</b>(79 の教訓)——<b>余裕を測った</b>。

| 測ったもの | 実測 |
|---|---|
| unlock から音が全部そろうまで | **86〜127ms**(6回・空いた箱) |
| verify がそこで待っていた時間 | ★**200ms 固定** |
| 余裕 | ★★**73〜114ms** |

★`sfxReady()` は **AudioContext が在るかしか見ていない** —— <b>バッファの有無は見ていない</b>。
★★<b>だから「箱が忙しいと音が鳴らない」形の番人だった</b>(候補 U の余裕は 5ms、こちらは 100ms 前後)。

★★★**直し方は 79 とまったく同じである**(設計判断61)——
<b>時間ではなく事実で待つ</b>(`sfxBuffers` が全部そろったこと)、
そして<b>わざとの遅れを同じバッチで入れる</b>(`SOUND_RESPONSE.delayMs = 600`)。

★**遅らせを入れる場所で1つ踏んだ**: 最初はクリックの直前に遅らせたが、**効かなかった**(116ms で終わった)。
`sfxPreload` は<b>ページを開いた時点で全部取りに行く</b>(裁定283 の (c))ので、
★★<b>遅らせるのは `goto` の前でなければならない</b>。
★★★<b>しかも開いてからクリックまでの 400ms より長くする</b> ——
そうしないと<b>クリックの時点でもう届いており、待ちが1ミリ秒も働かない</b>。

> ★<b>遅らせは「入れた」だけでは効かない。効いていることを1件で測ること</b>(裁定186)。

---

## 8. 版数(7-5)

| ファイル | 80 | ★81 | 理由 |
|---|---|---|---|
| `battle.js` | v=41 | ★**v=42** | ★一時公開ゾーンの描画・差分の語彙 |
| `battle.css` | v=56 | ★**v=57**(★参照する5枚すべて) | ★`.auto-revealed` 一式 |
| `manual-battle.js` | v=34 | v=34(**据え置き**) | ★★★**1文字も触っていない** |
| `SFX_VERSION` | 1 | 1(据え置き) | ★音声ファイルは1つも増えていない |

★★**据え置きの番人は1本も新設していない。**
78 が 77 のぶんを撤去して以降、据え置きを名指しする番人は1本も無い ——
★<b>撤去すべきものも1本も無い</b>。

---

## 9. ★★★このバッチが残した教訓

### 9-1. 「絞っている」と書いたコメントは、絞っていることの証拠ではない

`CardEffectRegistry` の 5747行は、こう書いていた:

> 公開領域(revealedZone)は本人のビューにしか出ない(GameViewBuilder が isSelf で絞る)ので、
> 既存の器がそのまま使える。

**絞っていなかった。**

> ★★★<b>77 の教訓(用意した側は「使われている」と思い込む)の、ちょうど<b>逆の顔</b>である</b> ——
> <b>使う側が「絞られている」と思い込んでいた。</b>
> ★<b>どちらも、書いた人が確かめずに書いた文である。</b>

★★<b>67 の教訓(コメントに書き写した本文は、本文が変わっても自分では変わらない)の一族でもある</b> ——
<b>写したのが本文ではなく「相手の実装の性質」だった</b>だけである。

### 9-2. 描いていない欄は、漏れていても誰も気づかない

`revealedCards` は **Batch 44 からビューに載っていた**。
非公開の束が相手へ届く穴も**そのときから在った**。
★<b>実害が出ていなかったのは、`battle.js` がこの欄を1度も読んでいなかったからにすぎない</b>。

> ★★★<b>「描いていないから安全」は、安全ではなく<b>先送り</b>である。</b>
> <b>描く日に、そのぶんまとめて出てくる。</b>

★<b>80 が「届いているか」を数えなければ、この穴は描いた日に本番で出ていた。</b>

### 9-3. 器が1つで意味が2つあると、まわりも全部1つになる

`revealedZone` は器が1つだった。だから:

- **ビューのフィルタ**も1つ(絞らない)になった
- **ログの文言**も1つ(「公開しました」)になった
- **写して消す段**は2箇所に同じ形で書かれ、旗を持てなかった

★★<b>意味を2つに分けた瞬間に、この3つが同時に直った。</b>

> ★<b>「同じ情報を2箇所に置かない」(設計判断28)の裏返しがある</b> ——
> <b>違う情報を1箇所に置くと、その1箇所に触れるすべてが道連れになる。</b>

### 9-4. 教訓を知っていることと、当てられることは別である

軸9(《光霊・ネフラ》)は、**77・79・80 が3度書いた教訓そのもの**だった ——
「規則が n 入口ぶんあるなら、番人も n 入口ぶん要る」。
★<b>知っていたのに、番人を書いた時点では2入口ぶんしか置いていなかった</b>。
★★<b>気づかせたのは、壊し検証の軸を「入口ごと」に立てる作業である</b>。

> ★★★<b>壊し検証は番人を試すためのものだが、<b>番人の数を決めさせる</b>道具でもある。</b>
> ★<b>軸を立てて当てる先が無かったら、それは足りない番人である。</b>
