# QTE 対戦アプリ — 引き継ぎ書 v82

最終更新: 2026-08-27。**Batch 74(73 の判断待ち14件をすべて片付けた + 進化ミニオンの一族13枚)を実施。**

| 項目 | 現在値 |
|---|---|
| JUnit | ★**889件 全緑**(840 → +49)|
| verify | ★**711件 全緑**(706 → +5)—— ★**74 は Java も JavaScript も変えている**(72 と同じ「両方あるバッチ」)|
| 効果の未実装 | **0枚**(据え置き) |
| 本文と実装の突き合わせ | **235 / 235枚(完了・据え置き)**。★**74 はカード本文を1文字も触っていない** |
| 版数 | ★**`battle.js` を上げた(v=35 → v=36)**。★★**`battle.css` は据え置き(v=53・5枚とも)** —— **74 は1文字も触っていない**。`manual-battle.js` v=34 / `SFX_VERSION`=1 も据え置き |
| 裁定 | ★★★**1〜341 の全341件が確定。欠番ゼロ**<br>★**328〜341 は 73 の判断待ちへの回答であり、74 の着手時に確定してその場で採番した**(324〜327 の反省の反映)。**新しい裁定は 342 から** |
| カード定義 | 235枚(★**1文字も触っていない**。★★ただし**効果は8枚ぶん変え、「ミニオン」の指すものを13枚ぶん広げた**) |
| ★**Java** | ★★★**変えた**(判定層・効果・アクション・サービス・状態・WS の7ファイル) |
| ★**JavaScript** | ★★★**変えた**(`battle.js` の3箇所) |
| ★新設 | `src/test/java/com/example/qte/Batch74RulingTest.java` /<br>`…/Batch74EvolutionPutTest.java` / `tools/batch74_break_check.py` /<br>`notes/batch74-design-notes.md` / `notes/batch74-ruling-requests.md` |
| ★消した名前(裁定196) | `GameActions.untapMana`(→ `untapManaAt`)/ `PutFromHandState`(→ `EffectPutState`)/<br>`putFromHandSequence`(→ `effectPutSequence`)/ `putOneFromHand` は残存 /<br>`ResumePoint.EVOLUTION_MATERIAL_FROM_HAND`(→ `EVOLUTION_MATERIAL_FOR_PUT`)/<br>`finishPutFromHand`(→ `finishEffectPut`) |
| 壊し検証 | `tools/batch74_break_check.py` ★**34軸(OK 34 / NG 0)** |
| ★判断待ち | ★★★**1件も残していない**(73 は14件残した) |

---

## ★★★0. 最初にやること

### ★clone の「新しさ」の確かめ方

`git log -1` の**日付でもコミットメッセージでも測れない**(69 で実際に踏んだ)。確かめるのは:

1. **`TargetCandidates.java` の `MINION_CARD` が `master.type().isMinion()` か**(74 の関門)
2. **`CardEffectRegistry.java` に `EffectPutSource` が在るか**(74 で新設)
3. **`GameActions.java` に `untapManaAt` が在り、`untapMana` が**<b>無い</b>**か**(74 で置換)
4. **`mvn test` の件数が 889件か**
5. **`tools/batch74_break_check.py` が在るか**

★★**73 の着手時のような「未 push」は 74 の着手時には無かった**(clone は `a41c5d6 batch73`)。
★**push は「そのバッチが終わった時点」で一度に行うこと。**

### 手順

1. `qte-project-reference.md` を読む(★**2-12 と設計判断54・55 が新設された**)。
2. `notes/qte-pitfalls.md` の該当節を読む
   (★特に**「裁定を『据え置く』と決めたときの落とし穴(Batch 74)」**
   **「効きすぎている実装は、別の規則の陰に隠れる(Batch 74)」**
   **「『暫定』は正しい理由で置かれていることがある(Batch 74)」**
   **「1つの欄に2つの意味を持たせると、片方でしか壊れない(Batch 74)」**
   **「シャッフルに依存する試験は番人ではない(Batch 74)」**
   **「写しが『揃って間違っている』と、揃っていることを測る番人は緑のまま(Batch 74)」**)。
3. 本ファイルと **`notes/batch74-design-notes.md`** を読む。
4. ★**`notes/batch74-ruling-requests.md` を読む** ——
   **判断待ちは0件だが、実装で決めたことと申し送りが書いてある**(とくに 2-1)。
5. ソースコード取得: `git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git`
6. `m2repo.zip` を取り込んで `mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test`。
   ★**889件全緑が出発点である。**緑でなければ止めて報告する。

---

## 1. ★★★74 がやったこと(要点)

### 1-1. B —— 73 の判断待ち13件をすべて片付けた(裁定328〜340)

**実装を変えた8件:**

| カード | 何を変えたか | 裁定 |
|---|---|---|
| 天界の守護神 ゾディアック | 「相手のミニオンが自分のリーダーを狙えない」側を外した | 328 |
| 蒼海の賢者 | 手札0枚でも起動でき、回復だけが起きる | 329 |
| 分那愚利 | 【召喚時】の対象を必須にした | 330 |
| 風弾の跳弾 | 相手の場が空でも使えるようにした | 331 |
| 神風の大号令 | 強化の期限を `THIS_TURN` → `PERMANENT` | 332 |
| 静空の風使い | アンタップするマナを本人に選ばせる | 333 |
| 回帰の風穴 | 2回目の詠唱もカードの使用として数える | 334 |
| 詠唱の宝珠 | 「場を離れたとき」→「破壊されたとき」 | 336 |

**据え置き5件(★番人だけ新設した):**

| カード | 裁定 | 73 の時点での番人 |
|---|---|---|
| 豊穣の地霊主 | 337 | 肯定側だけ在った(**否定側は無い**) |
| 傷痕の闘帝 | 338 | **1件も無い** |
| 背水の炎壁 | 339 | **1件も無い** |
| 悪夢 / 墓場の怨念集合体 | 340 | **1件も無い** |
| 聖光の守護聖 | 335 | (実装を変えた側) |

> ★★★**「変えない」と決めたことこそ、次の人が壊しやすい**(設計判断54)。
> 「本文に『自分の』が無いのだから両者を見るべきだ」という読みは、
> **裁定を知らなければ正しく見える。**

### 1-2. ★★★A —— 進化ミニオンの一族13枚(裁定341)

**13枚を個別に直していない。**直したのは**3種類だけ**である(裁定130)。

1. `TargetCandidates.Filter.MINION_CARD` の**1行**(`== MINION` → `isMinion()`)と、
   `battle.js` の写し**2箇所**
2. 各経路の合流点を**出どころ非依存の1本**へ寄せた
   (`effectPutSequence` / `requestManaSummon` / `resolveTrashRevive`)
3. `GameActions` の**2つの入口**(`reviveFromGrave` / `putManaCardIntoField`)に
   **素材を受ける口**を作った

★★**73 の絞り込みは種別の読み違いではなく、裁定226 を守るための暫定処置だった** ——
裁定308(b) が「暫定である」と**明記していた**。
★★★**暫定を外すには、置かれた理由(素材を確保する段が無い)のほうを先に解消しなければならない。**

★**素材をいつ選ぶかは「出す」か「召喚する」かで違う:**

| 経路 | 素材を選ぶ時点 | 【召喚時】 |
|---|---|---|
| 手札からの進化召喚 / 特殊召喚 / 禁忌 | **使用宣言のとき** | 発動する |
| 《黄泉の召喚主》の墓地からの**召喚** | **使用宣言のとき**(★74) | 発動する |
| 効果による「出す」(手札) | 解決中の割り込み | 発動する(裁定311) |
| 効果による「出す」(墓地・マナ・山札) | 解決中の割り込み(★74) | 発動しない |

### 1-3. ★★★74 が踏んだ3つの新しい顔

**(a) 効きすぎている実装が、別の規則の陰に隠れていた。**
《天界の守護神 ゾディアック》の本文に無い禁止は【守護】と完全に重なるので普段は見えず、
**【貫通】持ちのときだけ症状が出ていた** —— しかも判定が貫通より先に走っていた。
> **「壊しても落ちない」の裏返しは「壊れていても誰も気づかない」である。**

**(b) 写しが「揃って間違っている」と、揃っていることを測る番人は緑のまま。**
`MINION_CARD` は Java に1つ・`battle.js` に2つ、合わせて**3つの写し**がある。
verify 49-1 は「両方に居るか」を測っていたが、**73 まで3つとも揃って間違っていた**。
> **揃っていることは、正しいことではない。**
> ★74 は「名前が在るか」ではなく **「どう判定するか」** を測る軸を verify に足した。

**(c) シャッフルに依存する試験があった。**
`Batch68SummonTargetTest#サイクロンリフレッシュはウェポンもデッキに戻せる` は
「山札に戻してシャッフル → 2枚引く」を**山札だけ**で測っており、
**戻したウェポンをその場で引き当てると落ちる**(実測で約5%)。74 の全体試験で実際に落ちた。
> **「たまに落ちる」は「たまに嘘をつく」と同じである。**
> ★山札と手札の両方を見る形に直した。

### 1-4. ★★壊し検証で2つの軸が「空振り」した

| 軸 | 最初の壊し方 | なぜ空振りしたか |
|---|---|---|
| 12 | 《豊穣の地霊主》に「相手のリーダーも見る」を足した | ★**引く先が置いた側のままだった**ので、手札は1枚も増えなかった |
| 18 | `MINION_CARD` を無条件で通し、「マナのスペルは場に出せない」を照合先にした | ★**マナの経路は `TargetSpec.Filter` を1つも通らない**(ラムダが弾いている) |

★どちらも **「壊した」と「効いた」は別である**という同じ形である。
★軸18 は照合先を《禁忌の代償》へ変え、**そのための番人を1本新設した**。

---

## 74 の確認項目(★これを照合する)

- **新設**: `src/test/java/com/example/qte/Batch74RulingTest.java`(27件)/
  `…/Batch74EvolutionPutTest.java`(19件)/ `tools/batch74_break_check.py` /
  `notes/batch74-design-notes.md` / `notes/batch74-ruling-requests.md`
- **`effect/TargetCandidates.java`** …… `case MINION_CARD -> master.type().isMinion() ? null` である
- **`effect/CardEffectRegistry.java`** …… `EffectPutSource`(HAND / HAND_SELECTED / TRASH / MANA / DECK)と
  `EffectPutState`(7要素の record)と `effectPutSequence` / `putOneByEffect` /
  `requestEvolutionMaterialForPut` / `finishEffectPut` / `requestWindUserManaUntap` が在る
- **`effect/ResumePoint.java`** …… `WIND_USER_MANA_UNTAP` と `EVOLUTION_MATERIAL_FOR_PUT` が在り、
  `EVOLUTION_MATERIAL_FROM_HAND` が**無い**
- **`effect/RuleGuards.java`** …… `isLeaderDestructionPrevented` と `causedByOpponent` が在り、
  `minionAttackDenial` に ZODIAC の判定が**無い**(`leaderAttackDenial` には在る)
- **`game/GameActions.java`** …… `untapManaAt` が在り `untapMana` が**無い**。
  `onWeaponLeftPlay(room, owner, weapon, boolean byDestruction, boolean fromTaboo)` である。
  `reviveFromGrave` と `putManaCardIntoField` に**素材つきの版**が在る
- **`game/PlayerState.java`** …… `pendingExtraSpellCasts` が在る
- **`game/GameService.java`** …… `summonFromGrave` が `materialIds` を受け、
  `afterCardUsed` が `getPendingExtraSpellCasts` を消費する
- **`web/GameWsController.java`** …… `summonFromGrave` が `request.materialIds()` を渡す
- **`static/js/battle.js`** …… `MINION_CARD` の2箇所が `EVOLUTION` も通し、
  `openTrashPicker('summon')` が `EVOLUTION` を落とさず、
  `beginGraveSummon` が進化なら `beginEvolutionSelection` へ入る
- JUnit **889件全緑**
- verify **711/711**(★74 で +5)
- `python3 tools/batch74_break_check.py` が **OK 34 / NG 0**
  ★★**長いので3〜4つに分けて回し、前後で `git diff --stat` を突き合わせること**
  (例: `1..10` / `11..20` / `21..30` / `31..34`)。
  ★**31〜34 は verify を回す軸なので末尾に置いてある**(72 の教訓)
- `python3 tools/report_effects.py --summary` で **未実装 0枚**
- `python3 tools/check_all.py .` が **「項目 1・3・5・6 はすべてパスしました」**
- `python3 tools/check_card_text_numbers.py` が **OK(説明の付いた 11件を除く)**
  ★**74 で `KNOWN_GAPS` から `QTE-M-EARTH-35` を外した**(12件 → 11件)
- `python3 tools/mark_text_reviewed.py --check` が **「全235枚が突き合わせ済み」**
- ★`python3 tools/mark_text_reviewed.py --stale batch73` が **47枚**(67 が見た側。据え置き)
- `python3 tools/check_structure.py src/main/java` が **exit 0**
  ★★**74 で `resumeChoice` が一度 300行を超えた**(`handCardIdsAt` を切り出して戻した)
- `python3 tools/check_records.py src/main/java` の**不一致は3件のまま**(★既知の誤検出)
- ★**版数**: `battle.js` **v=36** / `battle.css` **v=53**(据え置き)

---

## 2. 次の作業(Batch 75)

どれをやるかは**マスターの選択**である。

★★**74 で「候補 Q(73 の判断待ち)」と「候補 P(進化の13枚)」の両方が終わった。**
表から消えている。

| 候補 | 内容 | 大きさ |
|---|---|---|
| **★★★L. 70〜74 の手触りの調整** | ★**実機で触ってみて出てくるもの**。★62 の音と同じ扱いで、**マスターの報告待ち**である。<br>★★**74 で1つ具体的な論点が出た** —— 宣言時に選んだ進化が「選べるのに出ない」ことがある(`notes/batch74-ruling-requests.md` 2-1)。<br>直すなら `CardView` に真偽値を1つ足すだけである | 小 |
| **★M+N. 部屋消失の検出 + 無人部屋の掃除** | ★71 が残した穴と 66 の積み残し。**同じ工事の裏表であり、まとめてやるのが安い**。★72 が退室を作ったので「部屋が空になる」経路が日常的になった。★★**裁定待ちが要らない**ので、今すぐ着手できる | 中 |
| **★O. 通常モードの確認を1本化する** | ★72 が片肺として書き残したもの。宣言のための素の `confirm()` が7箇所残っており、裁定53 の理由3(JS を止めると STOMP の受信も止まる)はそちらにも効く。★フォーカストラップ(裁定50)も同時に要る | 小〜中 |
| **A. 音の調整** | ★**実機で聞いた印象を反映する**(62 からの持ち越し。まだ未報告) | 小 |
| **B. 通常モードの演出** | 62 が作った差分の採取層に fx 層を乗せる。32a・32b・35・38 の4バッチぶん | 大 |
| **C. カードフェイスの1本化** | デッキメーカーの `.tile` を `.mcard` へ寄せる。★verify 28件の作り替えが同時に要る | 中〜大 |
| **★R. 相手の墓地を参照する器** | ★74 が「無い」と書き残したもの(裁定340)。★**現行の235枚に使い手が1枚も無い**ので、<br>**カードが増えるまで作らない**のが筋である(裁定178) | (作らない) |

★**わたし(クロエ)の推し**: **L(実機確認)→ M+N**。
★理由:
1. ★★★**実機確認が 35〜74 まで積み上がっている。**
   70 は遊び方を、71 は事故のときの見え方を、72 は試合の出入りを、
   **74 は「どのカードが選べるか」を変えた** —— どれも**人が触ってみないと良し悪しが決まらない**。
2. ★**74 は1つ、はっきりした論点を残した**(宣言時の進化が「選べるのに出ない」)。
   これは<b>マスターが実機で触れば10秒で決まる</b>種類の問いである。
3. ★★**M+N は裁定待ちが要らない。**判断待ちが0件になった今、
   「回答を待つあいだに進められる工事」という理由はもう無い ——
   **純粋に価値の大きい順で選べる。**

### 各バッチがやること(P2から継続・★74 で3つ足した)

1. `python3 tools/report_effects.py --summary` で未実装の枚数を確認する(**0枚が正常値**)。
2. ★★`python3 tools/mark_text_reviewed.py --check` も通す(67 で新設)。
   **カード本文を1文字でも触ったら、そのカードの実装を突き合わせ直して記録すること。**
3. **本文が2通り以上に読めるカードは、実装で決めずに必ず裁定を仰ぐ**(裁定184)。
   ★★★**依頼文は `notes/batchNN-ruling-requests.md` に1本にまとめること**(73)。
   ★★★★**確定したその場で採番すること**(★74 が実際に守った1例目である)。
4. **カード毎の JUnit を `support/AutoGameFixture` の上に書く**(裁定187)。
   ★★★**ただし本物の入口が存在しない挙動もある**(★74 の《聖光の守護聖》)——
   そのときは判定層を直接叩くが、**入口を通っている証明にはならない**と書き残すこと。
5. `mvn test` で回すこと(`surefire:test` はコンパイルしない。裁定208)。
6. ★**壊し検証の改変は「軸」ごとに1件ずつ当てる**(57 の教訓)。
   ★★★**出口が複数ある関数は、出口ごとに当てること**(71)。
7. ★★**「壊しても落ちない」を見たら、まず改変が当たっているかを疑う**(11の形)。
   ★★★**74 は既知の2つを実際に踏んだ** ——
   **(a) 改変そのものが効いていない**(側を足したが引く先が変わっていない)/
   **(b) 照合先がそこまで届いていない**(壊した場所を番人が通らない)。
   **どちらも「壊した」と「効いた」は別である。**
8. ★**画面(テンプレート)を作ったら、MockMvc で 200 と中身を測る試験を1件は置く**(61 の教訓)。
   ★★★**ただしそれを「効いている」の番人だと読まないこと**(71・72)。
9. ★★**音を触ったら、実機でマスターに聞いてもらう**(62 の教訓)。
10. ★★**「暫定でこうしておく」は、前提が消えても自分では戻らない**(64・**時間**)。
    ★★★**74 の追記: 暫定は「正しい理由」で置かれていることがある** ——
    **外すには、置かれた理由のほうを先に解消しなければならない。**
    ★**暫定を書くときは、外す条件を書くこと。**
11. ★★**「簡易版」は、本家が直っても自分では直らない**(65・**空間**)。
12. ★★★**「いつ直すか」を書いても、実装は自分では動かない**(66・**予定**)。
    ★★★★**74 の実例**: 《神風の大号令》の期限は Batch 59 が持ち越しに挙げてから
    **13バッチ動かなかった**。
13. ★★★**コメントに書いた本文は、本文が変わっても自分では変わらない**(67・**写し**)。
    ★★★★**74 の実例**: 《詠唱の宝珠》の「発注者確認済み」は Ver0.4 時代の確認だった。
14. ★★★**別の性質の「代用」として書いた条件は、代用元が変わっても自分では変わらない**(68・**代用**)。
15. ★★★**「まずは〜から」と書いた実装は、そこで止まったまま動かない**(69・**途中**)。
16. ★★★**「書いてあるのに効いていない」は、クラスの数を数える検証では見つからない**(70・**空文**)。
17. ★★★**片方のモードで直したことは、もう片方が直ったことではない**(71・**片肺**)。
    ★★★★**74 の実例**: 《風弾の跳弾》と《ラスト・アタック》は同型なのに、
    **Batch 51 が直したのは後者だけ**だった —— **モードではなく、カードどうしの片肺である。**
18. ★★★**理由の幅と結論の幅を揃えて書く**(72・**幅**)。
19. ★★★**送る側と受ける側を別々に見ている番人は、あいだを見ていない**(72b・**あいだ**)。
20. ★★★**誤った前提は、正しい確認を素通りさせる**(73・**前提**)。
21. ★★★**移行の「後に」書かれた実装は、移行の取りこぼしを探す目では見つからない**(73・**後発**)。
22. ★★★**母集団は、作業を始める前に固定して控えておく**(73)。
    ★**74 は設計解説の0章に母集団を先に書いてから着手した。**
23. ★★★**効きすぎている実装は、別の規則の陰に隠れる**(★74・**陰**)。
    **「壊しても落ちない」の裏返しは「壊れていても誰も気づかない」である。**
24. ★★★**「変えない」と決めたことにも番人を置く**(★74・**据え置き**)。
    **据え置きの5件のうち3件には番人が1つも無かった。**
25. ★★★**写しが「揃って間違っている」と、揃っていることを測る番人は緑のまま**(★74)。
    **写しがあるものは「どう判定するか」まで測ること。**
26. ★★★**シャッフルに依存する試験は番人ではない**(★74)。
    **乱数を使う効果は、結果ではなく「行き先の集合」を測ること。**
27. ★★**UI を直すバッチは、先に端の盤面を作って測る**(65 の教訓)。
28. ★★**「番人が無い」と思ったら、まず在るかどうかを見る**(65 の教訓。66〜73 で踏んだ)。
29. ★★**消した番人は必ず書き残す**(裁定196)。★**作らなかった番人も**(67)。
    ★★★**「壊しどころが無いもの」も**(70・72・73・★74)。
    ★★★★**消した名前も**(★74 は5つ消した)。
30. ★★★**番人は「回る場所」で選ぶ**(69・設計判断45)。
    ★★★★**両方あるバッチもある**(72・★74)。★★★★★**片方しか無いバッチもある**(73)。
    ★★★★★★**本物の入口が存在しない挙動もある**(★74)。
31. ★★**静的ファイルの版数はファイルに付く。**`battle.css` を触ったら**5枚とも上げる**(7-5)。
    ★★★**触っていないなら上げない**(73)。★★★★**74 は「据え置きであること」も測った。**
32. ★★★**長い壊し検証は、前後で差分を見る**(70)。殺されると壊したまま残る。
33. ★★★**遷移を起こしうる検証の項目は末尾に置く**(72)。
34. ★★★**WS の宛先や入口を増やしたら、その本文を表に足すこと**(72b・73)。
    ★★★★**74 の但し書き: 「既に在る項目を読むようになった」だけなら足す行は無い。**
35. ★★★**push は「そのバッチが終わった時点」で一度に行う**(73 の着手時に踏んだ)。

---

## 3. 発注者とのやりとり

- grep優先でファイルを渡り歩き、全体読み込みは避ける。
- **判断に迷う点はまとめて質問する。**1つずつ聞かない。
  ★★★**74 は着手時に14件をまとめて伺い、その場で裁定328〜341 として採番した。**
- ★★**カード235枚のような広い突き合わせは、文明ごとに分けて並行させると速い**(73)。
  ★★★**74 の追記: 「実装の調査」も同じ形で分けられる** ——
  74 は B の13件を3つに分けて並行で調べさせ、**報告だけを受け取って実装は1人で行った**。
  ★**報告だけを返させ、実装の変更は1箇所でまとめて行うこと。**
- 呼び方は「クロエ」、発注者は「マスター」。会話は日本語カジュアル体、
  **ドキュメントは通常文体(である調)。**

---

## 4. 既知の落とし穴

**★全文は `notes/qte-pitfalls.md` にある。**74 で6節を新設した。

| 節 | 内容 |
|---|---|
| ★**裁定を「据え置く」と決めたときの落とし穴(Batch 74)** | 「変えない」と決めたことこそ壊しやすい / 据え置きの3件に番人が無かった / 据え置きは壊し検証の軸にしにくい |
| ★**効きすぎている実装は、別の規則の陰に隠れる(Batch 74)** | ゾディアックの本文に無い禁止は【守護】と重なって見えなかった / 判定の順序が症状を決めていた |
| ★**「暫定」は正しい理由で置かれていることがある(Batch 74)** | 外すには理由のほうを先に解消する / 暫定を書くときは外す条件を書く |
| ★**1つの欄に2つの意味を持たせると、片方でしか壊れない(Batch 74)** | payload に進化カードIDと locator を畳んで、マナの経路でだけ壊れた |
| ★**シャッフルに依存する試験は番人ではない(Batch 74)** | 約5%で落ちる / 「たまに落ちる」は「たまに嘘をつく」 |
| ★**写しが「揃って間違っている」と、揃っていることを測る番人は緑のまま(Batch 74)** | `MINION_CARD` は3つの写しがあり、73 まで3つとも間違っていた |
| ★**本物の入口から観測できない挙動(4件目)** | 《聖光の守護聖》のリーダー破壊防止 |
| ★**相手の墓地を参照する仕組みが1つも無い** | `Kind.TRASH` は `Side` を見ない —— 黙って自分の墓地が返る |
| **総点検の続き —— 残り188枚 / WS の受け口の箱型化(Batch 73)** | (据え置き) |
| **クライアントとサーバの「あいだ」(Batch 72b)** | (据え置き) |
| **試合の出入り(Batch 72)** | (据え置き) |

---

## 5. デリバリー形式(変更なし)

マスターの Eclipse ワークスペース
(`C:\Users\奥村優斗\OneDrive\ドキュメント\eclipse_workフォルダ\qte-battle-batch0`)へ
直接書き込む。手順はv62の4章のとおり。

★★**74 はファイル単位の削除を1件も行っていない。**変更と新設だけである
(★消したのは**名前**であって、ファイルではない)。

反映前に必ず実行:

```bash
python3 tools/check_structure.py src/main/java
python3 tools/check_all.py .
python3 tools/check_records.py src/main/java     # ★既知の誤検出3件
python3 tools/check_undeclared.py src/main/resources/static/js/*.js
node --check src/main/resources/static/js/manual-battle.js
node --check src/main/resources/static/js/battle.js
NODE_PATH=/home/claude/.npm-global/lib/node_modules \
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers \
  python3 verify/build_harness.py && node verify/verify.js
mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test
python3 tools/check_legacy_ids.py
python3 tools/report_effects.py --summary
python3 tools/check_leader_abilities.py
python3 tools/check_card_text_numbers.py
python3 tools/mark_text_reviewed.py --check
```

---

## 6. チャット開始テンプレート

```
QTE Battle の開発を継続する。Batch 75 を行う。

読む順:
1. プロジェクトナレッジ内の `qte-project-reference.md`
   ★2-12(「ミニオン」という言葉の指すもの)と設計判断54・55 が Batch 74 で新設された。
2. プロジェクトナレッジ内の `claude/qte-handoff-v82.md`(本ファイル)
3. プロジェクトナレッジ内の `notes/batch74-ruling-requests.md`
   ★判断待ちは0件だが、実装で決めたことと申し送りが書いてある(とくに 2-1)。
4. プロジェクトナレッジ内の `notes/batch74-design-notes.md`
5. `notes/qte-pitfalls.md` の該当節
   (特に「裁定を『据え置く』と決めたときの落とし穴(Batch 74)」
    「効きすぎている実装は、別の規則の陰に隠れる(Batch 74)」
    「『暫定』は正しい理由で置かれていることがある(Batch 74)」
    「シャッフルに依存する試験は番人ではない(Batch 74)」
    「写しが『揃って間違っている』と、揃っていることを測る番人は緑のまま(Batch 74)」)

環境:
5. git clone --depth 1 https://github.com/okuson-droid/qte-battle-batch0.git
   ★★★clone に「そのバッチの続き」が入っているとは限らない(73 で実際に踏んだ)。
     確かめるのは「TargetCandidates の MINION_CARD が isMinion() か」
     「CardEffectRegistry に EffectPutSource が在るか」
     「GameActions に untapManaAt が在り untapMana が無いか」「mvn test が 889件か」である。
6. 接続フォルダの m2repo.zip を device_stage_files で取り込んで /root/m2work へ展開し、
   mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test を回す。
   ★889件全緑が出発点である。緑でなければ止めて報告する。
7. 74 の反映を本ファイルの「確認項目」で照合し、verify 711/711 と
   report_effects の「未実装0枚」、check_card_text_numbers と
   mark_text_reviewed --check の OK を確認する。
   ★mark_text_reviewed --stale batch73 は 47枚である(67 が見た側。これで正しい)。
   ★版数は battle.js v=36 / battle.css v=53 である。

作業:
8. 本ファイル2章「次の作業(Batch 75)」の候補からマスターが選んだものを行う。
   ★設計解説は新規ファイル `notes/batch75-design-notes.md` に書く。
   ★裁定を仰ぐ点は `notes/batch75-ruling-requests.md` に1本化し、
     ★★確定したその場で採番すること(74 がそうした)。新しい裁定は 342 から。
   ★壊し検証は `tools/batch75_break_check.py` を新設し、改変は「軸」ごとに1件ずつ当てる。
     ★★出口が複数ある関数は、出口ごとに当てること(71 の教訓)。
     ★★長い壊し検証は前後で差分を見ること(70 で殺されて壊したまま残った)。
     ★★★「壊した」と「効いた」は別である(74 で2軸が空振りした)。
   ★★カード本文を1文字でも触ったら、そのカードの実装を突き合わせ直し、
     tools/mark_text_reviewed.py で記録すること(理由の記入は必須)。
   ★★★母集団は作業を始める前に固定して控えること(73 の教訓)。
   ★★「変えない」と決めたことにも番人を置くこと(74 の教訓)。
   ★★★写しがあるものは「どう判定するか」まで測ること(74 の教訓)。
   ★★UI を触るなら、先に端の盤面を作って測ること(65・69〜72 の教訓)。
   ★★★番人を置く場所を選ぶ前に、そこまで届くかを確かめること(70〜74 の教訓)。
   ★★★静的ファイルを触っていないなら、版数は上げないこと(73 の教訓)。

呼び方は「クロエ」、こちらの呼び方は「マスター」で。会話は日本語カジュアル体、
ドキュメントは通常文体(である調)で。
```

---

## 7. 74 完了時点の積み残し

### ★★★74 が新たに載せたもの(マスターに諮る案件)

**★判断待ちは0件である。**代わりに<b>手触りの確認</b>が1つある。

- ★★**宣言時に選んだ進化が「選べるのに出ない」ことがある**
  (`notes/batch74-ruling-requests.md` 2-1)。
  手札から**割り込みで**選ばせる経路は「素材を確保できる進化」だけを候補にするが、
  **宣言時の対象指定ではその絞り込みが掛けられない**
  (`TargetSpec.Filter` に盤面の規則を足すと `battle.js` にも要る。裁定195・234)。
  ★<b>直すなら `CardView` に真偽値を1つ足すだけである</b>(`canSpecialSummonFromGrave` と同じ形)。
  ★★**マスターが実機で触って決めてほしい。**

### ★★マスターにお願いすること(実機確認)

- **Eclipse で refresh(F5)→ Run As → JUnit Test。**期待: **全889件が緑**。
- ★★★**74 の確認**(詳細は `notes/batch74-design-notes.md` の10章)
  - **《黄泉の召喚主》で墓地の進化ミニオンが一覧に出て、押すと素材選択に入るか。**
  - **《死者蘇生》《禁忌の代償》《リボーンライヴ・ノア》で墓地の進化を出せるか。**
  - **《喧嘩上等》《俺等地上覇夜露死苦》《勝鼓美》《素手喧嘩》でマナの進化を出せるか。**
  - **《ギガマウス・バイト》《英術・スケアロック》で手札の進化を選べるか**
    (★素材が場に居ないときは選べてしまうが、選ぶと出ない ——上の論点)。
  - **《降臨の伝道師》で【守護】の進化(《英霊・タイガラム》)が出るか。**
  - **ゾディアックが居るとき、【貫通】持ち(《急流の狙撃手》)でリーダーを殴れるか。**
  - **《蒼海の賢者》が手札0枚でも起動でき、2回復するか。**
  - **《静空の風使い》で「アンタップするマナを選んでください」が出るか。**
  - **《回帰の風穴》の強化使用で、使用枚数を数えるカードが2つ進むか。**
  - **《神風の大号令》の攻撃力+が、次のターンも残っているか。**
  - **《詠唱の宝珠》が、山札に戻ったとき / 禁忌由来のときに発動しないか。**
- ★★★**70・71・72・73 の実機確認がまだ未報告である。**
- ★**62 の音の実機確認がまだ未報告である**(手動モード11種・通常モード9種)。
- ★**64〜69 の画面も未報告である。**
- ★★★**GitHub の push は「そのバッチが終わった時点」で一度に行ってほしい。**

### 継続中の積み残し

- ★★**通常モードには無人部屋の掃除が無い**(66 の積み残し・候補 N)。
- ★★★**通常モードには「部屋が消えた」の検出が無い**(71 で明文化・候補 M)。
- ★★**通常モードには宣言のための素の `confirm()` が7箇所ある**(72 で片肺として明文化・候補 O)。
  ★フォーカストラップ(裁定50)も通常モードには無い。
- ★★**マリガンと割り込みの選択が「送れなかったら消えない」ことは名指しで測っていない**(71)。
- ★**右列の中段が溢れたことに人が気づけるか**は分からない(69)。
- ★★**ドラッグはタッチでは動かない**(70)。HTML5 のドラッグの制約である。
- ★★**本文と実装の突き合わせは 235/235 で完了した**(73)。★**74 は本文を触っていない。**
- ★★**手動モードの `applyManaOverlap` は重なりの上限を「覆う側」の添字で取っている**(65)。
- ★**通常モードに演出が無い**(候補 B)。★**カードフェイスの実装が2つある**(候補 C)。
- ★**カード名の表記は揃っていない**(「愚乱怒土地(グランドランド)」の半角括弧など)。
- **【常在】：のコロン**(3枚)。
- 35〜74 の実機確認が未報告のまま(46bと48は報告あり)。
- 進化召喚がモアニールの登場置換で止まったときの素材の扱い(裁定232)。
- **本物の入口から観測できない挙動が4件**(★74 で4件目が確定)——
  《英霊・コレキ》/《風弾の跳弾》/《悪夢》の封じの範囲 / ★《聖光の守護聖》のリーダー破壊防止。
- ★★**相手の墓地を参照する仕組みが1つも無い**(★74・裁定340)。
  ★**使い手が0枚なので器を作っていない**(裁定178)。増えたらここから。
- ★★**`resumeChoice` は 300行の上限すれすれである**(★74 で一度超えて分割した)。
- 手動モード関連の積み残しは一時停止中。

★**裁定は 1〜341 の全341件が確定し、欠番はゼロである。新しい裁定は 342 から。**
