# QTE Battle 既知の落とし穴(長期・追記専用)

作成: 2026-08-19(Batch 49 完了時点)。

**★この文書は追記専用である。バッチごとに書き直さない。**
Batch 49 まではハンドオフ書(`claude/qte-handoff-vNN.md`)の3章に全文を置き、
毎バッチ丸ごと写していた。写しは腐る(裁定171・188)うえ、v51 以降は
「詳細は v51 にある」という参照が増えて、実際に限界に来ていた。
そこで**落とし穴の正をこのファイル1つに移した**(裁定163 の自己適用)。

**書き方の決まり:**

- 新しい落とし穴は、該当する節に足す。節が無ければ節ごと足す。
- **間違いが分かったものは消さずに訂正する。**「★NN で訂正: 〜」の形で残す
  (裁定114: 注釈が番人を黙らせることがある。何が変わったかを追えるようにしておく)。
- ハンドオフ書には**この文書へのポインタと、直近バッチで足した節の名前だけ**を書く。
  全文をハンドオフ書へ写さないこと。
- 裁定番号の全文は `notes/qte-rulings.md` にある。ここでは番号で参照するだけにする。

---

### サンドボックスの制約

- **★★★Maven Central へは到達できない(403)が、`mvn test` は回る。**
  マスターの `.m2\repository` の zip(`m2repo.zip`・約74MB)を接続フォルダから取り込み:
  `unzip -q /mnt/user-data/uploads/qte-battle-batch0/m2repo.zip -d /root/m2work` →
  **`mvn -o -B "-Dmaven.repo.local=/root/m2work/repository" test`**(完全オフライン)。
  ★`unzip` は「backslashes as path separators」の警告で**終了コード1を返すが、展開は成功している**。
  ★`device_stage_files` は非同期である。74MB の転送は**数十秒かかる**ので、
  `unzip` の前にファイルの出現を待つこと。
  ★依存を pom に足したら zip の再作成をマスターに依頼すること(手順は 46a 設計解説追補)。
  ★`m2repo.zip` はコミットしない(.gitignore 済み)。
- **★★★テストの選択に日本語メソッド名を使えない**(裁定186)。
  `-Dtest='Class#日本語メソッド'` は **`Tests run: 0` + `BUILD SUCCESS`** になる。
  クラス単位(`-Dtest=Class`)で回し、**`target/surefire-reports/TEST-*.xml` を読んで**判定する。
  ★コンソール出力は日本語を `?` に潰すので、**grep での照合は必ず失敗する**。
- **★★テストのクラスパスに Jackson が無い**(48 で実測。★49 で再確認した)。
  テストから JSON を読みたいときは `ObjectMapper` を使わず、値を直接組み立てること。
  ★**66 で訂正: 今は在る。ただしパッケージが違う。**Spring Boot 4.0 の Jackson は
  **`tools.jackson.databind`**(Jackson 3)であり、`com.fasterxml.jackson.databind` は
  <b>テストのクラスパスに無い</b>。本番のコードも 46b 以降は `tools.jackson` を使っている。
  ★つまり「無い」のではなく「**古い名前で書くと無い**」である。
- **★★マスターの環境に `mvn` CLI は無い**(Eclipse の m2e のみ)。
  マスターへのテスト依頼は「Eclipse で Run As → JUnit Test」と書くこと。
  **箱内の `mvn -o test` が一次の答え合わせである。**
- **★★実サーバを起動できない。Thymeleaf の解決は確認できない。**
  テンプレートを変えたら**マスターに実サーバでの確認を必ず依頼すること**。
- **★★codeload の zip URL は 403 で落ちることがある。git clone は通る。**
- **★Playwright は `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers` で通る。**
  ★同梱 chromium の revision 1194(playwright@1.56.x)。
  グローバル導入なので `NODE_PATH=/home/claude/.npm-global/lib/node_modules` が要る。
- **★このセッションから GitHub へ push できない。** 納品は4章の直接書き込みである。
- **★★音は聞けない。**

### ★★消えたフィールド(46b の置き土産。裁定188)

Ver1.1 の `manual-cards.json` が実際に持つキーは**10個だけ**である:
`id / name / type / civilization / cost / attack / hp / imageId / ledgerCardId / text`。

| 消えたフィールド | 状況 |
|---|---|
| **`keywords`** | ★**48-hotfix で処理済み。**乗ったままの `faceText` が125枚で二重表示を作っていた |
| **`unlimitedCopies`** | ★**49 で洗った。乗ったままの分岐が3箇所あるが、どれも無害**(下記) |

`unlimitedCopies` を読んでいる3箇所(`ManualCardRepository` / `ManualDeckImporter.validate` /
`manual-deck-maker.html`)は、**常に false になるだけで挙動は正しい**
(46b の「同名4枚に例外は無い」どおりに動く)。
★**手動モードが停止中であることとテンプレートの確認コストから、49 では直さなかった。P5 で掃除する。**

★**探し方は決まっている** —— 消えたフィールド名で全ファイルを検索し、
読んでいる箇所の前提が今も生きているかを1つずつ確かめる。

### ★★闇文明・光文明の Ver1.1(Batch 50)

- **★★★「場以外から自分の墓地へ」は入口が1本しかない**
  (`GameActions.putIntoTrashFromElsewhere`)。手札からの discard・ミル・
  断罪の大天使によるドローの置換・裏向きマナの破壊が、すべてここを通る。
  ★**新しく「墓地に置く」処理を書くときは、必ずどちらの入口かを決めること** ——
  場を離れる墓地送りは `sendToTrashOrRestore`、それ以外はこちらである(裁定207)。
  `owner.getTrash().add(...)` を新しく直書きしてはいけない。
- **★★★カムバックキーパーは `TriggerType` ではない**(裁定206)。
  反応するのは<b>墓地に居る間</b>であり、`fire()` は場のミニオンしか引かない。
  ★**「トリガー型に揃える」直しをしてはいけない** —— 誰も登録できない。
- **★★★「場で破壊されたら戻らない」は条件分岐ではなく構造で決まっている。**
  `leaveFieldByDestruction` は `putIntoTrashFromElsewhere` を通らない。
  片方の入口をもう片方に流用した瞬間、**破壊されても永久に戻るミニオン**になる。
- **★★墓地から場へ出す経路も1本である**(`GameActions.reviveFromGrave`)。
  演舞の墓守の【常在】がここに1回だけ掛かる。黄泉還る水龍・ゾンストライカーも
  50 でこの経路に寄せた(以前は trash.remove + putIntoFieldByEffect の直書きだった)。
  ★もう1本の「墓地からの召喚」(黄泉の召喚主)は `GameService.summonFromGrave` にあり、
  そちらにも同じ発火口が置いてある。
- **★★`reviveFromGrave` の戻り値が `boolean` から `MinionInstance` に変わった**(★50)。
  同時に「場の末尾を取る」形を3箇所やめた(不滅のネクロマンサー・死者蘇生・降臨の伝道師)。
  ★**降臨の伝道師のそれは実害があった** —— `ON_ENTER` の中でさらに場が増えると、
  <b>関係の無いミニオンに3ダメージ</b>を与えていた。
- **★★★`putIntoFieldByEffect` が `null` を返す理由は2つになった。**
  (a) 場が満杯・踏み倒し禁止 = <b>カードは宙に浮いたまま</b>(呼び出し側が行き先を決める)、
  (b) 光霊・モアニールの置換 = <b>すでに山札の下に置いてある</b>。
  ★`null` だけを見て手札に戻すと、(b) で<b>カードが2枚に増える</b>。
  「出せなかった分は手札に戻す」カード(神の福音・ギガマウス・バイト)は、
  呼ぶ前に `isMinionZoneFull()` を自分で見る形に揃えてある。
- **★★モアニールの置換は3経路 + 3経路である。** 登場が
  `GameService.summonToField` と `GameActions.putIntoFieldByEffect`、
  ダメージが `GameActions.damageLeader` と `GameService.attack` と
  `GameService.leaderAttack`。**片方だけを直さないこと。**
- **★モアニールの登場置換は禁忌由来のカードを山札に戻さない。**
  禁忌カードは場を離れると消滅する(総合ルール3-6)ため、消滅ゾーンへ送る。
- **★バンユーの「1度しか攻撃できない」は場全体の合計である**(裁定200)。
  `MinionInstance.attacksUsedThisTurn`(個体)ではなく
  `PlayerState.minionAttacksUsedThisTurn`(場全体)を数える。**名前が似ているが別物である。**
  ★制限そのもの(`minionAttackLimitedOnTurn`)は**ターン番号の刻印**なので
  `startTurnReset()` で戻さない。戻すと、制限を掛けられた本人のターン開始で消えてしまう。
- **★アントマルエルは `fireAnyMinionEntered` に相乗りしている**(ロロイヨと同じ発火口)。
  ★**「ターンに一回」と書かれていないので回数制限は無い。**
  ロロイヨがターン刻印を要るのはテキストにそう書いてあるからであって、発火口の性質ではない。
- **★テングスンは「相手の」場を見る。** `StatCalculator.effectiveCost` の `owner` は
  <b>カードを使おうとしている側</b>なので、`state.opponentOf(owner)` を数える。
  封印されし禁忌魔人に次ぐ2枚目のコスト増加カードである。
- **★サモンズライトの【破壊時】の蘇生は自動決定(AutoChoice)である。**
  裁定192(盤面に残るものを決める選択は本人にさせる)の例外だが、構造上の理由がある ——
  【破壊時】は<b>相手のターン中にも起きる</b>のに対し、`GameService.resolveChoice` は
  ターンプレイヤーでなければ拒否する。相手ターンに問い合わせを出すと盤面が固まる。
  ★**相手のターンに発火しうる効果に、割り込み選択(PendingChoice)を使ってはいけない。**

### ★★画面とカード本文(Batch 61)

- **★★★同じものを見る画面が4つあって、4つとも形が違った**(★61)。
  カード本文が出る場所は /cards・/manual/cards・デッキメーカー・盤面の4つで、
  表・画像グリッド・カードフェイス×2 とばらばらだった。
  ★**「カード一覧」という名前のリンクは2つのロビーの両方にある。**
  どの画面の話かは、URL まで確かめてから動くこと。
- **★★★カードフェイスの実装は2つある**(★61 の発見)。
  デッキメーカーの `.tile` / `.t-*` は、盤面の `.mcard` / `.mcard-*` を
  <b>値まで写した手書きのコピー</b>である(あちらのコメントにも「盤面 .mcard-cost と同じ」とある)。
  ★**新しくカードを描く画面は `.mcard` を使うこと。**3つ目のコピーを作らない。
  ★1本化(`.tile` → `.mcard`)は<b>まだやってはいけない</b> ——
  機械検証545件のうち28件が `.tile` / `.t-*` を名指しで見ており、検証のほうが先に壊れる。
- **★★★カード本文の改行は HTML では消える**(★61)。
  235枚中28枚が改行を持つ。既定では空白1つに畳まれ、カード画像では2行のものが1行になる。
  `white-space: pre-wrap` を当てること。★`pre` にしてはいけない ——
  行内の折り返しまで止まり、長い1行がカードの外へはみ出す。
  ★宣言は `battle.css` の `.mcard-text` と デッキメーカーの `.t-text` / `.bc-text` の**2箇所**にある。
  番人も2つ要る(verify に2件置いた)。
- **★★本文の表記は放っておくとばらつく**(★61)。
  61 で76枚を「新カードの書き方」(全角丸括弧・半角数字・行末の句点・【】まわりの空白なし)へ揃えた。
  ★<b>揃えたことより、揃ったままでいることのほうが難しい。</b>
  `CardTextKeywordsTest` に**書き方の番人が4件**ある。落ちたら、出てくる一覧がそのまま直す対象である。
  ★これは「読み方」ではなく「書き方」の規則であり、**エンジンの挙動には影響しない** ——
  だからこそ試験が無いと誰も気づかない。
- **★★カード本文を触るときの番人は3つ**(★61)。
  ① `CardTextKeywordsTest` の169枚のキーワード照合 ② `EffectImplementationTest` の効果の印
  ③ JUnit 全部。この3つが緑なら、表記を触っても挙動は変わっていない。
- **★★★カード名は本文と別扱いにする**(★61)。
  「愚乱怒土地(グランドランド)」の半角括弧などは<b>名前の一部</b>であり、
  ログ・試験・デッキファイルが名前で照合している。表記の統一に巻き込まないこと。
- **★★Thymeleaf: th:each と th:replace を同じタグに書いてはいけない**(★61 で実際に踏んだ)。
  優先順位は th:replace(100)が th:each(200)より**先**である。同じタグに書くと
  差し込みが先に走り、まだ存在しない変数を渡して
  「Property or field '…' cannot be found on **null**」で 500 になる。
  th:each は外側の `th:block` に置き、差し込みは内側でやる。
- **★th:classappend の値に先頭の空白を入れない**(★61)。
  classappend 自身が空白を足すので、`? ' is-evolution' : ''` と書くと
  `class="qcard-face  is-evolution"` と<b>空白が2つ</b>になり、文字列で照合する試験が落ちる。
- **★★★画面が描けることは、誰も測っていなかった**(★61)。
  機械検証(verify)が見ているのはテンプレートから作った<b>ハーネス</b>であって、
  Spring がテンプレートを解決できるかではない。
  ★Thymeleaf の書き間違いは**Java のコンパイルを通る**。
  ページを1枚作ったら、MockMvc で 200 と中身を測る試験を1件は置くこと。
- **★Spring Boot 4 では @AutoConfigureMockMvc が使えないことがある**(★61)。
  それを提供するモジュールがこのプロジェクトの依存に入っていない(オフラインなので追加もできない)。
  `spring-test` の `MockMvcBuilders.webAppContextSetup(context)` で手で組み立てれば動く。
- **★★手動モードのカード定義が text を持たなかった理由は、もう成り立っていない**(★61)。
  設計書 3-1 の「新カード66枚にテキストが無く、既存168枚も信用できない」は Ver0.4 時代の話である。
  Ver1.1 では全235枚が本文を持つ。★**keywords を持たせない理由のほうは今も生きている**
  (キーワードはテキストから作るもの。裁定158)。

### ★★消すこと・揃えること・古い前提(Batch 60)

- **★★★「止めていた理由」が裁定なのか実装の都合なのかを分けて書く**(★60。裁定278)。
  57〜59 は《黄泉の召喚主》の墓地からの召喚に「対象を選ぶミニオンは出せない」ガードを置き、
  59 の時点でそれを「恒久のルール」と書いた。実際に止めていたのは
  **対象を運ぶ口が無かった**という実装の都合であり、
  裁定275(狭い読み)とは別物だった。60 で口を作ったらガードは消えた。
  ★**「今はこうするしかない」を「そういうルールである」と書くと、次の人は裁定まで疑う。**
- **★★似た経路が既にあるかを、見積もりの前に grep する**(★60)。
  裁定278 の依頼文は工事を「Java 3ファイル + JS 2本」と見積もったが、
  実際は Batch 53 が墓地からの【特殊召喚】のために敷いた導線
  (`GraveSummonRequest` / `beginSelection`)がそのまま使えて、新しい仕組みは0だった。
- **★★★検証は「支払い」より前に置く**(★60)。
  `summonFromGrave` に対象検証を足すとき、支払いの後ろに置くと
  「拒否されたのにマナがタップされ墓地が減る」が作れる。
  通常召喚(`playMinion`)と同じ順序 ——
  **検証 → 支払い → 除去 → 場に出す → 効果** —— に揃えること。
- **★★絞り込みが偶然守っているだけの穴は、次の1枚で開く**(★60。裁定282)。
  墓地から出すカード自身は、検証の時点ではまだ墓地に居る。
  `Kind.TRASH` の要求はそれを候補として見てしまうが、今は
  《墓場の怨念集合体》の `SPELL_CARD` 絞り込みが偶然弾いている。
  ★**「今のカードプールでは起きない」は、塞がない理由にならない。**
- **★★★本文が条件にしていないものを、実装が条件にしていないか疑う**(★60。裁定280)。
  《豊穣の地霊主》の本文は「マナにカードが置かれたとき」であって
  「表向きで置かれたとき」ではない。54〜59 の実装は**表向きしか数えていなかった**。
  ★**発火口を1つ作ったら、同じ意味の経路が他に何本あるかを数える。**
  裏向きの配置は6本あった。
- **★★共通化は「上限に達したときどうするか」の手前で止める**(★60)。
  裏向きの配置は `addFaceDownMana` 1本にまとめたが、
  上限(15枚)の判定は各経路に残した ——
  【還元】は墓地へ落とし、山札からは山札の上へ戻し、手札からは false を返す。
  振る舞いが違うものを1つにすると**嘘の共通化**になる。
- **★★★消したものは grep に出てこない。理由を設計解説に書く**(★60)。
  「片付け」のバッチほど、次の人は消えたものを「最初から無かった」と読む。
- **★★番人のために資料を丸ごと残すと、次の人はそれを資料として読む**(★60)。
  Ver0.4 台帳(101KB)を消すとキーワード抽出の169枚照合が死ぬので、
  **必要な169行だけ**を `src/test/resources/keyword-baseline.json`(7KB)に凍結した。
  ★台帳を「番人のために」置いておくと、コスト・本文が全部入っている以上、
  次の人はそれをカード台帳として読む。
- **★凍結した物差しは更新しない。**抽出規則を変えた結果そこが赤くなったら、
  直すのは物差しではなく**テスト側の「既知の差分表」**である。
  物差しを書き換えると、変わったことそのものが消える。
- **★★動かなくなったツールは「消す」と「止める」を使い分ける**(★60)。
  `build_id_map.py` / `rework_triage.py` は役目が終わったので消した。
  `convert_manual_cards.py` は**残して、実行すると理由を出して止まる形**にした ——
  CSV から `manual-cards.json` を作る手順を書き残しているのはあれだけであり、
  しかも**そのまま再実行すると全235枚の本文が消える**からである。
- **★★★同じ規則を2箇所に書くと、片方だけが直された状態が何バッチも続く**(裁定130。★60 の実例)。
  `unlimitedCopies`(同名無制限)は 46b に `DeckValidator` 側だけ撤去され、
  手動モード側(`ManualDeckImporter` / デッキメーカー)は**14バッチ生き残った**。
- **★★プリセットを厚くすると、プリセットを土台にした試験の前提が変わる**(★60)。
  `DeckValidatorTest.replaceOneMainCard` は「デッキに無いカードを1枚足す」前提だったが、
  Ver1.1 化でその文明のカードがほぼ全種類入った結果、行が重複して落ちた。
- **★★進化をデッキに入れるなら、素材にできる仲間も同じデッキに要る**(★60)。
  《海淵獣ゾクシム》の素材は「**水文明ではない**ミニオン1体」であり、
  メインデッキ(リーダーと同文明)には素材が1体も居ない ——
  **禁忌デッキから来る**。カードを疑う前に、試験の見ている範囲を疑うこと。
- **★★★版数を上げると機械検証が落ちることがある**(★60)。
  `verify/build_harness.py` が `battle.js(v=25)` と**版数まで含めた文字列一致**で
  Thymeleaf のタグを書き換えていたため、v=26 にした瞬間に一致しなくなり、
  スクリプトが読み込まれず `render is not defined` で落ちた。
  ★60 で正規表現化し、**置換が0件ならその場で止める**ようにした
  (`_sub_once`)。黙って一致しなくなるのがいちばん悪い。
- **★★★壊し検証は「実装を戻せたか」を測っていなかった**(★62 で実際に踏んだ)。
  ケース11 の改変が復元されず、**後続のケースがその汚染された内容を「元の姿」として保存した。**
  ★★**14ケース全部が OK と出たまま**である —— 汚染された状態でも、狙った番人はどちらにせよ
  落ちるからである。**スクリプトの答えは、この事故を1文字も語らなかった。**
  ★気づいたのはその後の最終 verify が5件落ちたときで、
  落ちた1件は**そのバッチが置いたばかりの番人**(「描き直しただけでは鳴らない」)だった。
  ★手当ては2つ ——(1) `write()` が書き戻せたことを読み返して確かめる
  (2) 全ケース終了後、対象ファイルが開始時の姿に戻っているかを突き合わせ、
  戻っていなければ **OK が何件出ていようと失敗とする**。
  ★★**「やった」と「戻した」は別の主張である。**状態を書き換える道具は、
  戻せたことまでを含めて自分で答えなければならない。
  ★これは下の 60 の教訓(結果ファイルが今回のものか)の**兄弟**である。片方だけでは足りない。
- **★★★壊し検証は「結果ファイルが今回のものか」を測っていなかった**(★60)。
  改変した Java がコンパイルできないと surefire は走らず、**前回の XML が残る** ——
  それを読んで「壊したのに落ちなかった」= NG と判定していた。
  ★60 で `run_class` が走らせる前に対象クラスの XML を消すようにした。
  **59 までの版にも同じ穴がある**(踏んでいなかっただけ)。

### ★★回数制限・封じ・観測できない軸(Batch 59)

- **★★★「ターンに n 回まで」の器は1本である**(`PlayerState.tryUseTurnLimited(key, turn, max)`。★59)。
  Batch 58 が《地脈の覚醒》専用に作った `tryUseLeylineAwakening(int)` は置き換えた ——
  マスターが**裁定264(冥魔剣=5回)と裁定276(地脈の覚醒=1回)を「同じ規則の2つの現れ方」として
  揃えて裁定した**ためである(裁定163)。次に回数制限のカードが出たら、鍵と回数を渡すだけでよい。
  ★**判定と記録は1つのメソッドの中**(58 からの継続)。★**ターン番号の刻印は1つでよい** ——
  ターン番号は両プレイヤーで共有された1本の数であり、制限の鍵ごとに違わない。
- **★★★「何も起こさなかったカード」は【還元】もしない**(裁定276)。
  58 は「【還元】はキーワードであって効果ではない」と読んでマナへ置いていたが、
  マスターがこれを上書きした。行き先は `SpellDisposition.TO_TRASH` で表す ——
  他の2値と違い、**行き先を別の場所へ置き換えるのではなく、通常の行き先を置き換えさせない**指示である。
- **★★★誘発の「種類」で分かれる規則は、判定を分岐の先頭に置く**(★59)。
  《悪夢》の【召喚時】封じは `CardEffectRegistry.fire()` の入口にある ——
  ON_SUMMON を焚く場所は通常召喚・進化召喚・特殊召喚・禁忌と複数あり、
  呼び出し側で止めると**足し忘れた経路だけが素通りする**(裁定163)。
  ★★**判定を【知識】の共通処理より「前」に置くこと。**後ろに置くと、
  【知識】が「順序のおかげで」守られる形になり、**壊し検証でその軸を狙えなくなる**(59 で実際に踏んだ)。
- **★★「相手が引いたとき」のような相互に誘発しあう効果は、放っておくと止まらない**(★59)。
  両者が《英知の水晶》を出すと A→B→A→… と往復し続ける。
  ★**誘発によるドローは数えない**(`GameActions` の再入ガード)。
  **本文に書かれていない制限だが、書かれたとおりに実装するとゲームが停止しない**ため
  足さざるを得ない。★足したことは裁定として報告すること(裁定279 で伺っている)。
- **★★ダメージを「0にする」のと「与えない」のは別物である**(裁定272。《大天使 ミカエル》)。
  量を0にして先へ進めると、適用側の `amount <= 0` で止まる作りに**依存する**ことになる。
  「ダメージを受けたときの誘発が起きない」を保証したいなら、**適用そのものを行わない**こと。
- **★★★盤面に依存する要求数を、対象指定システムは表現できない**(★59。《神風の大号令》)。
  `Requirement` の選択数は**登録時に固定される値**である。
  「min(2, 場のミニオン数)体ちょうど」は書けないので `upTo` で受けるしかなく、
  **プレイヤーが少なく選べてしまう**。★穴を残すなら、必ず設計解説と裁定依頼に書くこと。
- **★★★本物の入口から観測できない軸がある。**壊しても試験が落ちないとき、
  「試験が足りない」の前に「**そもそもその盤面が構造的に作れるか**」を疑う。
  ★59 で《悪夢》の封じの範囲(自分だけか両者か)がこれだった ——
  召喚できるのはターンプレイヤーだけで、印(`thisTurnAuras`)はターン終了時に消えるため、
  「相手が印を持ったまま自分が召喚する」盤面が存在しない。
  ★同じ立場のものが3件ある(《英霊・コレキ》/《風弾の跳弾》の「そうしたら」/ 本件)。
  **観測できる別の軸(《悪夢》なら持続)に試験を寄せ、観測できない軸は理由を明記して外す。**
- **★★「登場時」の効果は、経路によって数え方が1違うことがある**(★59。《ガイル・フォックス》)。
  使用カウンタは解決中にはまだ自身を数えない(裁定1)ので、
  **自身の召喚で登場した場合**と**効果で場に出た場合**では閾値が1ずれる。
  ★ON_SUMMON と ON_ENTER の**両方に登録し、閾値だけを変える**のが素直である
  (付与が冪等なら、通常召喚で両方通っても害が無い)。

### ★★常在の体力修正・実体を作る入口(Batch 58)

- **★★★【速攻】は `Keyword.HASTE`、【突進】は `Keyword.RUSH` である。**別のキーワードであり、
  日本語からは取り違えやすい。★58 で実際に踏んだ(《剛火の将》の常在が効かず試験が落ちた)。
  `tools/batch58_break_check.py` のケース3 がこの取り違えを番人として持っている。
- **★★★場に出るミニオンの実体を作る入口は `GameActions.newFieldMinion` 1本である**(★58)。
  召喚(`GameService.summonToField`)と効果による登場(`GameActions.putIntoFieldByEffect`)の
  両方がここを通る。★**`new MinionInstance(...)` を新しく直書きしないこと** ——
  《剛火の将》の常在(【速攻】持ちのHP+2)の加算量が写されず、
  <b>片方の経路から出たミニオンだけ</b>が常在を失う。
- **★★常在の値のうち「対戦中に変わらないもの」は場に出るときに1度だけ写す**
  (`MinionInstance.rushHpBonus`。`statPerUnderCard` と同じ流儀)。
  ★**動くもの(【速攻】を持つか)は読むたびに見る。**両方を写すと、
  効果で後から【速攻】を得たミニオンに常在が乗らなくなる。
- **★`MinionInstance.getMaxHp` の「盤面参照型が出たら StatCalculator へ移す」は今も有効である。**
  《剛火の将》は<b>参照が2つに割れていた</b>(動かないリーダー + 動くキーワード)ので移さずに済んだ。
  手札枚数のように読むたびに動く体力修正が出たら、そのときは本当に移すこと。
- **★★試験の足場も同じ値を写す**(`AutoGameFixture.putOnField`)。
  写さないと「召喚したときだけ効いて、最初から置いたミニオンには効かない」という
  <b>盤面には存在しない状態</b>を試験が作る。
- **★★「ターンに1回」の刻印がまた1つ増えた**(`PlayerState.tryUseLeylineAwakening`。★58)。
  ★**判定と記録は1つのメソッドにすること** —— 分けると「判定したが記録し忘れる」経路が生まれる。
  《地脈の覚醒》は同じターンに2枚目を使えるので実際に踏める。
- **★★【還元】でマナへ置かれるのは効果の解決より後である**(`GameService.playSpell` の順序)。
  したがって《地脈の覚醒》の「自分のマナから1枚」の候補に<b>自分自身は入らない</b>。
  ★順序を入れ替えると、唱えたばかりのスペルを自分で拾い直せるようになる。
- **★キーワードだけになったカードは「登録を消すのが実装」である**(《背水の烈火使い》)。
  `CardTextKeywords.hasEffectSentence` がキーワードだけの本文を効果なしと判定するので、
  `report_effects.py` の未実装にも計上されない。
- **★「変えていないことを測る試験」でも盤面は読まないと書けない**(《知恵の双翼》。★58)。
  【知識】は「登場時に1枚ドロー」のキーワードアクションなので、特殊召喚でも手札が1枚増える。
  これを勘定に入れずに書いた試験が落ちた。

### ★★進化エンジン(Batch 52)

- **★★★束は `MinionInstance` ではなく `StackedCard`(カードIDと出自だけ)である。**
  素材のタップ・ダメージ・攻撃回数を引き継がない(裁定224)ので、
  「場に居るミニオン」としての状態を1つも保つ必要がない。
  ★**この形が「束のカードの【破壊時】は発動しない」(裁定223・225)を無料で守っている** ——
  `CardEffectRegistry.fire` は `MinionInstance` を要求するので、束に対しては呼びようがない。
  ★**「実体で持つほうが自然だ」と直してはいけない** —— 直した瞬間、
  束の中のミニオンにダメージを与えたりトリガーを焚いたりする経路が構造として開く。
- **★★★進化召喚の入口は3つあり、着地は1箇所である。**
  `playCard`(通常)/ `specialSummon` / `playTabooCard` の3つが
  `GameService.summonToField(..., materials)` に合流する。
  素材を場から外して束にするのは<b>そこ1箇所</b>である。入口ごとに書くと必ずどれかが引き継ぎを忘れる。
  ★**特殊召喚でも禁忌でも素材は要る**(裁定226・228)。代替されているのはコストだけである。
- **★★素材を場から外すのは「場に出られる」ことが確定した後である。**
  《光霊・モアニール》の置換で場に出られなかった場合、**素材は場に残る**(裁定232)。
  順序を入れ替えると、進化もせず素材だけが消える。
- **★★★場を離れるときの同伴は4経路ある**(墓地・手札・マナ・山札)。
  入口は `GameActions.dispatchUnderCards` 1本だが、**山札だけは形が違う**
  (`underCardsForDeck` が戻すカードを集めて返す。《サイクロン・リフレッシュ》が唯一の使い手)。
  ★**新しく「ミニオンが場を離れる」処理を書くときは、束を運ぶかを必ず決めること。**
  `getMinionZone().remove(...)` を直書きすると束が消える。
- **★★マナへ置く経路の上限判定は「本体 + 束」の枚数で行う**(裁定225 の C2)。
  全部置けないなら1枚も動かさない。51 の「先に場から取り除くと行き先の無いカードが生まれる」の続きである。
- **★★★進化の素材条件は `isRegistered()` が数えない**(裁定233)。
  数えた瞬間、**効果が未実装の進化11枚から印が消える。**
  ★素材条件しか効果の文を持たない《海淵獣シラーカ》と、素材の数そのものが効果である
  《不敗鉄人闘太》だけが `CardEffectRegistry.IMPLEMENTED_CARDS` で名乗っている。
  ★`tools/report_effects.py` の「足し忘れ」検査も、素材条件だけに現れるIDを外してある。
- **★★★素材条件をクライアントに持たせない**(裁定234)。
  サーバが `CardView.evolutionMaterialIds` に候補の instanceId を絞り込んで載せ、
  `battle.js` は「その一覧に入っているか」しか見ない。
  ★**`TargetSpec.Filter` に素材条件を足してはいけない** —— 10個以上の `case` が
  `battle.js` の2箇所に増える(裁定195 が守る形になるが、そもそも両側に置かないほうがよい)。
  ★52 が足した `Filter` は `EVOLUTION_MINION` 1つだけで、これは《機神兵長茶爺》の
  <b>普通の対象指定</b>である(素材条件ではない)。
- **★闘太の【常在】は「値」ではなく「規則」を持たせてある**(`MinionInstance.statPerUnderCard`)。
  `StatModifier` として積むと、束が増えたときに更新し忘れるうえ、
  **進化を重ねたときに引き継がれてしまう**(裁定224 の (3) 違反)。
  ★**加算する場所は Attack と HP で違う** —— HP は `MinionInstance.getMaxHp`、
  Attack は `StatCalculator.effectiveAttack` である(Batch 12 からの非対称。52 は増やしていない)。
  ★`MinionInstance.getEffectiveAttack()` は**誰からも呼ばれていない**。束の加算もそこには無い。
- **★召喚酔いの免除は種別の規則である**(裁定157(1))。`RuleGuards` に
  `attacker.isEvolution()` として書いてあり、カード固有ではないので
  `IMPLEMENTED_CARDS` には現れない(名乗るべきカードが1枚も無い)。
- **★進化まわりでは「自分の」の省略を自明とみなす**(裁定221・231)。
  素材も《機神兵長茶爺》の対象も自分の場だけである。
  記法規約(裁定156(2)「自分の」と書いていない条件は両者を見る)の例外が2つできた。
- **★下が空の進化ミニオンは作れない**(`EvolutionSpec` が `minMaterials >= 1` を要求する)。
  作れるようになると《不敗鉄人闘太》が 0/0 で即死する。

### ★★【賢魂】の2つの姿(Batch 54)

- **★★★カードのテキストは【賢魂：n】を境に2つに割れる**(裁定248)。
  前半がミニオンの姿、後半がスペルの姿である。
  ★**割り方の規則は `CardTextKeywords` にしかない** —— `extract` は境目の手前まで、
  `soulKeywords` は後ろだけを読む。`CardEffectRegistry` も `battle.js` も
  <b>テキストを1文字も割らない</b>(裁定158・234)。
  ★**《白ノ霊知者》の【還元】が唯一の使い手である。**
  「本文全体を見るほうが自然だ」と直すと、ミニオンとして破壊されたときにマナへ行ってしまう。
- **★★★n をコードに書かない**(裁定248 の含意)。`SoulSpellSpec` は<b>効果と対象要求だけ</b>を持ち、
  n は `CardTextKeywords.soulCost` が唯一の出どころである。
  両方に書けば、カードデータを直したときにコードが古いままになる日が必ず来る。
  ★`SoulSpellTest.賢魂の登録とテキストは一致している` が食い違いを止める。
- **★★★賢魂の使用は「スペルの使用」である**(裁定247)。使用回数・`ON_CARD_USED`・
  スペル封じ・コスト軽減・`ON_NEXT_SPELL` のすべてが通常のスペルと同じ道具を通る。
  ★**コスト計算の規則を書き写さない** —— `StatCalculator.effectiveSoulCost` は
  基準コストと種別を差し替えて<b>同じ計算</b>へ流している(裁定163)。
- **★★入口を分けてある**(`play-soul` / `play-taboo-soul`)。
  どちらの姿で使うかは<b>プレイヤーの宣言</b>であって、カードの種別からは決まらない。
  ★**`playCard` に真偽値を足して合流させない** —— 冒頭の検証
  (場に出せるか・素材が足りるか)を賢魂のときだけ飛ばす分岐が中に増える(裁定207)。
- **★★使い終わったカードの行き先は4通りある**(裁定256)。
  墓地 /【還元】でマナ / 禁忌由来なら消滅 / **効果が決めきったなら動かさない**。
  ★4つ目が `SpellDisposition.KEPT_BY_EFFECT` である。他の2値が<b>置き換え先</b>を指すのに対し、
  これだけは<b>「もう動かすな」</b>という指示である。
  ★**効果が実際にカードを動かせなかったときは、この値を書かないこと** ——
  書かなければ通常どおり墓地へ行く(裁定252)。
- **★★禁忌由来の印は `EffectContext.fromTaboo` が運ぶ**(裁定250)。
  《スタンディングテント》の【賢魂：2】だけが使う ——
  <b>使用しているカード自身を場に出す</b>唯一の効果だからである。
  ★他の「効果で出す」は手札・墓地・マナ・山札から来るので、禁忌由来でありえない。
- **★★進化ミニオンに宣言時の対象指定(`TargetSpec`)を付けるのは危険である**(裁定258)。
  宣言時の検証は<b>素材を場から外す前</b>に走るので、
  `Kind.MINION` の対象に素材を選べてしまい、解決の頃には場に居ない。
  ★《英霊・タイガラム》は `Kind.HAND` なので安全である。
  《白ノ霊知者》の【召喚時】の破壊は割り込み(`PendingChoice`)にしてある。
- **★カードデータの表記ゆれは読む側で吸収する**(裁定257)。
  《グレイヴガールズファン》だけが全角の「１」である。**データを直さない** ——
  直すと手動モードとデッキメーカーの表示も一緒に動く。
- **★`IMPLEMENTED_CARDS` に《勝阿外》を入れていない。** `soulSpells` に載っており
  `isRegistered` が既に真を返すため、入れると<b>外して何も落ちない宣言</b>が2つ増える
  (53 のノアとまったく同じ判断。裁定176 の正しい読み方)。
- **★`report_effects.py` の `REGISTRY_MAPS` に `soulSpells` を足してある。**
  進化の素材条件(`evolutions`)と違い、賢魂は<b>そのカードの効果そのもの</b>なので数える
  (裁定233 との線引き)。★足し忘れると `NG: 未知の登録先がある` で止まる。
- **★裏向きでマナに置く経路が2本増えた**(`placeCardInManaFaceDown` /
  `placeTopOfDeckInManaFaceDown`)。★**`fireManaPlaced` は発火しない** ——
  51 以前からの非対称をそのまま踏襲した。**裁定を仰いでいない。P5 で拾うこと。**
- **★同じ語を含む2つの理由を、1つの試験で測らない**(★54 の壊し検証が実際に見つけた)。
  `requireSoul` は「そもそも【賢魂】を持たない」と「【賢魂】はあるが効果が未実装」の
  2段の検査を持つ。試験が `hasMessageContaining("【賢魂】")` だけを見ていたため、
  <b>1段目を丸ごと消しても2段目が拾って落ちなかった</b>。**文言まで測ること。**

### ★★進化の効果と登場の制限(Batch 53)

- **★★★「場に出られるか」を問うのは `GameActions.isFieldEntryBlocked` 1つである**(裁定246)。
  ★**`owner.isMinionZoneFull()` を呼び出し側に直書きしないこと。**
  《英霊・コレキ》により「出せない理由」が場の枚数以外にもできたので、
  満杯だけを見ると3種類の事故が起きる ——
  (a) 手札から抜いたカードが宙に浮いて<b>消える</b>(ギガマウス・バイト・神の福音)、
  (b) 墓地から消えないのに出られず<b>無限ループ</b>になる(黄泉還る水龍・ゾンストライカー)、
  (c) 先に取り除いたマナカードが<b>消える</b>(`putManaCardIntoField`)。
  ★<b>呼び出し側は理由を区別しない。</b>満杯でもコレキでも、やることは同じである。
- **★★★召喚の側だけは例外を投げる**(`GameService.requireCanEnterField`)。
  5つの入口(通常・進化・特殊召喚・禁忌・墓地からの召喚)が呼ぶ。
  <b>召喚は宣言そのものを弾き、効果は出せたぶんだけ出す</b>(裁定237(3))。
  ★この非対称は意図したものである。「揃える」直しをしないこと。
- **★★★束を作る処理は `GameActions.attachEvolutionMaterials` 1箇所である**(★53 で移した)。
  52 は `GameService.summonToField` の中に直接書いていたが、
  効果から進化を出す経路(《英術・スケアロック》)にも同じ束が要るようになった。
  ★**2箇所に書くと必ずどちらかが引き継ぎ(裁定224)を忘れる。**
- **★★★「場に出た後」の共通処理は `GameActions.fireEntryTriggers` 1箇所である**(★53)。
  登場の数え上げ + ON_ENTER + ON_ALLY_MINION_ENTER + fireAnyMinionEntered の4つ。
  召喚だけが持つのは<b>その直前の ON_SUMMON 1つ</b>である。
  ★**新しく「ミニオンが場に出る」処理を書くときは、必ずここを通すこと** ——
  通さないと《英霊・コレキ》の数がすり抜ける。
- **★登場の数え上げはターン番号の刻印である**(`PlayerState.minionEntryTurn` / `minionEntryCount`)。
  真偽値にできないのは、<b>相手のターン中の登場も数える</b>からである ——
  `startTurnReset()` では戻し切れない(`minionAttackLimitedOnTurn` と同じ理由)。
- **★★墓地からの【特殊召喚】は `SpecialSummonSpec.fromGrave` が唯一の宣言である**(裁定240)。
  `GameService.specialSummonFromGrave`(出す側)も
  `GameViewBuilder`(墓地の面に導線を出す側)も同じ値を読む。
  ★**手札からの特殊召喚と違うのは出どころだけ**で、条件・代替コスト・素材・着地は同じ道具である。
- **★★効果で出した進化は【召喚時】を発動しない**(裁定245)。登場時(ON_ENTER)のみである。
  裁定220(進化召喚は召喚である)は<b>進化召喚する場合</b>の話であり、衝突しない。
  ★素材が要ることは両方に共通する(裁定226)。
- **★★★「規則」はクライアントに置かない**(裁定234 の2度目)。
  《灰ノ霊呼者》の「【破壊時】を持つ」は本文の走査である(裁定242)。
  種別(`MINION_CARD`)や文明(`LIGHT_CIVILIZATION`)のような<b>属性の一致</b>は
  `TargetSpec.Filter` でよいが、<b>走査や計算を伴う判定</b>は候補を送る形にする。
  ★**53 は `Filter` を1つも足していない。**
- **★割り込みをまたいで文脈を運ぶときは `PlayerState` に置く**
  (`pendingEvolutionCardId`。`pendingSacrificeCount` と同じ形)。
  `PendingChoice` は候補の一覧しか持てない。
- **★カードは「場に出せる」ことが決まってから手札を離れる**(《英術・スケアロック》)。
  先に抜くと、素材が足りなくなったときに<b>どのゾーンにも居ないカード</b>が生まれる。
- **★★★`CardEffectRegistry` の表への登録は<b>カードIDのリテラル</b>で書く。**
  定数に置き換えると `tools/report_effects.py` が数え落とす
  (★53 で実際に「未実装7枚」が「8枚」とずれた)。定数化が要るのは<b>ルール側の判定点</b>である。
  ★あわせて、**そのツールの走査対象になる書式をコメントに例示しないこと**(裁定114)——
  53 で「未知の登録先がある」の誤検出を実際に起こした。
- **★`CardEffectRegistry.IMPLEMENTED_CARDS` は「表に1行も載らないカード」の一覧である。**
  表にも載っているカードを入れても、外して何も落ちない宣言が増えるだけである
  (★53 の壊し検証がそれを検出した。裁定176 の正しい読み方)。

### ★★火文明・土文明の Ver1.1(Batch 51)

- **★★★マナと場の行き来は入口が1本ずつしかない**
  (`GameActions.putManaCardIntoField` / `putFieldMinionIntoManaFaceDown`)。
  ★**`manaZone` や `minionZone` に直接 add / remove を書かないこと。**
  片方だけを直書きすると、ON_ENTER の発火・マナ上限の判定・禁忌の消滅・
  黄泉還る水龍の `manaLeft` のどれかが必ず抜ける。
- **★★`putManaCardIntoField` は「取り除く前に出せるかを確かめる」形である。**
  場が満杯・踏み倒し禁止・ミニオンでない、の3つを<b>先に</b>見る。
  順序を入れ替えると、マナからは消えたのに場にも山札にも居ないカードが生まれる。
  ★`manaLeft` の発火は<b>最後</b>である —— 先に発火すると黄泉還る水龍が場を埋めて
  本命が出せなくなる(`returnManaToHandAt` と同じ順序)。
- **★★`putFieldMinionIntoManaFaceDown` は破壊ではない。**【破壊時】は発動しない。
  禁忌由来のミニオンはマナに行かず消滅ゾーンへ送る(`bounceToHand` と同じ扱い)。
  ★マナ上限で置けないときは<b>場から動かさない</b>。
- **★★★攻撃時の割り込みは戦闘の解決を保留する**(裁定213。`GameState.pendingAttack`)。
  `GameService.attack` は「宣言 → 攻撃時効果 → <b>保留 or 戦闘</b>」で終わり、
  戦闘の中身は `resolveCombat` にある。選択が解決すると
  `resolveChoice` → `resumePendingAttack` が戦闘を再開する。
  ★**51 から地砕きの突撃兵も「マナを選んでから戦闘」に変わっている**(挙動の変更)。
  ★**新しく ON_ATTACK の効果を書くときは、割り込みを出すと戦闘が後ろへずれることを前提にすること。**
- **★★★`resolveChoice` はもうターンプレイヤーを要求しない**(裁定214)。
  選択待ちであること自体が根拠である。
  ★**対になる規則を消さないこと** —— `requireTurnPlayer` は
  <b>相手の</b> `pendingChoice` も見て手番の側を塞ぐ。
  これを外すと、相手が選んでいる最中に盤面が動いて候補の位置がずれる。
  ★**JavaScript は1行も変えていない。**`battle.js` の `renderPendingChoice` /
  `confirmChoice` は元から `myTurn` を見ていなかった。版数は3つとも据え置きである。
- **★「マナから場に出す」候補の絞り込みは `requestManaSummon` 1つに集約してある。**
  違うのは述語と再開先だけである。★候補が0体なら不発・1体なら自動決定・2体以上で問い合わせ。
- **★★向きの限定は本文どおりに写す**(裁定211)。
  《俺等地上覇夜露死苦》と《素手喧嘩》だけが「表向きの」と書いており、
  《勝鼓美》《喧嘩上等》は裏向きも取る。**「書き落としだろう」と補完しないこと。**
- **★裏向きでマナに置く経路は `fireManaPlaced` を発火しない**(51 より前からの非対称)。
  豊穣の地霊主の「マナにカードが置かれたとき」は<b>表向きの配置しか見ていない</b>。
  51 は既存の形をそのまま踏襲した(変えると風のマナ変換・禁忌の墓地利用・
  マナを貪る怨霊の挙動が変わる)。★裁定を仰いでいない。P5 で拾うこと。
- **★試験で `AutoGameFixture.giveMana` を使うと、支払い用のマナが
  「マナから出すミニオン」の候補に混ざる**(そよ風の使い魔はコスト1のミニオンである)。
  `FireEarthVer11EffectTest` は支払い用にスペルを置く `payMana` を自前で持っている。

### ★★水文明の Ver1.1(Batch 49)

- **★★★「場にミニオンが出た」は `TriggerType` ではなく発火口である**
  (`CardEffectRegistry.fireAnyMinionEntered`)。使い手2枚がどちらもリーダーの【常在】だから
  (裁定194)。★**「トリガー型に揃える」直しをしてはいけない** —— 登録する者が居ない。
- **★★★発火口は両者のリーダーを見る。**`ctx.owner()` は「場に出たミニオンの持ち主」であって
  「反応する側」ではない。**ドロー先は必ず watcher 側で指定する**(裁定156(2))。
- **★★発火は2箇所にある**(`GameService.summonToField` / `GameActions.putIntoFieldByEffect`)。
  **片方だけを直さないこと。**効果で出した場合も誘発する(裁定193)。
- **★★ロロイヨのカウンタは2本で独立である**(裁定156(1))。1本にまとめると、
  【守護】【潜伏】を両方持つミニオン1体で2枚引けなくなる。
  ★真偽値ではなく**ターン番号の刻印**である(相手のターン中にも消費されるため)。
- **★`putIntoFieldByEffect` は出せなければ `null` を返す**(場が満杯・出すことが禁じられている)。
  **場の末尾を取ってはいけない** —— `ON_ENTER` の中でさらに場が増えることがある。
- **★ギガマウス・バイトのコストは「手札の枚数」をそのまま引く**(自身を含む。裁定190)。
  `playSpell` が **検証 → `payCost` → `removePlayedAndTargets`** の順なので、
  コスト計算の時点で自身も選んだ3体もまだ手札にある。**選択の有無でコストは動かない。**
- **★「N体出す」は `Requirement.upTo`** である(裁定191。「あるだけ」を表す既存の形)。
  `filtered(..., count, false, ...)` にすると、候補が足りないときに使用できなくなる。
- **★アルキンティスの【常在】は保存しない。**評価するたびに場を見る(影潜む水刺客と同じ)。

### ★★風文明の Ver1.1(Batch 48)

- **★★★`ON_TURN_START` はターンプレイヤーの場だけで発火する**(裁定183)。
  `ON_TURN_END`(両者)と非対称である。**「両者に揃える」直しをしてはいけない。**
- **★★★裏を返すと、`ON_TURN_END` に登録した効果は既定で「1ターンに2回」効く**(★Batch 57)。
  《安らぎのガーディアン》の旧本文「ターンエンド時4回復」は、両者のターンの終わりに
  発火して**実質8回復**していた。Ver1.1 が「**自分の**ターンエンド時」と書き直したのは
  これを半分にするためである。本文が「自分の」と言っているターンエンド効果は、
  カード側で `ctx.state().turnPlayer() != ctx.owner()` を弾くこと ——
  `ON_TURN_END` が両者を回すこと自体は意図した設計であり、そちらを直してはならない。
- **★★★ターン開始の反復は `List.copyOf` である。**ハク霊の自壊が【破壊時】で
  コク霊を場に出すので、反復中に場が増える。**実体を回すと壊れる**。
- **★★「ターンに1回」で毎ターンリセットされるものは、真偽値ではなくターン番号を刻む**
  (`PlayerState.tryConsumeDestroyHeal`)。★**49 でこの形が3つ目・4つ目になった。**
- **★★両者を見る条件のカウンタは `GameState` に置く**(裁定185)。
  `GameState.minionsDestroyedThisTurn` は**行き先を問わず**数える。
  すぐ上の `PlayerState.minionsDestroyedThisTurn`(リスト)は蘇生候補のための記録で、
  **墓地に行ったものだけ**を持つ。**名前が似ているが別物である。**
- **★【召喚時】に「自分のミニオンを1体破壊」があるカードを `TargetSpec` にしない。**
  候補ゼロで**召喚そのものが弾かれる**。a9 の割り込み(`PendingChoice`)を使うこと。
- **★「破壊した数」は実際に破壊できた数である。**破壊の置換で場に残ったミニオンは数えない。
- **★墓地から「出す」は召喚ではない。**`ON_ENTER` のみが発動する。

### ★★カードの面の本文(Batch 48-hotfix)

- **★★★`battle.js` の `faceText` は「テキストに現れていないキーワードだけ」を畳む。**
  ビューの `keywords` は**印刷 + 効果で付与されたもの**である(`GameViewBuilder` が合成)。
  全部畳むと印刷ぶんが二重に出る(125枚が該当した)。
  全部やめると、そよ風の加護でもらった【守護】が面から消える。**どちらの極端も誤りである。**
- **★★通常モードだけが畳む処理を持っている。**デッキメーカーと手動モードは `text` をそのまま出す。
  **3つの画面で本文が食い違ったら、まず通常モードを疑う。**
- **★★verify 48-1 が番人。**本文は `manual-cards.json` を Node 側で読んで流し込む。
  **フィクスチャに本文を書き写さない**(裁定181)。

### ★★通常モードのゲームプレイ試験(Batch 48・49)

- **★★★足場は `src/test/java/com/example/qte/support/AutoGameFixture.java`。**
  盤面の初期状態だけを用意し、効果は `playCard` / `specialSummon` / `endTurn` /
  `nextPhase` / `resolveChoice` の**本物の入口**から起こす(裁定187)。
- **★トリガーを `CardEffectRegistry.fire` で直接叩かない。**
- **★★`endTurn` の後はマナチャージフェイズである。**そのまま `playCard` すると
  「この操作はメインフェイズでのみ行えます」で落ちる。**`game.nextPhase(room, playerId)` を挟む**
  (★49 で踏んだ。48 の試験は `endTurn` の後にカードを使わなかったので当たっていなかった)。
- **★ドロー数は「山札の減り」で測る。**手札で測ると、プレイしたカードが手札を離れるぶんと混ざる。
- **★既定のリーダーは常在効果を持たないものを使う**(風は `QTE-M-WIND-1`、水は `QTE-M-WATER-1`)。
- **★`fillDeck` を忘れると山札切れ敗北で試験が崩れる。**`beginTurn` は必ず1枚引く。

### ★★効果の実装状況・「効果未実装」の印(Batch 47)

- **★★★「実装済みか」を答えるのは `EffectImplementation` ただ1つである。**
- **★★★ルール側に効果を書いたら、そのクラスの `IMPLEMENTED_CARDS` に足す**(裁定176)。
  対象は `CardEffectRegistry` / `RuleGuards` / `StatCalculator` / `GameService` /
  `GameActions` / `PlayerState` の6つ。**足し忘れると、動くカードに「効果未実装」と書く。**
- **★★カードIDのリテラルはカード1枚につき1つだけにする**(裁定130・179)。
  ★**ただし `CardEffectRegistry` の表への登録はリテラルのままでよい** ——
  `report_effects.py` は `register("QTE-...")` / `xxx.put("QTE-...")` の**左辺の書式**で
  「登録あり」を数えるため、定数に置き換えると数え落とす。
  定数化が要るのは**ルール側の判定点**(`if` 文でIDを比べるクラス)である。
- **★★カードIDを書いてよいファイルは決まっている。**ルール側6クラス + `DeckFactory`。
- **★★`CardEffectRegistry` に新しい登録ヘルパを作ったら2箇所直す。**
  `isRegistered()` と `report_effects.py` の `helper` の一覧。
- **★印は部分実装を表せない**(裁定165)。
- **★★印の枚数(50)と効果文の枚数(215)はテストに書いてある**(裁定162)。
- **★★`report_effects.py` の判定は Java の写しである**。数字がずれたら**正である Java 側に合わせる**。

### ★★対象指定(TargetSpec)の落とし穴(Batch 49)

- **★★★`Filter` の値名はそのままクライアントへ送られる。**`battle.js` の
  **2箇所**(`checkFilter` 相当の分岐と、手札を光らせる分岐)に同じ `case` が要る(裁定195)。
  **片側だけでも例外は出ない** —— ただ選べなくなる。verify 49-1 が番人。
- **★`Requirement.upTo` は「あるだけ」を表す。**候補が足りないときに使用できなくなるのを避ける形。
- **★`Kind.HAND` で選ばれたカードは `removePlayedAndTargets` が手札から取り除く。**
  出せなかったぶんは効果側で手札に戻すこと(神の福音・ギガマウス・バイト)。
- **★選択の検証は状態を変える前に終わる。**弾かれたときに盤面が動いていないことが保証される。
- **★★★`usedMinionIds` は1枚のカードの検証中に全 `Requirement` で共有される**(★Batch 56)。
  `GameService.validateTargets` は「同じミニオンを重複して選べません」を **カード単位**で見る。
  したがって、**同じミニオンが2つの要求を同時に満たしうる効果**(ホーリー・シグナルの
  「最も攻撃力の高い」と「最も体力の低い」が同一体になる盤面など)を素直に2つの
  `Requirement` で書くと、**そのカードが使用不能になる**。例外が出るのは解決時ではなく
  検証時なので、試験を1体だけの盤面で書かないと気づけない。
  対処は、片方を `Requirement` として残し、もう片方を `AutoChoice` で効果解決時に
  自動決定することである(`AutoChoice.lowestCurrentHp` がその形)。
  **複数の対象を要求する新しいカードを作るときは必ずこの重なりを確認すること。**
- **★★リーダーの起動能力も `TargetSpec` を運べる**(★Batch 57 で2例目)。
  `GameService.useLeaderAbility` は `validateTargets` を通しており、
  `Kind.TRASH` の選択も既に動く(大地の巨神・冥府の禁皇)。新しい仕組みは要らない。
- **★★★「墓地からの召喚」(黄泉の召喚主)は対象の選択を運べない**(★Batch 57)。
  `GameService.summonFromGrave` は `TargetChoice` を受け取る口が無く、
  `summonToField` に `null` を渡している。**対象を読む【召喚時】がそこで発火すると
  NullPointerException で 500 になる。**闇のスターターは黄泉の召喚主と執念の暗殺者を
  同時に積むので実際に踏める穴だった。57 では理由付きの拒否に置き換えてあり、
  本来の形(手札と同じ選択を通すか)は裁定275 待ちである。

### ★★Ver1.1 移行(Batch 46 系)

- **★★★キーワードの正はテキスト。抽出は `CardTextKeywords` 1箇所**(裁定158)。
- **★★参照(`を持`…)と条件付き付与(`なら`…)はキーワードではない**(裁定159)。
- **★★台帳照合はまだ生きている**(裁定169)。読み口は `src/test/java/.../support/LedgerCards.java`。
- **★★台帳との差分9枚は `VER11_KEYWORD_CHANGES` に理由つきで持っている。**
- **★語彙は15種で固定**(`KNOWN_VOCABULARY`)。新しい `【】` が増えたらテストが落ちる。
- **★★対応表の出どころは `ledgerCardId` 1つ。** 別ファイルに保存しないこと。
- **★リーダーのコストは 0 である**(台帳は null だった)。
  ★**面には出ない** —— `cardFace` が `type !== 'LEADER'` で宝石を抑えている(49 で確認)。
- **★★手動モードへの影響はゼロに保つ**(`manual-cards.json` は読むだけ・書き換えない)。
- **★★カードIDの形を変えたら、IDを読むツールを全部通す**(裁定173)。
- **★★一括 sed をかけたら差分を1ファイルずつ読む**(裁定171)。

### ★★★通常モードの切断(Batch 71)

- **★★★番人は `send()` であって、オーバーレイではない**(33 の 1-2 を通常モードへ)。
  オーバーレイは**宣言**である。畳んで盤面を覗いている間もガードは効いている ——
  **「見えなくすること」を安全装置にすると、覗き見の導線を1つ足した瞬間に穴が開く。**
  ★通常モードの重ね順は 1000〜1500 の5層あり、「全部より上」を未来にわたって
  守り続けることを安全装置の根拠にはできない。
- **★★★1970 が最前面であることは、CSS を読んで決めない。**
  33 が 1970 を選んだのは**手動モードの重ね順**(1030〜1960)を見てのことである。
  通常モードは別の系統(`.info-modal` 1000 / ゲート 1060 / `#auto-playing` 1390 /
  `#auto-hover` 1400 / `#log-panel` 1450 / `#auto-zoom` 1500)であり、
  **端の盤面を描いて全要素の computed な z-index を数えて確かめた**(実測で 1500)。
  ★verify 71-3b が毎回数え直す(値を書かず「オーバーレイが最大であること」で測る)。
- **★★★同じ穴を塞ぐことと、同じ形で塞ぐことは別である。**
  手動モードの帯は**画面中央の固定ピル**だが、通常モードで同じ置き方をすると
  実測で **[進行: 手動](x:786)に余白0で接する**。33 がピルを選んだ理由は
  「全幅だとヘッダの部屋リンクのコピーまで隠れる」であり、**通常モードにその導線は無い** ——
  理由が違えば結論も違ってよい。
  → 通常モードの帯は**ヘッダ行の中の flex 要素**である(伸びしろ 285px・ヘッダの高さは 28px のまま)。
  ★★**写すのは判断の筋であって、値や置き場所ではない。**
- **★★★70 が増やした実害は「送れなかったのに畳む」である。**
  70 は確定待ち(`manaPay`)・進化素材(`evolution`)・対象選択(`pending`)という
  **ローカルにしか無い状態**を作った。69 までの書き方は全部「先に畳んでから送る」であり、
  切断中は **[確定]を押しても何も起きず、しかも選んだマナが消える**。
  → `send()` は**送ったかどうかを返し**、呼び出し側はそれを見る。
  ★手動モードの 33 も返り値を持っていたが、**使い手が1人も居なかった**。
- **★★先に畳んでから進み、失敗したら戻す(順序がある)。**
  「送れたときだけ畳む」と書くと落ちる —— `beginSelection` は途中で `render()` を呼ぶので、
  確定待ちを抱えたまま進むと**案内文が確定待ちのものに戻る**
  (「対象を選んでください」ではなく「払うマナを選んでください」が出る)。
  ★戻し方は `restoreManaPayment` 1本に置く(出口が4つあるため。裁定130)。
- **★★★「保つ」と「止まらせない」は両立させる。**
  対象選択だけは、全部埋まったまま保つと**もう一度撃つ入口がどこにも無い**
  (送信は「最後の対象を選んだ瞬間」に起きる)。
  → 送れなかったら**最後の1要求だけ巻き戻す**。プレイそのものは畳まない。
  ★状態を大事に抱えた結果、人が次に何もできなくなるなら、それは保っていない。
- **★★★通常モードにはゲートが2枚あり、扱いが違う。**
  席選択(`#seat-gate`)は切断の案内を**出さない**(まだ盤面に入っていない)。
  デッキ読み込み(`#deck-gate`)は**出す** —— **入室後**であり、読み込みの最後に
  `send('ready')` を撃つので、切断中はファイルを選んでも
  **「相手のデッキ待ち」の顔のまま黙って止まる**。
  ★**手動モードをそのまま写すと、こちらまで抑止してしまう**(壊し検証の軸11 が見張る)。
- **★★部屋消失(`connectionFatal`)の旗は作らなかった。**
  通常モードには `showRoomLostFatal` が無く(66 が作らなかった)、**旗を立てる人が居ない**。
  作れば `.mana-chip` と同じ「書いてあるのに効いていない器」になる(裁定178)。
  ★★**ただし穴は残る** —— サーバが再起動すると STOMP は再接続し続け、
  購読しても盤面が二度と来ない。人には「再接続中」のまま永遠に見える。
- **★★音はガードの後ろに置く。**62 は `send()` を音の取り付け点にした(裁定287)。
  ガードがその手前に入っていないと、**送っていない操作で手応えだけが返り「届いた」と誤解させる**。
- **★★★出口が複数ある関数は、出口ごとに番人を当てる。**
  `confirmManaPayment` には出口が4つあり、最初は**プレイの出口だけ**を測っていた ——
  マナチャージの出口を壊しても**誰も赤くしなかった**。
  ★これは「壊しても落ちない」の新しい形ではなく、**番人が壊した枝を通っていない**という
  65 の形である(あちらは「その分岐に入る盤面が作れない」、こちらは「作れるのに作っていない」)。
- **★★★前の項目が残した状態を、次の項目が自分の成果と取り違える(偽の緑)。**
  「席選択ゲートのときは案内を出さない」の検査は、直前の項目が `offlinePeeking` を
  立てたままだったため、**ゲートのせいではなく覗き見のせいで**隠れていた ——
  **ゲートの判定を消しても緑のまま通る**状態だった。
  ★気づけたのは、次の項目が同じ理由で落ちたからである。
  ★★68 の「番人そのものの書き方が誤っている」の新しい顔であり、
  69 の「検査が何の上に居るかを決めていない」の兄弟でもある。
- **★★実測は捨ててよいが、実測から出た不変量は番人に昇格させる。**
  事前実測のスクリプト(`verify/measure71.js`)は納品に含めていない ——
  測った値がすべて verify とコメントへ移ったからである。
  ★残すと、実装が動いたときに**それだけが古い前提のまま残る**(67 の教訓・写し)。
- **★揮発の送信が無いので `{ quiet: true }` の逃がし口を作っていない。**
  通常モードの送信13箇所はすべて盤面を動かす操作である。
  ★これは「まずは付けないでおく」ではなく(69 の教訓・途中)、**今そういう送信が無い**という事実である。
  揮発の送信を足す日が来たら、そのときに逃がし口も足すこと(`send` の Javadoc に書いてある)。

### ★★★手札からの操作を2つの入口にする(Batch 70)

- **★★★書いてあるのに効いていない CSS は、クラスの数を数える検証では見つからない。**
  マナの選択と禁忌の支払いの光り(`.mana-chip.mana-selectable` / `.mana-chip.taboo-payable`)は
  43 から在ったが、**44 で通常モードのマナが `.mana-tile` へ差し替わった時点で
  どのタイルにも当たらなくなっていた** —— クラスは付くのに、光らない。
  ★verify は `querySelectorAll('… .mana-tile.taboo-payable').length` を数えていたので、
  **1度も赤くならなかった**(設計判断45 の実例)。
  ★★**色・見た目は「同じ盤面の中で2つを比べる」形で実測すること**(裁定41)。
  70 では「支払いに入る前の色」と「入ったあとの色」が違うことで測るようにした。

- **★★★verify はフィクスチャで描く。Java のビューを壊しても verify には届かない。**
  ハーネスはサーバを起こさず、`verify/fixture.js` が組んだビューを `render()` に直接渡す。
  ★70 は最初「ビューが支払いの順を載せること」を verify でも測るつもりで書いたが、
  `GameViewBuilder` を壊しても**落ちなかった** —— **「2層に見えて1層」だったのである。**
  ★**サーバ側の値は JUnit にしか照合先が無い。**壊し検証で確かめること。

- **★★★壊し検証は、殺されると壊したまま残る。**
  70 の着手時、実行が上限時間で落とされ、**壊した1件がそのまま残った**。
  次の走行はそれを「開始時の姿」と思い込むので、**誰も赤くしない**。
  ★`finally` は SIGTERM では走らない。合図を捕まえて例外に変えること
  (`tools/batch70_break_check.py` の `main` を参照)。
  ★★**長い壊し検証は前後で `git status` / 差分を見ること。**

- **★★ドラッグ中に `render()` を呼んではいけない。**
  `render()` は `#my-hand` を作り直すので、**掴んでいる要素が DOM から消え**、
  ブラウザがドラッグを中断する。手動モードの 19b hotfix2 と同じ性質の事故である。
  ★ドラッグ中の見た目はクラスの付け外しだけで更新する。

- **★★落ちた場所は `e.target` では決まらない**(20a の A4。通常モードでも同じ)。
  `dragstart` の `e.target` はブラウザが祖先方向へ探して見つけた `draggable=true` の要素である。
  「どのカードの上に落ちたか」は `document.elementFromPoint(e.clientX, e.clientY)` で取る。

- **★★flex アイテムの幅を 0 にしても、中身が押し広げる。**
  `flex: 0 0 0px` にしても `min-width: auto`(flex の既定)で文字の幅が残る ——
  壊し検証で「枠を潰す」つもりの改変が効かなかった。器を消すなら `display: none` である。

- **★★「確認」と「どちらの姿で使うか」は別物である**(裁定321)。
  裁定321 が挟むなと言っているのは**誤操作を防ぐ確認**であって、
  【賢魂】か召喚か・特殊召喚か通常か、という**宣言**は入口を問わず要る。
  ★【賢魂】は落とし先で決まる(裁定318)ので、ドラッグでは問いそのものが消える。

- **★★「選ぶ余地が無いから自動でよい」は、層を選ぶ流儀である**(裁定319)。
  68 の「解決中の選択は、選ぶ余地が無ければ自動で決める」は**効果の解決の層**の話であり、
  **プレイを始めてよいかという層には効かない** ——
  誤操作の防止は「選ぶ余地の有無」とは別の軸である。

- **★★69 が書き残した心配のほうが外れていることがある。**
  `batch69-ui-feedback.md` の A-3 は「ホバーの面がパイルを覆う」ことを心配していたが、
  実測すると面は x:748〜980、右列は x:988 からで**重ならなかった**。
  実際に重なるのは**ゾーン一覧のモーダル**(x:383〜897)だけである。
  ★**測れることについては、書き残した心配も疑うこと。**

### ★★★通常モードの盤面と「途中で止まった実装」(Batch 69)

- **★★★「まずは〜から」と書いた実装は、そこで止まったまま動かない。**
  44 は `attachHover` の器を作りきったうえで「**まずはリーダーから**」と書き、
  呼び出しを2箇所で止めた。65 が「場のミニオンと手札にホバーが無い」と挙げたのはこれである。
  ★64 は時間、65 は空間、66 は予定、67 は写し、68 は代用、**69 は「途中」**である。
  ★★**時間・空間・予定・写し・代用は「離れていく」形だが、途中は動かない** ——
  そのバッチとしては正しく完了しているので、**試験は緑のまま誰も赤くしない**。
- **★★★「器が無い」と思ったら、まず在るかどうかを見る**(65 の教訓の、器についての形)。
  69 が足したのは `attachHover(el, minionFace)` と `attachHover(el, handFace)` の**2行**である。
  ★同じ罠は 67 でも踏んだ(`MinionView` に文明を足そうとしたら、45 が
  「card-library から引く」と注釈に書いていた)。
- **★★★横を詰めると縦が余る。**45 は右列を 400 → 280 に**横に**縮めた。
  余ったのは**縦**である —— 実測で `#auto-side-mid` に **225px** が空いていた。
  ★★**詰めても中央列は広がらない**(中央列は既に高さいっぱいの flex である)。
  だから埋めるなら**情報を置くしかない**。69 はフェイズの進行表を置いた。
- **★★★色の値を新しく決める前に、既に在る系統を探す。**
  「自陣と敵陣が見分けられない」は**半分だけ在った** ——
  `.opponent-side`(赤い上線)と `.my-side`(青い下線)が Batch 8 以前から在り、
  **色の系統はもう決まっていた**。足りなかったのは線ではなく、
  **その系統がミニオン行に当たっていないこと**である。
  ★CSS には「他のルールの色を参照する」機構が無いので、値は2箇所に置くしかない ——
  **一致は verify が実測どうしの突き合わせで見張る**(裁定130・65 の流儀)。
- **★★★ホバープレビューは対象を選んでいる最中に出してはいけない。**
  `.auto-hover` は `right:300px / top:64px` の**固定位置**に出るので、盤面の候補に重なる。
  68 で盤面から対象を選ぶ場面が15枚ぶん増えた以上、**見えないものは押せない**。
  ★判定は `hoverBlocked()` 1箇所。**「選んでいる最中」は5つある** ——
  `pending` / `evolution` / `tabooPay` / マリガン / ★★**`hasPendingChoice()`(割り込み)**。
  ★★★**5つ目を書き忘れた。**68 が【召喚時】の対象を移した先が割り込みなのだから、
  **いちばん起きるのはそこ**なのに、最初の実装は宣言時の `pending` しか見ていなかった。
- **★★判定を置く場所は2つ要る。**「これから出す」を止めるのは `attachHover` の入口と
  **350ms のタイマーの中**(待っているあいだに問い合わせが来る)、
  「もう出ている」を消すのは `render()` の入口である。片方だけでは足りない。
- **★★★同じ規則を8箇所に書かない。**パイルの枚数バッジの書き込みは
  44 以来8箇所に散っていた(両席 × 山札・墓地・消滅・禁忌)。
  「0枚なら暗くする」を足すにあたり `setPileCount` **1本に通した**(裁定130)。
  ★直書きに戻すと**そのパイルだけ** 0枚でも金色のままになる ——
  番人は `Batch69BoardTest#パイルの枚数バッジの書き込みは一本を通る`。
- **★★1つの印に2つの意味を持たせない。**山札0枚だけ光らせたくなるが、
  そこだけ例外を作ると「**0枚は暗い**」という読み方そのものが壊れる。
- **★★★`.auto-side-mid` は溢れる。**問い合わせが長いと(候補6件で実測 453px)、
  294px の枠に収まらず**ログのバーごと画面の外へ出る** ——
  親のどこにも `overflow` が無いので、**切られるのでも巻けるのでもなく、ただ見えなくなる**。
  ★69 で `overflow-y: auto` を足した。**`hidden` にしてはいけない** ——
  切ると「押せない確定ボタン」が生まれる(問い合わせは操作であって飾りではない)。
  ★**「巻ける状態に人が気づくか」は機械には測れない。**実機確認の依頼として残してある。
- **★★★`battle.css` の版数は5枚とも上げる**(`battle.html` / `cards.html` /
  `manual-cards.html` / `manual-battle.html` / `manual-deck-maker.html`)。
  版数はファイルに付くものであって、ページに付くものではない ——
  1枚だけ上げると、**古い版数で要求したページだけがキャッシュの古い CSS を受け取り続ける**。
- **★★★実マウスの検査は「何の上に居るか」まで決めておく。**
  69 の verify を初めて通したとき、手札のホバーだけが出なかった。原因は実装ではなく、
  **43-3(禁忌の帯の開閉)を試したあとの状態が残っていた**ことである ——
  `.auto-taboo-strip` は `position: absolute` で**手札の上に重なる**。
  ★先に畳むだけでなく、**畳まれていること自体を検査の条件に入れる**。
  条件にしないと、次に誰かが状態を残したときに**また同じ原因で落ちて、実装を疑う**。
- **★★clone の「新しさ」はコミットメッセージでも日付でも測れない。**
  69 の着手時、`git log -1` は日付が新しく(2026-08-27)、
  **メッセージだけが `batch67` のまま**で、中身は 68 が入っていた。
  ★確かめるべきは、**そのバッチが作ったはずのファイルが在るか / 消したはずのものが無いか /
  `mvn test` の件数がそのバッチの値か**である。

### ★★★【召喚時】の対象と、手札からの登場(Batch 68)

- **★★★ミニオンの対象は「場に出てから」選ぶ**(裁定282)。
  宣言時に対象を選ばせてよいのは**スペル・ウェポン・リーダーの起動能力・【賢魂】・
  特殊召喚の代替コスト**だけである —— どれも「場に出る」ことと結びついていない。
  ★境界は `CardEffectRegistry.declarationTargetSpecOf` **1メソッド**に閉じ込めてある。
  呼び出し側(通常召喚・進化召喚・禁忌・墓地からの召喚・ビュー)は自分で判断しない。
  ★番人: `Batch68SummonTargetTest#ミニオンは宣言時の対象要求を1件も持たない`。
- **★★★対象を要求するミニオンの要求は「1件」でなければならない。**
  1件の `PendingChoice` は1つの要求しか運べない ——
  2件目を足すと、割り込みは1件目しか問わず**2件目が黙って消える**。
  2つ問いたいカードは本文の順序どおりに**別の再開先**を持たせる
  (《生贄を求める邪鬼》→ `SACRIFICE_FIEND_OPPONENT`、
  《手札を喰らう大蟹》→ `CRAB_BOUNCE`)。
- **★★★`targetSpecs` はカードに1本しか無い。だから誘発の種別で絞る。**
  そこに書いてあるのは【召喚時】【登場時】の対象である。
  種別を見ずに「まだ対象を選んでいない」と判定すると、
  **同じカードの【破壊時】まで問い合わせに化ける**。
  ★68 の実装で実際に踏んだ —— 《サモンズライト》が破壊されたとき、
  蘇生の代わりにダメージ対象を問うようになり、**ノアと無関係な試験2件**が落ちた。
  症状の出る場所は穴の在り処から離れる。
- **★★対象を要求するミニオンは【召喚時】と【登場時】を同時に持ってはいけない。**
  `targetSpecs` が1本しか無いので、両方が焚かれると**同じ対象を2度問う**。
- **★★★裁定282 が「構造ごと消した」3つの穴。**
  裁定258 の罠(素材にしたミニオンを対象に選べる)・裁定282 の門(墓地から出すカード自身が
  候補に混じる)・48 の落とし穴(候補ゼロで召喚が弾かれる)——
  ★**守るコードを消したのではなく、穴のある状態を作れなくなった。**
  ★それでも「候補に現れない」を測る試験は残してある。
  消えたのが**順序に依存した事実**である以上、順序が戻れば穴も戻る。
- **★★★「手札から」効果で出たなら【召喚時】は発動する**(裁定311)。
  切り直したのは**経路ではなく出どころ**である。墓地・マナ・山札からは【登場時】だけ。
  ★`GameActions.putIntoFieldByEffect` の `FieldEntryOrigin` で運ぶ。
  **渡し忘れると【召喚時】が黙って発動しなくなる** ——
  しかも症状は「手札から出す効果」と「【召喚時】持ち」を**組み合わせたときだけ**出る。
  ★入口を分けなかったのは、あの入口が既に「`null` を返す理由が2つある」難所だからである
  (場が満杯 / モアニールの置換。50 の落とし穴)。
- **★★★《スタンディングテント》は 235枚でただ1つの例外である。**
  本文の「そのミニオンの【召喚時】は使えない」は、67 までは既定(裁定245)の**言い換え**だった。
  311 で既定が反転したので、**本物の例外**になった(総合ルール2-7)。
  ★この1枚だけ `FieldEntryOrigin.OTHER` のままである。
  番人を置いていないと、次の人が「渡し忘れ」だと思って直してしまう。
- **★★★誘発の種類を、別の性質の代用として使ってはいけない。**
  59 の《ガイル・フォックス》は「このターン使用した枚数」の閾値を
  **ON_SUMMON なら 2・ON_ENTER なら 3** と分けていた。
  これは「効果で出たなら【召喚時】は発動しない」が成り立つ間だけ正しい書き方であり、
  裁定311 で**黙って1枚甘くなった**。
  ★直し方は「自身が使用されているか」を直接読むこと(`MinionInstance.enteredByEffect`)。
  ★**代用は、代用元の規則が変わった瞬間に黙って別物になる。**
- **★★割り込みを跨ぐと「このターン使用したカードの枚数」は1枚増えている。**
  効果の解決が使用の後始末(`afterCardUsed`)の**後**に走るためである。
  ★64 から同じ形だが、68 で15枚がこの形に入った。試験の設定値がずれる
  (「使用2枚」を作るには `setCardsUsedThisTurn(1)` と書く)。裁定313 で確認を依頼中。
- **★★「素材を確保できるか」は `TargetSpec.Filter` に書けない**(裁定234)。
  Filter はクライアントへ送る値であり、盤面に依存する規則を入れると
  **クライアントが規則を持つ**ことになる。→ 割り込みへ移す。
  ★53 が《英術・スケアロック》で、68 が《聖なる降誕の儀式》《神の福音》で同じ判断をした。
- **★★使用条件と候補は「同じ関数」を呼ばせる**(裁定130)。
  67 は「両方を直す」で済ませたが、2箇所に同じ規則が書かれている限り
  片方だけ直る事故は起きる。68 は `putFromHandCandidates` 1本にした。
- **★`payload` を複数の値で畳むなら `split("\\|", -1)` の `-1` を忘れない。**
  既定の `split` は末尾の空要素を落とすので、`A||0` が3要素になって添字が壊れる。
- **★★割り込みの描画は `Kind` を数えていない**(64 の設計・68 で性質として固定)。
  `CONFIRM` の1分岐だけが特別で、あとは候補のラベルを並べるだけである。
  ★だから `Kind` に `WEAPON` を足しても JS は無変更で描ける。
  **kind ごとの分岐を書き足すと、次の Kind が空欄になる。**
  verify に架空の Kind を流し込む検査を置いてある。
- **★ウェポンの候補は "SELF" / "OPPONENT" という「側の名前」である。**
  ウェポンは1人1つなので instanceId を持たない。
  ★候補ラベルには**必ず側を添える** —— 同名を両者が装備していることは普通に起きる。
- **★★verify.js では「描画だけを見る検査」を「実際に押す検査」より前に置く。**
  候補が1つも描かれないと `locator.click()` が待ち続け、**検証全体が止まる** ——
  後ろの検査は走らないまま終わる。
  ★68 の壊し検証がこれを EMPTY として見つけた(番人の穴ではなく**番人の書き方**の誤り)。

### ★★★割り込み選択(Batch 64)

- **★★★割り込みは「中断」ではなく「後回し」である**(裁定301)。
  `GameActions.requestChoice` は選択を積んで**そのまま戻る**。呼び出し元の残りは走り続け、
  答えで動く部分だけが後から `resolveChoice` で走る。
  ★59〜63 の設計解説が書いていた「ドローはあらゆる場所から呼ばれるので中断点を作れない」は
  **誤りだった** —— 中断しないのだから、深いところから問い合わせても困らない。
  ★**代償**: 「してもよい」の結果は元の効果の残りより**後**に起きる。
- **★★1人は複数の問い合わせを同時に抱えうる**(裁定300)。`PlayerState` は待ち行列である。
  `getPendingChoice()` は**先頭を覗くだけ**で、取り出すのは `resolveChoice` が
  `pollPendingChoice()` で行う。★積む口は `requestChoice` 1つ、取り出す口は 1つ。
  ★上限(`MAX_PENDING_CHOICES`)は構築ルールではなく**誘発の暴走への防波堤**である。
- **★★★候補が「ゾーン内の位置」なら、待っている間に位置がずれる。**
  63 まではずれなかった(選択待ちの間は誰も盤面を動かせない)が、
  キューになると**手前の選択の解決**が同じゾーンを動かす。
  ★対策は `requestChoice` が積む瞬間に控え(`expectedCardIds`)を取り、
  `resolveChoice` が解決の直前に照合すること。**1件でもずれたら何も起こさない。**
  ★控えるのも読むのも `GameActions.cardIdAtZonePosition` の**1本**である(裁定110)。
  ★`PendingChoice` の 12a の javadoc「位置の解釈がずれないようにするため」は
  **半分しか正しくない**(送らせないのは盤面の識別子であって、位置はずれる)。64 で訂正した。
- **★はい/いいえは「候補1件・min=0・max=1」の選択として表す**(`Kind.CONFIRM`)。
  選べば「はい」、選ばなければ「いいえ」。**送受信の形は1つも増えていない。**
  ★「いいえ」を2つ目の候補にしてはいけない ——
  「何も選ばない」と意味が重なり、`min`/`max` の検証が正を決められなくなる。
- **★候補の一覧に表せない文脈は `PendingChoice.payload` が運ぶ。**
  53 は `PlayerState.pendingEvolutionCardId` というカード専用のフィールドで凌いでいた。
  ★`pendingSacrificeCount` は**寄せていない** —— あれは割り込みをまたぐ値ではなく、
  使用宣言から解決までの間だけ生きる値である(性質が違う)。
- **★成立しない選択肢は並べない**(裁定302)。山札が空なら「引きますか?」と問わない。
- **★候補が「選ぶ余地の無い数」なら問い合わせない。**
  0なら不発・1(または上限以下)なら自動で全部 —— 12b・51 から変えていない流儀である。
- **★★★制約に合わせて曲げた実装は、制約が消えても自分では戻らない。**
  《サモンズライト》は「`resolveChoice` はターンプレイヤーしか受け付けない」を理由に
  50 で自動決定にされ、51(裁定214)でその制限が外れた後も**13バッチ**そのままだった。
  コメントだけが古い理由を言い続けていた。
- **★★《英知の水晶》は相手のドローのたびに問う**(裁定301)。相手のターン開始ドローでも
  必ず1回問うので、答えるまで盤面が止まる。これは仕様である。
  ★そのドローは「相手が引いたとき」を**焚かない**(`drawCardsWithoutOpponentWatchers`)。
  裁定279 の表し方が「瞬間(再入フラグ)」から「**そのドローそのもの**」へ変わった。

### ★★★カードの本文と実装の食い違い(Batch 64・★67 で全面的に追記)

- **★★★`report_effects.py` の「未実装0枚」は「本文どおり」を意味しない**(裁定303)。
  あのツールが見るのは「登録が**在るか**」であって「登録が**本文どおりか**」ではない。
- **★実例**: 《不滅のネクロマンサー》(QTE-M-DARK-5)は Ver1.1 で本文が丸ごと差し替わって
  いたのに(蘇生 → 「出た時相手はカードを1枚引く」)、実装は Ver0.4 のままだった。
  `notes/rework-triage.md` に行が無く、**P5(55〜59)の作り直しから抜け落ちていた。**
  63 まで **Ver1.1 に存在しない効果が動いていた。**
- **★同じ抜けが他にもありうる。** Ver0.4 由来169枚のうち、
  作り直しの対象外とされた分は**誰も本文と実装を突き合わせていない**。
- **★ヘルパ名を書いた表が別の場所にあると、名前を変えた日に黙って数え落とす**(47 の教訓の再来)。
  `tools/report_effects.py` の `for helper in [...]` がそれである。
  登録のヘルパを増やす・名前を変えるときは、必ずここも直すこと。

- **★★★67 で7枚見つかった。**Ver0.4 由来169枚のうち、P5(55〜59)の作り直しの
  <b>対象外</b>とされた48枚を1枚ずつ突き合わせた結果である
  (母集団の出し方は `notes/batch67-design-notes.md` 1章)。

  | カード | Ver1.1 の本文 | 66 までの実装 |
  |---|---|---|
  | 《大地震》EARTH-11 | コスト**4**以下を破壊 | コスト**3**以下 |
  | 《聖剣 エクスカリバー》LIGHT-14 | 体力を**全て回復** | **2回復** |
  | 《生贄を求める邪鬼》DARK-3 | 自分2体+**相手1体**を破壊 | 自分1体・自壊。**相手への破壊が無い** |
  | 《禁忌の墓地利用》DARK-25 | **ミニオンでないカード** | **スペル**限定 |
  | 《ツイン・ストライク》WIND-11 | 自分の**風文明**ミニオン | 文明を見ていない |
  | 《聖なる降誕の儀式》LIGHT-11 | 【守護】を持つ**ミニオン** | 種別を見ておらず**進化が素材なしで出た** |
  | 《神の福音》LIGHT-12 | 光文明の【守護】**ミニオン** | 同上 |

- **★★★コメントが Ver0.4 の本文を書き写している。**7枚すべてがそうだった ——
  `// 大地震(0148): お互いのコスト<b>3</b>以下のミニオンをすべて破壊` のように、
  <b>台帳番号まで残っている</b>。46b の移行がその行を1文字も動かさなかった証拠である。
  > **コメントは、実装が古いことを教えてくれる最良の手がかりであり、
  > 同時に、機械が最も信用してはいけない場所である。**
  ★だから `tools/check_card_text_numbers.py` は<b>コメントの数字を数えない</b>。

- **★★★7枚を直しても、既存の JUnit は1件も落ちなかった**(★67)。
  「落ちなかった = 変えていない」ではなく、<b>そもそも測られていなかった</b>のである
  (`notes/rework-triage.md` 5章が P5 のときに書いた注意そのままの形)。

- **★★「未実装0枚」の隣に2本置いた**(★67)。どちらか1本では足りない。
  - `src/test/resources/text-impl-review.json`(**突き合わせ台帳**。番人は `CardTextReviewTest`)
    …… 突き合わせた時点の本文のハッシュを覚え、**本文が変わったのに突き合わせ直されていない**
    カードを赤にする。★<b>保証するのは未来だけである</b> ——
    初回登録の `reviewedIn` は当時の記録からの推定にすぎない。
    ★<b>赤くなったらハッシュを更新して緑にするのではない。</b>本文と実装を突き合わせ直すこと。
    更新は `tools/mark_text_reviewed.py --card ... --note ...`(**理由が必須**)。
  - `tools/check_card_text_numbers.py`(**数値の照合**)
    …… 本文の数値がそのカードの実装に現れているかを見る。
    ★<b>0 と 1 は数えない</b>(「1枚につき」は実装に現れない)。増えた側の数は必ず現れるので弱くならない。
    ★<b>説明の付いた不足は5つの型に整理してある</b>(賢魂のコスト / 2回攻撃 /
    割り込みの解決側 / 別クラスの定数 / 「n枚目」は n−1)。
    「どの型でもない」不足が出たら本当に疑うこと。

- **★★数を数える番人が守れる範囲には端がある**(★67 の壊し検証が実測した)。
  《大地震》の**判定だけ**を 3 に戻し、ログを「コスト4以下」のまま残すと、
  このツールは<b>文字列リテラルの 4 を数えて素通りする</b>。
  ★66 までの実際の姿は判定もログも 3 だった —— 実装者が本文を読まずに書けば両方が古くなる。
  **意味まで守るのは突き合わせ台帳の役目である。**

- **★誘発の種別を機械で突き合わせる番人は作らなかった**(★67・裁定196 の正直な扱い)。
  試作したが誤検出だらけだった —— 参照(「【破壊時】<b>を持つ</b>ミニオン」)と誘発の印を
  区別できず、発火口が `TriggerType` でないものが多く(裁定206・`ON_EQUIP`・
  `fireAnyMinionEntered`)、印を持たない効果もある(裁定307 で伺い中)。
  ★<b>この軸は台帳が上位互換である</b> —— 本文が変われば台帳が赤くなり、人が突き合わせる。
  ★もし将来作るなら、裁定159 の規則を2箇所に置かないために
  <b>JUnit で `CardTextKeywords` を使う</b>こと(Python で書くと規則が2つになる)。

- **★★★誤った抽出と広すぎる照合は、互いの間違いを隠す**(★67。verify 49-1)。
  66 までの 49-1 は「Java の Filter が `battle.js` に居る」を測っていたが、
  (1) 突き合わせる相手を `Kind.HAND` の要求に現れる値だけに絞っており、
  67 が足した2値(`Kind.MINION` / `Kind.TRASH`)は<b>足しても目に入らなかった</b>。
  (2) その `Kind.HAND` の抽出じたいが誤っていて、1つの文に2つの要求が並ぶカード
  (《機神兵長茶爺》)では `Kind.MINION` 側の Filter まで拾っていた(**52 から**)。
  ★**(2) が表に出なかったのは、照合先が「`battle.js` 全体」という広すぎる集合だったから**である。
  67 は JS の2箇所(`matchesFilters` / 手札のハイライト)を分けて測る形に作り替えた。
  > **片方だけを直すと、隠れていたもう片方が表に出る。
  > それは番人が壊れたのではなく、直り始めた音である。**

- **★器が無いと思ったら、まず在るかどうかを見る**(★67。65 の教訓の器の版)。
  《ツイン・ストライク》で「場のミニオンの文明」が初めて要ったので `MinionView` に
  文明を足そうとしたが、`battle.js` の注釈に<b>45 が答えを書いていた</b> ——
  足りない属性は card-library(`autoLibrary`)から引くと決まっている(設計判断28)。
  **Java の変更は0行で済んだ。**

### ★★★マナ行の重なり(Batch 65)

- **★★★同じ性質の値を CSS と JS の両方に置くと、挙動を決めるのは「意図」ではなく
  「詳細度」になる。** 45 の通常モードのマナ行がこれだった ——
  回転のはみ出しを吸うための下駄 `.auto-mana-row .mana-tile.tapped { margin-left: 14px }`
  (クラス3つ)は、重なり側 `.auto-mana-row.auto-mana-overlap .mana-tile + .mana-tile`
  (クラス4つ)に**負けていて一度も効いていなかった**。
  ★**間隔(gap / margin)と重なり(負の margin)は同じ軸の値である。**
  片方を CSS、片方を JS が持ってはいけない。65 は CSS から margin を全部外し、
  `battle.js` の `applyAutoManaOverlap` 1本にした。
- **★★★タップ表現が回転のとき、要素の幅と見た目の外接は違う。**
  64×80 のタイルを 90度回すと外接は 80×64 になり、**左右に 8px ずつはみ出す**。
  `scrollWidth` / `clientWidth` は**レイアウト上の幅しか見ない**ので、回転ぶんは数に入らない。
  重なりの計算に使う占有幅は「タップ済なら高さ、それ以外なら幅」である。
  ★実測: 45 の実装はマナ15枚がすべてタップ済のとき**行から 50px はみ出していた**。
- **★★「簡易版」と書いた実装は、本家が直っても自分では直らない。**
  手動モードの `applyManaOverlap` は Batch 34 hotfix で**同じ症状**(タップ済の占有幅を
  勘定に入れず右列へはみ出す)を直していた。通常モードのマナ行は 43〜45 で新しく作られたので、
  直された知見を継承しなかった。
  ★64 の教訓(「暫定でこうしておく」は前提が消えても戻らない)の**空間版**である ——
  64 は時間(制約が消えても戻らない)、65 は空間(隣が直っても直らない)。
- **★★重なりの上限は「覆われる側」のタイルの占有幅から引く。**
  `z-index` を左から昇順に振るので、覆われるのは**左のタイル**である。
  上限を取る対象は `0 〜 n-2` になる。
  ★**手動モードの `applyManaOverlap` は `1 〜 n-1`(覆う側)で取っている。**
  均一な行では同じ値になるが、タップ済と非タップが混ざる行では1枚ぶんずれる。
  **65 では手動モードを触っていない**(積み残し)。
- **★盤面の端は「測って」見つける。読んでからでは見立てが測り方を歪める。**
  45 のコメントは「同じ思想の簡易版」と書いてあり、それ自体は嘘ではない ——
  思想は同じで、勘定に入れる項が足りなかっただけである。
  ★**マナ15枚は端の盤面ではない。**アンタップは自分のターンにしか起きないので、
  終盤に MP を使い切れば「15枚すべてタップ済」が相手のターン中ずっと映る。
- **★★UI の不変量は、実装の定数を書かずに測れる**(裁定298 の回避)。
  65 の verify が測るのは2つだけである ——「はみ出さない」と
  「`(隣までの距離 - 実測幅)` が全ての隣接対で等しい(= 重なりが均等)」。
  ★**実測幅を使うのが肝**である。回転したタイルの幅はブラウザが 80px と答えるので、
  検証の側に「回転しているから 80」と書かずに済む。
- **★重なりの前後は `z-index` を読まずに `elementFromPoint` で測る**(裁定296)。
  名前は左寄せなので、右のタイルが上にいなければ**名前の先頭が隠れる**。
  ★重なっている対が0件なら測れていないので、**対の数が0でないこと**を条件に含める(裁定186)。
- **★★「番人が無い」と思ったら、まず在るかどうかを見る。**
  65 は「`battle.css` を読み込むテンプレートの版数が揃っていること」を新しく書きかけたが、
  **Batch 36 が同じものを既に持っていた**(`const` の二重宣言で Node が拒んで気づいた)。
  危険だと感じる場所は、たいてい前の誰かも危険だと感じている。
- **★★JavaScript には裁定304 の壁が無い。**「条件を落とす」改変で後続が到達不能になっても、
  Java のようにコンパイルが拒むことはなく**黙って別物が動く**。
  壊し検証では、文を消して後続を宙に浮かせる形を使わないこと ——
  式ごと置き換えるか、**常に偽になる条件**で包む。
- **★★★削除は、書き戻しの手順とは別の操作である。**
  64 は `effect/AutoChoice.java` を削除したはずだったが、ワークスペースに残ったまま
  push されており、**clone した 65 は 681/682 で始まった**。
  ファイルを消すバッチは、次のバッチが**番人の落ちで気づく**ことになる ——
  だから退役には必ず番人を置く(64 の `Class.forName` はそのとおり仕事をした)。

### ★★★通常モードの受付(Batch 66)

- **★★★通常モードに「全公開(一人回し)部屋」を作ってはいけない。**
  手動モードの全公開部屋が成り立つのは、あちらが**盤面の入れ物**でしかなく、
  伏せる仕組みが**人の目にしか無い**からである。通常モードは手札・裏向きマナ・禁忌を
  `GameViewBuilder` **1本**で伏せており、「1人で両席を操作する」はそのフィルタに
  **例外を1つ作る**ことと同義である。★マスター指示でもあるが、構造上の理由が先にある。
- **★★★観戦者の視界は `isSelf = false` を両席に当てるだけである。**
  観戦専用の「見せてよい範囲」を書くと**フィルタが2本になり、片方が必ず遅れる**(設計判断9)。
  ★守っているのは画面ではなく**サーバ**である —— 観戦者は playerId を持たないので、
  画面を書き換えて操作を送っても `GameState.playerOf` が弾く。
- **★★観戦者かどうかを判断する正は `GameState.hasPlayer` である。**
  部屋の席(`GameRoom`)は**試合が始まる前の帳簿**であり、盤面の持ち主を決めているのは
  `GameState` の2人である。66 の作業中に、席を持たない試験用の足場が
  **プレイヤーを観戦者と判定させて**落ちた(同じ問いに答えが2つあった)。
- **★★観戦者には下段の手札の中身が届かない。**空のままにすると「手札0枚」に見えるので、
  `handCount` から**裏面の列**を出す。★中身が無いことと枚数が分からないことは別である。
- **★★誰であるかは URL ではなく localStorage が決める**(`qte-auto-occupant-{roomId}`)。
  65 までの `/rooms/{id}/play?playerId=...` は、**URL を共有すると相手の席として入れ**、
  **URL を失うと戻れなかった**。手動モードの 19a と同じ形に揃えた。
- **★★席選択のゲートは盤面ページの「中」に置く。**独立ページにすると直リンクが素通りする。
  「必ず通る」を**分岐ではなく構造で**保証する(手動モードの 21b と同じ判断)。
- **★★★`const` の一時的死角(TDZ)を、自分の `catch` が飲み込む。**
  66 は接続の開始を1章(スクリプト冒頭)に置いたまま、その中で
  **まだ評価されていない `const`** を掴んで ReferenceError を出した。
  関数宣言は巻き上がるので**呼べてしまう** —— 呼べるのに中身が死ぬ。
  さらに `catch` が「入れませんでした」に変換したので、**部屋が無いときと区別が付かなかった**。
  ★教えてくれたのは verify であり、しかも**盤面の全項目が一斉に落ちる**形だった
  (ゲートが盤面を覆ってクリックを吸うため)。
  **受け止める catch は、書いた本人の間違いも受け止める。**握りつぶさず `console.error` も出すこと。
- **★★★効いていない CSS の1行は、壊し検証でしか見つからない。**
  両モードのロビーは `<body class="bg-dark text-light">` を持ちつつ、
  テンプレートの `<style>` に `body { background-color: ... }` を書いていた。
  **クラス(0,1,0)は要素(0,0,1)に勝つ**ので、この1行は **34 から 65 まで一度も効いていなかった**。
  値が同じだったので症状が出ず、誰も気づかなかった。
  ★66 は `body.bg-dark`(0,1,1)に直した(**色の値は1文字も変えていない**)。
  ★**65 のマナ行とまったく同じ形である**(設計判断28)。場所が違うだけで罠は同じ。
  > **壊し検証は、番人の弱さだけでなく「実装の死んだ行」も見つける。**
- **★★★「書いてあっても、実装は自分では動かない」(3例目)。**
  `RoomIds` の Javadoc には 17b が
  「既存ファイルの変更が許されるバッチ(19a)で差し替える」と書いていた。
  **19a は来たが差し替えられず、66 まで部屋IDの生成が2箇所にあった。**
  ★64 は時間(前提が消えても戻らない)、65 は空間(隣が直っても直らない)、
  **66 は予定(いつやるか書いてあっても動かない)**である。
- **★★ハーネスは Thymeleaf のパーサ水準コメント `<!--/* */-->` を落とすこと。**
  実ページでは Thymeleaf が配信前に消すので、落とさないハーネスは
  **実ページより多くの文字を持つ**。66 で実際に踏んだ ——
  「退役した言葉がページに残っていない」を測ったら、
  **テンプレートのコメントに書いた言葉**を拾って落ちた。
  ★`build_harness.py` の `_strip_parser_comments` が4つのハーネス全部に当たっている。
- **★★ハーネスの部屋ID(`TESTRM`)を一覧の作り物に入れ忘れると、席選択のゲートは
  「入れませんでした」の顔で開く。**★**開いてはいる**ので、
  「ゲートが開く」だけを見る検証は通ってしまい、席のボタンを1つも測らないまま合格になる。
  ★裁定186(仕事をしていないことを自分で区別できなければならない)の新しい顔である。
- **★プリセットデッキ(おまかせ)は 66 で退役した。**`DeckFactory` はデッキファイルを
  並べ替える2メソッドだけになり、依存を1つも持たない。
  ★試験用のデッキは `src/test/.../support/SampleDecks.java` が**カードマスタから組む**
  (IDを書き並べない ——書き並べると「本番から消したプリセットの写し」になる)。
- **★消した番人は必ず書き残す**(裁定196)。66 が消したのは
  `Batch60Test` ×3・`check_all.py` 6章・`DeckValidatorTest` の足場である。
  `EffectImplementationTest` の1件は**対象を広げて残した**(プリセット → デッキに入れられる全カード)。

### ★★★デッキファイルの形式(Batch 63)

- **★★★デッキファイルの形式は `taboo-elemental-deck`(version 2)1つである**(裁定290)。
  デッキを組む場所も**デッキメーカー(`/deck-maker`)1つ**になった。
  ★62 まで形式は2つあり、「手動モードで使っているデッキが通常モードで読み込めない」の原因だった。
- **★★欄の名前**: `format` / `version` / `deckName` / `leader.cardId` /
  `main[].cardId` + `qty` / `taboo[].cardId`。
  ★**旧形式の `formatVersion` / `leaderCardId` / `count` / 禁忌のID文字列配列は書かない**
  (読み取りは過渡期の `leaderId` と禁忌のID文字列だけ受け付ける)。
- **★★読み取りは2箇所ある** —— 通常モードが `DeckFileReader`、手動モードが
  `ManualDeckImporter.importJson`。**規約は共有、コードは複製である**(裁定292・裁定111)。
  ★**片方だけ直さないこと。**番人は verify 63-1(Java の `path("…")` の実引数を
  デッキメーカーの実際の書き出しと突き合わせる)。
- **★★★読み取りは裁かない**(裁定293)。同じカードIDの行を**まとめない** ——
  まとめると `DeckValidator` の重複検査が誰にも当たらなくなる。
- **★通常モードは名前でカードを解決しない**(裁定294)。version 1(Verβ・名前で書かれた形式)は
  手動モードだけが読む。
- **★旧形式のファイルは `python3 tools/convert_decks_to_v2.py` で移行できる。**
  リポジトリの `decks/*.json`(効果確認用12本)は 63 で移行済みである。
- **★★通常モードは zip(ユドナリウム)を読まない。** 後方互換は手動モードだけの話である。
- **★★退役は「経路・ファイル・案内」を同時に消す**(裁定295)。
  63 で消したもの: `/deck-builder` のマッピング / `deck-builder.html` / `deck-builder.js` /
  ロビーのリンク / `CardApiController`(`/api/cards`・`/api/implemented-civilizations`)。
- **★★テンプレートのHTMLコメントは配信される**(裁定297)。
  退役した経路の名前を素のコメントに書くと、ページに出て試験を落とす。
  Thymeleaf のパーサ水準コメント(`<!--/*` … `*/-->`)を使うこと。
- **★ロビーの要約は `deckSummary` という名前付きの関数である**(裁定296)。
  機械検証が取り出して実物に当てるので、**その場の式に戻さないこと。**

### デッキ構築(Batch 47 で変わった)

- **★★★デッキ構築の規則の正はサーバ**(通常モード = `DeckValidator`)(設計判断27)。
- **★★★効果が未実装でもデッキに入れられる**(47・裁定D2)。代わりに盤面に印が出る。
- **★★★進化ミニオンはメインにも禁忌にも入れられる**(★Batch 52 で解禁。裁定228)。
  ★**禁忌に入れた進化は、場を離れると消滅ゾーンへ行くので墓地に戻らない** ——
  《サモナーポップ・エンラ》の「墓地からの特殊召喚」を実機で見るには
  <b>メインデッキに入れる</b>必要がある(53 のチェックデッキが闇と光の2本あるのはこのため)。
  46b〜51 は `requireDeckable` が弾いていた(裁定166)が、エンジンができたので理由が消えた。
  ★**解禁の前提は「18枚すべてに素材条件が登録されていること」である** ——
  1枚でも欠けると、そのカードはデッキに入るのに場に出せない死に札になる
  (`EvolutionEngineTest.進化ミニオン18枚すべてに素材条件が登録されている` が番人)。
- **★文明なし(ピュア・エレメント)は入れられない**(`requireImplemented` の文明検査)。
- **★同名4枚に例外は無い**(46b で `UNLIMITED_COPIES` を撤廃)。
- **★リーダー選択は18人**。うち**4人**は起動能力・常在が未実装である
  (★49 でロロイヨが実装され、5人 → 4人になった)。
- **★`DeckValidator` は `CardEffectRegistry` に依存しない**(47)。
- **★★プリセットデッキはまだ Ver0.4 の構成である**(P5 の担当)。
  Ver1.1 の新カードで遊んでもらうには、デッキファイル(JSON)を渡すのが早い
  (48 は `decks/batch48-wind-check-deck.json`、49 は `decks/batch49-water-check-deck.json`)。
- ★デッキJSONを添えるときは、**`DeckValidator` を通ることを実際に確かめること**
  (通らないと実機確認そのものが始まらない)。★テストにJacksonが無いので、
  一時的な試験で読むときは正規表現で組み立てる。
  ★**63 で訂正: デッキJSONの形式は `taboo-elemental-deck` v2 である**(上の節)。
  12本の確認用デッキは 63 で移行済みであり、**そのまま通常モードのロビーで読める**
  (`LobbyPageTest` と `DeckFileReaderTest` が実物で測っている)。

### デッキメーカー(Batch 39・40)

- **★★★文明色の正は `battle.css` の `:root` の `--civ-*` 1箇所**(裁定108)。**機械判定あり。**
- **★★★autosave の枠は2つ**(`qte-deckmaker-draft` / `qte-deckmaker-undo`。裁定121)。
- **★★★保存してある内容はライブラリより先に読む**(裁定124)。**機械判定あり。**
- **★★スナップショットに項目を足したら `applySnapshot` にも足す**(裁定125)。
- **★★デッキ全体を置き換える経路は `replaceDeck` 1本。**
- **★★★カーブの区切りは `COST_KEYS`(フィルタと共有)**(裁定126)。カーブが数えるのは**メインだけ**(127)。
- **★★★書き出しを止めない。理由を見せてから通す**(裁定128)。**機械判定あり。**
- **★★規定枚数(40/8)と同名上限(4/1)は Java とブラウザの両方**(裁定130)。**機械判定あり。**
- **★★モーダルは `openInfoModal` / `closeInfoModal` を通す**(裁定51)。
- **★★確認モーダルは `manual-battle.js` の複製である**(裁定111)。片方だけ直さないこと。
- **★★Bootstrap を入れない**(裁定104)。閉じるのは `hidden` 属性。
- **★★★カーブの強調が効くのはメインデッキの節だけ**(裁定133)。
- **★押せるものを `<div>` で作らない**(裁定132)。
- **★「メインか禁忌か」は `isMainCard(c)` 1つで決まる。**
- **★★デッキメーカーには「効果未実装」の印を出していない**(47 設計解説 5-4)。

### 開始シーケンスの儀式(Batch 38)

- **★★★儀式は差分ではない。** 材料はサーバの `rites`、採るのは `diffViews` の**先頭**。
  **関門を `applyView` へ戻さないこと**(裁定82・83)。**機械判定あり。**
- **★★儀式は上限(`FX_BULK_LIMIT`)の特例ではない。**
- **★★★開始画面の保留(`riteHold`)を消さない**(裁定87)。ゲートは `fxAllowed()`(89)。
- **★儀式のゴーストは最初に全部作る**(90)。**★ダイスの文言はサーバの `label`。**
- **★★★儀式が語る配信では差分を語らない**(裁定93)。**機械判定あり。**
- **★★シャッフルは盤面に何も起こさない操作**(99)。**★★ピュア・エレメントは中央から飛ぶ**(101)。

### 効果音(Batch 37・38)

- **★★★発火点を増やさない**(裁定68)。音を足すときは **`SFX_FOR_KIND` の表**を直す。
- **★★演出の種類(`fx.kind`)を足したら、音の表にも足す**(裁定72)。**機械判定あり。**
- **★★★`prefers-reduced-motion` を音のゲートにしない**(裁定69)。**機械判定あり。**
- **★1配信につき1音**(70)。**★unlock を特定の操作へ結び直さない**(73)。**機械判定あり。**
- **★音の調整は `SFX_SPECS` の表だけを触る。** 現在 **11種**。
- ~~**★音声ファイルを同梱しない**(74)。**★★版数の系統を4つ目にしない。**~~
  **★★★62 で訂正: 裁定74 は失効した**(裁定283)。マスターの裁定により
  **音源は音声ファイルになり、版数の系統は4つ目が増えた。**
  ★<b>旧裁定が誤っていたのではない</b> —— 37 の時点では「4系統目を増やさない」ほうが
  正しかった。前提(マスターがフリー素材を用意する)が変わったので結論が変わっただけである。
  ★詳細は下の「★★音響のファイル化(Batch 62)」の節にある。

### ★★音響のファイル化(Batch 62)

- **★★★素材は CC0 に限る**(裁定285)。★理由は<b>リポジトリが公開だから</b>である ——
  `static/` に置いたファイルは GitHub 上で誰でも単体でダウンロードできるので、
  「アプリに組み込んでよい」では足りず**「再配布してよい」**でなければならない。
  ★効果音ラボは商用可・クレジット不要だが**再配布禁止**であり、**ここには置けない。**
  ★CC BY も採らない —— クレジット表記を画面に置くという設計判断が1つ増えるためである。
  ★**この規則の御利益は、以後この話を二度と考えなくてよくなることである。**
- **★★出所は `static/sounds/CREDITS.md` に必ず記録する**(裁定285)。**機械判定あり。**
  ★CC0 は表記を要求しないので、**これは表記ではなく記録である** ——
  後から「本当に CC0 だったか」を確かめられない状態にしない。
- **★★★調査だけで素材を決めない。実物の長さを測ること**(★62 で実際に踏みかけた)。
  Kenney の `click_002`〜`005` は**長さが 0.01 秒しかなく、ほとんど聞こえない**。
  ファイル名の並びだけを見て `tap` に当てていたら、鳴らない音を4つ仕込むところだった。
  ★`tap` には長さの揃った `select_001 / 002 / 007 / 008` を当ててある。
- **★`.ogg` のまま同梱しない。**iOS Safari は 17.3 以前で Ogg Vorbis を再生できない。
  CC0 は改変を無条件に許すので `ffmpeg` で `.mp3` へ変換する(手順は CREDITS.md)。
- **★★★音の版数は `SFX_VERSION` の1つだけである**(裁定284)。**機械判定あり。**
  ★**JS の版数と同じ数字を使い回さないこと** ——
  「音だけ差し替えたのに `manual-battle.js` の版数を上げる」と、1つの数字に意味が2つ載る。
  ★版数を持たない選択は採れない。このプロジェクトは `Cache-Control` を1つも設定しておらず、
  ブラウザが `Last-Modified` から有効期限を勝手に推定する。
- **★★★読み込みに失敗した音は鳴らない**(裁定283)。合成へのフォールバックは持たない ——
  **「どちらが鳴るか」の分岐を1つ増やすと、音が語るものが1つ減る。**
  ★ただし<b>鳴らない理由は状態行に出す</b>(裁定76 の場所をそのまま使う)。
- **★★取得(fetch)と復号(decode)を分ける。**復号には `AudioContext` が要るが、
  それを最初のユーザー操作より前に作ってはいけない(裁定73)。
  **取得はページを開いた時点で、復号は unlock の時点で**行う。
  ★この分割のおかげで、読み込み失敗を**操作より前に**検出できる。
- **★★散らすのは `tap` と `place` だけである**(裁定286)。**機械判定あり。**
  ★毎手鳴る音は<b>意味がすでに摩耗している</b>ので、散らしても情報は減らない。
  ★逆に `decisive`(1試合1回)を散らすと「これは前と同じ出来事か?」という迷いが生まれる ——
  **珍しい音は、珍しいからこそ同一であることが情報である。**
- **★★★`lpDown` に `error_` を当ててはいけない。LPが減るのは失敗ではない。**
  相手のLPが減るのは自分の成功であり、自分のLPが減るのも盤面の出来事である。
  `error_` を当てると、音が「しくじった」という意味を勝手に足す。
  ★`minimize`(下降)/ `maximize`(上昇)の対を当てる —— 裁定80 の関係が音の側でも保たれる。
- **★★★通常モードの音の取り付け点は `onMessage` であって `render` ではない**(★62)。
  `render(latestView)` は<b>画面の操作のたびにも呼ばれる</b>(15箇所)。
  あそこで差分を採ると「配信」と「再描画」を区別できない。
  **サーバから来た出来事の入口は `onMessage` の1箇所だけである。**
- **★★差分に現れない出来事だけ `send()` から鳴らす**(★62)。
  攻撃は view に<b>結果</b>(HPの減少・消滅)しか現れず、「攻撃した」はどこにも書かれていない。
  ★<b>カードのプレイをここで鳴らしてはいけない</b> —— 差分が `appear` として語るので、
  **1つの操作で2音**になる。手動モードの `commit` とまったく同じ形である。
- **★★★音の設定は両モードで複製である**(裁定289)。**機械判定あり。**
  `battle.js` の音のセクションと `battle.html` の `#sound-modal` は
  `manual-battle.js` / `manual-battle.html` の写しである(裁定111 と同じ落とし穴)。
  ★**片方だけ直さないこと。**番人は「両モードに共通して載っている音は同じファイル・同じ gain」
  「珍しさの順序が同じ」の2件である。
  ★`localStorage` の鍵は **`qte-manual-sound` を共有する**(改名していない) ——
  音量は人の性質であり、モードごとに合わせ直させるものではない(裁定31 の筋)。
  ★鍵の名前が手動モード専用に見えるが、**改名すると既に音量を合わせた人の設定が1回消える。**
- **★★通常モードの表は9種、手動モードは11種である**(★62)。同じではない。
  ★<b>この表は「そのモードで鳴る音」の一覧である。</b>鳴らない音を載せると表が嘘をつく。
  通常モードに `flip` / `dice` / `deal` は無く(「めくり」「先後のダイス」「ひと配り」という
  出来事が無い)、手動モードに `attack` は無い。

### モーダル・Esc・フォーカス(Batch 36・39・40)

- **★★★画面を覆うものは `modalStack` 1本で持つ**(裁定47)。
- **★★★Esc を下の層へ落とさない**(裁定49)。**機械判定あり。**
- **★初期フォーカスは「押させたいボタン」に載せない**(裁定52)。
- **★出口が無いもの**: 開始シーケンスの2つとマリガン、席選択ゲート(裁定34)。
- **★★破壊的操作に `confirm()` を書かない**(裁定53)。**機械判定あり。**
- **★★確認モーダルの z-index は 1965**(56)。**★★★`sticky` を負マージンで持ち上げない**(57)。

### 勝敗の宣言・決着(Batch 35)

- **★★★宣言は盤面に痕跡を残さない操作である。** 配信は `declarations`(構造)を別に運ぶ。
- **★★ログは `ManualLogEvent.declaration(...)` でしか作れない**(41)。
- **★★`declarations` と `rites` はログの末尾60行と同じ1周で作る**(42)。
- **★決着の帯は `seq` が増えたときだけ**(43)。**★★ターン帯は退役した**(17)。**機械判定あり。**

### メタ情報・設定(Batch 33・34)

- **★★`og:image` の絶対URLは `qte.public-base-url` から**(24・27)。**機械判定あり。**
- **★アイコンと OGP 画像は `tools/make_brand_assets.js` が生成する。**
- **★エラーページに CDN を書かない**(21)。**★★`title` は `TITLE_HINTS` / `tileTitle()` 経由**(29)。
- **★裏向きのカードの名前を `title` に出さない**(30)。

### 見た目の統一(Batch 34・39・47)

- **★★ロビー・カード一覧・盤面・デッキメーカーはすべて黒。** `#212529` / `#f8f9fa`。
- **★★`verify/build_harness.py` の `lobby_stub` も黒。片方だけ直さない。**
- **★★近い色を新しく作らない**(32)。薄い文字 `#adb5bd`、強調 `#ffca28`、良否 `#6ee7a0` / `#ff9b9b`。

### 切断・接続(Batch 33)

★★**Batch 71 で通常モードにも同じ4点セットが入った。**
この節は手動モードの記述だが、**筋はそのまま両モードに効く** ——
ただし**値と置き場所は写さないこと**(上の「通常モードの切断(Batch 71)」を参照)。

- **★★★操作を止めているのは `send()` のガード**(18)。**`send()` は真偽を返す。**
  ★★**Batch 71 でその返り値に使い手ができた** —— 通常モードは
  送れなかったらローカルの選択状態を畳まない。手動モードには今も使い手が居ない。
- **★切断の案内と部屋消失の案内は排他**(19)。
  ★**通常モードには部屋消失が無いので、排他の旗も作っていない**(71)。
- **★z-index の並び: fx層 1030 < 軌跡 1035 < ゾーンの帯 1040 < 全面表示 1050 <
  ポップオーバー 1055 < トースト 1058 < 席選択ゲート 1060 < 開始の帯 1900 <
  開始モーダル 1950/1960 < 確認モーダル 1965 < 切断オーバーレイ 1970 < 接続の帯 1975。**
  **★★`.info-modal` は z-index 1000 で、いちばん下。**★★★**演出の都合で崩さない**(87)。

### カードフェイスの質感(Batch 32c・41・47)

- **★★★フェイス(`.mcard-*`)に transform / filter を足さない。** 擬似要素も足さない。
  ★47 の印もフェイスの外(バッジ `.auto-badges`)に重ねている。**機械判定あり。**
- **★★★カスタムプロパティの中の `var()` は「宣言した要素」で解決される**(裁定140)。
- **★フェイスはコントラストの機械判定の対象外。そのぶん目視が必須。**
- **★★通常モードのバッジは `newBadgeBox` / `addBadge` / `attachBadges` で作る**(47)。

### 外部依存・右クリック(Batch 41)

- **★★外部ライブラリは `static/vendor/` から配る。CDN を参照しない**(裁定136・137)。
- **★★`stomp.js` が読めないと盤面が一生来ない。**「動かない」報告はまずここを疑う。
- **★★右クリックの受け皿を `document` に1本持っている**(138・139)。

### 演出・fx層(Batch 32a・32b・35・37・38)

- **★★演出のイベント検出は「ビューの差分」。材料は書式ではなく構造**(裁定40)。
- **★★「窓」のゾーン(counts > 届いた配列の長さ)では出現・消滅を出さない。**
- **★★配信の反映は `applyView` 1本。★★★開始シーケンスの関門は `diffViews` の中。**
- **★★fx層は `position: fixed` で `#manual-root` の外。**
- **★★★遷移のクラスは「最終状態」と一緒に当てる。**
- **★★`fxSpawn` の確定は `document.documentElement.offsetHeight`。**
- **★★実要素へ当てる演出は `onStop` で必ずクラスを剥がす。**
- **★演出の時間・上限は定数1箇所**(`FX_*`)。**★`fxEnabled` は消さない**(78)。

### 性能・接続(Batch 28・29)

- **★★1配信は27KBで頭打ちだった(29 で解消)。**
  (a) 山札は最上段1枚 (b) ログは末尾60行 (c) 描画関数の中で幅を実測しない
  (d) ログの差分追記は seq の連続性が前提 (e) 非同期は通し番号で古い応答を捨てる
- **★★`TaskScheduler` 型のBeanを増やさない。★★無言の `location.reload()` を書かない。**
- **★★部屋はメモリ上にしか無い。接続の寿命がゲームの寿命。**
- **★静的ファイルの版数は3系統**(`manual-battle.js` / `battle.js` / `battle.css`)。
  現在 **js v=33 / battle.js v=25 / css v=46**
  (★50・51 は据え置き。52 が `battle.js` を v=23 に、53 が v=24 に、
  **★54 が v=25 に上げた** ——【賢魂】の2導線のため)。
  ~~★★**4つ目の系統を増やさない**(74)。~~
  **★★★62 で訂正: 4つ目の系統が増えた**(音声ファイル・裁定283/284)。
  ★★**版数の系統は4つになった** —— `manual-battle.js` **v=34** / `battle.js` **v=27** /
  `battle.css` v=47 / **音声ファイル `SFX_VERSION`**。
  ★音の版数だけは `?v=` をテンプレートではなく<b>JS の定数</b>から付ける
  (音声ファイルは `th:src` で参照するものではなく、JS が実行時に読むためである)。
  **★★`battle.css` の版数は3テンプレートで揃える。機械判定あり。**
  ★★版数を上げたら `verify/build_harness.py` の置換文字列も同時に直す。
  **★片方だけ直すと、ハーネスが js を読み込まなくなり、検証スクリプトごと例外で死ぬ**
  (FAIL としては出ない。46b・47 で実測)。

### カードデータ・デッキ(Batch 24・40・46b)

- **★★`manual-cards.json` が全モード共通の正。**
- **★★デッキの標準形式は JSON。** 判別はサーバが先頭バイト(`PK`)で行う。
  ★**63 で追記: その JSON の形式は `taboo-elemental-deck` v2 の1つだけになった**
  (裁定290。「デッキファイルの形式(Batch 63)」の節を見ること)。
- **★★Thymeleafテンプレートのインライン式に注意。**
  ★デッキメーカーの `<script>` は `th:inline="none"` を保つ。

### 進化スタック(Batch 27。★手動モードの話。★通常モードの進化は Batch 52 にある)

- **★★進化スタックは FIELD にしか存在しない(不変条件)。**
- **★★1つの ref から複数枚が入りうる。**
- **★★「状態にはあるが画面のどこからも触れない」は消失と同じ扱い。**

### 手動モードの原則(要点)

- **★★手動モードで「判断」を実装しない** — 例外はウェポン付け替え(20b)と開始前処理(23)だけ。
- **★★共有ゾーン(PLAY / REVEAL)は席に属さない。`seatId` の無条件参照を書かない。**
- **★★スナップショットに項目を足したら `copy()` を直す。**
- **★★公開範囲・押せるか、の判定を2箇所に書かない。**
- **★★操作の型は `ManualOperationService.apply` が握る。リセットは絶対に止めない。**
- **★MPを直接増減する操作を作らない。カードIDをJavaにリテラルで書かない。**
- **★★手動モードは別の `ManualCardType` を持つ**(EVOLUTION 込み)。**型は共有しない。**

### UI の落とし穴(要点)

- **静的ファイルを変えたらキャッシュバスティングを上げ、`build_harness.py` も同時に直す。**
- **★★パイルの最上段は「山札だけ先頭・それ以外は末尾」**(`pileTopCard`)。
- **★★★タップ表現は場のタイル=減光+バッジ、マナ=回転で、意図的に異なる。**
- **★★高さを画面から逆算するときは「入れ物のパディング」まで引く。**
- **★盤面のラベルはコントラスト比 4.5:1 以上が機械判定される。**フェイスだけは対象外(目視)。
- **★ログのカード名リンクはサーバの `《名前》` 表記に依存。**
- **★★レイアウト計算で `getBoundingClientRect().top` を素で使わない。**
- **★★★描画の席と送信の席を混ぜない。**
- **★★クリック規約: 左=見る / 右=動かす。** 新しい `contextmenu` には必ず `preventDefault()`。
  ★**通常モードでも「右クリック=拡大」**(裁定142)。
- **★★対戦部屋では非公開ゾーンが `zones` にキーごと現れない。`zoneCount` を使う。**
- **★★flex: 可変幅の列に `min-width:0`。**
- **★★D&D検証は合成DragEventではなく実マウス(`page.mouse`)で。**
- **★`battle.css` を読み込むテンプレートはどれも黒背景。機械判定あり。**
  ★**65 で訂正: 3枚 → 5枚である。**`battle.html` / `manual-battle.html` /
  `manual-deck-maker.html` に加え、61 で `cards.html` と `manual-cards.html` が
  カードフェイスの正を共有するために読むようになった。
  ★**版数を上げるときは5枚まとめて上げること。**1枚でも上げ忘れると、
  そのページだけ古い CSS がキャッシュから出る —— 番人は 36 が置いている
  (「battle.css の版数はすべてのテンプレートで揃っている」)。
  ★件数は増えるので、数を覚えるのではなく**番人が数えていること**を覚えておく。
- **★★★盤面のミニオン面(タイル)は現在値を出す**(裁定143)。手札・禁忌・拡大は印刷値。
- **★★通常モードは 1280×800 の1画面に収める**(裁定146)。verify 43-1 が番人。
- **★★リーダーとパイルは右列・盤面は中央**(裁定150)。オーバーレイはヘッダを覆わない。
- **★★裏面の見た目の正は card-library の `meta.backImageId` 1つ**(裁定151)。
- **★★card-library の索引の鍵は `id` である**(46b)。
- **★検証ハーネス: `python3 verify/build_harness.py && node verify/verify.js`(★621項目・★67 で更新)。**
  ★**66 でハーネスが1枚増えた** —— `harness-auto-lobby.html`(通常モードのロビー)。
  手動モードのロビーと同じスタブを当て、**2つのロビーの実測色を突き合わせる**(裁定130)。
- **★★通常モードのロビーは暗色である**(66)。盤面と同じ #212529 / #f8f9fa。
  ★色は `body.bg-dark` に書く(`body` だけでは class に負ける。上の節を参照)。

### その他

- occupantId は localStorage(`qte-manual-occupant-{roomId}`)。切断猶予5分は
  `ManualCleanupScheduler` と `ManualViewBuilder` の2箇所。片方だけ変えない。
  **★localStorage の鍵は5つ**。
- 無人部屋は5分で自動削除。ゲームの正式名称は「クイン・タブーエレメント」。
- `tools/check_structure.py` は `List.<String>of()` を誤検出(一時変数に受けて回避)。
- `tools/check_records.py` の既知の誤検出**4件**(GameActions / GameService×2 / CardEffectRegistry。
  ★49 で行番号がまた移動した。中身は同じ)。
  ★**54 で訂正: 3件 → 4件になった。**`EffectContext` のコンストラクタが3つになったためで、
  性質は同じ(オーバーロードされたコンストラクタの呼び出し)である。
  ★**62 で再訂正: 実測は3件である**(`GameActions.java:1377` / `GameService.java:1819` /
  `GameService.java:1826`)。性質は同じ。★<b>件数は数えるたびに動く</b>ので、
  数を覚えるのではなく**出てきた行を目視して上記2種のどちらかであることを確かめる**こと。
- **★`fxRunning` などの `const` はグローバルオブジェクトに載らない。**
- **★`tools/check_undeclared.py` はオブジェクトリテラルの短縮メソッドを読めない。**
- **★検証項目54(タップ表現の非対称)は 120ms の観測窓に依存しており、まれに揺れる。**

### ★★検証環境と実環境の差

- **★★★盤面もロビーもデッキメーカーも黒背景。** 白いページは通常モードの lobby / cards のみ。
- **★★ハーネスは Bootstrap を落として代替を当てている。**
  **★★★スタブに書いてよいのは、落とした CDN の代わりだけ**(裁定39)。
- **★★判定を入れたら、判定が落ちることを1回確かめる**(裁定116)。
  ★36 で12通り、37 で12通り、38 で13通り、39 で16通り、40 で24通り、
  46a で10通り、46b で11通り、47 で12通り、48 で21通り + hotfix で3通り、
  49 で23通り(JUnit)+ 2通り(verify)、★**50 では19通り**を確認した。
  ★50 からは `tools/batch50_break_check.py` のように**壊し検証そのものをスクリプトにして残す**
  (次のバッチが同じ形をコピーして使える。4値で答える形も含めて雛形になる)。
- **★★★「壊したのに落ちない」を見たら、まず改変が当たっているかを疑う**(裁定196)。
  ★形は3つある —— (1) 改変が当たっていない(置換が0箇所/2箇所)、
  (2) 試験が値を実装から読むので定数の改変が効かない(裁定298・Batch 63)、
  (3) ★**改変でコンパイルが通らなくなり、試験が1件も走らない**(裁定304・Batch 64)。
  (3) は `if (cond) { … return; }` を素の `{ … return; }` にすると
  その先が到達不能になって起きる。**常に真になる条件へ書き換える**のが正しい壊し方である。
  ★★**50 では `mvn surefire:test` で回したせいで 19件中18件が NG になった**(裁定208)。
  このゴールはコンパイルを走らせない。**必ず `mvn test` で回すこと。**
  ★**49 でも実際に2件起きた**(改変が番人と代入の片方しか差し替えていない / 置換文字列が2箇所に一致)。
  確認用スクリプトは **OK / NG / EMPTY / SETUP-NG の4値**で答えさせること。
- **★★★比べる相手を間違えた検証は何も見ていない**(裁定181)。
- **★★★検証スクリプトは「仕事をしていない」を自分で区別できなければならない**(裁定186)。
- **★★「クラスが付いたか」を見る検証は、演出の検証にならない。測るのは結果の座標。**
- **★★`waitForFunction` や `locator.click()` が投げると検証スクリプトごと死ぬ**(`tryUi` で FAIL に)。
- **★★機械で測れないものを機械で測ろうとしない。測れる決めごとのほうを測る。**
- **★★検証の項目は状態を引き継ぐ。**末尾に足すときは、そこまでで何が起きているかを読む。
- **★★イベントは実際の入口から起こす。★半透明の背景は下地で意味が変わる。**

